---
description: Audit KDoc coverage on public Kotlin APIs. Reports undocumented symbols, per-module coverage, and re
intent: kdoc-audit
---

# /kdoc-audit

See `skills/kdoc-audit/SKILL.md` for the canonical definition. Invoke directly as `/kdoc-audit` — the skill is user-invocable (`disable-model-invocation: true`).
