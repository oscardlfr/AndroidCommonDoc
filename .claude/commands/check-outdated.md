---
description: Check dependency versions against Maven Central (TOML parser, kdoc-state v2 cache).
---

Check libs.versions.toml against Maven Central for outdated dependencies.

Read the skill file and follow all instructions:

```
$SKILL_DIR/check-outdated/SKILL.md
```

Arguments: $ARGUMENTS
