---
description: Full-project KDoc migration orchestrator. Adds KDoc to all undocumented public APIs, module by modul
intent: kdoc-migrate
---

# /kdoc-migrate

See `skills/kdoc-migrate/SKILL.md` for the canonical definition. Invoke directly as `/kdoc-migrate` — the skill is user-invocable (`disable-model-invocation: true`).
