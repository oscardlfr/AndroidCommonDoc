---
description: Sync documentation into Obsidian vault with L0/L1/L2 hierarchy. Use when asked to update, initialize
intent: sync-vault
---

# /sync-vault

See `skills/sync-vault/SKILL.md` for the canonical definition. Invoke directly as `/sync-vault` — the skill is user-invocable (`disable-model-invocation: true`).
