---
description: Monitor upstream doc sources for version drift and deprecations. Use when asked to check for outdate
intent: monitor-docs
---

# /monitor-docs

See `skills/monitor-docs/SKILL.md` for the canonical definition. Invoke directly as `/monitor-docs` — the skill is user-invocable (`disable-model-invocation: true`).
