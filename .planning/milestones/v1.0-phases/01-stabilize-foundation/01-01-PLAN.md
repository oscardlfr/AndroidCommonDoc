---
phase: 01-stabilize-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/viewmodel-state-patterns.md
  - docs/ui-screen-patterns.md
  - docs/testing-patterns.md
  - docs/gradle-patterns.md
  - docs/offline-first-patterns.md
  - docs/compose-resources-patterns.md
  - docs/resource-management-patterns.md
  - docs/kmp-architecture.md
autonomous: true
requirements:
  - PTRN-01
  - PTRN-02

must_haves:
  truths:
    - "Every pattern doc has a consistent header with Status, Last Updated, Aligned with, Platforms, and Library Versions fields"
    - "Every pattern doc contains explicit anti-pattern sections (DON'T blocks with explanations) alongside correct patterns (DO blocks)"
    - "Code samples pin actual library versions (koin 4.1.1, compose 1.7.x, kotlinx-coroutines-test 1.10.2, etc.)"
    - "All 7 required layers are covered: ViewModel, UI, testing, Gradle, offline-first, resources, compose resources"
  artifacts:
    - path: "docs/viewmodel-state-patterns.md"
      provides: "ViewModel layer patterns with anti-patterns and pinned versions"
      contains: "DON'T"
    - path: "docs/ui-screen-patterns.md"
      provides: "UI layer patterns with anti-patterns and pinned versions"
      contains: "DON'T"
    - path: "docs/testing-patterns.md"
      provides: "Testing patterns with anti-patterns and pinned versions"
      contains: "DON'T"
    - path: "docs/gradle-patterns.md"
      provides: "Gradle patterns with anti-patterns and pinned versions"
      contains: "DON'T"
    - path: "docs/offline-first-patterns.md"
      provides: "Offline-first patterns with anti-patterns and pinned versions"
      contains: "DON'T"
    - path: "docs/compose-resources-patterns.md"
      provides: "Compose resources patterns with anti-patterns and pinned versions"
      contains: "DON'T"
    - path: "docs/kmp-architecture.md"
      provides: "KMP architecture foundation (source set hierarchy)"
      contains: "DON'T"
  key_links:
    - from: "docs/*.md"
      to: "CLAUDE.md conventions"
      via: "Pattern alignment"
      pattern: "sealed interface|CancellationException|WhileSubscribed"
---

<objective>
Standardize all pattern docs to a consistent professional format with anti-patterns, pinned library versions, and compilable code samples.

Purpose: Pattern docs are the source of truth for all downstream artifacts (skills, AGENTS.md, quality gates). If they're inconsistent or incomplete, everything built on top inherits those gaps. This plan fixes the foundation before building on it.

Output: 8 pattern docs (7 required layers + kmp-architecture) all following the verified pattern doc structure from research, with explicit anti-patterns and pinned versions.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilize-foundation/01-CONTEXT.md
@.planning/phases/01-stabilize-foundation/01-RESEARCH.md

Reference the verified pattern doc structure from 01-RESEARCH.md section "Code Examples > Verified Pattern Doc Structure" as the target format for all docs.

Key user decisions:
- Include anti-patterns alongside correct patterns ("Do this" AND "Don't do this")
- Pin actual library versions in code samples (e.g., koin 4.1.1, compose 1.7.x)
- 9 existing docs; 7 required layers validated
- Pattern doc organization per research recommendation (adoption sequence)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize core pattern docs (ViewModel, UI, Testing, Gradle)</name>
  <files>docs/viewmodel-state-patterns.md, docs/ui-screen-patterns.md, docs/testing-patterns.md, docs/gradle-patterns.md</files>
  <action>
For each of the 4 core pattern docs, apply the verified pattern doc structure from research. Do NOT rewrite content -- augment and restructure existing content:

1. **Standardize header** to match this exact format:
```
# [Layer] Patterns

> **Status**: Active
> **Last Updated**: [Month Year] (v[N] - [Change description])
> **Aligned with**: [Reference projects]
> **Platforms**: [Platform list]
> **Library Versions**: [Pinned versions relevant to this doc]
```

2. **Add Library Versions field** to header with pinned versions from the code samples in each doc. Known versions to pin:
   - ViewModel: koin 4.1.1, kotlinx-coroutines 1.10.2
   - UI: compose 1.7.x, navigation3 (latest stable)
   - Testing: kotlinx-coroutines-test 1.10.2, mockk 1.14.7
   - Gradle: Kotlin 2.3.10, AGP 9.0.0, Compose 1.10.0

3. **Add anti-pattern sections** where missing. For each major section/topic, ensure there is a "DON'T (Anti-pattern)" code block alongside the "DO (Correct)" block. Format:
```kotlin
// BAD: [explanation of why this is wrong]
[code showing the anti-pattern]
```
Anti-patterns to ensure exist (add if missing):
   - ViewModel: data class UiState with boolean flags (vs sealed interface), Channel for UI events (vs SharedFlow), Dispatchers.Default in tests (vs injected dispatcher), platform deps in VM constructor
   - UI: business logic in composables, collecting state without lifecycle awareness, navigation via Channel
   - Testing: using mocks instead of fakes, testing without runTest, not rethrowing CancellationException
   - Gradle: nested module names (vs flat), resources in custom source sets (vs commonMain/composeResources)

4. **Verify all code samples have imports** or are clearly self-contained. If a code sample references a class, ensure the import or package is shown.

5. **Add "Key insight" one-liners** after anti-pattern pairs where they add value.

Do NOT change the substantive technical content or patterns described. Only restructure for consistency and add anti-patterns.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc" && for f in docs/viewmodel-state-patterns.md docs/ui-screen-patterns.md docs/testing-patterns.md docs/gradle-patterns.md; do echo "=== $f ===" && head -8 "$f" && echo "---" && grep -c "DON'T\|BAD:\|Anti-pattern" "$f" && echo "anti-pattern sections found"; done</automated>
  </verify>
  <done>All 4 core docs have standardized headers with Library Versions field, and each contains at least 3 anti-pattern sections with "DON'T" or "BAD:" markers.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize remaining pattern docs (offline-first, compose-resources, resource-management, kmp-architecture)</name>
  <files>docs/offline-first-patterns.md, docs/compose-resources-patterns.md, docs/resource-management-patterns.md, docs/kmp-architecture.md</files>
  <action>
Apply the same standardization as Task 1 to the remaining 4 docs:

1. **Standardize header** to the verified format (same as Task 1).

2. **Add Library Versions field**:
   - offline-first: Room (if referenced), DataStore, kotlinx-serialization version
   - compose-resources: Compose Multiplatform version, Gradle plugin version
   - resource-management: desktop-specific libraries if referenced
   - kmp-architecture: Kotlin version, KMP plugin version

3. **Add anti-pattern sections** where missing:
   - offline-first: synchronous-first data loading (vs offline-first), raw network calls without caching, missing conflict resolution
   - compose-resources: resources in custom source sets (vs commonMain/composeResources), missing `generateResClass = always`, hardcoded strings instead of resource keys, shared resources across modules without unique packages
   - resource-management: duplicating resources across source sets
   - kmp-architecture: duplicating code in androidMain+desktopMain (vs jvmMain), duplicating code in iosMain+macosMain (vs appleMain), using java.* imports in commonMain, nested module names causing AGP 9+ circular dependency

4. **Verify pinned versions** in all code samples. Replace any generic version references with specific pinned versions.

5. **Add "Key insight" one-liners** after anti-pattern pairs.

Note: `propuesta-integracion-enterprise.md` is NOT a pattern doc -- do not modify it.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc" && for f in docs/offline-first-patterns.md docs/compose-resources-patterns.md docs/resource-management-patterns.md docs/kmp-architecture.md; do echo "=== $f ===" && head -8 "$f" && echo "---" && grep -c "DON'T\|BAD:\|Anti-pattern" "$f" && echo "anti-pattern sections found"; done</automated>
  </verify>
  <done>All 4 remaining docs have standardized headers with Library Versions field, and each contains at least 2 anti-pattern sections. All 8 pattern docs now follow the same consistent structure.</done>
</task>

</tasks>

<verification>
Run across all 8 pattern docs:
1. Every doc starts with the standardized header format (Status, Last Updated, Aligned with, Platforms, Library Versions)
2. Every doc contains at least 2 anti-pattern sections (grep for "DON'T" or "BAD:")
3. Every doc has a Library Versions field in its header with specific pinned versions
4. All 7 required layers are covered (ViewModel, UI, testing, Gradle, offline-first, resources, compose resources)
</verification>

<success_criteria>
- All 8 pattern docs follow the verified pattern doc structure from research
- Each doc contains DO/DON'T pairs with explanations for all major topics
- Library versions are pinned in headers and code samples
- Docs are organized by adoption sequence (numbering/ordering reflects: architecture > gradle > viewmodel > UI > testing > compose-resources > offline-first)
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize-foundation/01-01-SUMMARY.md`
</output>
