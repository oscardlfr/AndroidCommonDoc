---
phase: 01-stabilize-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/params.json
  - skills/params.schema.json
  - scripts/ps1/run-parallel-coverage-suite.ps1
  - scripts/ps1/run-changed-modules-tests.ps1
  - scripts/sh/run-parallel-coverage-suite.sh
  - scripts/sh/run-changed-modules-tests.sh
  - Makefile
  - setup/copilot-templates/test.prompt.md
autonomous: true
requirements:
  - SCRP-01
  - SCRP-02

must_haves:
  truths:
    - "Every script uses --project-root (SH) or -ProjectRoot (PS1) -- zero instances of --project-path or -ProjectPath remain"
    - "The Makefile uses --project-root everywhere -- no --project-path references"
    - "Copilot test.prompt.md uses --project-root, not --project-path"
    - "A JSON parameter manifest exists at skills/params.json with all parameters from all 12 script pairs"
    - "Each parameter in the manifest has type, description, required, default, and mapping (ps1, sh, copilot, makefile) fields"
  artifacts:
    - path: "skills/params.json"
      provides: "Canonical parameter manifest -- single source of truth for all script interfaces"
      contains: "project-root"
    - path: "skills/params.schema.json"
      provides: "JSON Schema for parameter manifest validation"
      contains: "json-schema"
    - path: "scripts/ps1/run-parallel-coverage-suite.ps1"
      provides: "Fixed PS1 script using -ProjectRoot"
      contains: "ProjectRoot"
    - path: "scripts/sh/run-parallel-coverage-suite.sh"
      provides: "Fixed SH script using --project-root"
      contains: "project-root"
    - path: "Makefile"
      provides: "Fixed Makefile using --project-root everywhere"
      contains: "project-root"
  key_links:
    - from: "skills/params.json"
      to: "scripts/ps1/*.ps1"
      via: "mapping.ps1 field matches actual PS1 parameter names"
      pattern: "ProjectRoot"
    - from: "skills/params.json"
      to: "scripts/sh/*.sh"
      via: "mapping.sh field matches actual SH parameter names"
      pattern: "project-root"
    - from: "skills/params.json"
      to: "Makefile"
      via: "mapping.makefile field matches actual Makefile flags"
      pattern: "project-root"
---

<objective>
Create the parameter manifest as the single source of truth for all script interfaces, and fix all parameter naming drift across PS1, SH, Claude commands, Copilot prompts, and the Makefile.

Purpose: Parameter drift is the most concrete and measurable consistency problem in the project. Research confirmed 2/12 PS1 scripts use -ProjectPath, 2/12 SH scripts use --project-path, and the Makefile mixes both. This plan eliminates all drift and establishes the manifest that adapters (Plan 03-04) will consume.

Output: `skills/params.json` manifest with schema, all scripts fixed to use canonical names, Makefile consistent.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilize-foundation/01-CONTEXT.md
@.planning/phases/01-stabilize-foundation/01-RESEARCH.md

Key user decisions:
- `--project-root` wins over `--project-path` (matches PS1 -ProjectRoot convention and Gradle rootProject concept)
- Canonical case is kebab-case in manifest. Generator maps to PascalCase for PS1, kebab for SH.
- Standalone parameter manifest file as single source of truth for ALL parameters across ALL skills

Drift inventory from research:
- PS1: run-parallel-coverage-suite.ps1 and run-changed-modules-tests.ps1 use -ProjectPath (should be -ProjectRoot)
- SH: run-parallel-coverage-suite.sh and run-changed-modules-tests.sh use --project-path (should be --project-root)
- Copilot: test.prompt.md uses --project-path (should be --project-root)
- Makefile: coverage and coverage-report targets use --project-path (should be --project-root)

<interfaces>
<!-- Parameter manifest schema from research -->
From 01-RESEARCH.md:
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "parameters": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["type", "description", "mapping"],
        "properties": {
          "type": { "enum": ["string", "path", "boolean", "enum", "number"] },
          "description": { "type": "string" },
          "required": { "type": "boolean", "default": false },
          "default": {},
          "values": { "type": "array", "items": { "type": "string" } },
          "mapping": {
            "type": "object",
            "properties": {
              "ps1": { "type": "string" },
              "sh": { "type": "string" },
              "copilot": { "type": "string" },
              "makefile": { "type": "string" }
            }
          }
        }
      }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parameter manifest and JSON schema</name>
  <files>skills/params.json, skills/params.schema.json</files>
  <action>
Create the `skills/` directory at the repo root. Create two files:

**skills/params.schema.json**: The JSON Schema from research (section "Parameter Manifest Schema"). Copy it exactly from the research document.

**skills/params.json**: The canonical parameter manifest. Audit ALL 12 script pairs (PS1 + SH) plus all 16 Claude commands plus all 16 Copilot prompts to extract every unique parameter. For each parameter, define:

Required parameters to include (discovered from scripts and commands):

1. `project-root` -- path, "Path to the project root directory", default "$(pwd)", mapping: ps1="ProjectRoot", sh="--project-root", copilot="${input:projectRoot:Path to project root}", makefile="--project-root"
2. `module` -- string, "Gradle module path (e.g., core:domain, feature:home)", required false, mapping: ps1="Module", sh="--module", copilot="${input:module:Gradle module path}", makefile="--module"
3. `test-type` -- enum, values ["all", "common", "androidUnit", "androidInstrumented", "desktop"], default "all", mapping: ps1="TestType", sh="--test-type", copilot="${input:testType:all|common|androidUnit|androidInstrumented|desktop}"
4. `skip-coverage` -- boolean, default false, mapping: ps1="SkipCoverage", sh="--skip-coverage", copilot="${input:skipCoverage:true|false}"
5. `coverage-tool` -- enum, values ["jacoco", "kover", "auto", "none"], default "auto", mapping: ps1="CoverageTool", sh="--coverage-tool", copilot="${input:coverageTool:jacoco|kover|auto|none}"
6. `platform` -- enum, values ["android", "desktop", "auto"], default "auto", mapping: ps1="Platform", sh="--platform", copilot="${input:platform:android|desktop|auto}"
7. `skip-tests` -- boolean, default false, mapping: ps1="SkipTests", sh="--skip-tests"
8. `output-format` -- enum, values ["text", "json", "markdown"], default "text", mapping: ps1="OutputFormat", sh="--output-format"

Read EVERY PS1 and SH script to discover additional parameters not listed above. Common ones to look for: `--task`, `--variant`, `--device`, `--report-dir`, `--verbose`, `--clean`. Add them all to the manifest.

The manifest must include the `$schema` reference: `"$schema": "./params.schema.json"`.

Add a header comment field: `"_comment": "GENERATED SOURCE OF TRUTH -- All script parameters are defined here. Adapters read this file to generate tool-specific parameter syntax."`.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc" && test -f skills/params.json && test -f skills/params.schema.json && node -e "const p=JSON.parse(require('fs').readFileSync('skills/params.json','utf8')); const keys=Object.keys(p.parameters); console.log('Parameters:', keys.length, keys); if(!p.parameters['project-root']) throw new Error('Missing project-root'); if(!p.parameters['module']) throw new Error('Missing module'); console.log('VALID')"</automated>
  </verify>
  <done>skills/params.json exists with all discovered parameters, each having type/description/mapping fields. skills/params.schema.json validates the manifest structure. At minimum: project-root, module, test-type, skip-coverage, coverage-tool, platform are present.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all parameter naming drift across scripts, Makefile, and Copilot prompts</name>
  <files>scripts/ps1/run-parallel-coverage-suite.ps1, scripts/ps1/run-changed-modules-tests.ps1, scripts/sh/run-parallel-coverage-suite.sh, scripts/sh/run-changed-modules-tests.sh, Makefile, setup/copilot-templates/test.prompt.md</files>
  <action>
Fix every instance of parameter naming drift identified in research. Per user decision: `--project-root` is the canonical name.

**PS1 scripts to fix (replace -ProjectPath with -ProjectRoot):**
1. `scripts/ps1/run-parallel-coverage-suite.ps1` -- Find all occurrences of `ProjectPath` (in param declarations, usages, and splatting) and replace with `ProjectRoot`. Preserve all other logic.
2. `scripts/ps1/run-changed-modules-tests.ps1` -- Same: `ProjectPath` -> `ProjectRoot` everywhere.

**SH scripts to fix (replace --project-path with --project-root):**
3. `scripts/sh/run-parallel-coverage-suite.sh` -- Find all occurrences of `project-path` (in getopts/argument parsing, variable names, usage strings) and replace with `project-root`. Preserve all other logic.
4. `scripts/sh/run-changed-modules-tests.sh` -- Same: `project-path` -> `project-root` everywhere.

**Makefile to fix:**
5. `Makefile` -- Replace `--project-path` with `--project-root` in the `coverage` and `coverage-report` targets. The other targets (test, verify-kmp, build-run, sbom) already use `--project-root` -- verify they remain correct.

**Copilot prompt to fix:**
6. `setup/copilot-templates/test.prompt.md` -- Replace `--project-path` with `--project-root` in the bash implementation section. Also verify the PowerShell section uses `-ProjectRoot` (it likely already does from the existing file).

After all fixes, run a global search for any remaining `project-path` references in scripts/, Makefile, .claude/commands/, and setup/copilot-templates/ to catch any missed instances. If found, fix them.

Do NOT modify the .claude/commands/ files in this plan -- those will be regenerated from canonical definitions in Plan 04.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc" && echo "=== Checking for remaining project-path drift ===" && grep -rn "project-path\|ProjectPath" scripts/ Makefile setup/copilot-templates/ --include="*.ps1" --include="*.sh" --include="Makefile" --include="*.prompt.md" || echo "NO DRIFT FOUND -- all clean"</automated>
  </verify>
  <done>Zero instances of --project-path or -ProjectPath remain in any PS1 script, SH script, Makefile, or Copilot prompt file. All surfaces consistently use --project-root / -ProjectRoot.</done>
</task>

</tasks>

<verification>
1. `skills/params.json` is valid JSON and contains all script parameters with correct mappings
2. `skills/params.schema.json` validates the manifest structure
3. `grep -rn "project-path\|ProjectPath" scripts/ Makefile setup/copilot-templates/` returns zero results
4. All 12 script pairs still have matching parameter names between PS1 and SH versions
</verification>

<success_criteria>
- Parameter manifest exists at skills/params.json as the single source of truth
- Zero parameter naming drift across PS1, SH, Makefile, and Copilot prompts
- Every parameter has documented type, description, and per-tool mappings
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize-foundation/01-02-SUMMARY.md`
</output>
