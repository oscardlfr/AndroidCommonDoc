---
phase: 03-distribution-adoption
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - setup/setup-toolkit.sh
  - setup/setup-toolkit.ps1
  - setup/install-hooks.sh
  - setup/Install-Hooks.ps1
autonomous: false
requirements: [LINT-02, TOOL-03]
must_haves:
  truths:
    - "Running setup-toolkit.sh --project-root ../MyApp configures the full toolkit in the consuming project: build-logic includeBuild, plugin application, hooks configuration, and Copilot instructions"
    - "Individual scripts (install-hooks.sh, install-claude-skills.sh, install-copilot-prompts.sh) work independently for selective adoption"
    - "Setup scripts auto-modify consuming project build files with marker comments and .bak backups"
    - "Setup scripts are idempotent -- running twice produces the same result without duplicating entries"
    - "A consuming project after full setup can run Detekt with custom rules via Gradle and gets Claude Code hook enforcement"
  artifacts:
    - path: "setup/setup-toolkit.sh"
      provides: "Unified setup orchestrator calling individual installers + build file modification"
      min_lines: 80
    - path: "setup/setup-toolkit.ps1"
      provides: "Windows PowerShell equivalent of setup-toolkit.sh"
      min_lines: 60
    - path: "setup/install-hooks.sh"
      provides: "Installs Claude Code hooks into consuming project's .claude/settings.json"
      min_lines: 40
    - path: "setup/Install-Hooks.ps1"
      provides: "Windows PowerShell equivalent of install-hooks.sh"
      min_lines: 30
  key_links:
    - from: "setup/setup-toolkit.sh"
      to: "setup/install-claude-skills.sh"
      via: "bash invocation for Claude skills installation"
      pattern: "install-claude-skills"
    - from: "setup/setup-toolkit.sh"
      to: "setup/install-copilot-prompts.sh"
      via: "bash invocation for Copilot prompts installation"
      pattern: "install-copilot-prompts"
    - from: "setup/setup-toolkit.sh"
      to: "setup/install-hooks.sh"
      via: "bash invocation for Claude Code hooks installation"
      pattern: "install-hooks"
    - from: "setup/install-hooks.sh"
      to: ".claude/hooks/detekt-post-write.sh"
      via: "copies hook scripts to consuming project's .claude/hooks/"
      pattern: "detekt-post-write"
    - from: "setup/setup-toolkit.sh"
      to: "consuming project settings.gradle.kts"
      via: "inserts includeBuild for build-logic composite build"
      pattern: "includeBuild.*build-logic"
    - from: "setup/setup-toolkit.sh"
      to: "consuming project .github/copilot-instructions.md"
      via: "copies setup/copilot-templates/copilot-instructions-generated.md during Step 4 (Install Copilot prompts)"
      pattern: "copilot-instructions-generated"
---

<objective>
Create the unified setup-toolkit scripts and install-hooks scripts that wire everything together for consuming projects: auto-modify build files for the convention plugin, install Claude Code hooks, and orchestrate all individual installers.

Purpose: Reduces adoption to a single command (`setup-toolkit.sh --project-root ../MyApp`) while preserving selective adoption through individual scripts. This completes the distribution story for both LINT-02 (convention plugin wiring) and TOOL-03 (hooks installation).
Output: 4 scripts (SH/PS1 pairs) for hooks installation and unified toolkit setup.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-distribution-adoption/03-CONTEXT.md
@.planning/phases/03-distribution-adoption/03-RESEARCH.md
@.planning/phases/03-distribution-adoption/03-01-SUMMARY.md
@.planning/phases/03-distribution-adoption/03-02-SUMMARY.md

<interfaces>
<!-- Existing setup scripts pattern (from setup/install-claude-skills.sh) -->
Pattern: SCRIPT_DIR resolution, COMMON_DOC from parent, PARENT_DIR for sibling projects
Options: --dry-run, --force, --projects LIST, --set-env, --help
Logging: log_info (cyan), log_ok (green), log_warn (yellow), log_err (red)
Project discovery: auto-discover sibling Gradle projects or --projects flag
Summary: prints installed/skipped/errors counts

<!-- Convention plugin from Plan 01 -->
build-logic/settings.gradle.kts: rootProject.name = "build-logic"
Plugin ID: androidcommondoc.toolkit
Consuming project adds: includeBuild("../../AndroidCommonDoc/build-logic") to settings.gradle.kts
Consuming project adds: id("androidcommondoc.toolkit") to module build.gradle.kts plugins block

<!-- Hook scripts from Plan 02 -->
.claude/hooks/detekt-post-write.sh: PostToolUse hook for Write|Edit
.claude/hooks/detekt-pre-commit.sh: PreToolUse hook for Bash (git commit intercept)
Hook config in consuming project's .claude/settings.json:
```json
{
  "hooks": {
    "PostToolUse": [{
      "matcher": "Write|Edit",
      "hooks": [{"type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/detekt-post-write.sh", "timeout": 30}]
    }],
    "PreToolUse": [{
      "matcher": "Bash",
      "hooks": [{"type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/detekt-pre-commit.sh", "timeout": 60}]
    }]
  }
}
```

<!-- Existing .claude/settings.json structure in consuming projects -->
May have existing "permissions" block with deny rules. Hooks must be merged, not overwrite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create install-hooks scripts (SH/PS1 pair)</name>
  <files>setup/install-hooks.sh, setup/Install-Hooks.ps1</files>
  <action>
Create hook installation scripts following the exact pattern of existing install-claude-skills.sh and Install-CopilotPrompts.ps1:

**1. setup/install-hooks.sh**:

Same structure as install-claude-skills.sh:
- `#!/usr/bin/env bash` with `set -euo pipefail`
- SCRIPT_DIR, COMMON_DOC, PARENT_DIR resolution (same pattern)
- Options: `--dry-run`, `--force`, `--projects LIST`, `--help`, `--mode block|warn` (default: block)
- Same color logging functions (log_info, log_ok, log_warn, log_err)
- Same project discovery logic (auto-discover sibling Gradle projects)

For each discovered project:
1. **Copy hook scripts**: Copy `.claude/hooks/detekt-post-write.sh` and `.claude/hooks/detekt-pre-commit.sh` from AndroidCommonDoc to the consuming project's `.claude/hooks/` directory. Set executable permissions.

2. **Merge hooks into .claude/settings.json**: This is the critical step. The consuming project may already have a `.claude/settings.json` with permissions and potentially other hooks.
   - Use python3 for JSON merging (consistent with Phase 1/2 adapter pattern): read existing JSON, add/update the `hooks` key with PostToolUse and PreToolUse configurations, write back with 2-space indentation.
   - If `hooks` key already exists, merge arrays (don't overwrite existing hooks for other matchers).
   - If hooks for Write|Edit or Bash matchers already exist with AndroidCommonDoc commands, skip (idempotent).
   - Create `.claude/settings.json.bak` backup before modification per user decision.
   - The hook command paths use `$CLAUDE_PROJECT_DIR` variable which Claude Code resolves at runtime -- NOT absolute paths.
   - Add `"statusMessage": "Checking AndroidCommonDoc patterns..."` for user feedback.
   - Set timeouts: 30s for post-write (single file), 60s for pre-commit (staged files).

3. **Print summary** with installed/skipped/errors counts, plus a notice: "Restart Claude Code session (or use /hooks to review) for hooks to take effect" per RESEARCH.md pitfall #7.

**2. setup/Install-Hooks.ps1**:

PowerShell equivalent with same logic:
- Parameter block: `-DryRun`, `-Force`, `-Projects`, `-Mode` (default: "block")
- Same project discovery
- Copy hook scripts, merge settings.json using ConvertFrom-Json/ConvertTo-Json
- Same idempotency and backup behavior
  </action>
  <verify>
    <automated>cd /c/Users/34645/AndroidStudioProjects/AndroidCommonDoc && bash -n setup/install-hooks.sh && echo "install-hooks.sh syntax valid" && powershell.exe -NoProfile -Command "try { [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw setup/Install-Hooks.ps1), [ref]$null) | Out-Null; Write-Output 'Install-Hooks.ps1 syntax valid' } catch { Write-Output 'PS1 syntax error'; exit 1 }"</automated>
  </verify>
  <done>install-hooks.sh and Install-Hooks.ps1 exist with valid syntax, copy hook scripts to consuming projects, merge hook config into .claude/settings.json with backup and idempotency.</done>
</task>

<task type="auto">
  <name>Task 2: Create unified setup-toolkit scripts (SH/PS1 pair)</name>
  <files>setup/setup-toolkit.sh, setup/setup-toolkit.ps1</files>
  <action>
Create the unified setup orchestrator that configures everything in one command:

**1. setup/setup-toolkit.sh**:

Header and options following existing script patterns:
- `#!/usr/bin/env bash` with `set -euo pipefail`
- Options: `--project-root PATH` (required -- consuming project root), `--dry-run`, `--force`, `--skip-skills`, `--skip-copilot`, `--skip-hooks`, `--skip-gradle`, `--mode block|warn`, `--help`
- SCRIPT_DIR, COMMON_DOC resolution
- Validate --project-root exists and is a Gradle project (has settings.gradle.kts or settings.gradle)

Orchestration steps:

**Step 1: Modify settings.gradle.kts** (unless --skip-gradle):
- Create `.bak` backup of settings.gradle.kts
- Check for marker comment `// AndroidCommonDoc toolkit -- managed by setup script`. If present, skip (idempotent).
- Calculate relative path from consuming project to AndroidCommonDoc/build-logic/ (python3 one-liner using os.path.relpath)
- Insert `includeBuild("$REL_PATH")` with marker comment. Insert before the first `include(` or `includeBuild(` line, or at the end of the file if none exists. Use python3 for safe file manipulation:
  ```python
  # Read file, find insertion point, insert line, write back
  ```
- Log what was inserted

**Step 2: Modify module build.gradle.kts files** (unless --skip-gradle):
- Find all `build.gradle.kts` files in the consuming project that apply Android or KMP plugins (grep for `com.android.application`, `com.android.library`, `kotlin.multiplatform`)
- For each, create `.bak` backup
- Check for marker comment. If present, skip.
- Insert `id("androidcommondoc.toolkit")` inside the existing `plugins { }` block, just before the closing `}`. Use python3 for safe insertion.
- Log each modification

**Step 3: Install Claude skills** (unless --skip-skills):
- Call: `bash "$SCRIPT_DIR/install-claude-skills.sh" --projects "$(basename "$PROJECT_ROOT")" --force`

**Step 4: Install Copilot prompts and instructions** (unless --skip-copilot):
- Call: `bash "$SCRIPT_DIR/install-copilot-prompts.sh" --projects "$(basename "$PROJECT_ROOT")" --force`
- Copy `setup/copilot-templates/copilot-instructions-generated.md` to consuming project's `.github/copilot-instructions.md` (create `.github/` if needed, backup existing file if present). This wiring ensures Copilot users in the consuming project get the same pattern guidance as Claude Code users.

**Step 5: Install hooks** (unless --skip-hooks):
- Call: `bash "$SCRIPT_DIR/install-hooks.sh" --projects "$(basename "$PROJECT_ROOT")" --force --mode $MODE`

**Step 6: Print summary**:
- List everything that was configured
- Print "Next steps:" section:
  1. Set ANDROID_COMMON_DOC env var (if not already set)
  2. Build detekt-rules JAR: `cd $COMMON_DOC/detekt-rules && ./gradlew assemble`
  3. Sync Gradle in your project (for convention plugin)
  4. Restart Claude Code session for hooks to take effect
- Print "Selective adoption:" section listing individual scripts

**2. setup/setup-toolkit.ps1**:

PowerShell equivalent:
- Parameter block: `-ProjectRoot` (mandatory), `-DryRun`, `-Force`, `-SkipSkills`, `-SkipCopilot`, `-SkipHooks`, `-SkipGradle`, `-Mode` (default: "block")
- Same 6-step orchestration using PowerShell equivalents
- Call Install-ClaudeSkills.ps1, Install-CopilotPrompts.ps1, Install-Hooks.ps1
- Use Get-Content/Set-Content for build file modifications
- Same backup, idempotency, and summary behavior

IMPORTANT: All build file modifications use marker comments (`// AndroidCommonDoc toolkit -- managed by setup script`) for idempotency and easy identification of managed lines. All modifications create `.bak` backups per user decision.
  </action>
  <verify>
    <automated>cd /c/Users/34645/AndroidStudioProjects/AndroidCommonDoc && bash -n setup/setup-toolkit.sh && echo "setup-toolkit.sh syntax valid" && powershell.exe -NoProfile -Command "try { [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw setup/setup-toolkit.ps1), [ref]$null) | Out-Null; Write-Output 'setup-toolkit.ps1 syntax valid' } catch { Write-Output 'PS1 syntax error'; exit 1 }"</automated>
  </verify>
  <done>setup-toolkit.sh and setup-toolkit.ps1 exist with valid syntax, orchestrate all installation steps, auto-modify build files with backups and idempotency, and print comprehensive setup summary with next steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 3 toolkit distribution</name>
  <files>build-logic/, .claude/hooks/, setup/, adapters/</files>
  <action>
Human verifies the complete Phase 3 delivery across all three plans: convention plugin, hooks, adapters, and setup scripts.

What was built:
- Convention plugin (build-logic/) from Plan 01: Gradle precompiled script plugin with extension DSL
- Claude Code hooks (.claude/hooks/) from Plan 02: post-write and pre-commit enforcement
- Copilot instructions adapter (adapters/) from Plan 02: generates copilot-instructions.md from canonical docs
- Setup scripts (setup/) from this plan: install-hooks and unified setup-toolkit
  </action>
  <verify>
    <automated>cd /c/Users/34645/AndroidStudioProjects/AndroidCommonDoc && cd build-logic && ./gradlew assemble 2>&1 | tail -3 && cd .. && bash -n .claude/hooks/detekt-post-write.sh && bash -n .claude/hooks/detekt-pre-commit.sh && bash -n setup/install-hooks.sh && bash -n setup/setup-toolkit.sh && bash adapters/copilot-instructions-adapter.sh 2>&1 | tail -2 && echo "All Phase 3 artifacts verified"</automated>
  </verify>
  <done>All Phase 3 artifacts compile/validate. User has confirmed the complete toolkit distribution works end-to-end.</done>
</task>

</tasks>

<verification>
- setup/install-hooks.sh copies hook scripts and merges .claude/settings.json in consuming projects
- setup/setup-toolkit.sh orchestrates all 5 installation steps (gradle, skills, copilot, hooks, instructions)
- setup/setup-toolkit.sh copies copilot-instructions-generated.md to consuming project .github/copilot-instructions.md
- Both .sh scripts pass `bash -n` syntax check
- Both .ps1 scripts parse without errors
- Build file modifications use marker comments for idempotency
- Build file modifications create .bak backups
- Setup summary prints clear next steps
- Individual scripts (install-hooks, install-skills, install-copilot-prompts) still work independently
</verification>

<success_criteria>
- `setup-toolkit.sh --help` prints usage information
- `setup-toolkit.sh --project-root ../SomeProject --dry-run` shows planned modifications without writing
- `install-hooks.sh --dry-run` shows hook installation plan
- Scripts follow existing patterns (colors, logging, project discovery, summary format)
- PS1 equivalents exist for Windows users
- All build file modifications are reversible (backups created)
</success_criteria>

<output>
After completion, create `.planning/phases/03-distribution-adoption/03-03-SUMMARY.md`
</output>
