---
phase: 04-audit-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/settings.json
  - .claude/agents/template-sync-validator.md
  - setup/install-copilot-prompts.sh
  - docs/gradle-patterns.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "Contributors to AndroidCommonDoc get real-time Detekt enforcement via Claude Code hooks"
    - "template-sync-validator checks the generated copilot-instructions file, not the static template"
    - "Only setup-toolkit.sh writes copilot-instructions.md to consuming projects -- no duplicate write path"
    - "gradle-patterns.md version label matches the freshness manifest and passes freshness checks"
    - "ROADMAP.md accurately reflects Plan 03-04 completion"
    - "All 11 SUMMARY files have correct requirements_completed metadata"
  artifacts:
    - path: ".claude/settings.json"
      provides: "Hook self-registration for AndroidCommonDoc repo"
      contains: "PostToolUse"
    - path: ".claude/agents/template-sync-validator.md"
      provides: "Corrected Step 4 reference"
      contains: "copilot-instructions-generated.md"
    - path: "setup/install-copilot-prompts.sh"
      provides: "Cleaned script without copilot-instructions.md handling"
    - path: "docs/gradle-patterns.md"
      provides: "Corrected Compose Gradle Plugin version label"
      contains: "Compose Gradle Plugin 1.10.0"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Plan 03-04 checkbox"
      contains: "[x] 03-04-PLAN.md"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/detekt-post-write.sh"
      via: "PostToolUse hook command path"
      pattern: "detekt-post-write\\.sh"
    - from: ".claude/settings.json"
      to: ".claude/hooks/detekt-pre-commit.sh"
      via: "PreToolUse hook command path"
      pattern: "detekt-pre-commit\\.sh"
    - from: ".claude/agents/template-sync-validator.md"
      to: "setup/copilot-templates/copilot-instructions-generated.md"
      via: "Step 4 file reference"
      pattern: "copilot-instructions-generated\\.md"
    - from: "docs/gradle-patterns.md"
      to: "versions-manifest.json"
      via: "Library label maps to manifest key via check-doc-freshness.sh LIB_MAP"
      pattern: "Compose Gradle Plugin 1\\.10\\.0"
---

<objective>
Close all integration wiring gaps (INT-01 through INT-04) from the v1.0 milestone audit, update ROADMAP admin items, and verify SUMMARY metadata completeness.

Purpose: Harden v1.0 deliverables so every E2E flow works correctly, the self-enforcement dogfooding loop is complete, and project metadata is accurate.
Output: 5 files edited (settings.json, template-sync-validator.md, install-copilot-prompts.sh, gradle-patterns.md, ROADMAP.md), all 6 success criteria passing.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-audit-tech-debt-cleanup/04-CONTEXT.md
@.planning/phases/04-audit-tech-debt-cleanup/04-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Current state of .claude/settings.json (entire file -- will be rewritten): -->
```json
{
  "permissions": {
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force *)",
      "Bash(git clean -f *)",
      "Bash(git merge *)",
      "Bash(git checkout main)"
    ]
  }
}
```

<!-- Target state from RESEARCH.md Pattern 1 (add hooks section): -->
```json
{
  "permissions": {
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force *)",
      "Bash(git clean -f *)",
      "Bash(git merge *)",
      "Bash(git checkout main)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/detekt-post-write.sh",
            "timeout": 30
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/detekt-pre-commit.sh",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
```

<!-- template-sync-validator.md Step 4 (line 55) -- current: -->
`setup/copilot-templates/copilot-instructions.md`
<!-- target: -->
`setup/copilot-templates/copilot-instructions-generated.md`

<!-- install-copilot-prompts.sh -- two blocks to remove: -->
Lines 132-137: HAS_COPILOT_INSTRUCTIONS detection block
Lines 216-233: "Install copilot-instructions.md" block inside project loop

<!-- gradle-patterns.md line 7 -- current: -->
`Compose Multiplatform 1.10.0`
<!-- target: -->
`Compose Gradle Plugin 1.10.0`

<!-- ROADMAP.md line 73 -- current: -->
`- [ ] 03-04-PLAN.md`
<!-- target: -->
`- [x] 03-04-PLAN.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix integration wiring (INT-01 through INT-04)</name>
  <files>.claude/settings.json, .claude/agents/template-sync-validator.md, setup/install-copilot-prompts.sh, docs/gradle-patterns.md</files>
  <action>
Four targeted edits addressing each integration finding:

**INT-01 -- Hook self-registration in settings.json:**
Rewrite `.claude/settings.json` to add the `hooks` section alongside the existing `permissions` block. Use BOTH PostToolUse and PreToolUse per research recommendation (dogfooding principle -- toolkit repo should enforce its own patterns). The exact target JSON structure is provided in the interfaces block above. Preserve the existing `permissions.deny` array exactly as-is. The hook command paths use `$CLAUDE_PROJECT_DIR` environment variable (same pattern as `setup/install-hooks.sh` lines 191-206).

**INT-02 -- Fix template-sync-validator Step 4 reference:**
In `.claude/agents/template-sync-validator.md`, line 55, change the file reference from `setup/copilot-templates/copilot-instructions.md` to `setup/copilot-templates/copilot-instructions-generated.md`. Also check line 64 for a similar reference (`copilot-instructions.md doesn't list`) and update if it refers to the same static file. This aligns the validator with Phase 1's adapter pattern where generated output is canonical.

**INT-03 -- Remove duplicate copilot-instructions.md write path:**
In `setup/install-copilot-prompts.sh`, remove two specific blocks:
1. Lines 132-137: The `HAS_COPILOT_INSTRUCTIONS` detection block (from `# Check for copilot-instructions.md` through the closing `fi`)
2. Lines 216-233: The `# Install copilot-instructions.md` block inside the project loop (from `# Install copilot-instructions.md` through the closing `fi` of `if [ "$HAS_COPILOT_INSTRUCTIONS" = true ]`)
Per user decision: `setup-toolkit.sh` owns the `.github/copilot-instructions.md` write (it copies `copilot-instructions-generated.md`). `install-copilot-prompts.sh` keeps handling only `.prompt.md` files and `instructions/*.instructions.md` files. After removal, verify no remaining references to `HAS_COPILOT_INSTRUCTIONS` variable or `copilot-instructions.md` file in the script (except comments if any).

**INT-04 -- Fix version label in gradle-patterns.md:**
In `docs/gradle-patterns.md`, line 7, change `Compose Multiplatform 1.10.0` to `Compose Gradle Plugin 1.10.0`. The freshness script's LIB_MAP (line 85 of check-doc-freshness.sh) already maps "compose gradle plugin" to key "compose-gradle-plugin", and versions-manifest.json has "compose-gradle-plugin": "1.10.0" -- so this fix will resolve the [STALE] flag.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('.claude/settings.json')); assert 'PostToolUse' in d['hooks'] and 'PreToolUse' in d['hooks'], 'Missing hooks'" && grep -q "copilot-instructions-generated.md" .claude/agents/template-sync-validator.md && bash -n setup/install-copilot-prompts.sh && ! grep -q "HAS_COPILOT_INSTRUCTIONS" setup/install-copilot-prompts.sh && grep -q "Compose Gradle Plugin 1.10.0" docs/gradle-patterns.md</automated>
  </verify>
  <done>
- settings.json has PostToolUse and PreToolUse hook entries pointing to existing hook scripts
- template-sync-validator Step 4 references copilot-instructions-generated.md
- install-copilot-prompts.sh passes bash -n syntax check and has no copilot-instructions.md handling
- gradle-patterns.md reads "Compose Gradle Plugin 1.10.0"
  </done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP admin update and SUMMARY metadata verification</name>
  <files>.planning/ROADMAP.md</files>
  <action>
**ROADMAP admin:**
In `.planning/ROADMAP.md`, line 73, change `- [ ] 03-04-PLAN.md` to `- [x] 03-04-PLAN.md`. This reflects that Plan 03-04 (gap closure: testConfig afterEvaluate fix) was already executed and verified. Phase 3 status in the progress table (line 101) already shows "Complete" -- verify this is still the case and leave it unchanged.

**SUMMARY metadata verification (NOT implementation):**
Research confirmed all 11 SUMMARY files already have populated `requirements-completed` frontmatter. Verify this by running grep across all SUMMARY files and confirming:
- 01-01-SUMMARY.md: `[PTRN-01, PTRN-02]`
- 01-02-SUMMARY.md: `[SCRP-01, SCRP-02]`
- 01-03-SUMMARY.md: `[TOOL-01]`
- 01-04-SUMMARY.md: `[TOOL-01, TOOL-02]`
- 02-01-SUMMARY.md: `[LINT-01, LINT-03]`
- 02-02-SUMMARY.md: `[PTRN-03]`
- 02-03-SUMMARY.md: `[SCRP-03, QUAL-01, QUAL-02, QUAL-03]`
- 03-01-SUMMARY.md: `[LINT-02]`
- 03-02-SUMMARY.md: `[TOOL-03]`
- 03-03-SUMMARY.md: `[LINT-02, TOOL-03]`
- 03-04-SUMMARY.md: `[LINT-02, TOOL-03]`

If any file is missing or has incorrect values, fix it. If all match, report verification passed with no changes needed.
  </action>
  <verify>
    <automated>grep "\[x\] 03-04-PLAN" .planning/ROADMAP.md && test $(grep -rl "requirements-completed:" .planning/phases/*/ | wc -l) -eq 11</automated>
  </verify>
  <done>
- ROADMAP.md Plan 03-04 checkbox is checked [x]
- All 11 SUMMARY files confirmed to have populated requirements-completed frontmatter
  </done>
</task>

</tasks>

<verification>
Run all 6 success criteria as a single verification pass:

1. `python3 -c "import json; d=json.load(open('.claude/settings.json')); assert 'PostToolUse' in d['hooks'] and 'PreToolUse' in d['hooks']"` -- SC-1
2. `grep -q 'copilot-instructions-generated.md' .claude/agents/template-sync-validator.md` -- SC-2
3. `! grep -q 'HAS_COPILOT_INSTRUCTIONS' setup/install-copilot-prompts.sh` -- SC-3
4. `grep -q 'Compose Gradle Plugin 1.10.0' docs/gradle-patterns.md` -- SC-4
5. `grep -q '\[x\] 03-04-PLAN' .planning/ROADMAP.md` -- SC-5
6. `test $(grep -rl 'requirements-completed:' .planning/phases/*/ | wc -l) -eq 11` -- SC-6

Optional freshness validation: `bash scripts/sh/check-doc-freshness.sh` should no longer flag gradle-patterns.md as [STALE] for the Compose version.
</verification>

<success_criteria>
All 6 success criteria from ROADMAP.md Phase 4 pass:
1. settings.json has PostToolUse and PreToolUse hook entries
2. template-sync-validator Step 4 references copilot-instructions-generated.md
3. install-copilot-prompts.sh has a single consolidated copilot-instructions.md write path (via setup-toolkit.sh only)
4. gradle-patterns.md says "Compose Gradle Plugin 1.10.0" and freshness check passes
5. ROADMAP Plan 03-04 checkbox is checked
6. All 11 SUMMARY files have populated requirements_completed frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-tech-debt-cleanup/04-01-SUMMARY.md`
</output>
