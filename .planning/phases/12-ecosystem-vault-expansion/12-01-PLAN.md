---
phase: 12-ecosystem-vault-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ECOV-01
  - ECOV-02
  - ECOV-03

must_haves:
  truths:
    - "ECOV-01 through ECOV-07 requirement definitions exist in REQUIREMENTS.md"
    - "shared-kmp-libs documentation landscape is inventoried with findings"
    - "DawSync docs are audited for L0 promotion candidates"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ECOV requirement definitions"
      contains: "ECOV-01"
    - path: ".planning/phases/12-ecosystem-vault-expansion/12-DOC-AUDIT.md"
      provides: "Documentation layer audit report"
      min_lines: 30
  key_links: []
---

<objective>
Audit the documentation landscape across DawSync, shared-kmp-libs, and AndroidCommonDoc to understand what docs exist, identify layer misplacements, and formally define ECOV requirements in REQUIREMENTS.md.

Purpose: The audit informs the collector's collection configuration (what globs, what exclusions, what layer assignments). Without knowing what actually exists in the target repos, the collector refactoring would be done blind. Also formalizes ECOV requirements since they only exist in ROADMAP.md success criteria.
Output: 12-DOC-AUDIT.md report with findings + updated REQUIREMENTS.md with ECOV definitions
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-ecosystem-vault-expansion/12-CONTEXT.md
@.planning/phases/12-ecosystem-vault-expansion/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentation landscape audit across target repositories</name>
  <files>.planning/phases/12-ecosystem-vault-expansion/12-DOC-AUDIT.md</files>
  <action>
Scan the following three repositories and produce a structured audit report:

1. **shared-kmp-libs** (C:\Users\34645\AndroidStudioProjects\shared-kmp-libs):
   - List all .md files at root (CLAUDE.md, README.md, etc.)
   - List all files in docs/ directory
   - List all module-level READMEs (e.g., core-network-api/README.md)
   - Check for .planning/ directory (likely absent per research)
   - Check for .androidcommondoc/ directory (L1 overrides)
   - Note: This is the L1 ecosystem layer -- all docs here go to L1-ecosystem/

2. **DawSync** (C:\Users\34645\AndroidStudioProjects\DawSync):
   - List all .md files in docs/ (excluding docs/archive/)
   - List .planning/codebase/*.md architecture docs
   - Check CLAUDE.md, AGENTS.md at root
   - Check .androidcommondoc/docs/ for existing L1 overrides
   - Identify any docs in DawSync/docs/ that look like they should be L0 generic patterns (e.g., patterns covering Kotlin, Compose, or KMP that aren't DawSync-specific)
   - Check for sub-project directories: SessionRecorder-VST3 (nested) and DawSyncWeb (sibling at C:\Users\34645\AndroidStudioProjects\DawSyncWeb)
   - For each sub-project: list .md files, note build system type

3. **AndroidCommonDoc** (current repo):
   - List docs/ pattern docs (already known as L0)
   - List skills/ directories
   - List .planning/ relevant files (PROJECT.md, STATE.md)
   - Check CLAUDE.md, AGENTS.md

For each repository, produce:
- File inventory (path, approximate line count, classification: docs/ai/planning/architecture)
- Layer assignment recommendation (L0/L1/L2)
- Recommended collectGlobs and excludeGlobs
- Any L0 promotion candidates (DawSync docs that should be generic patterns)
- Any gaps (expected docs that don't exist)

Format the report as a structured markdown file at `.planning/phases/12-ecosystem-vault-expansion/12-DOC-AUDIT.md` with sections per repository, a summary table, and recommended vault-config.json project entries.

Do NOT move any files between repos -- this is a read-only audit producing a report.
  </action>
  <verify>
    <automated>test -f ".planning/phases/12-ecosystem-vault-expansion/12-DOC-AUDIT.md" && grep -c "shared-kmp-libs\|DawSync\|AndroidCommonDoc" ".planning/phases/12-ecosystem-vault-expansion/12-DOC-AUDIT.md" | xargs test 3 -le</automated>
  </verify>
  <done>Audit report exists with inventory of all three repositories, layer assignments, and recommended collection globs per project</done>
</task>

<task type="auto">
  <name>Task 2: Add ECOV requirement definitions to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Add the ECOV (Ecosystem Vault Expansion) requirements section to REQUIREMENTS.md under a new "### Ecosystem Vault Expansion" heading, after the existing "### Obsidian Documentation Hub" section.

Add these 7 requirements (derived from ROADMAP.md Phase 12 success criteria):

- **ECOV-01**: shared-kmp-libs conventions and documentation collected as L1 ecosystem layer, stored under L1-ecosystem/ in the vault separately from L0 generic patterns
- **ECOV-02**: App-specific docs (DawSync/docs/, architecture docs, AI instructions) collected as L2 with domain tagging, stored under L2-apps/{project}/ with docs/ai/planning subdivision
- **ECOV-03**: Architecture docs (.planning/codebase/) collected per project with vault_type "architecture" for cross-project reference
- **ECOV-04**: Sub-project support (auto-detect + config) enables nested projects (e.g., DawSync/SessionRecorder-VST3) to appear as sub-projects/ under their parent in the vault
- **ECOV-05**: Vault output structure uses layer-first top-level directories (L0-generic/, L1-ecosystem/, L2-apps/) with clean-slate migration from the flat structure
- **ECOV-06**: MOC pages updated with ecosystem-aware groupings: descriptive sublabels per layer, L2 grouped by project, sub-project nesting, auto-generated Home.md entry point
- **ECOV-07**: Configurable collection globs per project in vault-config.json via rich ProjectConfig objects (name, path, layer, collectGlobs, excludeGlobs, features, subProjects)

Also update the Traceability table to add ECOV-01 through ECOV-07 mapped to Phase 12 with status "Planned". Update the coverage count.

Mark all as `- [ ]` (unchecked) since they're not yet implemented.
  </action>
  <verify>
    <automated>grep -c "ECOV-0[1-7]" ".planning/REQUIREMENTS.md" | xargs test 14 -le</automated>
  </verify>
  <done>REQUIREMENTS.md contains all 7 ECOV requirement definitions under Ecosystem Vault Expansion heading, plus traceability entries mapping each to Phase 12</done>
</task>

</tasks>

<verification>
- 12-DOC-AUDIT.md exists with structured findings for all three target repos
- REQUIREMENTS.md has ECOV-01 through ECOV-07 with traceability to Phase 12
- No files in other repos were modified
</verification>

<success_criteria>
- Documentation landscape across shared-kmp-libs, DawSync, and AndroidCommonDoc is inventoried
- Recommended collection configurations (globs, excludes, layer assignments) are documented
- ECOV requirements are formally defined in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/12-ecosystem-vault-expansion/12-01-SUMMARY.md`
</output>
