---
phase: 12-ecosystem-vault-expansion
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - mcp-server/tests/unit/vault/collector.test.ts
  - mcp-server/tests/unit/vault/config.test.ts
  - mcp-server/tests/unit/vault/transformer.test.ts
  - mcp-server/tests/unit/vault/moc-generator.test.ts
  - mcp-server/tests/unit/vault/vault-writer.test.ts
  - mcp-server/tests/unit/vault/tag-generator.test.ts
  - mcp-server/tests/unit/vault/wikilink-generator.test.ts
  - mcp-server/tests/unit/vault/sub-project-detector.test.ts
  - mcp-server/tests/unit/vault/glob-expander.test.ts
  - mcp-server/tests/integration/vault-sync.test.ts
autonomous: true
requirements:
  - ECOV-01
  - ECOV-02
  - ECOV-03
  - ECOV-04
  - ECOV-05
  - ECOV-06
  - ECOV-07

must_haves:
  truths:
    - "All 10 test files exist with describe blocks and .todo() placeholder tests"
    - "Each stub test file imports from the correct source module"
    - "Running vitest on the stub files produces 'todo' results (not errors)"
    - "Test stubs define the behavior contracts that Plans 02-06 must satisfy"
  artifacts:
    - path: "mcp-server/tests/unit/vault/collector.test.ts"
      provides: "Stub tests for L0/L1/L2 collection, architecture docs, glob-based collection"
      min_lines: 30
    - path: "mcp-server/tests/unit/vault/config.test.ts"
      provides: "Stub tests for ProjectConfig schema, defaults, migration from old format"
      min_lines: 20
    - path: "mcp-server/tests/unit/vault/transformer.test.ts"
      provides: "Stub tests for layer-first paths, slug disambiguation, vault_source_path"
      min_lines: 20
    - path: "mcp-server/tests/unit/vault/moc-generator.test.ts"
      provides: "Stub tests for Home.md, ecosystem groupings, 7 MOC pages"
      min_lines: 20
    - path: "mcp-server/tests/unit/vault/vault-writer.test.ts"
      provides: "Stub tests for clean-slate migration, layer-first directories"
      min_lines: 20
    - path: "mcp-server/tests/unit/vault/tag-generator.test.ts"
      provides: "Stub tests for architecture/ecosystem/app/sub-project tags"
      min_lines: 15
    - path: "mcp-server/tests/unit/vault/wikilink-generator.test.ts"
      provides: "Stub tests for display-text format with project-prefixed slugs"
      min_lines: 15
    - path: "mcp-server/tests/unit/vault/sub-project-detector.test.ts"
      provides: "Stub tests for cross-tech detection, Gradle sub-module exclusion"
      min_lines: 20
    - path: "mcp-server/tests/unit/vault/glob-expander.test.ts"
      provides: "Stub tests for **, *, literal, exclude, deduplication patterns"
      min_lines: 20
    - path: "mcp-server/tests/integration/vault-sync.test.ts"
      provides: "Stub tests for full L0/L1/L2 pipeline end-to-end"
      min_lines: 20
  key_links: []
---

<objective>
Create stub test files for all 10 vault test files required by Phase 12. Each stub defines the behavioral contracts (as .todo() tests) that the implementation plans (02-06) must satisfy. This is the Nyquist Wave 0 -- test scaffolds exist before any production code changes.

Purpose: Ensures every implementation task in Plans 02-06 has a corresponding test file that can be run. The stubs use Vitest's `it.todo()` to declare expected behaviors without implementation. As Plans 02-06 execute, Plan 07 rewrites these stubs into full tests. The stubs also serve as living documentation of what each module must do.
Output: 10 test stub files (7 rewrites of existing Phase 11 tests + 2 new files + 1 integration rewrite)
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ecosystem-vault-expansion/12-CONTEXT.md
@.planning/phases/12-ecosystem-vault-expansion/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub test files for vault unit tests (7 rewrites + 2 new)</name>
  <files>mcp-server/tests/unit/vault/config.test.ts, mcp-server/tests/unit/vault/collector.test.ts, mcp-server/tests/unit/vault/transformer.test.ts, mcp-server/tests/unit/vault/tag-generator.test.ts, mcp-server/tests/unit/vault/wikilink-generator.test.ts, mcp-server/tests/unit/vault/moc-generator.test.ts, mcp-server/tests/unit/vault/vault-writer.test.ts, mcp-server/tests/unit/vault/glob-expander.test.ts, mcp-server/tests/unit/vault/sub-project-detector.test.ts</files>
  <action>
Create 9 stub test files. Each file must:
- Use `describe` / `it.todo()` pattern (Vitest globals mode)
- NOT import from source modules yet (the source files will be rewritten in Plans 02-06, imports would fail)
- Define the behavioral contracts as todo descriptions
- Be syntactically valid TypeScript that Vitest can parse without errors

**glob-expander.test.ts** (NEW):
```typescript
describe("glob-expander", () => {
  describe("expandGlobs", () => {
    it.todo("literal pattern 'CLAUDE.md' matches only root CLAUDE.md");
    it.todo("wildcard 'docs/*.md' matches docs/foo.md but not docs/sub/bar.md");
    it.todo("double-star 'docs/**/*.md' matches nested files at any depth");
    it.todo("exclude patterns filter out matching files");
    it.todo("multiple include globs combine results with deduplication");
    it.todo("empty include globs returns empty array");
    it.todo("non-existent directory returns empty array");
    it.todo("paths normalized to forward slashes");
  });
});
```

**sub-project-detector.test.ts** (NEW):
```typescript
describe("sub-project-detector", () => {
  describe("detectSubProjects", () => {
    it.todo("CMakeLists.txt in Gradle parent detected as sub-project");
    it.todo("package.json in Gradle parent detected as sub-project");
    it.todo(".git directory detected as independent sub-project");
    it.todo("build.gradle.kts in Gradle parent NOT detected (Gradle sub-module)");
    it.todo("maxDepth=1 only scans immediate children");
    it.todo("skips known non-project directories (build, node_modules, .gradle)");
    it.todo("empty directory returns empty array");
  });
});
```

**config.test.ts** (REWRITE -- replace Phase 11 tests with Phase 12 stubs):
```typescript
describe("vault config", () => {
  describe("getDefaultConfig", () => {
    it.todo("returns version: 1");
    it.todo("returns empty projects array (ProjectConfig[])");
    it.todo("does not include includeTemplates field");
    it.todo("autoClean defaults to false");
  });
  describe("getDefaultGlobs", () => {
    it.todo("returns standard doc patterns for L1");
    it.todo("returns standard doc patterns for L2");
  });
  describe("getDefaultExcludes", () => {
    it.todo("includes build, node_modules, .gradle, dist, archive patterns");
    it.todo("excludes .planning/phases/** but not .planning/codebase/**");
  });
  describe("loadVaultConfig", () => {
    it.todo("loads new ProjectConfig schema from file");
    it.todo("old string[] format returns defaults with warning");
    it.todo("missing version field treated as old format");
  });
  describe("saveVaultConfig", () => {
    it.todo("writes version: 1 to output file");
  });
});
```

**collector.test.ts** (REWRITE):
```typescript
describe("vault collector", () => {
  describe("collectL0Sources", () => {
    it.todo("collects pattern docs with layer L0 and L0-generic/patterns/ path");
    it.todo("collects skills with L0-generic/skills/ path");
    it.todo("collects toolkit CLAUDE.md under L0-generic/AndroidCommonDoc/ai/");
    it.todo("collects .planning/codebase/ as architecture sourceType");
  });
  describe("collectProjectSources", () => {
    it.todo("L1 project routes to L1-ecosystem/{name}/ paths");
    it.todo("L2 project routes to L2-apps/{name}/ paths");
    it.todo("CLAUDE.md classified as sourceType claude-md in ai/ subdivision");
    it.todo("docs/*.md classified as sourceType docs in docs/ subdivision");
    it.todo(".planning/codebase/ classified as architecture in planning/ subdivision");
    it.todo("custom collectGlobs override default patterns");
    it.todo("excludeGlobs filter out matching files");
  });
  describe("collectAll", () => {
    it.todo("combines L0 + project sources");
    it.todo("auto-discovers projects when config.projects is empty");
    it.todo("collects sub-project sources when configured");
    it.todo("runs detectSubProjects when subProjectScanDepth > 0");
  });
});
```

**transformer.test.ts** (REWRITE):
```typescript
describe("vault transformer", () => {
  describe("transformSource", () => {
    it.todo("L0 source gets bare slug without project prefix");
    it.todo("L1 source gets project-prefixed slug");
    it.todo("L2 source gets project-prefixed slug");
    it.todo("layer field populated on VaultEntry from source");
    it.todo("vault_source_path added to enriched frontmatter");
    it.todo("architecture sourceType mapped correctly in VAULT_TYPE_MAP");
  });
  describe("transformAll", () => {
    it.todo("all slugs are unique across layers");
    it.todo("wikilinks injected using layer-qualified slug pool");
  });
});
```

**tag-generator.test.ts** (REWRITE):
```typescript
describe("vault tag-generator", () => {
  it.todo("architecture tag added for architecture sourceType");
  it.todo("ecosystem tag added for L1 layer");
  it.todo("app tag added for L2 layer");
  it.todo("sub-project tag added when subProject present");
  it.todo("existing scope/targets/layer/project tags preserved");
});
```

**wikilink-generator.test.ts** (REWRITE):
```typescript
describe("vault wikilink-generator", () => {
  it.todo("project-prefixed slugs use display-text format [[slug|displayName]]");
  it.todo("bare L0 slugs produce [[slug]] format");
  it.todo("code blocks not modified");
  it.todo("inline code not modified");
  it.todo("existing wikilinks not double-wrapped");
});
```

**moc-generator.test.ts** (REWRITE):
```typescript
describe("vault moc-generator", () => {
  describe("generateAllMOCs", () => {
    it.todo("returns 7 MOC entries (was 6)");
  });
  describe("generateHomeMOC", () => {
    it.todo("Home.md contains layer count table");
    it.todo("Home.md contains MOC navigation links");
    it.todo("Home.md includes last sync date");
  });
  describe("generateByLayerMOC", () => {
    it.todo("has descriptive sublabels: L0 -- Generic Patterns");
    it.todo("has descriptive sublabels: L1 -- Ecosystem (shared-kmp-libs)");
    it.todo("has descriptive sublabels: L2 -- App-Specific");
    it.todo("L2 entries grouped by project with sub-headers");
    it.todo("sub-projects listed under parent project");
  });
  describe("generateByProjectMOC", () => {
    it.todo("layer annotations after project headers");
    it.todo("sub-projects in their own sub-section");
  });
  describe("generateAllDecisionsMOC", () => {
    it.todo("includes architecture sourceType entries");
  });
});
```

**vault-writer.test.ts** (REWRITE):
```typescript
describe("vault writer", () => {
  describe("migrateToLayerFirst", () => {
    it.todo("detects old flat structure and removes patterns/, skills/, projects/");
    it.todo("preserves .obsidian/ during migration");
    it.todo("returns false when no old structure exists");
  });
  describe("writeVault", () => {
    it.todo("creates layer-first directory structure (L0-generic, L1-ecosystem, L2-apps)");
    it.todo("manifest entries include layer field");
    it.todo(".obsidian/ only created on init, not on sync");
  });
});
```
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/vault/ --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>All 9 unit test stub files exist with .todo() tests. Vitest runs them without parse errors, showing todo count.</done>
</task>

<task type="auto">
  <name>Task 2: Create stub integration test for layer-first e2e pipeline</name>
  <files>mcp-server/tests/integration/vault-sync.test.ts</files>
  <action>
Rewrite `mcp-server/tests/integration/vault-sync.test.ts` as a stub with .todo() tests for the layer-first pipeline. Same approach as Task 1 -- no source imports, just behavioral contracts.

```typescript
describe("vault-sync integration (layer-first)", () => {
  describe("initVault", () => {
    it.todo("creates .obsidian/ directory with config files");
    it.todo("creates _vault-meta/ with manifest and README");
    it.todo("creates L0-generic/ with patterns/ and skills/ subdirs");
    it.todo("creates L1-ecosystem/{project}/ with ai/ and docs/ subdirs");
    it.todo("creates L2-apps/{project}/ with ai/, docs/, planning/ subdirs");
    it.todo("auto-detects sub-projects under L2 parent");
    it.todo("creates 00-MOC/ with Home.md and 6 other MOC files");
    it.todo("SyncResult.written > 0 and errors empty");
  });
  describe("syncVault (incremental)", () => {
    it.todo("unchanged files report SyncResult.unchanged > 0");
    it.todo("SyncResult.written is 0 or minimal on no-change sync");
  });
  describe("getVaultStatus", () => {
    it.todo("configured: true after init");
    it.todo("fileCount > 0");
    it.todo("layers.L0 > 0, layers.L1 > 0, layers.L2 > 0");
    it.todo("projects includes configured project names");
  });
});
```
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/integration/vault-sync.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Integration test stub exists with .todo() tests for the full layer-first pipeline. Vitest runs without errors.</done>
</task>

</tasks>

<verification>
- All 10 test files exist: `ls mcp-server/tests/unit/vault/*.test.ts mcp-server/tests/integration/vault-sync.test.ts`
- Vitest parses all files without errors: `cd mcp-server && npx vitest run tests/unit/vault/ tests/integration/vault-sync.test.ts`
- Todo count matches expected behavioral contracts (50+ todos across all files)
</verification>

<success_criteria>
- 10 test stub files created (9 unit + 1 integration)
- All stubs use .todo() pattern (no failing tests, just placeholders)
- Vitest can run and report the todo count without errors
- Behavioral contracts cover all ECOV requirements
</success_criteria>

<output>
After completion, create `.planning/phases/12-ecosystem-vault-expansion/12-00-SUMMARY.md`
</output>
