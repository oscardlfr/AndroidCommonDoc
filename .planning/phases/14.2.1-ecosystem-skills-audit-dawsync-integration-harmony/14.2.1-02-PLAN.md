---
phase: 14.2.1-ecosystem-skills-audit-dawsync-integration-harmony
plan: 02
type: execute
wave: 2
depends_on: [14.2.1-01]
files_modified:
  - skills/accessibility/SKILL.md
  - skills/android-test/SKILL.md
  - skills/auto-cover/SKILL.md
  - skills/best-practices/SKILL.md
  - skills/core-web-vitals/SKILL.md
  - skills/coverage/SKILL.md
  - skills/coverage-full/SKILL.md
  - skills/doc-reorganize/SKILL.md
  - skills/extract-errors/SKILL.md
  - skills/generate-rules/SKILL.md
  - skills/ingest-content/SKILL.md
  - skills/monitor-docs/SKILL.md
  - skills/performance/SKILL.md
  - skills/run/SKILL.md
  - skills/sbom/SKILL.md
  - skills/sbom-analyze/SKILL.md
  - skills/sbom-scan/SKILL.md
  - skills/seo/SKILL.md
  - skills/sync-vault/SKILL.md
  - skills/sync-versions/SKILL.md
  - skills/test/SKILL.md
  - skills/test-changed/SKILL.md
  - skills/test-full/SKILL.md
  - skills/test-full-parallel/SKILL.md
  - skills/validate-patterns/SKILL.md
  - skills/verify-kmp/SKILL.md
  - skills/web-quality-audit/SKILL.md
autonomous: true
requirements: [SKILL-03]
must_haves:
  truths:
    - "Every L0 skill has name field (lowercase, hyphens only)"
    - "Every L0 skill has description field (1-2 sentences, specific trigger conditions)"
    - "Every L0 skill has allowed-tools field restricting tool access"
    - "Side-effect skills (deploy, commit, sync) have disable-model-invocation: true"
    - "Background knowledge skills have user-invocable: false where appropriate"
    - "No skill uses legacy metadata block instead of standard frontmatter"
    - "Skill descriptions fit within 2% context budget (concise, not verbose)"
  artifacts:
    - path: "skills/test/SKILL.md"
      provides: "Modernized test skill with full frontmatter"
      contains: "allowed-tools"
    - path: "skills/sync-vault/SKILL.md"
      provides: "Modernized sync-vault skill with disable-model-invocation"
      contains: "disable-model-invocation: true"
    - path: "skills/accessibility/SKILL.md"
      provides: "Modernized accessibility skill (formerly .agents/skills/)"
      contains: "allowed-tools"
  key_links:
    - from: "skills/*/SKILL.md"
      to: "Claude Code skill discovery"
      via: "frontmatter name + description fields"
      pattern: "name:.*description:"
---

<objective>
Rewrite all 27 L0 skills to current Claude Code 2026 frontmatter best practices.

Purpose: Current skills use legacy `metadata` blocks and lack critical fields (allowed-tools, disable-model-invocation). Modernized skills get proper discovery, tool restrictions, and context budget management.
Output: All 27 skills with standardized frontmatter: name, description, allowed-tools, and disable-model-invocation/user-invocable where appropriate.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-CONTEXT.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-RESEARCH.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-01-SUMMARY.md

<interfaces>
<!-- Current Claude Code 2026 skill frontmatter fields (from research) -->
Required fields:
- name: lowercase letters, numbers, hyphens only (max 64 chars)
- description: 1-2 sentences, Claude uses this to decide when to auto-load

Optional fields:
- allowed-tools: restricts tool access when skill is active (e.g., Bash, Read, Grep, Glob)
- disable-model-invocation: true for side-effect skills (deploy, commit, sync, vault operations)
- user-invocable: false for background knowledge Claude should know but users shouldn't invoke
- context: fork for isolated subagent execution
- model: override model for this skill

Fields to REMOVE (legacy):
- metadata: { author, version, params } -- replace with standard frontmatter fields
- license: -- not a Claude Code field
- $ARGUMENTS can stay in body for argument substitution
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite existing 21 L0 skills frontmatter</name>
  <files>skills/android-test/SKILL.md, skills/auto-cover/SKILL.md, skills/coverage/SKILL.md, skills/coverage-full/SKILL.md, skills/doc-reorganize/SKILL.md, skills/extract-errors/SKILL.md, skills/generate-rules/SKILL.md, skills/ingest-content/SKILL.md, skills/monitor-docs/SKILL.md, skills/run/SKILL.md, skills/sbom/SKILL.md, skills/sbom-analyze/SKILL.md, skills/sbom-scan/SKILL.md, skills/sync-vault/SKILL.md, skills/sync-versions/SKILL.md, skills/test/SKILL.md, skills/test-changed/SKILL.md, skills/test-full/SKILL.md, skills/test-full-parallel/SKILL.md, skills/validate-patterns/SKILL.md, skills/verify-kmp/SKILL.md</files>
  <action>
    For each of the 21 existing skills in skills/:

    1. Read the current SKILL.md
    2. Rewrite the YAML frontmatter to use ONLY standard Claude Code 2026 fields:
       - `name`: keep existing name (already lowercase-hyphen format)
       - `description`: rewrite to be concise (1-2 sentences), specific about trigger conditions (e.g., "Use when asked to run tests" not just "Run tests")
       - `allowed-tools`: add appropriate tool restrictions. Most skills need: Bash, Read, Grep, Glob. MCP-using skills may also need specific MCP tools.
       - `disable-model-invocation: true`: add for side-effect skills: sync-vault, generate-rules, doc-reorganize, monitor-docs (these modify filesystem/vault)
       - Remove `metadata: { author, version, params }` block entirely
       - Remove `license:` field
    3. Keep the body content (Usage, Behavior, Implementation sections) mostly intact -- only fix references to removed metadata fields (e.g., "Uses parameters from params.json" can stay if params.json is still used)

    **Classification for disable-model-invocation:**
    - true (side-effect, user-only): sync-vault, generate-rules, doc-reorganize, monitor-docs, sbom, sbom-scan, sbom-analyze, ingest-content
    - false/omit (safe for auto-invocation): test, test-full, test-changed, test-full-parallel, coverage, coverage-full, auto-cover, extract-errors, validate-patterns, verify-kmp, sync-versions, run, android-test

    **Context budget awareness:** Keep descriptions SHORT. Total description text across all 27 skills must fit in ~16,000 chars (2% of context). Aim for 50-100 chars per description.
  </action>
  <verify>
    <automated>count=0; for d in skills/*/; do if [ -f "$d/SKILL.md" ]; then if grep -q "^name:" "$d/SKILL.md" && grep -q "^description:" "$d/SKILL.md"; then count=$((count+1)); fi; fi; done; echo "Skills with modern frontmatter: $count" && test $count -ge 21</automated>
  </verify>
  <done>All 21 existing skills have name, description, allowed-tools fields; legacy metadata blocks removed; side-effect skills have disable-model-invocation: true</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite 6 merged web skills frontmatter</name>
  <files>skills/accessibility/SKILL.md, skills/best-practices/SKILL.md, skills/core-web-vitals/SKILL.md, skills/performance/SKILL.md, skills/seo/SKILL.md, skills/web-quality-audit/SKILL.md</files>
  <action>
    For each of the 6 web skills merged from .agents/skills/ in Plan 01:

    1. Read the current SKILL.md
    2. Rewrite YAML frontmatter to standard Claude Code 2026 format:
       - `name`: keep existing (accessibility, best-practices, core-web-vitals, performance, seo, web-quality-audit)
       - `description`: concise trigger description (e.g., "Audit web accessibility following WCAG 2.1. Use when asked about a11y, WCAG, or screen reader support.")
       - `allowed-tools`: Read, Grep, Glob, Bash (web audit skills need filesystem access)
       - `user-invocable: false` for pure knowledge skills (best-practices, core-web-vitals, performance, seo) -- these provide background context, not executable workflows
       - Remove `license:` field
       - Remove `metadata:` block if present
    3. Keep body content intact -- these are reference/knowledge documents

    **Classification:**
    - user-invocable: true (executable): accessibility (runs audits), web-quality-audit (runs comprehensive audit)
    - user-invocable: false (background knowledge): best-practices, core-web-vitals, performance, seo

    **Dual-agent note:** Skills must be readable by both Claude Code and GitHub Copilot. Keep content in plain markdown with clear headings. No Claude-specific syntax in the body (only frontmatter is Claude-specific).
  </action>
  <verify>
    <automated>for s in accessibility best-practices core-web-vitals performance seo web-quality-audit; do if ! grep -q "^name:" "skills/$s/SKILL.md" || ! grep -q "^description:" "skills/$s/SKILL.md"; then echo "FAIL: $s missing fields" && exit 1; fi; done && echo "PASS: all 6 web skills modernized"</automated>
  </verify>
  <done>All 6 web skills have modern frontmatter; knowledge-only skills marked user-invocable: false; no legacy metadata blocks remain in any L0 skill</done>
</task>

</tasks>

<verification>
- `grep -rL "^name:" skills/*/SKILL.md` returns 0 results (all have name field)
- `grep -rL "^description:" skills/*/SKILL.md` returns 0 results (all have description field)
- `grep -r "^metadata:" skills/*/SKILL.md` returns 0 results (no legacy metadata blocks)
- `grep -r "^license:" skills/*/SKILL.md` returns 0 results (no legacy license fields)
- `grep -c "disable-model-invocation: true" skills/*/SKILL.md` returns 8+ (side-effect skills)
</verification>

<success_criteria>
- All 27 L0 skills use name, description, allowed-tools frontmatter
- Legacy metadata/license fields removed from all skills
- Side-effect skills have disable-model-invocation: true
- Background knowledge skills have user-invocable: false
- Description text is concise (50-100 chars each, fits in 2% context budget)
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-02-SUMMARY.md`
</output>
