# Phase 14.2.1: Ecosystem Skills Audit & DawSync Integration Harmony - Context

**Gathered:** 2026-03-15
**Status:** Ready for planning

<domain>
## Phase Boundary

Audit all skills, agents, and commands across L0 (AndroidCommonDoc), L1 (shared-kmp-libs), and L2 (DawSync + subprojects) for structural consistency, correct delegation, and modern patterns. Consolidate dual skill locations, fix alignment gaps, establish DawSyncWeb connectivity for multi-console workflows, uniformize sub-document patterns, extend MCP tooling, and run full ecosystem quality gate. Rewrite skills/agents to current best practices. Scope: AndroidCommonDoc + shared-kmp-libs + DawSync + DawSyncWeb + SessionRecorder-VST3. Vault re-sync as final step.

</domain>

<decisions>
## Implementation Decisions

### Skill location consolidation
- **Merge .agents/skills/ (6 web skills) into skills/** after auditing each for L0 genericity (no DawSync-specific content)
- **Eliminate .agents/ directory entirely** — agents stay at .claude/agents/ (Claude Code standard), skills at skills/
- L0 must be completely generic — zero DawSync or app-specific content at L0
- Layer isolation: L0 skills accessible by all layers, L1/L2 skills private to their layer
- Update all DawSync delegate references to point to new canonical skills/ path
- Full agent audit across all 3 projects: verify all delegate paths resolve, modernize definitions
- L1 (shared-kmp-libs) skill needs assessed during audit — Claude's discretion on whether L1 benefits from its own skills

### Skill/agent modernization
- **Audit + rewrite in this phase** — not just documentation, actual rewrite to current best practices
- Skills must work for both Claude Code and GitHub Copilot (dual-agent compatibility)
- Copilot template approach (auto-generate vs dual-author): Claude's discretion based on current best practices
- Delegate pattern mechanism (YAML frontmatter vs registry-based): Claude's discretion — choose most maintainable approach
- Skill-to-doc linking (skills referencing L0 pattern docs for context): Claude's discretion
- Skills vs commands distinction (when to use which structure): Claude's discretion based on current Claude Code patterns
- Worktree awareness in skills: Claude's discretion on how much is needed vs. native Claude Code behavior

### GSD workflow integration
- **Verify GSD + L0/L1/L2 harmony** — GSD is used in all projects
- Check GSD skills don't conflict with L0 skills in naming or behavior
- Verify DawSync GSD config.json is correct, phase branches work from worktrees
- Verify GSD works correctly when invoked from DawSync, shared-kmp-libs, and AndroidCommonDoc consoles

### Command-skill alignment
- Command-skill 1:1 mapping requirement: Claude's discretion on threshold (complex behaviors get skills, simple prompts may stay command-only)
- 5 L0 skills without commands (doc-reorganize, generate-rules, ingest-content, monitor-docs, sync-vault): Claude's discretion on adding wrappers
- Verify all 7 DawSync L2-only commands (deploy-web, lint-web, nuke-builds, roadmap, run-clean, test-m4l, validate-strings) are still correct
- L0 command parameterization validation: Claude's discretion on static analysis + smoke test approach
- Delegate UX (descriptions in delegate files): Claude's discretion
- Command/skill naming convention: Claude's discretion on standardization vs. backward compatibility
- Script parity (Bash/PowerShell): Claude's discretion — leverage existing script-parity-validator agent
- L0 purity re-evaluation of promoted commands: Claude's discretion — spot-check during audit

### DawSyncWeb/subproject connectivity
- DawSyncWeb and SessionRecorder-VST3 currently have NO .claude/ directory
- **Claude's discretion on full approach**: whether subprojects get own .claude/, inherit from parent, or minimal setup
- Web-specific commands (deploy-web, lint-web) placement: Claude's discretion based on Claude Code subproject resolution
- SessionRecorder-VST3 (C++/JUCE): Claude's discretion on whether it benefits from skill infrastructure
- Multi-console coherence mechanism: Claude's discretion (GSD branching, subproject naming, etc.)
- DawSyncWeb GSD state (own .planning/ vs share DawSync's): Claude's discretion
- DawSyncWeb CLAUDE.md: Claude's discretion — bootstrap now if needed for multi-console, or wait for Phase 15
- MCP server accessibility from consuming project consoles: Claude's discretion — may need MCP server improvements
- Agent orchestration blueprint for future phase: Claude's discretion on scope (document what it would look like, capture as deferred if new phase needed)

### Sub-document pattern uniformity
- **Full cross-project validation** across all 3 projects (L0: 16 hubs, L1: 2 hubs, L2: 10+ hubs)
- Uniformity strictness level: Claude's discretion based on what validate-doc-structure can enforce
- **Fix everything found** — audit + fix in same phase, not log-only
- validate-doc-structure extension with sub-doc checks (orphan detection, parent-hub cross-reference): Claude's discretion
- Hub doc ## Sub-documents format standardization: Claude's discretion
- l0_refs coverage: Claude's discretion — verify existing + spot-check for gaps
- doc-template.md update with sub-doc conventions: Claude's discretion on template vs separate guide
- Frontmatter quality: Claude's discretion on validation scope (run validate-doc-structure as minimum)

### Inventory & validation
- Skill/agent/command inventory format (JSON, markdown, or both): Claude's discretion — professional, machine-readable + human-readable
- Skill catalog doc (like module-catalog.md): Claude's discretion on whether it adds value
- Automated command-skill alignment validation (MCP tool or quality gate): Claude's discretion
- L0 toolkit-internal agents (doc-code-drift-detector, etc.): Claude's discretion on L0 vs toolkit-internal classification

### Quality gate
- **Full ecosystem health check** — run ALL existing validators (validate-doc-structure, script-parity-validator, skill-script-alignment, template-sync-validator) across all 3 projects
- **Vault re-sync as final step** — verify consolidated skills/agents appear correctly in Obsidian
- Vault MOC updates for skills: Claude's discretion on whether MOC needs a Skills section

### Claude's Discretion
Claude has extensive discretion on implementation approach across all areas. The user's guiding principle:

> "Professional, enterprise, modern, clean, solid, maintainable, spec-driven, AI-oriented approach"

This means: choose the approach that a well-run engineering team would adopt. Prefer automation over manual processes, consistency over case-by-case, and long-term maintainability over quick fixes. When in doubt, build the tool/validation that prevents the problem from recurring.

</decisions>

<specifics>
## Specific Ideas

- "I need to be able to work in multiple consoles in DawSync (main project in worktrees) and DawSyncWeb, and everything should stay coherent"
- "Does it make sense, now that we have all the docs well organized, to create different agents for each area of DawSync that are coordinated and know how to use AndroidCommonDoc tools?" → Captured as deferred idea / potential new phase
- "Maybe you need to improve the MCP Server too" — MCP server may need updates for cross-project accessibility
- "Professional, enterprise, modern, clean, solid, maintainable, spec-driven, AI-oriented" — quality bar for ALL implementations
- Skills should work for both Claude Code and GitHub Copilot
- GSD workflow is used in all modules — harmony across L0/L1/L2 is essential

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `skills/` (21 skills): Main L0 skill directory — canonical location after consolidation
- `.agents/skills/` (6 web skills): accessibility, best-practices, core-web-vitals, performance, seo, web-quality-audit — to be audited and merged
- `.claude/agents/` (11 agents): 5 toolkit-internal + 6 generic (beta-readiness, cross-platform-validator, doc-alignment, release-guardian, test-specialist, ui-specialist)
- `.claude/commands/` (32 commands): L0 command wrappers, many promoted from DawSync in Phase 14
- `mcp-server/src/tools/validate-doc-structure.ts`: Quality validator — extend for skill/agent/sub-doc checks
- `mcp-server/src/registry/scanner.ts`: Dynamic scanner — may need skill scanning capability
- `setup/copilot-templates/`: Existing Copilot prompt templates from Phase 3

### Established Patterns
- Delegate pattern: YAML frontmatter with `delegate: true` + `canonical: path` — used by DawSync commands and skills
- Hub+sub-doc pattern: hub has `## Sub-documents`, sub-docs have `parent:` frontmatter field
- L0/L1/L2 layer hierarchy with full replacement semantics (Phase 9)
- GSD config per project in `.planning/config.json`
- Sub-project support (Phase 12, ECOV-04) for DawSyncWeb and SessionRecorder-VST3

### Integration Points
- DawSync `.claude/commands/` (39 files): 32 delegates to L0, 7 L2-specific
- DawSync `.claude/agents/` (11 files): 6 delegates to L0, 5 L2-specific (daw-guardian, data-layer-specialist, domain-model-specialist, freemium-gate-checker, producer-consumer-validator)
- DawSync `.claude/skills/` (6 dirs): All delegate to L0 .agents/skills/ — broken path after consolidation
- DawSyncWeb: No .claude/ directory — needs connectivity for multi-console workflow
- SessionRecorder-VST3: No .claude/ directory — C++/JUCE domain, may or may not benefit
- shared-kmp-libs: No .claude/commands/, skills/, or agents/ — only .planning/ for GSD
- `~/.androidcommondoc/vault-config.json`: Vault config for re-sync
- MCP server: May need updates for cross-project accessibility

</code_context>

<deferred>
## Deferred Ideas

- **Coordinated DawSync agent system** — Specialized agents for each DawSync domain area (data layer, UI, testing, DAW integration, billing/freemium) that are coordinated and leverage L0 tools for magnificent development. User specifically requested this be captured as a potential new phase.
- **Agent blueprint document** — If not done in this phase, a spec document describing what the coordinated agent system would look like should be created to inform the future phase.

</deferred>

---

*Phase: 14.2.1-ecosystem-skills-audit-dawsync-integration-harmony*
*Context gathered: 2026-03-15*
