---
phase: 14.2.1-ecosystem-skills-audit-dawsync-integration-harmony
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/INVENTORY.md
  - skills/accessibility/SKILL.md
  - skills/best-practices/SKILL.md
  - skills/core-web-vitals/SKILL.md
  - skills/performance/SKILL.md
  - skills/seo/SKILL.md
  - skills/web-quality-audit/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02]
must_haves:
  truths:
    - "Every skill, agent, and command across L0, L1, and L2 is cataloged in a structured inventory"
    - "Each inventory entry includes type, location, delegation status, and modernization needs"
    - "6 web skills from .agents/skills/ exist in skills/ directory"
    - ".agents/ directory no longer exists"
    - "All skills are in a single canonical location (skills/)"
  artifacts:
    - path: ".planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/INVENTORY.md"
      provides: "Full ecosystem inventory of skills, agents, and commands"
      contains: "L0.*L1.*L2"
    - path: "skills/accessibility/SKILL.md"
      provides: "Consolidated accessibility skill from .agents/skills/"
      contains: "accessibility"
    - path: "skills/web-quality-audit/SKILL.md"
      provides: "Consolidated web-quality-audit skill from .agents/skills/"
      contains: "web-quality-audit"
  key_links:
    - from: "skills/"
      to: ".agents/skills/"
      via: "6 web skills moved from .agents/skills/ to skills/"
      pattern: "skills/(accessibility|best-practices|core-web-vitals|performance|seo|web-quality-audit)"
---

<objective>
Produce a full ecosystem inventory of every skill, agent, and command across L0/L1/L2, then consolidate the 6 web skills from `.agents/skills/` into `skills/` and eliminate the `.agents/` directory.

Purpose: The inventory provides the foundation for all subsequent modernization work. The consolidation eliminates the non-standard `.agents/skills/` location so all skills live in one canonical place before rewriting begins.
Output: INVENTORY.md catalog, 6 web skills moved to skills/, .agents/ directory removed.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-CONTEXT.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-RESEARCH.md

Project paths:
- L0: C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc
- L1: C:/Users/34645/AndroidStudioProjects/shared-kmp-libs
- L2: C:/Users/34645/AndroidStudioProjects/DawSync
- DawSyncWeb: C:/Users/34645/AndroidStudioProjects/DawSync/web
- SessionRecorder-VST3: C:/Users/34645/AndroidStudioProjects/DawSync/SessionRecorder-VST3
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce full ecosystem inventory</name>
  <files>.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/INVENTORY.md</files>
  <action>
    Scan all 3 projects + 2 subprojects and produce a structured INVENTORY.md with the following sections:

    **L0 (AndroidCommonDoc):**
    - Skills (21 in skills/ + 6 in .agents/skills/): For each, record name, location, has frontmatter name/description fields (Y/N), has allowed-tools (Y/N), has disable-model-invocation (Y/N), modernization needs
    - Commands (32 in .claude/commands/): For each, record name, matching skill exists (Y/N), should-promote-to-skill assessment
    - Agents (11 in .claude/agents/): For each, record name, has modern frontmatter (tools/model/memory/skills fields Y/N), classification (toolkit-internal vs generic), modernization needs

    **L1 (shared-kmp-libs):**
    - Commands (9 in .claude/commands/): For each, record name, L0 equivalent exists (Y/N), is delegate (Y/N -- all should be N per research), action needed (convert to delegate)
    - Agents (1: api-contract-guardian): Record frontmatter fields, L1-specific assessment

    **L2 (DawSync):**
    - Commands (39 in .claude/commands/): For each, record name, delegate (Y/N), if delegate: path correct (Y/N), if L2-specific: still valid (Y/N)
    - Skills (6 in .claude/skills/): For each, record name, delegate path, path valid (Y/N -- all should be N since pointing to .agents/skills/)
    - Agents (11 in .claude/agents/): For each, record name, delegate (Y/N), if delegate: path correct (Y/N), if L2-specific: has modern frontmatter (Y/N)

    **Subprojects:**
    - DawSyncWeb: .claude/ exists (N), needs (minimal .claude/ for multi-console)
    - SessionRecorder-VST3: .claude/ exists (N), needs (skip -- C++/JUCE, minimal benefit)

    **Summary section:** Total counts, broken delegates count, modernization needs count, command-skill alignment gaps

    Use `ls` and `head` to read frontmatter from each file. Do NOT read full file contents for all files -- scan frontmatter only (first 15 lines) to check for key fields.
  </action>
  <verify>
    <automated>test -f ".planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/INVENTORY.md" && grep -c "L0\|L1\|L2" ".planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/INVENTORY.md"</automated>
  </verify>
  <done>INVENTORY.md exists with complete catalog of all skills (27+6), commands (32+9+39), and agents (11+1+11) with per-entry assessment</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate .agents/skills/ into skills/ and remove .agents/</name>
  <files>skills/accessibility/SKILL.md, skills/best-practices/SKILL.md, skills/core-web-vitals/SKILL.md, skills/performance/SKILL.md, skills/seo/SKILL.md, skills/web-quality-audit/SKILL.md</files>
  <action>
    1. For each of the 6 web skills in .agents/skills/ (accessibility, best-practices, core-web-vitals, performance, seo, web-quality-audit):
       a. Check if any supporting files exist beyond SKILL.md in each .agents/skills/<name>/ directory
       b. Copy the entire directory to skills/<name>/ (preserving all files)
       c. Verify the copy is complete

    2. Audit each copied skill for L0 genericity:
       - Search for DawSync-specific references (dawsync, DawSync, daw-) in each SKILL.md
       - If found, remove or generalize them (these should be generic web quality skills)
       - Per research: these are generic web skills, so they should be clean

    3. After all 6 are copied and verified, remove the .agents/ directory entirely:
       - `rm -rf .agents/`
       - Verify .agents/ no longer exists

    4. Search the L0 codebase for any remaining references to .agents/skills/:
       - `grep -r ".agents/skills" .` (exclude .git/)
       - If any references found in L0 files, update them to point to skills/

    IMPORTANT: Do NOT update DawSync delegate references yet -- that happens in Plan 05 (SKILL-07).
  </action>
  <verify>
    <automated>test -d "skills/accessibility" && test -d "skills/web-quality-audit" && test ! -d ".agents" && echo "PASS: 6 skills consolidated, .agents removed"</automated>
  </verify>
  <done>6 web skills exist in skills/ directory, .agents/ directory deleted, no L0 references to .agents/skills/ remain</done>
</task>

</tasks>

<verification>
- INVENTORY.md exists with structured catalog of all ecosystem skills/agents/commands
- skills/ contains 27 skill directories (21 existing + 6 merged)
- .agents/ directory does not exist
- `grep -r ".agents/skills" . --include="*.md" --exclude-dir=.git --exclude-dir=.planning` returns 0 results in L0
</verification>

<success_criteria>
- Complete ecosystem inventory with per-entry modernization assessment
- All 27 skills in single skills/ directory
- .agents/ directory eliminated
- No stale .agents/skills/ references in L0 codebase
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-01-SUMMARY.md`
</output>
