---
phase: 14.2.1-ecosystem-skills-audit-dawsync-integration-harmony
plan: 07
type: execute
wave: 4
depends_on: [14.2.1-02, 14.2.1-03, 14.2.1-04, 14.2.1-05, 14.2.1-06]
files_modified: []
autonomous: false
requirements: [SKILL-11]
must_haves:
  truths:
    - "validate-doc-structure reports 0 errors across all 3 projects"
    - "script-parity-validator passes (Bash/PowerShell parity)"
    - "skill-script-alignment passes (skills match scripts)"
    - "template-sync-validator passes (Copilot templates current)"
    - "Vault re-synced with updated skills and agents"
    - "No stale references to .agents/skills/ anywhere in the ecosystem"
    - "Human approves Obsidian vault navigation and skill visibility"
  artifacts: []
  key_links:
    - from: "quality-gate-orchestrator"
      to: "script-parity-validator, skill-script-alignment, template-sync-validator, doc-code-drift-detector"
      via: "4-gate quality pipeline"
      pattern: "Gate [1-4]"
    - from: "validate-doc-structure"
      to: "L0/L1/L2 docs/"
      via: "cross-project validation"
      pattern: "0 errors"
---

<objective>
Run full ecosystem quality gate: all validators across all 3 projects, re-sync vault with updated skills/agents, and get human approval.

Purpose: Final verification that all modernization work is correct, consistent, and complete across the entire ecosystem.
Output: All validators passing, vault re-synced, human-approved.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-CONTEXT.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-RESEARCH.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-05-SUMMARY.md

Project paths:
- L0: C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc
- L1: C:/Users/34645/AndroidStudioProjects/shared-kmp-libs
- L2: C:/Users/34645/AndroidStudioProjects/DawSync
- Vault config: ~/.androidcommondoc/vault-config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all validators across ecosystem</name>
  <files></files>
  <action>
    Run the full quality gate pipeline:

    **Gate 1: validate-doc-structure cross-project**

    Run MCP validate-doc-structure tool (or equivalent command) against all 3 projects:
    - L0: `cd mcp-server && npx jest --testPathPattern="doc-structure" --bail 1`
    - Also invoke validate-doc-structure against L1 and L2 paths via MCP tool or direct script

    Expected: 0 errors across all 3 projects.

    **Gate 2: Stale reference check**

    Comprehensive grep for stale references:
    - `grep -r ".agents/skills" C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/ --include="*.md" --exclude-dir=.git --exclude-dir=.planning --exclude-dir=node_modules` -- expect 0 results
    - `grep -r ".agents/skills" C:/Users/34645/AndroidStudioProjects/DawSync/.claude/ --include="*.md"` -- expect 0 results
    - `grep -r ".agents/skills" C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/.claude/ --include="*.md"` -- expect 0 results

    **Gate 3: Skill frontmatter validation**

    Verify all L0 skills have required frontmatter:
    - Every skills/*/SKILL.md has `name:` field
    - Every skills/*/SKILL.md has `description:` field
    - Every skills/*/SKILL.md has `allowed-tools:` field (or explicit omission rationale)
    - No skill has legacy `metadata:` block

    **Gate 4: Agent frontmatter validation**

    Verify all L0 agents have required frontmatter:
    - Every .claude/agents/*.md has `name:`, `description:`, `tools:`, `model:` fields

    **Gate 5: Delegate integrity**

    Verify all delegates across L1 and L2 have correct `canonical:` paths:
    - shared-kmp-libs: 9 command delegates point to valid L0 commands
    - DawSync: 6 skill delegates, 6 agent delegates, 32 command delegates all resolve

    **Gate 6: MCP test suite**

    Run full MCP server test suite: `cd mcp-server && npm test`
    Expected: All tests green (416+ tests).

    If ANY gate fails: fix the issue before proceeding. Document what was fixed and why.
  </action>
  <verify>
    <automated>stale=$(grep -r ".agents/skills" C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/ C:/Users/34645/AndroidStudioProjects/DawSync/.claude/ C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/.claude/ --include="*.md" --exclude-dir=.git --exclude-dir=.planning --exclude-dir=node_modules 2>/dev/null | wc -l) && skills_ok=$(for f in skills/*/SKILL.md; do grep -q "^name:" "$f" && grep -q "^description:" "$f" || { echo "FAIL: $f"; exit 1; }; done && echo "ok") && agents_ok=$(for f in .claude/agents/*.md; do grep -q "^name:" "$f" && grep -q "^description:" "$f" && grep -q "^tools:" "$f" || { echo "FAIL: $f"; exit 1; }; done && echo "ok") && cd mcp-server && npm test 2>&1 | tail -5 && echo "Stale refs: $stale, Skills: $skills_ok, Agents: $agents_ok" && test $stale -eq 0</automated>
  </verify>
  <done>All 6 quality gates pass: validate-doc-structure 0 errors, no stale references, all skills/agents have modern frontmatter, all delegates resolve, MCP tests green</done>
</task>

<task type="auto">
  <name>Task 2: Re-sync vault with updated skills and agents</name>
  <files></files>
  <action>
    1. Invoke vault sync to pick up all the skill and agent changes:
       - Use the sync-vault MCP tool or the skill: `/sync-vault`
       - This should collect the modernized skills from skills/, updated agents from .claude/agents/

    2. Verify vault health:
       - Use vault-status MCP tool or check `~/.androidcommondoc/vault-config.json` for vault path
       - Verify vault has correct file count (should reflect 27 skills, 11 agents)
       - Check for orphaned vault files (skills that were in .agents/skills/ should be collected from skills/ now)

    3. If vault MOC needs a Skills section update (per research recommendation), add it during sync.

    4. Report vault sync results: files updated, files added, files removed, total count.
  </action>
  <verify>
    <automated>node -e "const c=require('C:/Users/34645/.androidcommondoc/vault-config.json'); console.log('Vault path:', c.vault_path || c.vaultPath || 'configured')" 2>/dev/null || echo "Vault config accessible"</automated>
  </verify>
  <done>Vault re-synced with all 27 modernized skills and 11 modernized agents visible; no orphaned vault files from old .agents/skills/ path</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of ecosystem modernization</name>
  <files></files>
  <action>
    Present the complete ecosystem modernization results for human review:
    - 27 L0 skills consolidated and modernized with Claude Code 2026 frontmatter
    - 11 L0 agents modernized with tools, model, memory, skills preloading
    - shared-kmp-libs 9 commands converted to L0 delegates
    - DawSync 6 broken skill delegates fixed (now point to skills/ not .agents/skills/)
    - DawSync L2-specific agents modernized
    - DawSyncWeb minimal .claude/ setup established
    - validate-doc-structure extended with sub-doc orphan/hub checks
    - Sub-document cross-references fixed across L0/L1/L2
    - GSD workflow integration verified across all projects
    - Vault re-synced with all changes

    Human verification steps:
    1. Open AndroidCommonDoc in Claude Code console -- verify skills load, .agents/ gone
    2. Open DawSync in Claude Code console -- verify delegate commands work
    3. Open Obsidian vault -- verify skills section, graph view, agent visibility
    4. Spot-check: skills/test/SKILL.md, DawSync/.claude/skills/accessibility/SKILL.md, shared-kmp-libs/.claude/commands/test.md
  </action>
  <verify>Human approves the ecosystem state or describes issues to fix</verify>
  <done>Human types "approved" confirming all modernization work is correct and complete</done>
</task>

</tasks>

<verification>
- All quality gates pass (6 gates)
- Vault re-synced successfully
- Human approves ecosystem state
</verification>

<success_criteria>
- validate-doc-structure: 0 errors across L0/L1/L2
- No .agents/skills/ references anywhere in ecosystem
- All skills have modern frontmatter
- All agents have modern frontmatter
- All delegates resolve correctly
- MCP tests all green
- Vault reflects all changes
- Human approved
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-07-SUMMARY.md`
</output>
