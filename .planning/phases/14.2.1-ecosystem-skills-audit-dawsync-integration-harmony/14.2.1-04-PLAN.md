---
phase: 14.2.1-ecosystem-skills-audit-dawsync-integration-harmony
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/tools/validate-doc-structure.ts
  - mcp-server/tests/unit/tools/validate-doc-structure.test.ts
autonomous: true
requirements: [SKILL-10]
must_haves:
  truths:
    - "validate-doc-structure detects orphan sub-docs (sub-docs with parent field pointing to non-existent hub)"
    - "validate-doc-structure detects hub docs with Sub-documents section listing files that don't exist"
    - "validate-doc-structure checks parent field in sub-docs matches actual hub filename"
    - "Sub-document cross-references are correct across all 3 projects (L0: 16 hubs, L1: 2 hubs, L2: 10+ hubs)"
    - "All sub-doc pattern violations found are fixed in the same plan"
  artifacts:
    - path: "mcp-server/src/tools/validate-doc-structure.ts"
      provides: "Extended validate-doc-structure with sub-doc orphan and parent-hub checks"
      contains: "orphan.*sub-doc\\|parent.*hub"
    - path: "mcp-server/tests/unit/tools/validate-doc-structure.test.ts"
      provides: "Tests for sub-doc validation checks"
      contains: "orphan.*sub-doc\\|parent.*hub"
  key_links:
    - from: "mcp-server/src/tools/validate-doc-structure.ts"
      to: "docs/**/*/SKILL.md"
      via: "Sub-document pattern validation scanning"
      pattern: "parent.*hub.*Sub-documents"
---

<objective>
Extend validate-doc-structure with sub-document orphan and parent-hub cross-reference checks, then run validation across all 3 projects and fix all violations found.

Purpose: Sub-document pattern (hub + sub-docs with parent field) is used across the ecosystem but has never been programmatically validated for cross-reference correctness. Extending the validator prevents future drift.
Output: Extended MCP tool with sub-doc checks, all sub-doc violations fixed across L0/L1/L2.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-CONTEXT.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-RESEARCH.md

<interfaces>
<!-- From mcp-server/src/tools/validate-doc-structure.ts -->
<!-- validateDocsDirectory() returns structured JSON with errors/warnings arrays -->
<!-- Extend with new check: sub_doc_errors -->

Sub-document pattern conventions:
- Hub doc: contains "## Sub-documents" heading with bullet list of sub-doc filenames
- Sub-doc: has `parent: <hub-filename>` in YAML frontmatter
- Cross-reference: hub lists sub-doc -> sub-doc points back to hub
- Orphan sub-doc: has parent field but parent file doesn't exist or doesn't list it
- Missing sub-doc: hub lists file in Sub-documents but file doesn't exist

Project paths for cross-project validation:
- L0: C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/docs/
- L1: C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/docs/
- L2: C:/Users/34645/AndroidStudioProjects/DawSync/docs/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend validate-doc-structure with sub-doc checks</name>
  <files>mcp-server/src/tools/validate-doc-structure.ts, mcp-server/tests/unit/tools/validate-doc-structure.test.ts</files>
  <behavior>
    - Test: Reports error when sub-doc has parent field pointing to file that doesn't exist
    - Test: Reports error when hub's Sub-documents section lists a file that doesn't exist
    - Test: Reports error when sub-doc's parent field doesn't match any hub that lists it in Sub-documents
    - Test: Reports no error for correctly linked hub + sub-doc pair
    - Test: Handles hub with no Sub-documents section (not an error -- it's a standalone doc)
    - Test: Handles sub-doc with no parent field gracefully (warning if it contains "parent" in frontmatter but empty)
  </behavior>
  <action>
    1. In validate-doc-structure.ts, add two new validation functions:
       - `checkSubDocOrphans(entries)`: For each entry with `parent` frontmatter field, verify the parent file exists and contains a `## Sub-documents` section listing this file
       - `checkHubSubDocRefs(entries)`: For each entry with `## Sub-documents` heading, parse the bullet list of sub-doc filenames and verify each file exists with correct `parent` field

    2. Wire into validateDocsDirectory():
       - After existing checks, run sub-doc validation
       - Add results to output under `sub_doc_errors` key
       - Errors: missing parent file, missing sub-doc file, parent-hub mismatch
       - Warnings: sub-doc without parent field in a directory where hubs exist

    3. Write unit tests covering all behaviors listed above.
       Use the existing test pattern with mock filesystem entries.

    4. Run tests: RED first (new tests fail), then GREEN (implement to pass).
  </action>
  <verify>
    <automated>cd mcp-server && npx jest --testPathPattern="validate-doc-structure" --bail 1</automated>
  </verify>
  <done>validate-doc-structure reports sub-doc orphans and hub reference mismatches; all tests green</done>
</task>

<task type="auto">
  <name>Task 2: Run cross-project validation and fix all sub-doc violations</name>
  <files>Various docs across L0/L1/L2 (fixes as needed)</files>
  <action>
    1. Run the extended validate-doc-structure against all 3 projects:
       - L0 (AndroidCommonDoc): ~16 hub docs
       - L1 (shared-kmp-libs): ~2 hub docs
       - L2 (DawSync): ~10+ hub docs

    2. For each violation found:
       - Orphan sub-doc (parent doesn't list it): Add the sub-doc to the parent's Sub-documents section
       - Missing sub-doc (hub lists file that doesn't exist): Remove the stale reference from the hub, or create the missing sub-doc if content exists elsewhere
       - Parent mismatch (sub-doc points to wrong hub): Fix the parent field to point to correct hub

    3. Re-run validation after fixes to confirm 0 sub-doc errors across all 3 projects.

    IMPORTANT: This plan operates on the docs/ directories across L0/L1/L2. Do NOT modify skill files, agent files, or command files. Only fix doc cross-references.

    Project paths:
    - L0: C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/docs/
    - L1: C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/docs/
    - L2: C:/Users/34645/AndroidStudioProjects/DawSync/docs/
  </action>
  <verify>
    <automated>cd mcp-server && npx jest --testPathPattern="validate-doc-structure" --bail 1 && node -e "const {validateDocsDirectory}=require('./dist/tools/validate-doc-structure.js'); const paths=['C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/docs','C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/docs','C:/Users/34645/AndroidStudioProjects/DawSync/docs']; let fail=false; for(const p of paths){const r=validateDocsDirectory(p); if(r.sub_doc_errors && r.sub_doc_errors.length>0){console.error('FAIL:',p,r.sub_doc_errors);fail=true;}} if(fail)process.exit(1); console.log('PASS: 0 sub-doc errors across all 3 projects');"</automated>
  </verify>
  <done>validate-doc-structure reports 0 sub-doc errors across L0, L1, and L2; all hub/sub-doc cross-references are bidirectionally correct</done>
</task>

</tasks>

<verification>
- `cd mcp-server && npm test` -- full suite green
- validate-doc-structure includes sub_doc_errors in output
- Cross-project validation shows 0 sub-doc errors for all 3 projects
</verification>

<success_criteria>
- validate-doc-structure extended with orphan sub-doc and hub reference checks
- All sub-document pattern violations across L0/L1/L2 fixed
- 0 sub-doc errors reported by extended validator
- All existing MCP tests still green
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-04-SUMMARY.md`
</output>
