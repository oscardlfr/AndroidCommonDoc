---
phase: 14.2.1-ecosystem-skills-audit-dawsync-integration-harmony
plan: 03
type: execute
wave: 2
depends_on: [14.2.1-01]
files_modified:
  - .claude/agents/beta-readiness-agent.md
  - .claude/agents/cross-platform-validator.md
  - .claude/agents/doc-alignment-agent.md
  - .claude/agents/doc-code-drift-detector.md
  - .claude/agents/quality-gate-orchestrator.md
  - .claude/agents/release-guardian-agent.md
  - .claude/agents/script-parity-validator.md
  - .claude/agents/skill-script-alignment.md
  - .claude/agents/template-sync-validator.md
  - .claude/agents/test-specialist.md
  - .claude/agents/ui-specialist.md
  - .planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/COMMAND-SKILL-ALIGNMENT.md
autonomous: true
requirements: [SKILL-04, SKILL-05]
must_haves:
  truths:
    - "All 11 L0 agents have modern frontmatter (name, description, tools, model)"
    - "Agents that benefit from skill preloading have skills field"
    - "Agents with persistent learning have memory field"
    - "Toolkit-internal agents are clearly distinguished from generic agents"
    - "Command-skill alignment is documented: which commands have matching skills, which should be promoted"
    - "5 skills without commands are assessed for command wrapper needs"
    - "16 commands without skills are assessed for skill promotion"
  artifacts:
    - path: ".claude/agents/test-specialist.md"
      provides: "Modernized test-specialist with skills preloading"
      contains: "skills:"
    - path: ".claude/agents/quality-gate-orchestrator.md"
      provides: "Modernized quality-gate-orchestrator with tool restrictions"
      contains: "tools:"
    - path: ".planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/COMMAND-SKILL-ALIGNMENT.md"
      provides: "Alignment analysis document"
      contains: "commands without skills"
  key_links:
    - from: ".claude/agents/test-specialist.md"
      to: "skills/test/SKILL.md"
      via: "skills preloading field"
      pattern: "skills:.*test"
    - from: ".claude/agents/quality-gate-orchestrator.md"
      to: ".claude/agents/script-parity-validator.md"
      via: "orchestration delegation"
      pattern: "script-parity|skill-script|template-sync|doc-code-drift"
---

<objective>
Modernize all 11 L0 agents to current Claude Code 2026 subagent patterns and produce a command-skill alignment analysis.

Purpose: Agents need modern frontmatter (tools, model, memory, skills preloading) for proper isolation and capability restriction. Command-skill alignment analysis informs whether commands should be promoted to skills or vice versa.
Output: 11 modernized agent files, command-skill alignment analysis document.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-CONTEXT.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-RESEARCH.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-01-SUMMARY.md

<interfaces>
<!-- Current Claude Code 2026 subagent frontmatter (from research) -->
Required:
- name: agent identifier
- description: when to use this agent (Claude uses for auto-delegation)
- tools: tool access restrictions (Read, Grep, Glob, Bash, etc.)
- model: model override (sonnet, opus, haiku)

Optional:
- memory: user/project/local (persistent cross-session learning)
- skills: list of skill names to preload into subagent context
- hooks: lifecycle events (PreToolUse, PostToolUse, Stop)
- isolation: worktree for git worktree isolation
- background: true for concurrent execution
- maxTurns: limit agentic turns

L0 Agent classifications:
- Toolkit-internal (5): doc-code-drift-detector, quality-gate-orchestrator, script-parity-validator, skill-script-alignment, template-sync-validator
- Generic (6): beta-readiness-agent, cross-platform-validator, doc-alignment-agent, release-guardian-agent, test-specialist, ui-specialist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modernize all 11 L0 agents</name>
  <files>.claude/agents/beta-readiness-agent.md, .claude/agents/cross-platform-validator.md, .claude/agents/doc-alignment-agent.md, .claude/agents/doc-code-drift-detector.md, .claude/agents/quality-gate-orchestrator.md, .claude/agents/release-guardian-agent.md, .claude/agents/script-parity-validator.md, .claude/agents/skill-script-alignment.md, .claude/agents/template-sync-validator.md, .claude/agents/test-specialist.md, .claude/agents/ui-specialist.md</files>
  <action>
    For each of the 11 L0 agents:

    1. Read the current agent file
    2. Rewrite/add YAML frontmatter with standard Claude Code 2026 fields:
       - `name`: existing name (keep as-is)
       - `description`: concise description with trigger conditions
       - `tools`: appropriate tool restrictions for each agent's purpose
       - `model`: sonnet for most agents (cost-effective), opus for quality-gate-orchestrator

    3. Add optional fields where beneficial:
       - `memory: project` for agents that build knowledge: test-specialist, doc-alignment-agent, release-guardian-agent
       - `skills:` preloading for agents that benefit from skill knowledge:
         - test-specialist: [test, coverage]
         - doc-alignment-agent: [validate-patterns, doc-reorganize]
         - ui-specialist: [accessibility, web-quality-audit]
         - release-guardian-agent: [sbom, verify-kmp]

    4. Keep body content intact -- modernize only frontmatter. The body is the system prompt.

    **Toolkit-internal agents (5):** These are orchestrated by quality-gate-orchestrator and shouldn't be user-invoked directly. However, Claude Code doesn't have a "hidden" field for agents -- they're always discoverable. Add a clear note in description: "Internal validator -- invoked by quality-gate-orchestrator."

    **Agent modernization checklist per file:**
    - [ ] name field present
    - [ ] description field with trigger conditions
    - [ ] tools field with appropriate restrictions
    - [ ] model field specified
    - [ ] memory field where beneficial
    - [ ] skills field where beneficial
    - [ ] Body/system prompt content preserved
  </action>
  <verify>
    <automated>count=0; for f in .claude/agents/*.md; do if grep -q "^name:" "$f" && grep -q "^tools:" "$f" && grep -q "^model:" "$f"; then count=$((count+1)); fi; done; echo "Agents with modern frontmatter: $count" && test $count -eq 11</automated>
  </verify>
  <done>All 11 L0 agents have name, description, tools, model fields; memory and skills fields added where beneficial; body content preserved</done>
</task>

<task type="auto">
  <name>Task 2: Produce command-skill alignment analysis</name>
  <files>.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/COMMAND-SKILL-ALIGNMENT.md</files>
  <action>
    Produce COMMAND-SKILL-ALIGNMENT.md with the following analysis:

    **Section 1: Commands WITH matching skills (16 matches)**
    List the 16 L0 commands that have a matching skill in skills/. For each, note:
    - Command name and skill name (should match)
    - Whether command content duplicates skill or is genuinely different
    - Recommendation: keep both (command as simple alias), deprecate command, or merge

    **Section 2: Commands WITHOUT matching skills (16 commands)**
    List: brainstorm, bump-version, changelog, doc-check, doc-update, feature-audit, merge-track, metrics, package, pre-release, prioritize, start-track, sync-roadmap, sync-tech-versions, unlock-tests, verify-migrations
    For each, assess:
    - Complexity: simple prompt vs complex workflow
    - Recommendation: promote to skill (if complex, benefits from supporting files/tool restrictions) or keep as command-only (if simple prompt wrapper)

    **Section 3: Skills WITHOUT matching commands (5 skills)**
    List: doc-reorganize, generate-rules, ingest-content, monitor-docs, sync-vault
    For each, assess:
    - Whether a command wrapper adds value (users can invoke via /skill-name already)
    - Recommendation: add command wrapper or leave skill-only

    **Section 4: Summary table** with final recommendation per item.

    Per user's discretion guidance: complex behaviors get skills, simple prompts stay command-only. No strict 1:1 mapping required.
  </action>
  <verify>
    <automated>test -f ".planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/COMMAND-SKILL-ALIGNMENT.md" && grep -c "Recommendation" ".planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/COMMAND-SKILL-ALIGNMENT.md"</automated>
  </verify>
  <done>COMMAND-SKILL-ALIGNMENT.md exists with per-item assessment of all 37 commands and 27 skills, with clear promote/keep/deprecate recommendations</done>
</task>

</tasks>

<verification>
- All 11 agents in .claude/agents/ have name, description, tools, model frontmatter
- test-specialist has skills: [test, coverage] in frontmatter
- quality-gate-orchestrator has model: opus (or sonnet -- executor's discretion)
- COMMAND-SKILL-ALIGNMENT.md covers all 32 L0 commands and all 27 L0 skills
</verification>

<success_criteria>
- 11 agents modernized with full Claude Code 2026 frontmatter
- Skills preloading configured for agents that benefit (test-specialist, doc-alignment, ui-specialist, release-guardian)
- Memory enabled for knowledge-building agents
- Command-skill alignment documented with actionable recommendations
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-03-SUMMARY.md`
</output>
