---
phase: 14.2.1-ecosystem-skills-audit-dawsync-integration-harmony
plan: 06
type: execute
wave: 3
depends_on: [14.2.1-02]
files_modified: []
autonomous: true
requirements: [SKILL-09]
must_haves:
  truths:
    - "GSD config.json is correct in all 3 projects that use it (AndroidCommonDoc, DawSync)"
    - "GSD skills at L0 do not conflict with project-specific skills in naming or behavior"
    - "DawSync worktree branch template (feature/precloud-{slug}) creates branches correctly"
    - "GSD commands work correctly when invoked from DawSync, shared-kmp-libs, and AndroidCommonDoc consoles"
  artifacts: []
  key_links:
    - from: "DawSync/.planning/config.json"
      to: "GSD workflow"
      via: "phase_branch_template configuration"
      pattern: "feature/precloud"
    - from: "AndroidCommonDoc/.planning/config.json"
      to: "GSD workflow"
      via: "GSD config"
      pattern: "mode.*yolo"
---

<objective>
Verify GSD workflow integration across all 3 projects -- config correctness, skill naming conflicts, and worktree branch template functionality.

Purpose: GSD is used in all projects. After L0 skill modernization, verify GSD skills don't conflict with L0 skills and that configs are correct.
Output: Verified GSD integration with any issues fixed.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-CONTEXT.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-RESEARCH.md
@.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-02-SUMMARY.md

Project paths:
- L0: C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc
- L1: C:/Users/34645/AndroidStudioProjects/shared-kmp-libs
- L2: C:/Users/34645/AndroidStudioProjects/DawSync

GSD instance: C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify GSD configs and skill naming across all projects</name>
  <files></files>
  <action>
    **Part A: Config verification**

    1. Read and validate config.json in each project:
       - AndroidCommonDoc: .planning/config.json -- verify mode, workflow flags
       - DawSync: .planning/config.json -- verify mode, git.phase_branch_template
       - shared-kmp-libs: Check if .planning/config.json exists (may not have one -- that's okay, GSD uses defaults)

    2. For each config, verify:
       - JSON is valid
       - Required fields present (mode, commit_docs at minimum)
       - No conflicting settings between projects

    **Part B: GSD skill naming conflict check**

    1. List all GSD skills from the GSD instance directory:
       `ls C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/skills/` (if it exists)
       Or check for GSD commands: `ls C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/`

    2. Compare GSD skill/command names with L0 skill names (from skills/ directory)
    3. If any name collisions found: document and recommend resolution (rename L0 skill or GSD skill)
    4. GSD uses `/gsd:*` namespaced commands, so collisions are unlikely -- verify this

    **Part C: Worktree branch template verification**

    1. DawSync config has `feature/precloud-{slug}` template
    2. Verify the template is syntactically valid for git branch creation
    3. Check if DawSync has any worktrees currently: `cd C:/Users/34645/AndroidStudioProjects/DawSync && git worktree list`
    4. If worktrees exist, verify GSD would work from worktree directory (check that .planning/ is accessible)

    **Part D: Cross-project GSD invocation check**

    1. Verify each project's .planning/ directory exists and has STATE.md or config.json
    2. Verify GSD tools binary works from each project:
       `cd <project> && node C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/bin/gsd-tools.cjs init plan-phase test 2>&1 | head -5`
       (Don't actually create plans -- just verify the tool initializes)

    If ANY issues found, fix them. If configs need updates, update them. Document all findings in SUMMARY.
  </action>
  <verify>
    <automated>for p in "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc" "C:/Users/34645/AndroidStudioProjects/DawSync"; do if [ -f "$p/.planning/config.json" ]; then node -e "JSON.parse(require('fs').readFileSync('$p/.planning/config.json','utf8'))" || { echo "INVALID JSON: $p"; exit 1; }; fi; done && echo "PASS: configs valid"</automated>
  </verify>
  <done>GSD configs valid in all projects, no skill naming conflicts, worktree branch template verified, GSD tools initialize correctly from all project directories</done>
</task>

</tasks>

<verification>
- All .planning/config.json files are valid JSON
- No GSD skill names collide with L0 skill names
- DawSync branch template is valid
- GSD tools initialize from all 3 project directories
</verification>

<success_criteria>
- GSD config.json verified correct in AndroidCommonDoc and DawSync
- No naming conflicts between GSD and L0 skills
- Worktree branch template verified functional
- GSD invocation works from all 3 projects
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.1-ecosystem-skills-audit-dawsync-integration-harmony/14.2.1-06-SUMMARY.md`
</output>
