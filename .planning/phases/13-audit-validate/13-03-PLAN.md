---
phase: 13-audit-validate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-audit-validate/audit-manifest-shared-kmp-libs.json
  - .planning/phases/13-audit-validate/audit-manifest-androidcommondoc.json
autonomous: true
requirements:
  - AUDIT-03
  - AUDIT-04

must_haves:
  truths:
    - "Every shared-kmp-libs module has a gap analysis entry documenting what exists and what documentation is missing"
    - "shared-kmp-libs modules grouped by category (Foundation, I/O & Network, Storage, Security & Auth, Error Mappers, Domain-specific, Others) with per-group assessment"
    - "shared-kmp-libs docs/ files (5) assessed for L0 promotion candidates"
    - "shared-kmp-libs CLAUDE.md (57 lines) quality assessed with gaps flagged for Phase 15"
    - "All 23 AndroidCommonDoc pattern docs reviewed for completeness and accuracy gaps"
    - "AndroidCommonDoc gaps identified relative to the consolidated corpus knowledge"
  artifacts:
    - path: ".planning/phases/13-audit-validate/audit-manifest-shared-kmp-libs.json"
      provides: "Per-module gap analysis, per-file AI-readiness scores, L0 promotion candidates, CLAUDE.md assessment"
    - path: ".planning/phases/13-audit-validate/audit-manifest-androidcommondoc.json"
      provides: "Per-file pattern doc completeness and accuracy gap assessment"
  key_links:
    - from: "audit-manifest-shared-kmp-libs.json"
      to: "Plan 04 report assembly"
      via: "JSON merge into final audit-manifest.json"
      pattern: "shared-kmp-libs.*files"
    - from: "audit-manifest-androidcommondoc.json"
      to: "Plan 04 report assembly"
      via: "JSON merge into final audit-manifest.json"
      pattern: "AndroidCommonDoc.*files"
---

<objective>
Audit shared-kmp-libs modules for documentation gaps (per-module doc plan) and review all 23 AndroidCommonDoc pattern docs for completeness and accuracy. Produce two audit manifests that identify what documentation exists, what is missing, and where gaps exist relative to the consolidated corpus.

Purpose: shared-kmp-libs has 51 module directories but only 14 with README.md files -- the gap analysis tells Phase 14 exactly what to write. AndroidCommonDoc's 23 pattern docs need review against findings from all 4 projects to identify coverage holes.

Output: `audit-manifest-shared-kmp-libs.json` + `audit-manifest-androidcommondoc.json`
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-audit-validate/13-CONTEXT.md
@.planning/phases/13-audit-validate/13-RESEARCH.md

Source projects:
@C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/
@docs/

Reference for version checking:
@versions-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit shared-kmp-libs modules and documentation</name>
  <files>.planning/phases/13-audit-validate/audit-manifest-shared-kmp-libs.json</files>
  <action>
Audit shared-kmp-libs at `C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/`.

Part A -- Per-module gap analysis:
Enumerate all module directories (research found 51, grouped as: Foundation ~5, I/O and Network ~6, Storage ~10, Security and Auth ~7, Error Mappers ~8, Domain-specific ~8, Others ~7). For EACH module directory:
- Check if README.md exists (only 14 of 51 have one per research)
- If README.md exists: assess quality, line count, whether it covers public API surface, usage examples, enforced conventions
- If README.md is missing: document what documentation is needed based on module name and any source files present
- Group assessment: Error mappers (8 modules) follow identical patterns and can share a group-level documentation template. Storage variants (10 modules) share common API patterns similarly.

Produce a `module_gaps` section with per-module entries:
```json
{
  "module": "core-network-api",
  "category": "I/O & Network",
  "has_readme": true,
  "readme_lines": 45,
  "readme_quality": "partial -- missing usage examples and enforced conventions",
  "doc_plan": "Add usage examples for NetworkExecutor, document retry/timeout configuration, add enforced convention table",
  "priority": "high"
}
```

Priority classification: "high" for foundation/core modules consumed by many others, "medium" for feature modules, "low" for internal/error-mapper modules that follow established templates.

Part B -- docs/ file audit (5 files):
Read each of the 5 docs/ files (ERROR_HANDLING_PATTERN.md, TESTING_STRATEGY.md, and others). For each:
- Classify: L0 candidate (generic KMP pattern from official docs) or L1 (shared-kmp-libs specific)
- AI-readiness score (0-5, standard criteria)
- Flag for L0 promotion if content is generic enough (per CONTEXT.md: "ERROR_HANDLING_PATTERN, TESTING_STRATEGY may be generic enough")
- Note version references and staleness (research found kover 0.9.4 in TESTING_STRATEGY vs 0.9.1 in manifest)

Part C -- CLAUDE.md assessment:
Read shared-kmp-libs CLAUDE.md (57 lines). Assess:
- AI-readiness score
- Coverage: does it cover all module categories? Build patterns? L0 references?
- Gaps: what is missing that an AI agent would need?
- Phase 15 notes: what needs to change in the CLAUDE.md rewrite

Part D -- All other markdown files:
Enumerate remaining markdown files (~32+ total per research). Apply standard per-file audit entry (same fields as WakeTheCave/DawSync manifests).

Output structure for audit-manifest-shared-kmp-libs.json:
```json
{
  "project": "shared-kmp-libs",
  "path": "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs",
  "layer": "L1",
  "audit_mode": "full",
  "generated": "<ISO timestamp>",
  "file_count": <actual>,
  "files": [ ...per-file entries with standard fields... ],
  "module_gaps": {
    "total_modules": <N>,
    "modules_with_readme": <N>,
    "modules_without_readme": <N>,
    "by_category": {
      "Foundation": [ ...module entries... ],
      "IO_Network": [ ...module entries... ],
      "Storage": [ ...module entries... ],
      "Security_Auth": [ ...module entries... ],
      "Error_Mappers": [ ...module entries... ],
      "Domain_Specific": [ ...module entries... ],
      "Others": [ ...module entries... ]
    }
  },
  "l0_promotion_candidates": [ { "path": "...", "rationale": "..." } ],
  "claude_md_assessment": {
    "line_count": 57,
    "ai_readiness_score": <N>,
    "gaps": [ "..." ],
    "phase_15_notes": "..."
  },
  "summary": {
    "total_files": <N>,
    "by_classification": { "ACTIVE": <N>, "SUPERSEDED": <N>, "UNIQUE": <N> },
    "ai_readiness_avg": <float>,
    "doc_coverage_percent": <float>,
    "high_priority_gaps": <N>,
    "medium_priority_gaps": <N>,
    "low_priority_gaps": <N>
  }
}
```
  </action>
  <verify>
    <automated>node -e "const m=JSON.parse(require('fs').readFileSync('.planning/phases/13-audit-validate/audit-manifest-shared-kmp-libs.json','utf8')); console.log('files:', m.file_count, 'modules total:', m.module_gaps.total_modules, 'with readme:', m.module_gaps.modules_with_readme, 'without:', m.module_gaps.modules_without_readme); if(!m.module_gaps.by_category) throw new Error('Missing module categories'); if(!m.claude_md_assessment) throw new Error('Missing CLAUDE.md assessment'); console.log('PASS');"</automated>
  </verify>
  <done>All shared-kmp-libs modules have gap analysis entries, docs/ files assessed for L0 promotion, CLAUDE.md quality assessed with Phase 15 notes, all markdown files have AI-readiness scores</done>
</task>

<task type="auto">
  <name>Task 2: Review AndroidCommonDoc 23 pattern docs for completeness and accuracy gaps</name>
  <files>.planning/phases/13-audit-validate/audit-manifest-androidcommondoc.json</files>
  <action>
Review all pattern docs in `docs/` directory of AndroidCommonDoc (23 files per research, all with valid YAML frontmatter).

For EACH pattern doc:
- Read the full content
- Check frontmatter completeness: scope, sources, targets, version, last_updated, description, slug, status fields
- Check if monitor_urls are configured (only 5 of 23 have them per research)
- AI-readiness score (0-5, standard criteria)
- Completeness assessment: Does the doc cover the topic comprehensively? Are there known patterns from WakeTheCave or DawSync that this doc SHOULD cover but does not? (Cross-reference against common KMP/Android topics from the research findings)
- Accuracy assessment: Are code examples current? Do conventions match what consuming projects actually use?
- Gap identification: What topics, patterns, or conventions are missing from the L0 pattern library that SHOULD be there based on the overall ecosystem?

For docs WITHOUT monitor_urls (18 of 23): Flag as needing URL configuration, and if possible, suggest the official documentation URL that should be monitored (e.g., for a Kotlin coroutines pattern doc, suggest the kotlinlang.org/docs/coroutines-guide.html URL).

Produce a completeness gap list -- topics that exist in WakeTheCave or DawSync docs that suggest AndroidCommonDoc should have coverage but currently does not. These are "gap candidates" for Phase 14 (STRUCT-04).

Output structure for audit-manifest-androidcommondoc.json:
```json
{
  "project": "AndroidCommonDoc",
  "path": "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc",
  "layer": "L0",
  "audit_mode": "full",
  "generated": "<ISO timestamp>",
  "file_count": <actual>,
  "files": [
    {
      "path": "docs/<filename>.md",
      "slug": "<slug from frontmatter>",
      "current_layer": "L0",
      "recommended_layer": "L0",
      "ai_readiness_score": <0-5>,
      "ai_readiness_breakdown": { ... },
      "classification": "ACTIVE",
      "has_monitor_urls": <boolean>,
      "suggested_monitor_urls": [ "..." ],
      "frontmatter_completeness": {
        "has_scope": true,
        "has_sources": true,
        "has_targets": true,
        "has_version": true,
        "has_last_updated": true,
        "has_monitor_urls": false,
        "missing_fields": [ "monitor_urls" ]
      },
      "completeness_gaps": [ "Missing coverage of X pattern found in WakeTheCave/DawSync docs" ],
      "accuracy_issues": [ "Code example uses deprecated API" ],
      "versionRefs": [ { "tech": "...", "found": "...", "current": "...", "stale": false } ],
      "notes": ""
    }
  ],
  "summary": {
    "total_files": <N>,
    "ai_readiness_avg": <float>,
    "docs_with_monitor_urls": <N>,
    "docs_without_monitor_urls": <N>,
    "completeness_gaps_count": <N>,
    "accuracy_issues_count": <N>,
    "coverage_gap_candidates": [
      {
        "topic": "Navigation3 patterns",
        "evidence": "WakeTheCave has extensive navigation docs, AndroidCommonDoc has none",
        "priority": "high"
      }
    ]
  }
}
```
  </action>
  <verify>
    <automated>node -e "const m=JSON.parse(require('fs').readFileSync('.planning/phases/13-audit-validate/audit-manifest-androidcommondoc.json','utf8')); console.log('files:', m.file_count, 'avg AI-readiness:', m.summary.ai_readiness_avg, 'gaps:', m.summary.completeness_gaps_count, 'with monitor_urls:', m.summary.docs_with_monitor_urls); if(m.file_count < 20) throw new Error('Expected ~23 pattern docs'); if(!m.summary.coverage_gap_candidates) throw new Error('Missing coverage gap candidates'); console.log('PASS');"</automated>
  </verify>
  <done>All 23 AndroidCommonDoc pattern docs reviewed with completeness/accuracy gap analysis, frontmatter completeness checked, coverage gap candidates identified from cross-project analysis, monitor_urls suggestions for docs missing them</done>
</task>

</tasks>

<verification>
- audit-manifest-shared-kmp-libs.json exists with module gap analysis covering all module categories
- audit-manifest-androidcommondoc.json exists with completeness/accuracy review of all 23 pattern docs
- shared-kmp-libs module gaps have per-module documentation plan with priority levels
- AndroidCommonDoc coverage gap candidates identified from cross-project knowledge
- Both manifests use consistent schema fields compatible with WakeTheCave and DawSync manifests
</verification>

<success_criteria>
- shared-kmp-libs documentation gaps identified with per-module doc plan (AUDIT-03 success criterion)
- AndroidCommonDoc 8 pattern doc groups reviewed for completeness and accuracy gaps against consolidated corpus (AUDIT-04 success criterion)
- Gap inventory ready for Phase 14's documentation writing work
</success_criteria>

<output>
After completion, create `.planning/phases/13-audit-validate/13-03-SUMMARY.md`
</output>
