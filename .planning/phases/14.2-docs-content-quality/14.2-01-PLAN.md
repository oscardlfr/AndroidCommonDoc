---
phase: 14.2-docs-content-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/registry/types.ts
  - mcp-server/src/registry/scanner.ts
  - mcp-server/src/tools/validate-doc-structure.ts
  - mcp-server/tests/unit/registry/scanner.test.ts
  - mcp-server/tests/unit/tools/validate-doc-structure.test.ts
  - mcp-server/tests/integration/doc-structure.test.ts
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "PatternMetadata includes optional l0_refs string array field"
    - "Scanner extracts l0_refs from frontmatter when present, ignores when absent"
    - "validate-doc-structure reports docs over 500 lines as errors"
    - "validate-doc-structure reports hub docs over 100 lines as errors"
    - "validate-doc-structure reports sections over 150 lines as warnings"
    - "validate-doc-structure validates l0_refs resolve to valid L0 slugs"
    - "validate-doc-structure reports frontmatter completeness score"
    - "All existing tests remain green after extensions"
  artifacts:
    - path: "mcp-server/src/registry/types.ts"
      provides: "PatternMetadata with l0_refs field"
      contains: "l0_refs"
    - path: "mcp-server/src/registry/scanner.ts"
      provides: "l0_refs extraction from frontmatter"
      contains: "l0_refs"
    - path: "mcp-server/src/tools/validate-doc-structure.ts"
      provides: "Size limit checks, l0_refs validation, frontmatter completeness"
      contains: "l0_refs"
    - path: "mcp-server/tests/unit/registry/scanner.test.ts"
      provides: "Scanner l0_refs extraction tests"
      contains: "l0_refs"
    - path: "mcp-server/tests/unit/tools/validate-doc-structure.test.ts"
      provides: "Size limit + l0_refs validation + completeness tests"
      contains: "l0_refs"
  key_links:
    - from: "mcp-server/src/registry/scanner.ts"
      to: "mcp-server/src/registry/types.ts"
      via: "PatternMetadata.l0_refs extraction"
      pattern: "l0_refs.*Array\\.isArray"
    - from: "mcp-server/src/tools/validate-doc-structure.ts"
      to: "mcp-server/src/registry/scanner.ts"
      via: "scanDirectory for L0 slug collection"
      pattern: "scanDirectory"
---

<objective>
Extend MCP tooling with l0_refs cross-layer reference support and quality validation checks.

Purpose: Provide automated validation tools that subsequent plans use to verify their doc quality work. MCP tooling must be ready before any doc content changes begin.
Output: Extended PatternMetadata type, scanner l0_refs extraction, validate-doc-structure with size limits + l0_refs validation + frontmatter completeness scoring.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2-docs-content-quality/14.2-CONTEXT.md
@.planning/phases/14.2-docs-content-quality/14.2-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From mcp-server/src/registry/types.ts:
```typescript
export interface PatternMetadata {
  scope: string[];
  sources: string[];
  targets: string[];
  version?: number;
  last_updated?: string;
  description?: string;
  slug?: string;
  status?: string;
  excludable_sources?: string[];
  monitor_urls?: MonitorUrl[];
  rules?: RuleDefinition[];
  layer?: Layer;
  parent?: string;
  project?: string;
  category?: string;
  // ADD: l0_refs?: string[];
}
```

From mcp-server/src/registry/scanner.ts (metadata extraction pattern):
```typescript
// In scanDirectory, metadata extraction block includes:
category: typeof data.category === "string" ? data.category : undefined,
// ADD similar pattern for l0_refs
```

From mcp-server/src/tools/validate-doc-structure.ts:
```typescript
// validateDocsDirectory() returns structured JSON with errors/warnings arrays
// Extend with new check categories: size_limits, l0_refs_validation, frontmatter_completeness
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PatternMetadata + scanner with l0_refs extraction</name>
  <files>mcp-server/src/registry/types.ts, mcp-server/src/registry/scanner.ts, mcp-server/tests/unit/registry/scanner.test.ts</files>
  <behavior>
    - Test: Scanner extracts l0_refs when present as string array in frontmatter
    - Test: Scanner returns undefined for l0_refs when field absent (backward compatibility)
    - Test: Scanner handles non-array l0_refs gracefully (ignores malformed data)
    - Test: Existing scanner tests continue to pass unchanged
  </behavior>
  <action>
    1. Add `l0_refs?: string[]` to PatternMetadata interface in types.ts
    2. In scanner.ts metadata extraction block, add l0_refs extraction following the same pattern as category:
       `l0_refs: Array.isArray(data.l0_refs) ? (data.l0_refs as string[]) : undefined`
    3. Add tests in scanner.test.ts for l0_refs extraction: present, absent, malformed
    4. Run scanner tests to verify RED then GREEN
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/registry/scanner.test.ts --bail 1</automated>
  </verify>
  <done>PatternMetadata includes l0_refs field, scanner extracts it, all scanner tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend validate-doc-structure with quality checks</name>
  <files>mcp-server/src/tools/validate-doc-structure.ts, mcp-server/tests/unit/tools/validate-doc-structure.test.ts, mcp-server/tests/integration/doc-structure.test.ts</files>
  <behavior>
    - Test: Reports error when active doc exceeds 500 lines
    - Test: Reports error when hub doc (contains "## Sub-documents") exceeds 100 lines
    - Test: Reports warning when any ## section exceeds 150 lines
    - Test: Reports error when l0_refs contains slug not found in L0 registry
    - Test: Reports no error when l0_refs contains valid L0 slugs
    - Test: Skips size checks on archive/ docs
    - Test: Frontmatter completeness score: counts present fields out of 10 required (scope, sources, targets, slug, status, layer, category, description, version, last_updated)
    - Test: Integration test confirms all current L0 docs pass extended validation
  </behavior>
  <action>
    1. In validate-doc-structure.ts, add new validation functions:
       - `checkSizeLimits(filepath, content, isArchived)`: checks 500-line absolute max, 100-line hub max, 150-line section max. Skip archive docs.
       - `validateL0Refs(entries, l0Slugs)`: for each entry with l0_refs, verify each ref exists in L0 slug set
       - `frontmatterCompleteness(metadata)`: score 0-10 counting present fields from required set
    2. Wire new checks into the main `validateDocsDirectory()` function. Add results to the structured JSON output under new keys: `size_errors`, `l0_ref_errors`, `completeness_scores`.
    3. For l0_refs validation: collect L0 slugs by scanning AndroidCommonDoc docs/ (use existing scanDirectory or accept as parameter).
    4. Add unit tests for each new check function with edge cases.
    5. Extend integration test to verify current L0 docs pass the new size checks (they should -- research confirmed all hubs under 100 lines, but 2 sub-docs slightly over 300 need to be caught as warnings, not errors).
    6. Run full test suite.

    IMPORTANT: l0_refs is OPTIONAL. Do not break existing docs. Size checks: 500 = error, hub 100 = error, section 150 = warning. Archive docs excluded from all size checks.
  </action>
  <verify>
    <automated>cd mcp-server && npm test</automated>
  </verify>
  <done>validate-doc-structure reports size violations, l0_refs resolution errors, and frontmatter completeness scores. All 387+ tests green.</done>
</task>

</tasks>

<verification>
- `cd mcp-server && npm test` -- full suite green (387+ tests, 0 failures)
- PatternMetadata in types.ts includes l0_refs field
- Scanner in scanner.ts extracts l0_refs
- validate-doc-structure.ts has size limit checks, l0_refs validation, frontmatter completeness
</verification>

<success_criteria>
- l0_refs field added to PatternMetadata as optional string[]
- Scanner extracts l0_refs from frontmatter YAML
- validate-doc-structure extended with 3 new check categories (size, l0_refs, completeness)
- All existing + new tests green
- Archive docs excluded from size checks
</success_criteria>

<output>
After completion, create `.planning/phases/14.2-docs-content-quality/14.2-01-SUMMARY.md`
</output>
