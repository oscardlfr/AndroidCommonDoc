---
phase: 14.2-docs-content-quality
plan: 09
type: execute
wave: 4
depends_on: [14.2-02, 14.2-03, 14.2-04, 14.2-05, 14.2-06, 14.2-07, 14.2-08]
files_modified:
  - mcp-server/tests/integration/doc-structure.test.ts
  - mcp-server/src/vault/sync-engine.ts
  - mcp-server/src/vault/moc-generator.ts
autonomous: false
requirements: [QUAL-08]
must_haves:
  truths:
    - "validate-doc-structure reports 0 errors across all 3 projects"
    - "No active doc exceeds 500 lines across any project"
    - "All hub docs under 100 lines"
    - "All l0_refs resolve to valid L0 slugs"
    - "Vault re-synced with no errors"
    - "Obsidian vault navigation and graph view work correctly"
    - "Full MCP test suite green"
  artifacts:
    - path: "mcp-server/tests/integration/doc-structure.test.ts"
      provides: "Extended integration tests for Phase 14.2 quality checks"
      contains: "l0_refs"
  key_links:
    - from: "validate-doc-structure"
      to: "all 3 project docs/"
      via: "cross-project validation"
      pattern: "validateDocsDirectory"
    - from: "vault sync"
      to: "all docs"
      via: "sync-engine init mode"
      pattern: "sync.*init"
---

<objective>
Quality gate: validate doc quality across all 3 projects, re-sync vault, and human-verify Obsidian navigation.

Purpose: Ensure all Phase 14.2 work is correct, consistent, and properly reflected in the Obsidian vault. This is the final verification step before the phase is complete.
Output: All validation passing, vault re-synced, human-approved Obsidian navigation.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14.2-docs-content-quality/14.2-CONTEXT.md
@.planning/phases/14.1-docs-subdirectory-reorganization/14.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-project validation + vault re-sync + integration tests</name>
  <files>
    mcp-server/tests/integration/doc-structure.test.ts,
    mcp-server/src/vault/sync-engine.ts,
    mcp-server/src/vault/moc-generator.ts
  </files>
  <action>
    **Part A: Extend integration tests for Phase 14.2**

    Add new test cases to doc-structure.test.ts:
    1. All active L0 docs pass size checks (hub <100, sub-doc <300, absolute <500)
    2. All active L1 docs pass size checks
    3. All active L2 non-diagram docs pass size checks
    4. l0_refs in L1 docs resolve to valid L0 slugs
    5. l0_refs in L2 docs resolve to valid L0 slugs
    6. Frontmatter completeness: all L1 docs score 10/10
    7. Frontmatter completeness: L2 non-diagram docs score 10/10
    8. No active doc across any project exceeds 500 lines

    **Part B: Run validate-doc-structure across all 3 projects**

    Execute the extended validator against:
    1. AndroidCommonDoc docs/ (L0) -- should report 0 errors
    2. shared-kmp-libs docs/ (L1) -- should report 0 errors
    3. DawSync docs/ (L2) -- should report 0 errors (excluding diagrams from size checks as they are small)

    Fix any issues found. Document fixes in SUMMARY.

    **Part C: Vault re-sync**

    1. Re-sync the vault using init mode (clean rebuild):
       - Write a temporary script that imports sync-engine and runs with init mode
       - Or use the MCP sync-vault tool if available via CLI
    2. Verify vault-status shows healthy state:
       - No orphans (or only expected ones)
       - All L0/L1/L2 docs reflected
       - Sub-doc structure preserved in vault
    3. Update MOC generator if needed to handle new sub-docs correctly

    **Part D: Run full test suite**

    `cd mcp-server && npm test` -- all tests must pass.
  </action>
  <verify>
    <automated>cd mcp-server && npm test</automated>
  </verify>
  <done>Extended integration tests pass. validate-doc-structure reports 0 errors across all 3 projects. Vault re-synced successfully. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Obsidian vault and doc quality</name>
  <action>Human verifies Phase 14.2 output in Obsidian vault</action>
  <verify>Human approval</verify>
  <done>Human types "approved" confirming vault navigation, graph view, and doc quality are correct</done>
  <what-built>
    Phase 14.2 complete:
    - 8 oversized L0 docs split into hub+sub-doc format
    - 2 oversized L1 docs split, all L1 frontmatter completed, l0_refs added
    - 95 DawSync docs (33 non-diagram + 62 diagram) have full 10-field frontmatter
    - 18 oversized L2 docs split into hub+sub-doc format
    - l0_refs cross-layer references added, L0 content duplication eliminated
    - DawSyncWeb + SessionRecorder-VST3 subproject docs frontmatted with delegation
    - MCP tooling extended with l0_refs, size checks, frontmatter completeness
    - Vault re-synced with all changes
  </what-built>
  <how-to-verify>
    1. Open Obsidian vault at the configured vault path
    2. Verify Home.md shows category-grouped navigation with new sub-docs
    3. Click through 2-3 hub docs -- verify sub-doc links work
    4. Check graph view -- verify L0/L1/L2 docs are connected
    5. Search for "l0_refs" in vault -- verify reference blocks render correctly
    6. Verify no broken wikilinks (Obsidian shows them in red)
    7. Run `cd mcp-server && npm test` to confirm all tests green
    8. Optionally: run validate-doc-structure tool via MCP to verify 0 errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd mcp-server && npm test` -- full suite green
- validate-doc-structure 0 errors across all 3 projects
- Vault re-synced with healthy status
- Human approves Obsidian navigation
</verification>

<success_criteria>
- All validation passing across all 3 projects
- No active doc exceeds 500 lines
- All hub docs under 100 lines
- All l0_refs resolve to valid L0 slugs
- Vault re-synced with no errors
- Human approves Obsidian vault
- Full MCP test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/14.2-docs-content-quality/14.2-09-SUMMARY.md`
</output>
