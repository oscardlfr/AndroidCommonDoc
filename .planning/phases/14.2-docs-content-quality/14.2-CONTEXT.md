# Phase 14.2: Documentation Content Quality & Cross-Layer References - Context

**Gathered:** 2026-03-15
**Status:** Ready for planning

<domain>
## Phase Boundary

Apply sub-document pattern to all hub docs across L0/L1/L2, eliminate content duplication by replacing with L0 reference blocks, standardize YAML frontmatter on all docs missing it, extend MCP tooling for quality validation, and align DawSync subprojects (DawSyncWeb, SessionRecorder-VST3) with parent doc delegation. Scope: AndroidCommonDoc (L0) + shared-kmp-libs (L1) + DawSync (L2) + DawSync subprojects. Archived docs excluded from splitting but included for frontmatter.

</domain>

<decisions>
## Implementation Decisions

### Sub-doc splitting scope
- **All docs, all layers** get hub+sub-doc treatment when they exceed limits
- Hub <100 lines, sub-docs <300 lines, 500 lines absolute max (carried from Phase 14)
- **Active docs only** — archived docs (archive/) stay as-is, no splitting
- Splitting threshold: Claude's discretion for professional, enterprise, spec-driven approach
- L0: 8 standalone docs (250-342 lines) evaluated for splitting based on content structure
- L1: oversized active docs (convention-plugins 514 lines, api-exposure-pattern 333 lines) split
- L2: 17 active docs over 400 lines split (PRODUCT_SPEC 1667, TESTING 868, NAVIGATION 648, etc.)

### L2 frontmatter standardization
- **Full frontmatter on ALL docs** — scope, sources, targets, slug, status, layer, category, description, version, last_updated
- Applies to all 133 DawSync docs + DawSyncWeb + SessionRecorder-VST3 docs
- **Re-audit existing** category assignments from Phase 14.1 (verify correctness, don't trust blindly)
- Business/legal/product docs get same frontmatter fields adapted to domain (scope: [business, strategy], targets: [web, android], etc.)
- DawSyncWeb and SessionRecorder-VST3 included in this phase

### Cross-layer reference style
- **Replace duplicated content with L0 reference blocks** — delete duplicated sections, replace with standardized reference block format
- **Audit before removing** — check for valuable L2 content that should be promoted to L0 BEFORE deleting. Zero content loss
- **New `l0_refs` frontmatter field** — lists L0 slugs this doc delegates to (e.g., `l0_refs: [testing-patterns, error-handling-patterns]`)
- **Both frontmatter + inline blocks** — `l0_refs` enables automated validation; inline blocks help human readers at point of reference
- **DawSyncWeb delegates to DawSync** — marketing/product/legal docs live in DawSync (parent L2). DawSyncWeb references via delegation. SessionRecorder-VST3 same pattern
- **Audit DawSync + DawSyncWeb .planning/** — create unified doc responsibility map showing which docs are canonical where

### Content quality bar
- **5/5 AI-readiness everywhere** — domain-appropriate criteria per doc type
- Technical docs (L0 patterns, L1 modules): code examples, platform splits, anti-patterns, monitor_urls, complete frontmatter
- Business/legal/product docs: complete frontmatter, clear structure (sections <150 lines), cross-references, decision rationale, status/date tracking
- "5/5" means an AI agent can load and act on it — adapted per domain

### MCP tooling updates
- **Extend validate-doc-structure** with new quality checks:
  - `l0_refs` field resolves to valid L0 slugs
  - Sections under 150 lines
  - Hub docs under 100 lines
  - No doc over 500 lines (active docs only)
  - Frontmatter completeness score
- PatternMetadata extended with `l0_refs` field
- Scanner extracts and validates cross-layer references

### Claude's Discretion
- Exact splitting threshold for L0 docs in the 250-342 line range (professional judgment)
- How to structure L0 reference blocks (exact markdown format)
- Hub+sub-doc split points for each oversized doc (which sections become sub-docs)
- Execution order across projects (L0 first to establish references, then L1/L2)
- Which L2 content gets promoted to L0 during dedup audit
- Domain-appropriate quality criteria definitions per doc type
- DawSync subproject doc discovery and delegation mechanism

</decisions>

<specifics>
## Specific Ideas

- "Always review that we don't lose valuable doc content — maybe some L2 content should be promoted to L0 first"
- "DawSyncWeb should share marketing/product/legal doc rules with DawSync parent"
- "I have confusion about which marketing/product decisions were made in each project's planning — need to unify and delegate doc responsibilities correctly"
- "Professional, solid, clean, enterprise, spec-driven development" — quality bar for all implementations
- Model 5/5 AI-readiness docs remain the reference: testing-patterns.md, viewmodel-state-patterns.md, compose-resources-patterns.md

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `docs/testing/testing-patterns.md` + sub-docs: Model hub+sub-doc pattern to replicate
- `docs/gradle/gradle-patterns.md` + sub-docs: Second existing hub+sub-doc example
- `mcp-server/src/registry/scanner.ts`: Dynamic scanner — needs `l0_refs` field extraction
- `mcp-server/src/registry/frontmatter.ts`: YAML parser — extend for `l0_refs` field
- `mcp-server/src/tools/validate-doc-structure.ts`: Quality validator — extend with new checks
- `mcp-server/src/registry/project-discovery.ts`: Cross-project discovery — reuse for subproject detection

### Established Patterns
- Hub+sub-doc: hub has `## Sub-documents` section, sub-docs have `parent: <hub_slug>` frontmatter
- YAML frontmatter: scope, sources, targets, slug, status, monitor_urls, description, version, last_updated, layer, category
- Category vocabulary: L0 core (architecture, testing, error-handling, ui, gradle, offline-first, compose, resources, di, navigation, storage, guides) + L1/L2 additions
- L0>L1>L2 full replacement semantics in resolver (Phase 9)
- Sub-project support established in Phase 12 (ECOV-04)
- Vault excludes archive/ directories (Phase 14.1)

### Integration Points
- `docs/**/*.md` (44 files): L0 docs — split oversized, maintain as reference targets for L1/L2
- `C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/docs/**/*.md` (28 files): L1 docs — add l0_refs, split oversized active docs
- `C:/Users/34645/AndroidStudioProjects/DawSync/docs/**/*.md` (133 files): L2 docs — full frontmatter, dedup, split active oversized
- DawSyncWeb docs: marketing/product/legal delegation to DawSync parent
- SessionRecorder-VST3 docs: delegation to DawSync parent
- `~/.androidcommondoc/vault-config.json`: Update for subproject doc collection
- DawSync + DawSyncWeb `.planning/` directories: audit for marketing/product decision unification

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 14.2-docs-content-quality*
*Context gathered: 2026-03-15*
