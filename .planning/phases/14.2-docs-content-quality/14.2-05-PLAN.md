---
phase: 14.2-docs-content-quality
plan: 05
type: execute
wave: 2
depends_on: [14.2-01]
files_modified:
  # 62 DawSync diagram docs under docs/architecture/diagrams/
  - C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/
autonomous: true
requirements: [QUAL-05]
must_haves:
  truths:
    - "All 62 DawSync diagram docs have full 10-field YAML frontmatter"
    - "Diagram docs use scope: [architecture, diagrams] and sources: [dawsync]"
    - "Diagram docs use targets: [android, desktop] and layer: L2"
    - "Category is architecture for all diagram docs"
    - "Slug derived from lowercase-kebab-case of filename"
  artifacts:
    - path: "C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/"
      provides: "62 diagram docs with full frontmatter"
      contains: "scope:"
  key_links:
    - from: "DawSync/docs/architecture/diagrams/**/frontmatter"
      to: "DawSync/docs/architecture/diagrams/ subdirectory"
      via: "category: architecture matching parent subdirectory"
      pattern: "category: architecture"
---

<objective>
Add full 10-field YAML frontmatter to all 62 DawSync architecture diagram docs.

Purpose: Diagram docs currently have NO frontmatter at all, making them invisible to MCP tools and vault indexing. This is mechanical/templated work -- all diagrams share the same scope, sources, targets, and layer values.
Output: All 62 diagram docs have complete frontmatter following a consistent template.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14.2-docs-content-quality/14.2-CONTEXT.md

<interfaces>
<!-- Diagram doc frontmatter template (all 62 share this structure) -->
```yaml
---
scope: [architecture, diagrams]
sources: [dawsync]
targets: [android, desktop]
slug: diagram-name-lowercase-kebab
status: active
layer: L2
category: architecture
description: "DawSync [specific diagram topic] architecture diagram"
version: 1
last_updated: "2026-03"
---
```

<!-- DawSync diagram directory structure (from Phase 14.1-04) -->
DawSync/docs/architecture/diagrams/
  A-repository-pattern/     (repository pattern diagrams)
  B-offline-sync/           (offline sync flow diagrams)
  C-audio-engine/           (audio engine architecture)
  D-capture-system/         (capture system diagrams)
  E-session-management/     (session management)
  F-export-pipeline/        (export pipeline diagrams)
  G-usecase-flows/          (use case flow diagrams)
  H-module-dependencies/    (module dependency diagrams)

<!-- Key constraint -->
scope: [architecture, diagrams] is correct for all -- they are DawSync-internal
sources: [dawsync] -- project-specific, not from external libraries
These will NOT appear in L0/L1 find-pattern searches (correct behavior)
They WILL appear with project filter: find-pattern --project=DawSync
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontmatter to diagram docs in subdirectories A-D</name>
  <files>C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/A-repository-pattern/*.md, C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/B-offline-sync/*.md, C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/C-audio-engine/*.md, C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/D-capture-system/*.md</files>
  <action>
    For each markdown file in diagram subdirectories A through D:

    1. List all .md files: `find DawSync/docs/architecture/diagrams/{A,B,C,D}* -name "*.md"`
    2. For each file:
       a. Read the file to get the first heading (# Title) for the description
       b. Derive slug from filename: lowercase, remove .md, replace spaces/underscores with hyphens
       c. Add frontmatter block at the top:
          ```yaml
          ---
          scope: [architecture, diagrams]
          sources: [dawsync]
          targets: [android, desktop]
          slug: {derived-slug}
          status: active
          layer: L2
          category: architecture
          description: "DawSync {diagram topic from heading} architecture diagram"
          version: 1
          last_updated: "2026-03"
          ---
          ```
       d. IMPORTANT: If file already has frontmatter (some might from Phase 14.1), upgrade it to include all 10 fields rather than adding a second frontmatter block
    3. Quote all description values with double quotes
    4. Count files processed per subdirectory for verification

    This is mechanical/templated work. The only field that varies per file is `slug` and `description`.
  </action>
  <verify>
    <automated>cd C:/Users/34645/AndroidStudioProjects/DawSync && find docs/architecture/diagrams/A* docs/architecture/diagrams/B* docs/architecture/diagrams/C* docs/architecture/diagrams/D* -name "*.md" -exec grep -L "^scope:" {} \; 2>/dev/null | wc -l</automated>
  </verify>
  <done>All diagram docs in subdirectories A-D have full 10-field YAML frontmatter. Zero files missing scope field.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontmatter to diagram docs in subdirectories E-H + verify all 62</name>
  <files>C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/E-session-management/*.md, C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/F-export-pipeline/*.md, C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/G-usecase-flows/*.md, C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/diagrams/H-module-dependencies/*.md</files>
  <action>
    1. Same process as Task 1 for subdirectories E through H:
       - List all .md files: `find DawSync/docs/architecture/diagrams/{E,F,G,H}* -name "*.md"`
       - For each file: read heading, derive slug, add full frontmatter block
       - Quote descriptions, handle existing frontmatter by upgrading

    2. After completing all subdirectories, verify the total:
       - `find DawSync/docs/architecture/diagrams -name "*.md" | wc -l` should be ~62
       - `find DawSync/docs/architecture/diagrams -name "*.md" -exec grep -L "^scope:" {} \;` should return 0
       - Spot-check 3-4 files for correct slug derivation and description accuracy

    3. Also check for any diagram docs outside A-H subdirectories:
       - `find DawSync/docs/architecture/diagrams -maxdepth 1 -name "*.md"` (root-level diagram files)
       - Add frontmatter to any root-level diagram docs found
  </action>
  <verify>
    <automated>cd C:/Users/34645/AndroidStudioProjects/DawSync && missing=$(find docs/architecture/diagrams -name "*.md" -exec grep -L "^scope:" {} \; 2>/dev/null | wc -l) && total=$(find docs/architecture/diagrams -name "*.md" | wc -l) && echo "Total: $total, Missing scope: $missing (should be 0)"</automated>
  </verify>
  <done>All 62 DawSync diagram docs have full 10-field YAML frontmatter. Zero docs missing scope field. Total count verified.</done>
</task>

</tasks>

<verification>
- All 62 diagram docs have scope, sources, targets, slug, status, layer, category, description, version, last_updated
- Category is "architecture" for all diagram docs
- Slugs are lowercase-kebab-case
- Descriptions are quoted
</verification>

<success_criteria>
- All 62 DawSync diagram docs have complete 10-field YAML frontmatter
- Consistent template: scope [architecture, diagrams], sources [dawsync], targets [android, desktop], layer L2
- slug and description are doc-specific, derived from filename/heading
- Zero docs in diagrams/ without frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/14.2-docs-content-quality/14.2-05-SUMMARY.md`
</output>
