---
phase: 18-gsd-v1-to-gsd-2-migration
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - CLAUDE.md
  - C:/Users/34645/.claude/CLAUDE.md
autonomous: true
requirements: [GSD2-CONFIG, GSD2-VALIDATE, GSD2-CLEANUP]

must_haves:
  truths:
    - "Each migrated project has a preferences.md with correct model and budget settings"
    - "All 4 migrated projects pass gsd doctor with no errors"
    - "Completion state in .gsd/ matches original .planning/ state for each project"
    - "GSD v1 workflow files are archived or removed"
    - "CLAUDE.md files no longer reference GSD v1 commands or .planning/ paths"
  artifacts:
    - path: ".gsd/preferences.md"
      provides: "L0 project preferences"
    - path: "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/.gsd/preferences.md"
      provides: "L1 project preferences"
    - path: "C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave/.gsd/preferences.md"
      provides: "L2 DawSync project preferences"
  key_links:
    - from: "preferences.md"
      to: "gsd CLI behavior"
      via: "model selection, budget ceiling, timeout config"
      pattern: "models.*claude"
---

<objective>
Configure preferences for all migrated projects, validate migration integrity across the ecosystem, clean up GSD v1 infrastructure, and update CLAUDE.md references.

Purpose: Ensure all projects are correctly configured for GSD-2 operation and remove v1 artifacts that could cause confusion.
Output: Configured preferences.md per project, validated migrations, cleaned v1 infrastructure, updated CLAUDE.md files.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-gsd-v1-to-gsd-2-migration/18-CONTEXT.md
@.planning/phases/18-gsd-v1-to-gsd-2-migration/18-RESEARCH.md
@.planning/phases/18-gsd-v1-to-gsd-2-migration/18-01-SUMMARY.md
@.planning/phases/18-gsd-v1-to-gsd-2-migration/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure preferences.md for all projects and validate migrations</name>
  <files></files>
  <action>
For each migrated project, configure preferences.md using `/gsd prefs` or by directly editing the generated preferences.md. Use these settings per user decision:

**All projects (base settings):**
```yaml
models:
  research: claude-sonnet-4-6
  planning:
    model: claude-opus-4-6
    fallbacks: [claude-sonnet-4-6]
  execution: claude-sonnet-4-6
  completion: claude-sonnet-4-6
token_profile: balanced
git:
  isolation: worktree
skill_discovery: suggest
```

**Per-project tuning (Claude's discretion):**
- AndroidCommonDoc (L0 toolkit): budget_ceiling: 30.00, soft_timeout: 15min (doc/tooling work is lighter)
- shared-kmp-libs (L1): budget_ceiling: 40.00, soft_timeout: 20min (KMP module work is medium)
- DawSync (L2): budget_ceiling: 50.00, soft_timeout: 25min (complex app with multi-track)
- DawSyncWeb: budget_ceiling: 30.00, soft_timeout: 15min (web frontend, lighter scope)

Configure each:
```bash
cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc && gsd prefs
cd C:/Users/34645/AndroidStudioProjects/shared-kmp-libs && gsd prefs
cd C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave && gsd prefs
cd <dawsyncweb-path> && gsd prefs
```

**Validation pass:** After configuring, run validation across all projects:
```bash
cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc && gsd doctor && gsd status
cd C:/Users/34645/AndroidStudioProjects/shared-kmp-libs && gsd doctor && gsd status
cd C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave && gsd doctor && gsd status
cd <dawsyncweb-path> && gsd doctor && gsd status
```

Compare completion state with v1 state:
- AndroidCommonDoc: was 97% (99/101 plans). Verify .gsd/ shows equivalent progress.
- shared-kmp-libs: 1 phase with 1 summary. Verify preserved.
- DawSync: Track-E at 50% (plans 01-05 of 12 complete). Verify current position.
- DawSyncWeb: 11 phases. Verify count matches.
  </action>
  <verify>
    <automated>cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc && gsd doctor && cd C:/Users/34645/AndroidStudioProjects/shared-kmp-libs && gsd doctor && cd C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave && gsd doctor</automated>
  </verify>
  <done>All 4 projects have preferences.md with correct settings. gsd doctor passes in all projects. gsd status shows completion state matching v1.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up GSD v1 infrastructure and update CLAUDE.md files</name>
  <files>CLAUDE.md, C:/Users/34645/.claude/CLAUDE.md</files>
  <action>
**Step 1: Archive .planning/ directories**

Per research recommendation, rename .planning/ to .planning-v1-archive/ in each project and add to .gitignore. This preserves history without polluting active structure.

```bash
# AndroidCommonDoc
cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc
mv .planning .planning-v1-archive
echo ".planning-v1-archive/" >> .gitignore

# shared-kmp-libs
cd C:/Users/34645/AndroidStudioProjects/shared-kmp-libs
mv .planning .planning-v1-archive
echo ".planning-v1-archive/" >> .gitignore

# DawSync
cd C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave
mv .planning .planning-v1-archive
echo ".planning-v1-archive/" >> .gitignore

# DawSyncWeb (if it has .planning/)
# Same pattern
```

**Step 2: Clean up GSD v1 global infrastructure**

The GSD v1 infrastructure is at ~/.ccs/instances/cuenta1/get-shit-done/. Do NOT delete it yet -- rename to indicate deprecated status:

```bash
mv ~/.ccs/instances/cuenta1/get-shit-done ~/.ccs/instances/cuenta1/get-shit-done-v1-archived
```

**Step 3: Update CLAUDE.md files**

Read and update each CLAUDE.md to remove GSD v1 references:

1. `~/.claude/CLAUDE.md` (L0 global): Remove any references to .planning/ directories, GSD v1 workflows, config.json, or gsd-tools.cjs. Replace with note that GSD-2 (`gsd` CLI) is the active workflow tool.

2. `C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/CLAUDE.md` (L0 toolkit): Update any .planning/ references. The Workflow section mentions GSD -- update to reference GSD-2 patterns.

Keep changes minimal -- only update GSD-related references, do not restructure the files.

**Step 4: Remove GSD v1 Claude commands (if they exist)**

Check for and remove GSD v1 commands:
```bash
ls ~/.claude/commands/gsd/ 2>/dev/null
```

If GSD v1 command files exist at ~/.claude/commands/gsd/, remove them since GSD-2 has built-in commands:
```bash
rm -rf ~/.claude/commands/gsd/
```
  </action>
  <verify>
    <automated>ls C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/.planning 2>/dev/null; echo "---"; ls C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/.planning-v1-archive/STATE.md 2>/dev/null; echo "---"; ls ~/.ccs/instances/cuenta1/get-shit-done 2>/dev/null; echo "---"; gsd --version; echo "---"; grep -r '\.planning/' C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/CLAUDE.md C:/Users/34645/.claude/CLAUDE.md 2>/dev/null && echo "FAIL: .planning/ refs still in CLAUDE.md" || echo "PASS: no .planning/ refs in CLAUDE.md"; echo "---"; cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc && gsd doctor</automated>
  </verify>
  <done>.planning/ renamed to .planning-v1-archive/ in all projects, GSD v1 global infra archived, CLAUDE.md files updated to reference GSD-2, gsd CLI still operational</done>
</task>

</tasks>

<verification>
- preferences.md exists in .gsd/ of all 4 projects with correct model settings
- gsd doctor passes in all 4 projects
- .planning/ directories archived (renamed, gitignored)
- GSD v1 global infrastructure archived
- CLAUDE.md files no longer reference .planning/ or v1 commands
- gsd CLI still works after cleanup
</verification>

<success_criteria>
All projects configured with proper preferences. Migration integrity validated across the ecosystem. GSD v1 infrastructure archived (not deleted -- available for rollback for 30 days). CLAUDE.md files reference GSD-2.
</success_criteria>

<output>
After completion, create `.planning/phases/18-gsd-v1-to-gsd-2-migration/18-03-SUMMARY.md`

Note: Since .planning/ will be archived in this plan, the summary should be written BEFORE archiving, or written to the .gsd/ directory as GSD-2 expects.
</output>
