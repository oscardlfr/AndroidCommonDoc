---
phase: 18-gsd-v1-to-gsd-2-migration
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified: []
autonomous: false
requirements: [GSD2-MIGRATE-L2]

must_haves:
  truths:
    - "DawSync has a .gsd/ directory with all 25+ phases migrated"
    - "DawSync track-E (Phase 12) shows plans 01-05 complete and plans 06-12 pending"
    - "DawSync completed tracks (A, C, D) are preserved as historical data"
    - "DawSyncWeb has a .gsd/ directory with 11 phases migrated"
  artifacts:
    - path: "C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave/.gsd/"
      provides: "Migrated GSD-2 state for DawSync"
    - path: "DawSyncWeb .gsd/"
      provides: "Migrated GSD-2 state for DawSyncWeb"
  key_links:
    - from: "DawSync .planning/ track-E Phase 12"
      to: ".gsd/ milestone/slice structure"
      via: "/gsd migrate + manual track mapping"
      pattern: "M001.*detection"
---

<objective>
Migrate DawSync (L2) and DawSyncWeb from .planning/ to .gsd/, handling the complex parallel track structure.

Purpose: DawSync is the most complex migration with 6 parallel tracks and an in-progress track-E at 50%. Correct mapping is critical for continued development.
Output: DawSync and DawSyncWeb .gsd/ directories with track-E positioned for immediate continuation.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-gsd-v1-to-gsd-2-migration/18-CONTEXT.md
@.planning/phases/18-gsd-v1-to-gsd-2-migration/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DawSync from .planning/ to .gsd/</name>
  <files></files>
  <action>
Navigate to DawSync and run the migration:
```bash
cd C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave
gsd
/gsd migrate
```

DawSync has 25+ phase directories with parallel tracks (A-F):
- Track A: Data Resilience (8.3a) -- COMPLETE
- Track B: macOS Parity (8.2) -- Planned
- Track C: Analytics (9.1) -- COMPLETE, blocked on Track E merge
- Track D: VST3 Hardening (8.4) -- COMPLETE
- Track E: Detection Engine (12) -- IN PROGRESS 50% (plans 01-05 complete, 06-12 pending)
- Track F: DawSyncWeb -- separate repo

CRITICAL: Review the migration preview carefully:
1. Verify ALL 25+ phases appear (including decimal: 8.1, 8.1.1, 8.1.2, 8.1.3, 8.1.4, 8.2, 8.3a, 8.3b, 8.4, 9.1, 9.2)
2. Verify Track-E Phase 12 shows 5 completed plans and 7 pending plans
3. Verify completion state for completed tracks (A, C, D) is preserved

After migration, verify track mapping:
```bash
gsd doctor
gsd status
```

If the migration flattens tracks into a single sequence, note this for post-migration restructuring. Per research recommendation, DawSync tracks should ideally map to separate GSD-2 milestones:
- M001: Track-E Detection Engine (active, priority)
- M002: Track-B macOS Parity (planned)
- Completed tracks: archived

If `/gsd migrate` does not auto-create milestone separation for tracks, document the actual output structure for the human checkpoint in Task 2.
  </action>
  <verify>
    <automated>cd C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave && gsd doctor</automated>
  </verify>
  <done>DawSync .gsd/ directory exists, gsd doctor passes, track-E Phase 12 shows correct completion state (5/12 plans complete)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify DawSync track mapping</name>
  <action>Human verifies DawSync track mapping after migration.</action>
  <what-built>
DawSync migration from .planning/ to .gsd/. The parallel track structure (6 tracks A-F) has been migrated. Track-E (Detection Engine) is the active work at 50% completion.
  </what-built>
  <how-to-verify>
1. Run `cd C:/Users/34645/AndroidStudioProjects/WakeTheCave/WakeTheCave && gsd status` and review the output
2. Verify track-E shows as the active/current work with plans 06-12 as next tasks
3. Check if tracks mapped to separate milestones or flattened into one sequence
4. If tracks are flattened: decide whether manual restructuring is needed now or can be deferred
5. Confirm the migration looks correct for your DawSync workflow
  </how-to-verify>
  <resume-signal>Type "approved" if DawSync migration looks correct, or describe any issues with track mapping</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Migrate DawSyncWeb from .planning/ to .gsd/</name>
  <files></files>
  <action>
Locate DawSyncWeb's .planning/ directory and run migration. DawSyncWeb is a subproject of DawSync with its own .planning/ directory containing 11 phases (including decimal: 8.1, 8.5, 8.5.1).

Find DawSyncWeb location first:
```bash
# DawSyncWeb is likely under DawSync or a sibling directory
ls C:/Users/34645/AndroidStudioProjects/WakeTheCave/DawSyncWeb/.planning/ 2>/dev/null
ls C:/Users/34645/AndroidStudioProjects/DawSyncWeb/.planning/ 2>/dev/null
```

Navigate to DawSyncWeb and run migration:
```bash
cd <dawsyncweb-path>
gsd
/gsd migrate
```

Review preview -- verify all 11 phases appear including decimal numbering (8.5, 8.5.1).

After migration:
```bash
gsd doctor
gsd status
```
  </action>
  <verify>
    <automated>cd "$(find C:/Users/34645/AndroidStudioProjects -maxdepth 3 -name '.gsd' -path '*DawSyncWeb*' -type d 2>/dev/null | head -1 | xargs dirname)" && gsd doctor</automated>
  </verify>
  <done>DawSyncWeb .gsd/ directory exists, gsd doctor passes from DawSyncWeb project root, all 11 phases migrated</done>
</task>

</tasks>

<verification>
- `gsd doctor` passes in both DawSync and DawSyncWeb
- `gsd status` in DawSync shows track-E as active work at ~50%
- DawSyncWeb shows all 11 phases migrated
- Human has approved the DawSync track mapping
</verification>

<success_criteria>
All L2 projects migrated. DawSync's parallel track structure is preserved or mapped appropriately. Track-E is positioned for immediate continuation. DawSyncWeb migration is clean.
</success_criteria>

<output>
After completion, create `.planning/phases/18-gsd-v1-to-gsd-2-migration/18-02-SUMMARY.md`
</output>
