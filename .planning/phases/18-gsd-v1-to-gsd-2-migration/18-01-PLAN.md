---
phase: 18-gsd-v1-to-gsd-2-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [GSD2-INSTALL, GSD2-MIGRATE-L0, GSD2-MIGRATE-L1]

must_haves:
  truths:
    - "gsd-pi CLI is installed globally and responds to gsd --version"
    - "AndroidCommonDoc has a .gsd/ directory with migrated state from .planning/"
    - "shared-kmp-libs has a .gsd/ directory with migrated state from .planning/"
    - "gsd config wizard has been completed (LLM provider configured)"
  artifacts:
    - path: ".gsd/"
      provides: "Migrated GSD-2 state for AndroidCommonDoc"
    - path: "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/.gsd/"
      provides: "Migrated GSD-2 state for shared-kmp-libs"
  key_links:
    - from: "gsd-pi CLI"
      to: ".gsd/ directories"
      via: "/gsd migrate command"
      pattern: "gsd migrate"
---

<objective>
Install GSD-2 CLI globally and migrate the two simplest projects (L0 AndroidCommonDoc, L1 shared-kmp-libs) as pilot migrations.

Purpose: Establish GSD-2 tooling and validate migration mechanics on simple projects before tackling DawSync's complex parallel tracks.
Output: gsd-pi installed, gsd config completed, L0 and L1 .gsd/ directories created with migrated state.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-gsd-v1-to-gsd-2-migration/18-CONTEXT.md
@.planning/phases/18-gsd-v1-to-gsd-2-migration/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install gsd-pi and complete initial setup</name>
  <files></files>
  <action>
Install GSD-2 CLI globally:
```bash
npm install -g gsd-pi
```

Verify installation:
```bash
gsd --version
```
Expected: version 2.22.0 or higher.

Run the first-time setup wizard:
```bash
gsd config
```
This configures the LLM provider (Anthropic) and tool keys. Follow the interactive prompts to set up the API key.

IMPORTANT: The package name is `gsd-pi` (NOT `gsd-2`). The CLI command is `gsd`.
  </action>
  <verify>
    <automated>gsd --version</automated>
  </verify>
  <done>gsd --version returns a version number (2.22.0+), gsd config has been completed successfully</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AndroidCommonDoc (L0) from .planning/ to .gsd/</name>
  <files></files>
  <action>
Navigate to AndroidCommonDoc and run the migration:
```bash
cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc
gsd
/gsd migrate
```

The migration will:
1. Show a preview of how .planning/ maps to .gsd/ (phases to slices, plans to tasks)
2. Wait for confirmation before writing
3. Handle decimal phase numbering (14.1, 14.2, 14.2.1, 14.3) -- verify these appear correctly in preview

CRITICAL CHECK: AndroidCommonDoc has 18 phases with deeply nested decimal numbering (14.2.1). Verify the preview shows all phases before confirming. If any phases are missing or flattened, do NOT confirm -- investigate the issue.

After migration completes, validate:
```bash
gsd doctor
gsd status
```

Verify:
- Phase count in .gsd/ matches 18 phases from .planning/
- Completion percentages look reasonable (v1.2 was at 97%)
- No errors from gsd doctor
  </action>
  <verify>
    <automated>cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc && gsd doctor</automated>
  </verify>
  <done>AndroidCommonDoc .gsd/ directory exists, gsd doctor passes, gsd status shows ~97% progress matching v1 state</done>
</task>

<task type="auto">
  <name>Task 3: Migrate shared-kmp-libs (L1) from .planning/ to .gsd/</name>
  <files></files>
  <action>
Navigate to shared-kmp-libs and run the migration:
```bash
cd C:/Users/34645/AndroidStudioProjects/shared-kmp-libs
gsd
/gsd migrate
```

shared-kmp-libs has minimal .planning/ content (1 phase directory: 12.6-kotlinx-io-migration, 1 summary file). This is the simplest migration.

Review the preview. Confirm if it looks correct.

After migration:
```bash
gsd doctor
gsd status
```

Verify:
- .gsd/ directory created
- Phase 12.6 content preserved
- gsd doctor passes with no errors
  </action>
  <verify>
    <automated>cd C:/Users/34645/AndroidStudioProjects/shared-kmp-libs && gsd doctor</automated>
  </verify>
  <done>shared-kmp-libs .gsd/ directory exists, gsd doctor passes, migration content preserved</done>
</task>

</tasks>

<verification>
- `gsd --version` returns 2.22.0+
- `gsd doctor` passes in both AndroidCommonDoc and shared-kmp-libs
- `gsd status` in AndroidCommonDoc shows progress matching v1 state (~97%)
- No .gsd/ directory conflicts or missing phases
</verification>

<success_criteria>
GSD-2 is installed and operational. L0 and L1 projects have been migrated from .planning/ to .gsd/ with all state preserved. Pilot migrations validate that the migration tool handles decimal phase numbering correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/18-gsd-v1-to-gsd-2-migration/18-01-SUMMARY.md`
</output>
