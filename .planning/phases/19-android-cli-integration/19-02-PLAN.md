# Plan 19-02: UI Validation Pipeline — DawSync Unblocker

**Phase:** 19 Android CLI Integration
**Wave:** 2
**Depends on:** 19-POC (schema pinned)
**Blast radius:** MEDIUM
**Requirements:** AC-CLI-02

## Goal

Detect automatically that "tests pass but app is broken" via `android screen capture --annotate` + `android layout --diff` in runtime. Closes memory `feedback_ui_validation_broken.md`.

## Scope

### New MCP tool
- Duplicate pattern of `mcp-server/src/tools/compose-preview-audit.ts` as `mcp-server/src/tools/android-layout-diff.ts`
- Implement parser using schema documented in `19-POC-FINDINGS.md`
- Register in `mcp-server/src/tools/index.ts:40-41` (import + call); update counter "39 tools" → "40 tools" in `index.ts:129`
- Propagate count update to `CLAUDE.md:105`

### Agent integration (ui-specialist)
- Extend `setup/agent-templates/ui-specialist.md` inserting new "Step 8: Runtime UI Validation" after Step 7 "Compose Testing"
- Update `## MCP Tools` section (line 98-101) adding `android-layout-diff`
- **Sync dual-location**: replicate edit to `.claude/agents/ui-specialist.md` (risk #1 from Phase 1 mapping)

### Quality gater integration
- Extend `setup/agent-templates/quality-gater.md:167-179` Step 9 with layout-diff invocation in the quality gate flow
- Sync to `.claude/agents/quality-gater.md`

### Windows-specific documentation
- New `docs/guides/getting-started/android-cli-windows.md` with physical device guidance via adb

## Success Criteria

1. `android-layout-diff` MCP tool against a DawSync build where `UiState.Loading` renders empty (current bug) returns FAIL with `file:line` identifying the broken Composable (not a generic "layout differs" message)
2. `ui-specialist` agent invoked on a DawSync PR reproducing the "tests pass but app broken" state reports the failure in its `FINDINGS_START/END` JSON block with category `ui-accessibility` and severity `HIGH`
3. Byte-identical parity between `setup/agent-templates/ui-specialist.md` and `.claude/agents/ui-specialist.md` for Step 8 (verified by `validate-agents` MCP tool)
4. Quality gate orchestrator (`/full-audit`) includes layout-diff findings in consolidated report without duplicate dedupe keys
5. On a Windows host with no connected device, tool emits clear error referencing `docs/guides/getting-started/android-cli-windows.md` — does not hang or exec-fail silently
6. `cd mcp-server && npm test` green
7. `/pre-pr` green

## Files to modify

- `mcp-server/src/tools/index.ts` (register new tool, bump counter)
- `setup/agent-templates/ui-specialist.md` (add Step 8)
- `.claude/agents/ui-specialist.md` (sync parity)
- `setup/agent-templates/quality-gater.md` (extend Step 9)
- `.claude/agents/quality-gater.md` (sync parity)
- `CLAUDE.md` (tool count)

## Files to create

- `mcp-server/src/tools/android-layout-diff.ts`
- `docs/guides/getting-started/android-cli-windows.md`

## Verification

1. `cd mcp-server && npm test` passes (including new tests for `android-layout-diff`)
2. `/validate-agents` confirms parity dual-location
3. `/ui-specialist` on a prepared DawSync PR reproducing the bug reports findings with correct schema
4. `/full-audit` run includes findings without duplicates
5. On Windows without device: tool returns exit code 2 + error message referencing windows doc
6. `/pre-pr` green

## Risks & Mitigations

- **Parser fragility**: schema pinned to v0.7; version check in `android-layout-diff.ts` emits warning if `android --version` reports different (not fail, just warn)
- **Dual-location drift**: success criteria 3 enforces byte parity; also add to `validate-agents` output the parity check for Step 8

## Notes

- Cannot start until 19-POC exits criteria met (schema pinned).
- High-value plan: directly closes the #1 DawSync priority from memory.
