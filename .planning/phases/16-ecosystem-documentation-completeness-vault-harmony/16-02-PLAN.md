---
phase: 16-ecosystem-documentation-completeness-vault-harmony
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DawSync/docs/**/*.md (category frontmatter fixes)
  - DawSync/docs/architecture/diagrams/**/*.md (diagram category audit)
  - DawSync/SessionRecorder-VST3/**/*.md (subproject doc assessment)
  - DawSyncWeb/**/*.md (subproject doc assessment)
autonomous: true
requirements: [P16-CATEGORY, P16-SUBPROJ]

must_haves:
  truths:
    - "All DawSync docs use categories from the 9-category unified vocabulary"
    - "All 62 DawSync diagram docs have semantically correct categories"
    - "Cross-layer category audit passes for L0, L1, and L2"
    - "DawSync subproject documentation is assessed and gaps addressed"
  artifacts:
    - path: "DawSync/docs/"
      provides: "All DawSync docs with correct unified-vocabulary categories"
      contains: "category:"
    - path: "DawSync/SessionRecorder-VST3/"
      provides: "Subproject docs with verified frontmatter"
      contains: "scope:"
  key_links:
    - from: "DawSync/docs/**/*.md"
      to: "mcp-server/src/tools/validate-doc-structure.ts"
      via: "APPROVED_CATEGORIES and SUBDIR_TO_CATEGORIES validation"
      pattern: "category:"
---

<objective>
Fix DawSync category alignment across all docs (31+ mismatched files including diagrams), run cross-layer category audit across L0/L1/L2, and assess/complete DawSync subproject documentation.

Purpose: Complete the category consolidation that Phase 14.3 started, ensuring all ecosystem docs use the 9-category unified vocabulary. Assess and fill any documentation gaps in DawSyncWeb and SessionRecorder-VST3 subprojects.
Output: All DawSync docs with correct categories, cross-layer audit clean, subproject docs assessed and gaps filled.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-ecosystem-documentation-completeness-vault-harmony/16-CONTEXT.md
@.planning/phases/16-ecosystem-documentation-completeness-vault-harmony/16-RESEARCH.md
@.planning/phases/14.3-skill-materialization-registry/14.3-09-SUMMARY.md

<interfaces>
<!-- Category vocabulary and mapping from validate-doc-structure.ts -->

9 unified categories:
architecture, testing, data, security, build, guides, domain, product, ui

SUBDIR_TO_CATEGORIES mapping (from Phase 14.3):
- architecture -> [architecture, data, ui]
- business -> [product]
- legal -> [product]
- references -> [product]
- guides -> [guides, testing, ui]
- tech -> [build, security]

Phase 14.3-09 fixed 9 docs (testing x5, ui x2, data x1, security x1).
Remaining gap: 31/84 files still with mismatched categories.

DawSync subprojects:
- SessionRecorder-VST3: 8 docs (README, CHANGELOG, EULA, MACOS_BUILD_INSTRUCTIONS, TESTING, src/README, installer/macos/README, installer/windows/README) -- C++/JUCE domain, not KMP
- DawSyncWeb: External sibling at C:\Users\34645\AndroidStudioProjects\DawSyncWeb -- Astro web project, minimal docs needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DawSync category alignment and cross-layer audit</name>
  <files>DawSync/docs/**/*.md, DawSync/docs/architecture/diagrams/**/*.md</files>
  <action>
    1. Run validate-doc-structure on DawSync to identify all category mismatches. Read the tool output to understand the current state.

    2. Scan ALL DawSync docs (text docs + 62 architecture diagrams) and fix category frontmatter:
       - Files in business/ -> category: product
       - Files in legal/ -> category: product
       - Files in references/ -> category: product
       - Files in tech/ -> evaluate each: build-related get category: build, security-related get category: security
       - Files in guides/ -> evaluate each: testing-related get category: testing, UI-related get category: ui, otherwise category: guides
       - Files in architecture/ -> evaluate each: most stay architecture, but data-focused docs get category: data, UI-focused get category: ui
       - Diagram files: audit whether each diagram is truly "architecture" or would be better as "data", "ui", or "security" based on content. Default to architecture unless clearly another domain.

    3. Run validate-doc-structure on L0 (AndroidCommonDoc) and L1 (shared-kmp-libs) to check for any category drift from rapid Phase 14.1-14.3 changes. Fix any issues found.

    4. Same-pass reference updates per CONTEXT.md: if any file's category change would affect cross-references or wikilinks, update those references in the same commit.

    IMPORTANT: Category changes are frontmatter-only -- do NOT move files between subdirectories (per Phase 14.3-07 locked decision). The SUBDIR_TO_CATEGORIES mapping permits multiple categories per subdirectory.
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run</automated>
  </verify>
  <done>All DawSync docs use 9-category unified vocabulary. All 62 diagrams audited for category accuracy. Cross-layer audit shows 0 category violations across L0, L1, L2. validate-doc-structure passes on all 3 projects.</done>
</task>

<task type="auto">
  <name>Task 2: Assess and complete DawSync subproject documentation</name>
  <files>DawSync/SessionRecorder-VST3/**/*.md, DawSyncWeb/**/*.md</files>
  <action>
    1. SessionRecorder-VST3 assessment:
       - Read all 8 existing docs in C:/Users/34645/AndroidStudioProjects/DawSync/SessionRecorder-VST3/
       - Check frontmatter completeness (Phase 14.2-08 added domain-appropriate frontmatter -- verify it's correct with C++/JUCE scope)
       - Verify cross-references to parent DawSync project for shared concerns (legal, business, product)
       - If any docs are missing frontmatter or have stale content, fix them
       - SessionRecorder-VST3 is a C++/JUCE project, NOT KMP -- frontmatter should use C++/JUCE domain scope (sources: [juce, steinberg-sdk])

    2. DawSyncWeb assessment:
       - Read existing docs/content in C:/Users/34645/AndroidStudioProjects/DawSyncWeb/
       - This is an Astro web project. Phase 14.2.1-05 noted: "DawSyncWeb inherits L0 skills via DawSync root -- no own skills, agents, or .planning/ needed"
       - Phase 14.2-08 noted: "DawSyncWeb README disk-only (gitignored in DawSync)"
       - Assess if a README.md or other documentation would improve the subproject. If so, create minimal docs with appropriate frontmatter (scope: [web, astro], layer: L2, sources: [dawsync])
       - If current documentation is adequate, document this finding in the summary

    3. For both subprojects: ensure vault-config.json subProject entries will collect any new/updated docs correctly (the DawSync entry already has DawSyncWeb as a subProject).
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run</automated>
  </verify>
  <done>SessionRecorder-VST3 docs verified with correct C++/JUCE frontmatter and parent cross-references. DawSyncWeb assessed and any gaps addressed. Both subprojects ready for vault collection.</done>
</task>

</tasks>

<verification>
1. `cd mcp-server && npx vitest run` -- full test suite passes
2. Run validate-doc-structure on all 3 projects -- 0 errors
3. All DawSync docs have categories from the 9-vocabulary set
4. Subproject documentation is complete with correct frontmatter
</verification>

<success_criteria>
- All DawSync docs (text + diagrams) aligned to unified 9-category vocabulary
- Cross-layer audit passes for L0, L1, L2 -- no category violations
- SessionRecorder-VST3 and DawSyncWeb subproject documentation assessed and complete
- No wikilink or cross-reference breakage from category changes
</success_criteria>

<output>
After completion, create `.planning/phases/16-ecosystem-documentation-completeness-vault-harmony/16-02-SUMMARY.md`
</output>
