---
phase: 16-ecosystem-documentation-completeness-vault-harmony
plan: 06
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03, 16-04, 16-05]
files_modified:
  - mcp-server/src/vault/moc-generator.ts
  - mcp-server/tests/unit/vault/moc-generator.test.ts
  - kmp-knowledge-vault/ (vault output directory)
autonomous: false
requirements: [P16-VAULT, P16-HUMAN]

must_haves:
  truths:
    - "Vault contains all 52 module READMEs under L1-ecosystem/shared-kmp-libs/"
    - "MOC pages include module README entries with proper grouping"
    - "Wikilinks injected for all new module slugs across vault docs"
    - "vault-status reports healthy after sync"
    - "Human verifies Obsidian graph view, MOC navigation, and wikilink coverage"
  artifacts:
    - path: "kmp-knowledge-vault/"
      provides: "Complete vault with all Phase 16 changes"
      contains: "core-common"
    - path: "mcp-server/src/vault/moc-generator.ts"
      provides: "Enhanced MOC generation handling 52 module entries"
      contains: "module"
  key_links:
    - from: "kmp-knowledge-vault/L1-ecosystem/shared-kmp-libs/"
      to: "shared-kmp-libs/core-*/README.md"
      via: "Vault collector glob expansion"
      pattern: "core-"
    - from: "kmp-knowledge-vault/Home.md"
      to: "kmp-knowledge-vault/L1-ecosystem/"
      via: "MOC navigation links"
      pattern: "L1"
---

<objective>
Perform full vault resync with MOC improvements, wikilink refresh, and human-verified Obsidian graph view.

Purpose: Bring the Obsidian vault into harmony with all Phase 16 source changes (38 new READMEs, 14 upgraded READMEs, category fixes, subproject docs). Ensure the vault is navigable and the graph view is readable.
Output: Fully synced vault, enhanced MOC pages, expanded wikilinks, human-verified Obsidian experience.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-ecosystem-documentation-completeness-vault-harmony/16-CONTEXT.md
@.planning/phases/16-ecosystem-documentation-completeness-vault-harmony/16-RESEARCH.md

<interfaces>
<!-- Vault pipeline code interfaces -->

From mcp-server/src/vault/types.ts:
```typescript
export interface VaultConfig {
  version: number;
  vaultPath: string;
  projects: ProjectConfig[];
  autoClean?: boolean;
  lastSync?: string;
}

export interface ProjectConfig {
  name: string;
  path: string;
  layer: Layer;
  collectGlobs?: string[];
  excludeGlobs?: string[];
  features?: Record<string, unknown>;
  subProjects?: SubProjectConfig[];
}
```

From mcp-server/src/vault/moc-generator.ts:
- Generates 4 MOC pages: Home.md, All Patterns, All Skills, All Decisions
- Module READMEs will appear as sourceType "docs" entries
- RESEARCH.md recommends adding a new "All Modules" MOC page (Option 2)

Vault config location: C:\Users\34645\.androidcommondoc\vault-config.json
Vault output: C:\Users\34645\AndroidStudioProjects\kmp-knowledge-vault

Key wikilink facts:
- Slug pool expands automatically with new vault entries
- Existing regex boundary matching handles hyphenated module names correctly
- No code changes needed for wikilink injection (per RESEARCH.md)

Collector routing concern (RESEARCH.md Open Question 1):
- core-*/README.md files get relativePath like "core-common/README.md"
- Collector classifyFile() may need verification for correct vault path routing
- Verify module READMEs don't collide at same vault path
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance MOC generator for module entries and run full vault sync</name>
  <files>mcp-server/src/vault/moc-generator.ts, mcp-server/tests/unit/vault/moc-generator.test.ts</files>
  <action>
    1. Read moc-generator.ts to understand current MOC page generation (4 pages: Home.md, All Patterns, All Skills, All Decisions).

    2. Evaluate whether the 52 module READMEs need a dedicated "All Modules" MOC page (RESEARCH.md Option 2) or if they integrate well into existing pages. Decision criteria:
       - If module entries would overwhelm "All Patterns" (which is meant for L0 pattern docs), create a separate "All Modules" MOC
       - If they integrate cleanly, add them to existing MOC pages with clear section separation

    3. Implement the chosen approach. If adding "All Modules" MOC:
       - Group modules by category (security, data, architecture, domain, etc.)
       - Include module name, one-line description, platform icons
       - Link to module README in vault
       - Add navigation link from Home.md to All Modules page

    4. Write/extend tests for the new MOC behavior.

    5. Verify collector routing for module READMEs (RESEARCH.md Open Question 1):
       - Run a test to confirm core-common/README.md gets a unique vault path (not colliding with other README.md files)
       - If routing needs fixing, update collector.ts classifyFile() to handle module README paths correctly

    6. Run full vault sync:
       - Execute sync-vault via MCP tool or direct function call
       - Verify all 52 module READMEs appear in vault under L1-ecosystem/shared-kmp-libs/
       - Verify wikilink injection picks up new module slugs
       - Run vault-status to confirm health
       - Check orphan count -- should be 0 or near-0

    7. Verify final state:
       - Run `cd mcp-server && npx vitest run` to confirm all tests pass
       - Confirm vault-status shows healthy sync with updated file count
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run</automated>
  </verify>
  <done>MOC generator handles 52 module entries (either via dedicated "All Modules" page or enhanced existing pages). Full vault sync complete -- all module READMEs in vault, wikilinks refreshed, vault-status healthy. All MCP tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of vault harmony in Obsidian</name>
  <files>kmp-knowledge-vault/Home.md</files>
  <action>
    Present the complete Phase 16 results to the user for visual and functional verification. All automated work is done -- module READMEs written, categories fixed, subprojects assessed, vault synced, MOC enhanced. This checkpoint confirms human-perceptible qualities: Obsidian graph readability, MOC navigation accuracy, wikilink coverage, and overall vault harmony.

    Verification steps for the user:
    1. Open Obsidian vault at C:\Users\34645\AndroidStudioProjects\kmp-knowledge-vault
    2. Check Home.md -- does it link to module documentation section?
    3. Navigate to L1-ecosystem/shared-kmp-libs/ -- are all 52 module README files present?
    4. Open a module README (e.g., core-encryption) -- does it have proper frontmatter, accurate content, wikilinks?
    5. Check graph view -- is it readable? Do module nodes cluster logically by category?
    6. Navigate MOC pages -- do module entries appear with correct grouping?
    7. Spot-check wikilinks: open a doc that mentions "core-common" -- is it linked to the core-common README?
    8. Check DawSync docs in vault -- are categories consistent with unified vocabulary?
    9. Run vault-status MCP tool -- does it report healthy?
  </action>
  <verify>Human confirms the vault is in harmony by checking Obsidian graph, MOC navigation, wikilinks, and module README accuracy</verify>
  <done>User types "approved" confirming: (1) All 52 module READMEs in vault with accurate content, (2) MOC pages navigable with module grouping, (3) graph view readable with logical clustering, (4) wikilinks connecting module names across docs, (5) DawSync categories consistent</done>
</task>

</tasks>

<verification>
1. `cd mcp-server && npx vitest run` -- full test suite passes
2. vault-status reports healthy sync
3. All 52 module READMEs present in vault
4. MOC pages include module entries
5. Wikilinks expanded with new module slugs
6. Human verifies Obsidian graph and navigation
</verification>

<success_criteria>
- Vault fully synced with all Phase 16 source changes
- 52 module READMEs visible in vault under L1-ecosystem/shared-kmp-libs/
- MOC pages handle new module entries without overwhelming existing structure
- Wikilinks connect module names across all vault docs
- vault-status healthy, orphan count near-zero
- Human approves Obsidian graph view, navigation, and wikilink coverage
</success_criteria>

<output>
After completion, create `.planning/phases/16-ecosystem-documentation-completeness-vault-harmony/16-06-SUMMARY.md`
</output>
