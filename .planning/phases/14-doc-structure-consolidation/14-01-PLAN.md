---
phase: 14-doc-structure-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - versions-manifest.json
  - mcp-server/src/registry/types.ts
  - mcp-server/src/registry/scanner.ts
  - docs/doc-template.md
  - .planning/phases/14-doc-structure-consolidation/verify-doc-compliance.cjs
autonomous: true
requirements: [STRUCT-01, STRUCT-02]

must_haves:
  truths:
    - "Standard doc template exists with all mandatory frontmatter fields and section structure"
    - "PatternMetadata type includes layer, parent, and project optional fields"
    - "MCP server builds successfully with extended types"
    - "versions-manifest.json has correct kover and compose-multiplatform values"
    - "Verification script can check any doc for template compliance"
  artifacts:
    - path: "docs/doc-template.md"
      provides: "Standard documentation template with section structure and frontmatter reference"
      contains: "scope"
    - path: "mcp-server/src/registry/types.ts"
      provides: "Extended PatternMetadata with layer, parent, project fields"
      exports: ["PatternMetadata"]
    - path: "versions-manifest.json"
      provides: "Corrected version references for kover and compose-multiplatform"
      contains: "kover"
    - path: ".planning/phases/14-doc-structure-consolidation/verify-doc-compliance.cjs"
      provides: "Automated verification script for doc template compliance"
  key_links:
    - from: "mcp-server/src/registry/scanner.ts"
      to: "mcp-server/src/registry/types.ts"
      via: "PatternMetadata import"
      pattern: "import.*PatternMetadata.*types"
    - from: "docs/doc-template.md"
      to: "mcp-server/src/registry/types.ts"
      via: "frontmatter fields match PatternMetadata"
---

<objective>
Define the standard documentation template, extend MCP types for new frontmatter fields, fix versions-manifest.json, and create verification tooling.

Purpose: Everything else in Phase 14 depends on the template definition and type extensions. This plan establishes the contracts that all subsequent doc work implements against.
Output: doc-template.md (living L0 reference), extended PatternMetadata types, corrected versions-manifest.json, verification script
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-doc-structure-consolidation/14-CONTEXT.md
@.planning/phases/14-doc-structure-consolidation/14-RESEARCH.md
@.planning/phases/13-audit-validate/audit-report.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From mcp-server/src/registry/types.ts:
```typescript
export type Layer = "L0" | "L1" | "L2";

export interface PatternMetadata {
  scope: string[];
  sources: string[];
  targets: string[];
  version?: number;
  last_updated?: string;
  description?: string;
  slug?: string;
  status?: string;
  excludable_sources?: string[];
  monitor_urls?: MonitorUrl[];
  rules?: RuleDefinition[];
  // Phase 14 will ADD: layer?, parent?, project?
}

export interface RegistryEntry {
  slug: string;
  filepath: string;
  metadata: PatternMetadata;
  layer: Layer;
  project?: string;
}
```

From versions-manifest.json (current -- needs fixes):
```json
{
  "kover": "0.9.1",          // STALE: upstream is 0.9.7, ecosystem uses 0.9.4
  "compose-multiplatform": "1.7.x",  // UNCLEAR: may be confused with compose-gradle-plugin 1.10.0
  "compose-gradle-plugin": "1.10.0"
}
```

Model 5/5 docs for template reference:
- docs/testing-patterns.md (105 lines, hub)
- docs/viewmodel-state-patterns.md (162 lines, hub)
- docs/compose-resources-patterns.md (95 lines, hub)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix versions-manifest.json and extend PatternMetadata types</name>
  <files>versions-manifest.json, mcp-server/src/registry/types.ts, mcp-server/src/registry/scanner.ts</files>
  <action>
1. Update versions-manifest.json:
   - Change kover from "0.9.1" to "0.9.4" (version used by shared-kmp-libs and DawSync; upstream is 0.9.7 but ecosystem hasn't upgraded yet)
   - Change compose-multiplatform from "1.7.x" to "1.8.x" (or the actual Compose Multiplatform version -- investigate shared-kmp-libs libs.versions.toml for the real value). Clarify the distinction: compose-multiplatform is the JetBrains multiplatform plugin, compose-gradle-plugin is the Google Compose compiler plugin
   - Update the "updated" date to "2026-03-14"

2. Extend PatternMetadata in types.ts:
   - Add `layer?: Layer` -- L0/L1/L2 classification
   - Add `parent?: string` -- hub slug for sub-docs (enables hub-to-subdoc relationship)
   - Add `project?: string` -- project name for L1/L2 docs (e.g., "shared-kmp-libs", "DawSync")
   - All three fields MUST be optional (using `?:`) to avoid breaking existing docs that lack them

3. Update scanner.ts to extract the new fields:
   - After existing field extraction, add:
     - `layer: typeof data.layer === "string" ? (data.layer as Layer) : undefined`
     - `parent: typeof data.parent === "string" ? data.parent : undefined`
     - `project: typeof data.project === "string" ? data.project : undefined`

4. Build the MCP server to verify: `cd mcp-server && npm run build`
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server" && npm run build && npm test</automated>
  </verify>
  <done>PatternMetadata has layer/parent/project optional fields, scanner extracts them, MCP server builds and tests pass, versions-manifest.json has correct kover and compose-multiplatform values</done>
</task>

<task type="auto">
  <name>Task 2: Define standard doc template and create verification script</name>
  <files>docs/doc-template.md, .planning/phases/14-doc-structure-consolidation/verify-doc-compliance.cjs</files>
  <action>
1. Study the 3 model 5/5 docs (testing-patterns.md, viewmodel-state-patterns.md, compose-resources-patterns.md) to extract the successful pattern:
   - Read each file, note section ordering, frontmatter structure, content density, code example placement

2. Create docs/doc-template.md as a living L0 reference document that IS the template:
   - Full YAML frontmatter with ALL mandatory fields: scope, sources, targets, slug, status, monitor_urls, description, version, last_updated, layer (L0|L1|L2). Optional fields: parent (for sub-docs), project (for L1/L2), rules, excludable_sources
   - Hard limits clearly stated: 150 lines per section, 300 lines per standalone doc, 500 lines absolute max per file
   - Section ordering (use rules-first approach based on model docs -- the 5/5 docs lead with rules and patterns, not context):
     a. Frontmatter (YAML)
     b. Title (H1)
     c. Overview (2-3 paragraphs, core principles)
     d. Rules (the actionable patterns -- DO/DON'T format)
     e. Code Examples (platform-split when platform differences exist, commonMain-only otherwise)
     f. Anti-Patterns (inline with rules per model doc pattern, not a separate section)
     g. Platform Notes (only when platform-specific behavior exists)
     h. Sub-documents (for hub docs only -- links to sub-docs)
     i. Related Patterns (cross-references to other docs)
     j. References (external links)
   - Hub doc template section (<100 lines): Overview + Sub-documents listing + Quick Reference
   - Sub-doc template section: Full frontmatter with parent field, self-contained content loadable without hub
   - Give the template itself proper frontmatter: scope: [documentation, template], layer: L0, slug: doc-template

3. Create verify-doc-compliance.cjs verification script:
   - Accepts a file path or glob pattern as argument
   - For each markdown file:
     a. Parse YAML frontmatter (use the pattern from existing verify scripts in Phase 13)
     b. Check required frontmatter fields present: scope, sources, targets, slug, status, description
     c. Check line count limits: warn >300, error >500
     d. Check section count (at least 3 H2 sections)
     e. Report results as JSON: { file, valid, warnings[], errors[] }
   - Usage: `node verify-doc-compliance.cjs "docs/*.md"`
  </action>
  <verify>
    <automated>node "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/.planning/phases/14-doc-structure-consolidation/verify-doc-compliance.cjs" "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/docs/testing-patterns.md"</automated>
  </verify>
  <done>doc-template.md exists in docs/ with complete template specification, frontmatter, and all section templates. verify-doc-compliance.cjs validates the 3 model docs as compliant. Template stays under 300 lines.</done>
</task>

</tasks>

<verification>
- `cd mcp-server && npm run build && npm test` passes with no errors
- `node verify-doc-compliance.cjs "docs/testing-patterns.md"` returns valid
- `node verify-doc-compliance.cjs "docs/viewmodel-state-patterns.md"` returns valid
- docs/doc-template.md exists with proper frontmatter and all section templates
- versions-manifest.json kover value is no longer "0.9.1"
</verification>

<success_criteria>
- Standard doc template defined and committed as docs/doc-template.md
- PatternMetadata extended with layer, parent, project (all optional)
- Scanner extracts new fields from frontmatter
- MCP server builds and all tests pass
- versions-manifest.json corrected
- Verification script operational
</success_criteria>

<output>
After completion, create `.planning/phases/14-doc-structure-consolidation/14-01-SUMMARY.md`
</output>
