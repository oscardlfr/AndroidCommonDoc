---
phase: 14-doc-structure-consolidation
plan: 10
type: execute
wave: 4
depends_on: [14-02, 14-03, 14-04, 14-05, 14-06, 14-07, 14-08, 14-09]
files_modified:
  - ~/.androidcommondoc/vault-config.json
autonomous: false
requirements: [STRUCT-06]

must_haves:
  truths:
    - "vault-config.json updated with globs for new L0 directories (.agents/skills/, .claude/agents/, .claude/commands/)"
    - "Vault re-sync completes successfully with no errors"
    - "Vault reflects the new consolidated structure (new L0 docs, L1 shared-kmp-libs docs, archived DawSync files excluded)"
    - "Quality gate passes with no regressions in MCP server, registry scanner, or monitor-sources"
  artifacts:
    - path: "~/.androidcommondoc/vault-config.json"
      provides: "Updated vault configuration with new collection globs"
  key_links:
    - from: "~/.androidcommondoc/vault-config.json"
      to: "mcp-server/src/vault/collector.ts"
      via: "vault-config.json drives collector behavior"
---

<objective>
Update vault-config.json for new L0 directories, re-sync the Obsidian vault, and run the full quality gate to verify no regressions across the entire documentation ecosystem.

Purpose: Final verification that all Phase 14 changes are coherent -- the vault reflects the consolidated structure, the MCP server handles new frontmatter fields, and quality gates pass.
Output: Updated vault, quality gate report, phase complete
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-doc-structure-consolidation/14-CONTEXT.md

<interfaces>
<!-- Vault config and MCP tools -->

vault-config.json location: ~/.androidcommondoc/vault-config.json
Current config: 4 projects (shared-kmp-libs, DawSync, WakeTheCave, OmniSound)
New L0 directories to add: .agents/skills/, .claude/agents/, .claude/commands/

MCP tools:
- sync-vault: init/sync/clean modes
- vault-status: health info
- validate-all: quality gate orchestrator
- monitor-sources: upstream version monitoring

Quality gate command: cd mcp-server && npm run build && npm test
Vault sync: via MCP tool or direct invocation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update vault-config.json and re-sync vault</name>
  <files>~/.androidcommondoc/vault-config.json</files>
  <action>
1. Read current vault-config.json at `C:/Users/34645/.androidcommondoc/vault-config.json`
2. Update the AndroidCommonDoc project configuration:
   - Add `.agents/skills/**/*.md` to collectGlobs (for promoted skills)
   - Add `.claude/agents/*.md` to collectGlobs (for promoted agents)
   - Add `.claude/commands/*.md` to collectGlobs (for promoted commands)
   - Verify docs/ glob is already present and covers new docs (navigation3-patterns, di-patterns, agent-consumption-guide, storage-patterns, claude-code-workflow, doc-template)

3. Update the DawSync project configuration:
   - Add exclusion globs for archived files: `docs/archive/**` to excludeGlobs (so archived superseded docs don't appear in vault)
   - Verify .androidcommondoc/docs/ glob captures L2>L1 overrides

4. Update the shared-kmp-libs project configuration:
   - Add `docs/*.md` to collectGlobs if not already present (for new security, storage, error mapper, domain docs)
   - Add `core-*/README.md` to collectGlobs if not already present (for updated foundation READMEs)

5. Re-sync the vault:
   - Build MCP server first: `cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server && npm run build`
   - Run vault sync via direct script invocation or MCP tool
   - Check sync output for errors

6. Verify vault health:
   - Check file count (should be significantly higher than before Phase 14)
   - Check for orphans (files in vault that no longer exist in source)
   - Check that new L0 docs appear under L0-generic/ in vault
   - Check that shared-kmp-libs docs appear under L1-ecosystem/ in vault
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server" && npm run build && npm test</automated>
  </verify>
  <done>vault-config.json updated, vault re-synced with new structure, MCP server builds and tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Run full quality gate and final verification</name>
  <files>All docs/*.md, shared-kmp-libs/docs/*.md, DawSync/docs/ (read-only verification -- no files created)</files>
  <action>
1. Run the doc compliance verification script on ALL L0 docs:
   ```bash
   node C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/.planning/phases/14-doc-structure-consolidation/verify-doc-compliance.cjs "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/docs/*.md"
   ```

2. Verify line limits on ALL docs:
   - No L0 doc exceeds 500 lines: `wc -l docs/*.md | sort -n | tail -10`
   - Hub docs under 100 lines
   - Sub-docs under 300 lines

3. Verify frontmatter compliance:
   - All docs have layer field: `grep -L "layer:" docs/*.md` should return only archive/template docs
   - All sub-docs have parent field
   - All docs have slug field

4. Verify shared-kmp-libs documentation coverage:
   - Count docs in shared-kmp-libs/docs/: should be 15+ (7 security + 8 storage/error + 6 others + catalog)
   - All have L1 frontmatter

5. Verify DawSync consolidation:
   - 12+ files in docs/archive/superseded/
   - No Kotlin 2.3.0 refs remaining
   - 46+ delegate files

6. Run quality gate:
   - MCP server build: `cd mcp-server && npm run build && npm test`
   - If validate-all tool is available, run it

7. Generate a final tally report for the SUMMARY:
   - Total new L0 docs created
   - Total new L1 docs created
   - Total files archived
   - Total delegates created
   - Total line count reduction from splits
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc" && wc -l docs/*.md | sort -n | tail -3 && echo "---shared-kmp-libs---" && ls "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/docs/"*.md 2>/dev/null | wc -l</automated>
  </verify>
  <done>All verification checks pass, quality gate passes, final tally documented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of vault and documentation quality</name>
  <action>Human verifies the vault opens correctly in Obsidian and documentation quality meets standards.</action>
  <what-built>Complete documentation ecosystem restructuring: standard template applied across L0/L1/L2, 6 oversized docs split, 47 files promoted to L0, 52 shared-kmp-libs modules documented, DawSync consolidated, vault re-synced</what-built>
  <how-to-verify>
    1. Open Obsidian vault at the configured vault path
    2. Verify the vault opens without errors and the graph view shows interconnected documents
    3. Navigate L0-generic/ -- confirm new docs appear (navigation3-patterns, di-patterns, agent-consumption-guide, storage-patterns, promoted skills/agents/commands)
    4. Navigate L1-ecosystem/ -- confirm shared-kmp-libs module docs appear (security, storage, error mappers, domain, catalog)
    5. Navigate L2-apps/DawSync/ -- confirm DawSync overrides appear and archived files are excluded
    6. Spot-check 2-3 shared-kmp-libs module docs -- verify they contain real API signatures (not placeholder text)
    7. Spot-check 2-3 promoted commands -- verify they are parameterized (no DawSync-specific hardcoded paths)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- MCP server builds and tests pass
- Vault sync completes without errors
- Doc compliance script reports no errors on L0 docs
- No doc exceeds 500 lines
- All docs have required frontmatter fields
- Shared-kmp-libs has 15+ docs
- DawSync has 12+ archived files and 46+ delegates
- Human verifies vault in Obsidian
</verification>

<success_criteria>
- vault-config.json updated with all new collection globs
- Vault re-synced and reflects consolidated structure
- Quality gate passes (MCP server builds, tests pass)
- All verification scripts pass
- Human approves vault visual inspection
- Phase 14 complete -- documentation ecosystem follows standard structure
</success_criteria>

<output>
After completion, create `.planning/phases/14-doc-structure-consolidation/14-10-SUMMARY.md`
</output>
