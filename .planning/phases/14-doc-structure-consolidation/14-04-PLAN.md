---
phase: 14-doc-structure-consolidation
plan: 04
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - .agents/skills/accessibility/SKILL.md
  - .agents/skills/best-practices/SKILL.md
  - .agents/skills/core-web-vitals/SKILL.md
  - .agents/skills/performance/SKILL.md
  - .agents/skills/seo/SKILL.md
  - .agents/skills/web-quality-audit/SKILL.md
  - .claude/agents/beta-readiness-agent.md
  - .claude/agents/cross-platform-validator.md
  - .claude/agents/doc-alignment-agent.md
  - .claude/agents/release-guardian-agent.md
  - .claude/agents/test-specialist.md
  - .claude/agents/ui-specialist.md
  - .claude/commands/test.md
  - .claude/commands/coverage.md
  - .claude/commands/build.md
  - .claude/commands/ [plus 29 additional command files per audit manifest]
  - docs/claude-code-workflow.md
autonomous: true
requirements: [STRUCT-04]

must_haves:
  truths:
    - "8 web-quality skills exist in AndroidCommonDoc .agents/skills/ and are valid skill definitions"
    - "6 generic agents exist in AndroidCommonDoc .claude/agents/ with parameterized project references"
    - "32 generic commands exist in AndroidCommonDoc .claude/commands/ as templates"
    - "All promoted content is production-ready and follows established skill/agent/command patterns"
  artifacts:
    - path: ".agents/skills/accessibility/SKILL.md"
      provides: "Promoted L0 accessibility audit skill"
      contains: "accessibility"
    - path: ".claude/agents/release-guardian-agent.md"
      provides: "Promoted L0 release guardian agent"
    - path: ".claude/commands/test.md"
      provides: "Promoted L0 test command template"
  key_links:
    - from: ".agents/skills/accessibility/SKILL.md"
      to: "DawSync .agents/skills/accessibility/SKILL.md"
      via: "Promoted copy (DawSync original becomes delegate in Plan 09)"
---

<objective>
Promote 47 DawSync L0 candidates to AndroidCommonDoc: 8 web-quality skills (copy as-is), 6 generic agents (parameterize), 32 generic commands (create template system), and 1 workflow doc (partial extraction).

Purpose: Maximize L0/L1 coverage so L2 projects get value by delegation. These patterns are generic KMP/development patterns that should live at L0.
Output: 47 new files in AndroidCommonDoc (.agents/skills/, .claude/agents/, .claude/commands/)
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-doc-structure-consolidation/14-CONTEXT.md
@.planning/phases/13-audit-validate/audit-report.md

<interfaces>
<!-- DawSync source locations for promotion -->

Skills (8) at C:/Users/34645/AndroidStudioProjects/DawSync/.agents/skills/:
- accessibility/, best-practices/, core-web-vitals/, performance/, seo/, web-quality-audit/
- Each has SKILL.md + rules/ subdirectory
- These are Lighthouse-based audit skills with NO DawSync-specific content

Agents (6) at C:/Users/34645/AndroidStudioProjects/DawSync/.claude/agents/:
- beta-readiness-agent.md, cross-platform-validator.md, doc-alignment-agent.md
- release-guardian-agent.md, test-specialist.md, ui-specialist.md
- These reference DawSync-specific files -- need parameterization

Commands (32) at C:/Users/34645/AndroidStudioProjects/DawSync/.claude/commands/:
- test.md, coverage.md, build.md, deploy-web.md, sbom.md, sync-versions.md, etc.
- These use Claude Code's $ARGUMENTS natively and reference $ANDROID_COMMON_DOC
- Need parameterization for project-specific paths

Workflow (1): C:/Users/34645/AndroidStudioProjects/DawSync/docs/CLAUDE_CODE_WORKFLOW.md
- Mix of generic AI workflow patterns and DawSync-specific skill references
- Partial extraction: generic workflow patterns promote to L0

Existing AndroidCommonDoc skill pattern:
- skills/ directory has 22 skill directories with SKILL.md + rules/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote 8 web-quality skills and 6 generic agents to L0</name>
  <files>.agents/skills/accessibility/SKILL.md, .agents/skills/best-practices/SKILL.md, .agents/skills/core-web-vitals/SKILL.md, .agents/skills/performance/SKILL.md, .agents/skills/seo/SKILL.md, .agents/skills/web-quality-audit/SKILL.md, .claude/agents/beta-readiness-agent.md, .claude/agents/cross-platform-validator.md, .claude/agents/doc-alignment-agent.md, .claude/agents/release-guardian-agent.md, .claude/agents/test-specialist.md, .claude/agents/ui-specialist.md</files>
  <action>
**Skills (8 -- copy as-is with minor adjustments):**

1. Read each DawSync skill directory at `C:/Users/34645/AndroidStudioProjects/DawSync/.agents/skills/{name}/`
2. For each skill, copy SKILL.md and all rules/*.md files to AndroidCommonDoc `.agents/skills/{name}/`
3. Create `.agents/skills/` directory structure in AndroidCommonDoc if it doesn't exist
4. Note: Two of the audit skills listed in the audit report are actually skill directories with sub-skills (web-quality-audit has multiple sub-rules). Copy the entire directory structure.
5. Verify each copied SKILL.md follows the established skill pattern (Usage, Parameters, Behavior, Implementation, Expected Output, Cross-References)
6. If any skill references DawSync-specific paths, replace with generic paths (e.g., replace `DawSync/` with `$PROJECT_ROOT/`)

**Agents (6 -- parameterize):**

1. Read each DawSync agent at `C:/Users/34645/AndroidStudioProjects/DawSync/.claude/agents/{name}.md`
2. Create `.claude/agents/` directory in AndroidCommonDoc if it doesn't exist
3. For each agent, create a parameterized L0 version:
   - Replace DawSync-specific file references (e.g., `DawSync/build.gradle.kts`) with parameterized references (e.g., `$PROJECT_ROOT/build.gradle.kts`)
   - Replace DawSync-specific module names with generic patterns (e.g., `dawsync-app` -> `{app-module}`)
   - Replace DawSync-specific domain terminology with generic equivalents where possible
   - Add a header comment: `<!-- L0 Generic Agent - Promoted from DawSync. Project-specific overrides via L2. -->`
   - Keep the core logic, patterns, and structure intact
4. Skip agents that are inherently DawSync-specific (daw-guardian, domain-model-specialist, freemium-gate-checker, producer-consumer-validator, data-layer-specialist) -- these stay as DawSync L2 only
  </action>
  <verify>
    <automated>ls "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/.agents/skills/" && echo "---agents---" && ls "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/.claude/agents/"</automated>
  </verify>
  <done>8 skill directories exist in .agents/skills/ with SKILL.md and rules/, 6 agent files exist in .claude/agents/ with parameterized references (no DawSync-specific hardcoded paths)</done>
</task>

<task type="auto">
  <name>Task 2: Promote 32 generic commands to L0 and extract workflow doc</name>
  <files>.claude/commands/test.md, .claude/commands/coverage.md, .claude/commands/build.md [plus 29 additional command files per audit manifest -- 32 total], docs/claude-code-workflow.md</files>
  <action>
**Command Template System (32 commands):**

1. Read each DawSync command at `C:/Users/34645/AndroidStudioProjects/DawSync/.claude/commands/{name}.md`
2. Create `.claude/commands/` directory in AndroidCommonDoc if it doesn't exist
3. For each of the 32 generic commands, classify and handle:

   **Category A -- Fully generic, copy with minimal changes** (commands like test, coverage, lint-web, deploy-web, sbom, nuke-builds, run-clean):
   - Copy as-is if they only reference `$ARGUMENTS` and `$ANDROID_COMMON_DOC`
   - These are already parameterized through Claude Code's native variable expansion

   **Category B -- Need path parameterization** (commands like sync-versions, verify-kmp, validate-patterns):
   - Replace DawSync-specific module paths with `$ARGUMENTS` or generic paths
   - Replace hardcoded `DawSync/` with project-relative references
   - Keep the `/test` command as the exemplar pattern (it already uses `$ARGUMENTS` well)

   **Category C -- Need domain abstraction** (commands like merge-track, start-track, feature-audit):
   - Replace DawSync-specific workflow terms with generic equivalents
   - Add a comment noting that projects should customize the workflow-specific commands

4. For each promoted command, add a header:
   ```
   <!-- L0 Generic Command - Promoted from DawSync -->
   <!-- Usage: /command-name [args] -->
   ```

5. Ensure the 32 commands from the audit manifest are all accounted for. The DawSync directory has 39 commands total -- skip the 7 that are DawSync-specific (determined by checking if they reference domain-specific concepts like DAW, session recording, MIDI, etc.)

**Workflow Doc (1 -- partial extraction):**

6. Read `C:/Users/34645/AndroidStudioProjects/DawSync/docs/CLAUDE_CODE_WORKFLOW.md`
7. Extract generic Claude Code workflow patterns (how to use skills, commands, agents effectively; context management; parallel task patterns)
8. Create `docs/claude-code-workflow.md` in AndroidCommonDoc with L0 frontmatter
9. Keep under 300 lines; reference specific skills/agents/commands by pattern, not DawSync-specific names
  </action>
  <verify>
    <automated>ls "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/.claude/commands/" | wc -l && echo "---workflow---" && test -f "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/docs/claude-code-workflow.md" && echo "PASS"</automated>
  </verify>
  <done>32+ command files exist in .claude/commands/, claude-code-workflow.md exists in docs/ with L0 frontmatter, no DawSync-specific hardcoded references in any promoted file</done>
</task>

</tasks>

<verification>
- Skills directories exist: `ls .agents/skills/` shows 6+ directories (some DawSync skills may share names with existing AndroidCommonDoc skills)
- Agent files exist: `ls .claude/agents/` shows 6 files
- Commands exist: `ls .claude/commands/ | wc -l` shows 30+
- No DawSync hardcoded refs: `grep -r "DawSync" .agents/skills/ .claude/agents/ .claude/commands/` returns 0 matches (or only in comments explaining provenance)
- Workflow doc exists: `test -f docs/claude-code-workflow.md`
</verification>

<success_criteria>
- 8 web-quality skill directories promoted to L0
- 6 generic agents promoted with parameterized references
- 32 generic commands promoted as L0 templates
- 1 workflow doc partially extracted to L0
- Zero DawSync-specific hardcoded paths in promoted content
- All promoted files follow established patterns (skill format, agent format, command format)
</success_criteria>

<output>
After completion, create `.planning/phases/14-doc-structure-consolidation/14-04-SUMMARY.md`
</output>
