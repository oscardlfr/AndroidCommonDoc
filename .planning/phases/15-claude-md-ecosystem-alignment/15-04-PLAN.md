---
phase: 15-claude-md-ecosystem-alignment
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - adapters/claude-md-copilot-adapter.sh
  - adapters/generate-all.sh
  - mcp-server/tests/integration/claude-md-validation.test.ts
autonomous: false
requirements: [CLAUDE-07]

must_haves:
  truths:
    - "Smoke test confirms all behavioral rules from canonical checklist are preserved in the rewritten CLAUDE.md files"
    - "Copilot instructions generated from rewritten CLAUDE.md files for at least AndroidCommonDoc project"
    - "Integration test validates all 4 CLAUDE.md files against template structure and canonical coverage"
    - "Human verifies that CLAUDE.md ecosystem is coherent and the delegation model works"
  artifacts:
    - path: "adapters/claude-md-copilot-adapter.sh"
      provides: "Copilot adapter that generates .github/copilot-instructions.md from CLAUDE.md files"
      contains: "copilot-instructions"
    - path: "adapters/generate-all.sh"
      provides: "Updated adapter pipeline including CLAUDE.md Copilot adapter"
      contains: "claude-md-copilot-adapter"
    - path: "mcp-server/tests/integration/claude-md-validation.test.ts"
      provides: "Integration tests validating all CLAUDE.md files in the ecosystem"
      contains: "describe.*claude-md"
  key_links:
    - from: "adapters/claude-md-copilot-adapter.sh"
      to: "C:/Users/34645/.claude/CLAUDE.md"
      via: "Reads L0 rules for flattened Copilot output"
      pattern: "CLAUDE\\.md"
    - from: "adapters/claude-md-copilot-adapter.sh"
      to: "CLAUDE.md"
      via: "Reads project rules for Copilot output"
      pattern: "CLAUDE\\.md"
    - from: "mcp-server/tests/integration/claude-md-validation.test.ts"
      to: "docs/guides/canonical-rules.json"
      via: "Loads canonical checklist for coverage validation"
      pattern: "canonical-rules"
---

<objective>
Run smoke tests to verify behavioral rules are preserved, build Copilot adapter for CLAUDE.md, and perform final ecosystem validation with human checkpoint.

Purpose: Prove that the CLAUDE.md rewrite preserved every behavioral rule (no silent drops), extend the adapter pipeline to generate Copilot instructions from CLAUDE.md as SSOT, and get human confirmation that the ecosystem is coherent.
Output: Copilot adapter, integration tests, smoke test results, human-verified ecosystem
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-CONTEXT.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-RESEARCH.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-01-SUMMARY.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-02-SUMMARY.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-03-SUMMARY.md

Adapter pipeline:
@adapters/generate-all.sh
@adapters/copilot-instructions-adapter.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Copilot adapter and integration tests</name>
  <files>adapters/claude-md-copilot-adapter.sh, adapters/generate-all.sh, mcp-server/tests/integration/claude-md-validation.test.ts</files>
  <action>
**Part A: Copilot adapter from CLAUDE.md**

Create `adapters/claude-md-copilot-adapter.sh` that generates `.github/copilot-instructions.md` by flattening L0 + project CLAUDE.md rules:

1. Read `~/.claude/CLAUDE.md` (L0 global rules)
2. Read project-root `CLAUDE.md` (L0 toolkit / L1 / L2 rules)
3. Merge rules, applying overrides from L0 Overrides table if present
4. Output flattened Copilot instructions in plain markdown format:
   ```markdown
   <!-- GENERATED from CLAUDE.md files -- DO NOT EDIT MANUALLY -->
   <!-- Regenerate: bash adapters/claude-md-copilot-adapter.sh -->
   # Coding Instructions

   ## Architecture
   - {rules from architecture section}

   ## ViewModel
   - {rules from ViewModel section}

   ...
   ```
5. Write to `setup/copilot-templates/copilot-instructions-from-claude-md.md`

Implementation approach: Use a Python3 inline script (matching the pattern of `copilot-instructions-adapter.sh`) that:
- Reads both CLAUDE.md files
- Extracts sections and bullet-point rules
- Skips identity headers, Developer Context, and temporal sections (Wave 1 is project context, not Copilot-relevant)
- Flattens into Copilot format (no frontmatter, natural language bullets)
- Groups by section heading

Keep the existing `copilot-instructions-adapter.sh` (extracts from docs/ pattern docs) as a separate adapter -- different source, different purpose.

Update `adapters/generate-all.sh` to add the new adapter:
```bash
echo "Generating Copilot instructions from CLAUDE.md..."
bash "$SCRIPT_DIR/claude-md-copilot-adapter.sh"
echo ""
```

**Part B: Integration tests**

Create `mcp-server/tests/integration/claude-md-validation.test.ts` that validates the actual CLAUDE.md files on disk:

Test suites:
1. `describe("L0 global CLAUDE.md")` -- Reads ~/.claude/CLAUDE.md, validates identity header present, no project names in generic sections, under 150 lines
2. `describe("L0 toolkit CLAUDE.md")` -- Reads AndroidCommonDoc/CLAUDE.md, validates identity header, L0 inheritance, under 150 lines
3. `describe("L1 shared-kmp-libs CLAUDE.md")` -- Reads shared-kmp-libs/CLAUDE.md, validates identity header, L0 inheritance declared, no L0 rule duplication, under 150 lines
4. `describe("L2 DawSync CLAUDE.md")` -- Reads DawSync/CLAUDE.md, validates identity header, L0+L1 inheritance, Wave 1 present, no L0 rule duplication, under 150 lines
5. `describe("cross-file validation")` -- Validates no circular/upward references across all files, no version contradictions

These are real filesystem reads (integration tests, not unit tests). Use `path.resolve()` for cross-platform paths. Tests should be runnable from the mcp-server directory.

**Part C: Smoke test via canonical checklist**

The integration tests implement the smoke test (CLAUDE-07) by validating canonical rule coverage:
- Load `canonical-rules.json`
- For each rule at each layer, verify the rule's keywords appear in the correct CLAUDE.md file
- Report coverage percentage per layer and overall
- Warn (not error) if coverage drops below 90% -- some rules may be implicit via inheritance

This checklist-based approach satisfies CLAUDE-07 without actual code generation. The validate-claude-md tool + integration tests provide programmatic verification that all rules are preserved.
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/integration/claude-md-validation.test.ts && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Copilot adapter generates instructions from CLAUDE.md files. Integration tests validate all 4 CLAUDE.md files on disk. Smoke test confirms canonical rule coverage above 90% per layer. Full MCP test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of CLAUDE.md ecosystem alignment</name>
  <action>
Human reviews the complete CLAUDE.md ecosystem alignment. Claude has automated everything -- this checkpoint verifies the results are correct and coherent.

What was built:
1. Canonical rule checklist (docs/guides/canonical-rules.json) with all behavioral rules inventoried
2. CLAUDE.md template (docs/guides/claude-md-template.md) with identity header, sections, override format
3. validate-claude-md MCP tool with template structure, canonical coverage, circular reference, override, and version consistency checks
4. Rewritten CLAUDE.md files for all 4 locations (L0 global, L0 toolkit, L1, L2) with identity headers and delegation
5. Copilot adapter generating instructions from CLAUDE.md as SSOT
6. Integration tests validating all files + smoke test via canonical checklist
  </action>
  <verify>
Human performs the following checks:

1. **Review identity headers**: Open each CLAUDE.md file and verify the Layer/Inherits/Purpose blockquote looks correct:
   - `~/.claude/CLAUDE.md` -- L0 Generic KMP, no project names in generic rules
   - `AndroidCommonDoc/CLAUDE.md` -- L0 Toolkit, inherits L0 Generic
   - `shared-kmp-libs/CLAUDE.md` -- L1 Ecosystem Library, inherits L0
   - `DawSync/CLAUDE.md` -- L2 Application, inherits L0+L1, Wave 1 tracks present

2. **Verify rule preservation**: Open `docs/guides/canonical-rules.json` and scan the rules array -- confirm critical rules are present (sealed UiState, CancellationException rethrow, 5-layer architecture, DO NOT DISTURB, flat module naming)

3. **Test the validate-claude-md tool** (optional): Run in a Claude Code session against AndroidCommonDoc to verify it reports zero errors

4. **Check Copilot output**: Open `setup/copilot-templates/copilot-instructions-from-claude-md.md` and verify it contains flattened rules

5. **Run the full test suite**: `cd mcp-server && npm test` -- all tests should pass
  </verify>
  <done>Human approves the CLAUDE.md ecosystem alignment. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- `cd mcp-server && npm test` -- full suite passes (520+ tests expected)
- `cd mcp-server && npx vitest run tests/integration/claude-md-validation.test.ts` -- all integration tests pass
- `bash adapters/claude-md-copilot-adapter.sh` -- generates Copilot instructions without errors
- Human verifies identity headers, rule preservation, and ecosystem coherence
</verification>

<success_criteria>
- Smoke test via canonical checklist confirms 90%+ rule coverage across all layers
- Copilot adapter generates `.github/copilot-instructions.md` content from CLAUDE.md files
- Integration tests validate all 4 CLAUDE.md files against template and canonical checklist
- Human approves the ecosystem alignment
- Full MCP test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-claude-md-ecosystem-alignment/15-04-SUMMARY.md`
</output>
