---
phase: 15-claude-md-ecosystem-alignment
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - C:/Users/34645/.claude/CLAUDE.md
  - CLAUDE.md
  - C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md
autonomous: true
requirements: [CLAUDE-03, CLAUDE-04, CLAUDE-05, CLAUDE-06]

must_haves:
  truths:
    - "Every CLAUDE.md file has the mandatory identity header with Layer, Inherits, and Purpose"
    - "L0 global (~/.claude/CLAUDE.md) contains zero project name references in generic rule sections"
    - "L1 (shared-kmp-libs) and L2 (DawSync) CLAUDE.md files contain zero duplicated L0 rules"
    - "DawSync Wave 1 parallel tracks section is preserved in the L2 rewrite"
    - "All files stay under 150 lines and under 4000 tokens initial load"
    - "validate-claude-md tool reports zero errors when run against all rewritten files"
    - "Context delegation flows correctly: L0 auto-loads, L1 adds ecosystem specifics, L2 adds application specifics"
  artifacts:
    - path: "C:/Users/34645/.claude/CLAUDE.md"
      provides: "L0 global rules with identity header, Developer Context marked user-specific"
      contains: "Layer.*L0"
    - path: "CLAUDE.md"
      provides: "L0 toolkit rules for AndroidCommonDoc with identity header"
      contains: "Layer.*L0"
    - path: "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md"
      provides: "L1 rules with identity header and L0 inheritance declaration"
      contains: "Layer.*L1"
    - path: "C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md"
      provides: "L2 rules with identity header, L0+L1 inheritance, and Wave 1 tracks"
      contains: "Layer.*L2"
  key_links:
    - from: "C:/Users/34645/.claude/CLAUDE.md"
      to: "Claude Code native auto-loading"
      via: "~/.claude/ directory convention"
      pattern: "auto-loaded"
    - from: "CLAUDE.md"
      to: "C:/Users/34645/.claude/CLAUDE.md"
      via: "Convention-based delegation (auto-loaded by Claude Code)"
      pattern: "Inherits.*L0"
    - from: "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md"
      to: "C:/Users/34645/.claude/CLAUDE.md"
      via: "Convention-based delegation (auto-loaded by Claude Code)"
      pattern: "Inherits.*L0"
    - from: "C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md"
      to: "C:/Users/34645/.claude/CLAUDE.md"
      via: "Convention-based delegation (L0 auto-loaded, L1 referenced)"
      pattern: "Inherits.*L0.*L1"
---

<objective>
Rewrite all 4 CLAUDE.md files using the standard template from Plan 01, adding identity headers, override sections, and delegation declarations. Validate each rewrite against the canonical rule checklist and validate-claude-md tool from Plan 02.

Purpose: Formalize the CLAUDE.md ecosystem structure with consistent identity headers, explicit delegation chains, and override support. Every file follows the template, stays within budget, and validates cleanly.
Output: Rewritten ~/.claude/CLAUDE.md, AndroidCommonDoc/CLAUDE.md, shared-kmp-libs/CLAUDE.md, DawSync/CLAUDE.md
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-CONTEXT.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-RESEARCH.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-01-SUMMARY.md
@.planning/phases/15-claude-md-ecosystem-alignment/15-02-SUMMARY.md

Template reference:
@docs/guides/claude-md-template.md
@docs/guides/canonical-rules.json

Current files to rewrite:
@C:/Users/34645/.claude/CLAUDE.md
@CLAUDE.md
@C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md
@C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite L0 global and L0 toolkit CLAUDE.md files</name>
  <files>C:/Users/34645/.claude/CLAUDE.md, CLAUDE.md</files>
  <action>
Rewrite both L0 CLAUDE.md files using the template from `docs/guides/claude-md-template.md`.

**~/.claude/CLAUDE.md (L0 global):**

Add identity header:
```markdown
# Shared KMP Development Rules

> **Layer:** L0 (Generic KMP)
> **Inherits:** None (root of delegation chain)
> **Purpose:** Shared behavioral rules for all Kotlin Multiplatform projects

These rules auto-load for every project via Claude Code's ~/.claude/CLAUDE.md mechanism.
This file contains ONLY generic KMP rules -- no project-specific context.
```

Keep ALL existing rule sections (KMP Source Set Rules, Module Naming, Architecture, Error Handling, ViewModel Rules, Testing Rules, DI, Navigation, Compose Resources, File Naming, Build Patterns, Git Flow, Worktree Rules, Agent Team File Ownership). These are the L0 generic rules.

**Developer Context section**: Keep it but restructure:
```markdown
## Developer Context (User-Specific)

<!-- Corporate users: replace this section with your team context -->
- Solo developer working on KMP ecosystem
- shared-kmp-libs is THE version authority -- apps consume its catalog
- GSD workflow active in all projects -- respect .planning/ directories
- Detailed pattern docs at ~/.claude/docs/ -- consult when you need depth
```

Key change: Remove project name references (DawSync, OmniTrack, shared-kmp-libs) from this section. The statement "Solo developer working on KMP ecosystem" is generic enough. The reference to shared-kmp-libs as "version authority" is a generic pattern statement. If this specific line is considered too project-specific, replace with "The shared library project is THE version authority -- apps consume its catalog."

Wait -- per CONTEXT.md: "L0 must NOT reference any project by name." The Developer Context section currently mentions "DawSync, OmniTrack, shared-kmp-libs". Since `~/.claude/CLAUDE.md` is user-scoped, and the research recommends keeping dev context but marking it user-specific, proceed as follows:
- Remove "DawSync, OmniTrack, shared-kmp-libs" from the bullet point
- Replace with generic phrasing: "Solo developer working on KMP ecosystem projects"
- Keep "shared-kmp-libs" reference ONLY if it's phrased generically. Since "shared-kmp-libs" IS a project name, rephrase to "The shared KMP library project is THE version authority"
- Keep the ~/.claude/docs/ reference (it's a generic path, not a project name)

Ensure file stays under 110 lines (current is 100, adding identity header adds ~6 lines).

**AndroidCommonDoc/CLAUDE.md (L0 toolkit):**

Add identity header:
```markdown
# AndroidCommonDoc Project Rules

> **Layer:** L0 (Pattern Toolkit)
> **Inherits:** L0 Generic KMP (via ~/.claude/CLAUDE.md, auto-loaded by Claude Code)
> **Purpose:** L0 pattern toolkit providing docs, skills, agents, MCP server, and architecture enforcement

Shared KMP rules in ~/.claude/CLAUDE.md (auto-loaded by Claude Code).
This file contains ONLY rules specific to AndroidCommonDoc as the L0 toolkit.
```

Keep ALL existing sections (What This Project Is, MCP Server, Skills & Agents, Pattern Docs, Development Rules, Build & Test, Vault Sync). Only add the identity header and preserve the existing delegation statement.

Ensure file stays under 70 lines (current is 62, adding header adds ~5 lines).

After rewriting both files, run validate-claude-md against them to verify zero errors.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const l0=fs.readFileSync(require('os').homedir()+'/.claude/CLAUDE.md','utf8'); const tk=fs.readFileSync('CLAUDE.md','utf8'); console.log('L0 lines:', l0.split('\n').length); console.log('TK lines:', tk.split('\n').length); if(!l0.includes('Layer:')) throw new Error('L0 missing identity header'); if(!tk.includes('Layer:')) throw new Error('TK missing identity header'); if(l0.includes('DawSync') || l0.includes('OmniTrack')) throw new Error('L0 references project names in generic rules'); if(l0.split('\n').length > 150) throw new Error('L0 exceeds 150 lines'); if(tk.split('\n').length > 150) throw new Error('TK exceeds 150 lines'); console.log('OK')"</automated>
  </verify>
  <done>~/.claude/CLAUDE.md has identity header, zero project name references in generic rules, Developer Context marked user-specific, under 150 lines. AndroidCommonDoc/CLAUDE.md has identity header with L0 toolkit inheritance, under 150 lines. Both validate cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite L1 and L2 CLAUDE.md files</name>
  <files>C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md, C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md</files>
  <action>
Rewrite both downstream CLAUDE.md files using the template.

**shared-kmp-libs/CLAUDE.md (L1):**

Add identity header:
```markdown
# shared-kmp-libs Project Rules

> **Layer:** L1 (Ecosystem Library)
> **Inherits:** L0 Generic KMP (via ~/.claude/CLAUDE.md, auto-loaded by Claude Code)
> **Purpose:** Foundation KMP library suite providing reusable modules consumed by apps via composite builds

Shared KMP rules in ~/.claude/CLAUDE.md (auto-loaded by Claude Code).
This file contains ONLY shared-kmp-libs module-specific rules.
```

Keep ALL existing sections (Critical Rules, Build Commands, Module Catalog, How to Add a Module, Test Coverage Target). The version authority statement stays as project-specific context.

Currently no L0 overrides -- do NOT add an empty overrides section. Only add if the project actually overrides something.

Ensure file stays under 70 lines (current is 62, adding header adds ~5 lines).

**DawSync/CLAUDE.md (L2):**

Add identity header:
```markdown
# DawSync Project Rules

> **Layer:** L2 (Application)
> **Inherits:** L0 Generic KMP (via ~/.claude/CLAUDE.md, auto-loaded), L1 shared-kmp-libs conventions
> **Purpose:** Creative Project OS for musicians -- the Identity Layer for music projects

Shared KMP rules in ~/.claude/CLAUDE.md (auto-loaded by Claude Code).
This file contains ONLY DawSync-specific rules and context.
```

Keep ALL existing sections:
- Project Overview
- Key Architecture Decisions (DO NOT DISTURB, Producer/Consumer, SSOT, Feature Gates, No Turbine)
- Build Commands
- Module Structure
- Mandatory Doc Consultation
- Key Dependencies
- **Wave 1: Parallel Pre-Cloud Tracks** (per CONTEXT.md locked decision: "DawSync Wave 1 stays")
- Test Coverage

The "No Turbine" rule in Key Architecture Decisions is a DawSync-specific override of testing approach. Document it explicitly:

Add an L0 Overrides section ONLY if DawSync actually overrides an L0 rule. Check canonical-rules.json: "No Turbine" is a DawSync-specific addition (L0 says "StateFlow: subscribe in backgroundScope" but doesn't mandate Turbine). If it's not contradicting L0, it doesn't need an override entry -- it's an L2-specific addition. Only add the overrides table if there's a genuine override.

Ensure file stays under 95 lines (current is 85, adding header adds ~5 lines).

After rewriting both files, run validate-claude-md to verify:
1. Both have valid identity headers
2. No duplicated L0 rules
3. No upward references (L1 doesn't reference L2, neither references L0 by file path)
4. Both under 150 lines
5. DawSync Wave 1 tracks preserved

Commit each project's CLAUDE.md in its own repository:
- shared-kmp-libs: commit in the shared-kmp-libs repo
- DawSync: commit in the DawSync repo
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const l1=fs.readFileSync('C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md','utf8'); const l2=fs.readFileSync('C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md','utf8'); console.log('L1 lines:', l1.split('\n').length); console.log('L2 lines:', l2.split('\n').length); if(!l1.includes('Layer:')) throw new Error('L1 missing identity header'); if(!l2.includes('Layer:')) throw new Error('L2 missing identity header'); if(!l2.includes('Wave 1')) throw new Error('DawSync missing Wave 1'); if(!l1.includes('Inherits:')) throw new Error('L1 missing inheritance'); if(!l2.includes('Inherits:')) throw new Error('L2 missing inheritance'); if(l1.split('\n').length > 150) throw new Error('L1 exceeds 150 lines'); if(l2.split('\n').length > 150) throw new Error('L2 exceeds 150 lines'); console.log('OK')"</automated>
  </verify>
  <done>shared-kmp-libs/CLAUDE.md has L1 identity header with L0 inheritance, zero duplicated L0 rules, under 150 lines. DawSync/CLAUDE.md has L2 identity header with L0+L1 inheritance, Wave 1 tracks preserved, under 150 lines. Both validate cleanly with validate-claude-md.</done>
</task>

</tasks>

<verification>
- All 4 CLAUDE.md files have identity headers with correct layer and inheritance declarations
- `~/.claude/CLAUDE.md` has zero project name references in generic rule sections
- DawSync Wave 1 tracks section preserved
- All files under 150 lines
- validate-claude-md reports zero errors across all files
- No duplicated L0 rules in L1/L2 files
- No upward/circular references detected
</verification>

<success_criteria>
- Every CLAUDE.md starts with the mandatory identity header (Layer, Inherits, Purpose)
- L0 global is fully generic -- no DawSync/OmniTrack/shared-kmp-libs in generic rules
- L1 declares L0 inheritance, contains only module-specific rules
- L2 declares L0+L1 inheritance, contains only application-specific rules, preserves Wave 1
- validate-claude-md tool reports 0 errors when run against all 4 files
- All files under 150 lines
</success_criteria>

<output>
After completion, create `.planning/phases/15-claude-md-ecosystem-alignment/15-03-SUMMARY.md`
</output>
