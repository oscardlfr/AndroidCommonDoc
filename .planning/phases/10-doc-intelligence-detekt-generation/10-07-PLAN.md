---
phase: 10-doc-intelligence-detekt-generation
plan: 07
type: execute
wave: 4
depends_on: ["10-05", "10-06"]
files_modified:
  - mcp-server/src/tools/check-freshness.ts
  - mcp-server/src/tools/index.ts
  - README.md
  - AGENTS.md
  - CHANGELOG.md
  - mcp-server/tests/integration/server.test.ts
autonomous: false
requirements:
  - DOC-09

must_haves:
  truths:
    - "check-freshness tool consolidated into monitor-sources with backward-compatible alias"
    - "No dead code, orphaned scripts, or stale references remain from v1.0-v1.1 evolution"
    - "README accurately reflects all v1.1 capabilities (Konsist, guards, MCP, registry, doc intelligence, rules)"
    - "AGENTS.md accurately reflects current tool and agent inventory"
    - "CHANGELOG.md summarizes all v1.1 capabilities"
    - "Generated and hand-written rules compile together and tests pass end-to-end"
    - "All frontmatter is complete and consistent across 23 docs"
    - "Toolkit follows its own conventions (naming, file structure, error handling)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.1 milestone changelog entry"
      contains: "v1.1"
    - path: "README.md"
      provides: "Updated project documentation reflecting Phase 10 capabilities"
    - path: "AGENTS.md"
      provides: "Updated agent/tool inventory"
  key_links:
    - from: "mcp-server/src/tools/index.ts"
      to: "mcp-server/src/tools/monitor-sources.ts"
      via: "check-freshness alias points to monitor-sources"
      pattern: "check-doc-freshness.*alias|backward.compat"
---

<objective>
v1.1 milestone audit and cleanup: consolidate overlapping tools, remove dead code, update all documentation to reflect current state, verify convention compliance, and generate CHANGELOG.md.

Purpose: Phase 10 is the last functional phase of v1.1. This plan ensures the repository is professional, consistent, and fully documented before milestone closure. No stale references, no dead code, no drift between docs and reality.
Output: Clean repository, accurate docs, CHANGELOG.md, end-to-end verification.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-doc-intelligence-detekt-generation/10-CONTEXT.md
@.planning/phases/10-doc-intelligence-detekt-generation/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tool consolidation, dead code audit, convention compliance, and docs update</name>
  <files>mcp-server/src/tools/check-freshness.ts, mcp-server/src/tools/index.ts, mcp-server/tests/integration/server.test.ts, README.md, AGENTS.md, CHANGELOG.md</files>
  <action>
1. **Consolidate check-freshness into monitor-sources:**
   - In `check-freshness.ts`: Replace the full implementation with a thin wrapper that calls `registerMonitorSourcesTool` logic under the alias "check-doc-freshness". OR: Register "check-doc-freshness" as an alias tool in `index.ts` that delegates to monitor-sources with `tier: "all"` parameter. This maintains backward compatibility for existing agent prompts that reference "check-doc-freshness".
   - Update the check-freshness tool description to note it's an alias for monitor-sources.
   - Keep the check-freshness tests passing (they should work against the alias).

2. **Dead code audit:**
   - Search for unused imports, dead functions, orphaned files across the entire repository:
     - Check scripts/ for any scripts that are no longer referenced by quality gate agents or skills
     - Check .claude/agents/ for any agents that reference removed tools or scripts
     - Check mcp-server/src/ for any modules not imported by any other module or test
   - Remove dead code found. List what was removed in the summary.

3. **Convention compliance (dog-food the rules):**
   - Verify the toolkit follows its own conventions:
     - File naming: `.ts` files in mcp-server follow kebab-case (check for any camelCase files)
     - Error handling: All async functions in MCP tools have try/catch with logger.error
     - No console.log anywhere in mcp-server (existing ESLint rule should catch this)
   - Fix any violations found.

4. **Registry/frontmatter hardening:**
   - Verify all 23 docs have complete frontmatter (scope, sources, targets at minimum).
   - Verify scanner handles new fields (monitor_urls, rules) cleanly with existing and new docs.

5. **Update documentation:**
   - **README.md**: Update to reflect all v1.1 capabilities including Phase 10 features (doc monitoring, rule generation, content ingestion). Update MCP tool count, skill count, Detekt rule information.
   - **AGENTS.md**: Update agent/tool inventory to include new monitoring and generation tools.
   - **CHANGELOG.md**: Create or update with v1.1 milestone entry summarizing all capabilities added across Phases 5-10:
     - Tech debt cleanup (Phase 5)
     - Konsist architecture tests (Phase 6)
     - Consumer guard tests (Phase 7)
     - MCP server (Phase 8)
     - Pattern registry & discovery (Phase 9)
     - Doc intelligence & Detekt generation (Phase 10)

6. **End-to-end integration verification:**
   - Run `cd mcp-server && npm test` (all MCP server tests)
   - Run `./gradlew :detekt-rules:test` (all Detekt rule tests including any generated ones)
   - Verify both pass clean.
  </action>
  <verify>
    <automated>cd mcp-server && npm test && cd .. && ./gradlew :detekt-rules:test</automated>
  </verify>
  <done>check-freshness consolidated into monitor-sources with backward-compatible alias. Dead code removed. Convention compliance verified. All frontmatter complete. README, AGENTS.md, CHANGELOG.md accurate. End-to-end tests pass across both TypeScript and Kotlin stacks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 10 and v1.1 milestone</name>
  <what-built>
Complete Phase 10 implementation: doc monitoring system, Detekt rule generation, content ingestion, 3 skills, CI workflow, v1.1 cleanup. The toolkit now has:
- Source monitoring with tiered checking and review state tracking (MCP tool + skill + CI cron)
- Detekt rule generation from pattern doc frontmatter (MCP tool + skill)
- Content ingestion from arbitrary sources with graceful URL fallback (MCP tool + skill)
- 11 MCP tools (up from 8), 19 skills (up from 16)
- check-freshness consolidated into monitor-sources with backward-compatible alias
- Dead code removed, conventions verified, frontmatter hardened
- README, AGENTS.md, CHANGELOG.md updated for v1.1
  </what-built>
  <how-to-verify>
1. Run full test suite: `cd mcp-server && npm test && cd .. && ./gradlew :detekt-rules:test`
2. Verify MCP tool count: use Claude Code to call `rate-limit-status` -- should show the server responds
3. Verify skills exist: `ls skills/monitor-docs/ skills/generate-rules/ skills/ingest-content/`
4. Verify CI workflow: `cat .github/workflows/doc-monitor.yml` -- check cron schedule
5. Verify CHANGELOG.md exists and covers v1.1 capabilities
6. Spot-check README.md accuracy for Phase 10 features
7. Verify backward compatibility: check-doc-freshness tool still responds (alias to monitor-sources)
  </how-to-verify>
  <resume-signal>Type "approved" to confirm v1.1 milestone is ready, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd mcp-server && npm test` -- full MCP server tests green
- `./gradlew :detekt-rules:test` -- Detekt rule tests green
- `test -f CHANGELOG.md` -- changelog exists
- All 23 docs have valid frontmatter (scanner test coverage)
- No console.log in mcp-server/ (ESLint)
</verification>

<success_criteria>
- check-freshness consolidated with backward-compatible alias
- No dead code, orphaned scripts, or stale references
- README, AGENTS.md, CHANGELOG.md accurately reflect v1.1 state
- All frontmatter complete and consistent
- End-to-end tests pass across TypeScript and Kotlin stacks
- Human verification confirms the full system works
</success_criteria>

<output>
After completion, create `.planning/phases/10-doc-intelligence-detekt-generation/10-07-SUMMARY.md`
</output>
