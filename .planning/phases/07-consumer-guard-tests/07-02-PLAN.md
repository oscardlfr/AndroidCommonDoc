---
phase: 07-consumer-guard-tests
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/build.gradle.kts
  - C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/GuardScopeFactory.kt
  - C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/ArchitectureGuardTest.kt
  - C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/NamingGuardTest.kt
  - C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/PackageGuardTest.kt
  - C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/ModuleIsolationGuardTest.kt
  - C:/Users/34645/AndroidStudioProjects/DawSync/settings.gradle.kts
  - C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/build.gradle.kts
  - C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/GuardScopeFactory.kt
  - C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/settings.gradle.kts
  - C:/Users/34645/AndroidStudioProjects/OmniSound/konsist-guard/build.gradle.kts
  - C:/Users/34645/AndroidStudioProjects/OmniSound/settings.gradle.kts
autonomous: false
requirements: [GUARD-02, GUARD-03]

must_haves:
  truths:
    - "Install script runs successfully against DawSync producing konsist-guard/ module"
    - "DawSync guard tests compile and run via ./gradlew :konsist-guard:test"
    - "DawSync canary assertion confirms non-empty scope under com.dawsync"
    - "Install script runs successfully against DawSync worktree (track-E) producing konsist-guard/ module"
    - "DawSync worktree guard tests compile and canary passes"
    - "OmniSound install produces konsist-guard/ module with canary pass"
    - "Architecture violations in DawSync (if any) are reported to user, not fixed"
    - "All three consumer settings.gradle.kts files include ':konsist-guard'"
  artifacts:
    - path: "C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/build.gradle.kts"
      provides: "Generated build file for DawSync guard module"
      contains: "konsist"
    - path: "C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/GuardScopeFactory.kt"
      provides: "DawSync-configured scope factory"
      contains: "com.dawsync"
    - path: "C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/build.gradle.kts"
      provides: "Generated build file for DawSync worktree guard module"
      contains: "konsist"
    - path: "C:/Users/34645/AndroidStudioProjects/OmniSound/konsist-guard/build.gradle.kts"
      provides: "Generated build file for OmniSound guard module"
      contains: "konsist"
  key_links:
    - from: "setup/install-guard-tests.sh"
      to: "DawSync/konsist-guard/"
      via: "--package com.dawsync --target DawSync"
      pattern: "com\\.dawsync"
    - from: "DawSync/konsist-guard/GuardScopeFactory.kt"
      to: "DawSync production code"
      via: "scopeFromProduction() filtered by com.dawsync"
      pattern: "scopeFromProduction"
    - from: "setup/install-guard-tests.sh"
      to: "DawSync/.claude/worktrees/track-E/konsist-guard/"
      via: "--package com.dawsync --target DawSync worktree"
      pattern: "com\\.dawsync"
    - from: "setup/install-guard-tests.sh"
      to: "OmniSound/konsist-guard/"
      via: "--package com.omnisound --target OmniSound"
      pattern: "com\\.omnisound"
---

<objective>
Validate guard templates by installing them in DawSync (full architecture scan), DawSync worktree track-E (canary pass on feature branch), and OmniSound (canary pass), proving the end-to-end flow works from install to test execution across multiple consumers and worktrees.

Purpose: GUARD-03 requires validation against at least one consuming project. DawSync gets a full scan (all guard tests run against real code). The DawSync worktree at `.claude/worktrees/track-E` (branch: `feature/precloud-full-test-coverage-wave`) gets a separate install to validate that guard tests work in git worktree checkouts -- per user decision "Validate in DawSync worktree as well." OmniSound gets a canary pass (templates install and scope assertions confirm non-empty). Any architecture violations found in DawSync are reported to the user for fix in their track-E terminal -- we do NOT fix consumer code in this phase.

Output: konsist-guard/ modules installed and validated in DawSync (main + worktree) and OmniSound. Checkpoint for violation triage.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-consumer-guard-tests/07-CONTEXT.md
@.planning/phases/07-consumer-guard-tests/07-RESEARCH.md
@.planning/phases/07-consumer-guard-tests/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -- install script and template files -->

setup/install-guard-tests.sh:
```bash
# Usage: bash install-guard-tests.sh --package PACKAGE [--target DIR] [--dry-run] [--force]
# Creates konsist-guard/ module in target, substitutes __ROOT_PACKAGE__
# Appends include(":konsist-guard") to settings.gradle.kts
```

guard-templates/GuardScopeFactory.kt.template:
```kotlin
object GuardScopeFactory {
    fun projectScope(): KoScope  // scopeFromProduction() + canary
    fun hasFilesInPackage(scope: KoScope, packagePrefix: String): Boolean
}
```

Consumer project paths:
- DawSync (main): C:/Users/34645/AndroidStudioProjects/DawSync (root package: com.dawsync)
- DawSync (worktree track-E): C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E (root package: com.dawsync, branch: feature/precloud-full-test-coverage-wave)
- OmniSound: C:/Users/34645/AndroidStudioProjects/OmniSound (root package: com.omnisound)

Consumer test command: cd <consumer> && ./gradlew :konsist-guard:test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and validate guard tests in DawSync</name>
  <files>
    C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/build.gradle.kts,
    C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/GuardScopeFactory.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/ArchitectureGuardTest.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/NamingGuardTest.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/PackageGuardTest.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/ModuleIsolationGuardTest.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/settings.gradle.kts
  </files>
  <action>
Run the install script from Plan 01 against DawSync:

```bash
bash setup/install-guard-tests.sh --package com.dawsync --target "C:/Users/34645/AndroidStudioProjects/DawSync"
```

After installation, verify the generated files:
1. Check `konsist-guard/build.gradle.kts` contains `konsist:0.17.3` (not `__ROOT_PACKAGE__`)
2. Check generated .kt files contain `com.dawsync` (not `__ROOT_PACKAGE__`)
3. Check `settings.gradle.kts` contains `include(":konsist-guard")`

Then run the guard tests:
```bash
cd "C:/Users/34645/AndroidStudioProjects/DawSync" && ./gradlew :konsist-guard:test
```

**If `kotlin("jvm")` without version fails** (consumer's pluginManagement doesn't resolve it): fall back to pinning the version in the template. Update `build.gradle.kts.template` to use `kotlin("jvm") version "2.1.20"` (match DawSync's Kotlin version from their build) or read the consumer's Kotlin version from their `libs.versions.toml` and substitute it.

**If `scopeFromProduction()` performance is poor** (>60s for DawSync's 20+ modules): switch to `scopeFromPackage("com.dawsync..")` in GuardScopeFactory.kt.template and regenerate DawSync's files.

**If architecture violations are found**: Capture the full test output. Record which tests failed, which files violated, and what the violation was. DO NOT fix the violations -- they are in DawSync's codebase and will be addressed by the user in their track-E terminal.

**If tests fail due to template bugs** (e.g., wrong Konsist API usage, import issues): fix the templates in guard-templates/ first, then re-run the install script with `--force` and re-run tests.

**Critical: template fixes flow both ways.** If you fix a template to make DawSync work, the fix must be in the template file (guard-templates/), not just in the generated DawSync file. Re-run install with --force after template fixes.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/DawSync" && ./gradlew :konsist-guard:test 2>&1 | tail -20</automated>
  </verify>
  <done>DawSync konsist-guard/ module exists with all 5 generated test files + build.gradle.kts. Guard tests compile and execute. Canary assertion confirms non-empty scope under com.dawsync. Test results captured (pass or fail with violation details).</done>
</task>

<task type="auto">
  <name>Task 2: Install and validate guard tests in DawSync worktree (track-E)</name>
  <files>
    C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/build.gradle.kts,
    C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/GuardScopeFactory.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/ArchitectureGuardTest.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/NamingGuardTest.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/PackageGuardTest.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/src/test/kotlin/com/dawsync/konsist/guard/ModuleIsolationGuardTest.kt,
    C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/settings.gradle.kts
  </files>
  <action>
Run the install script against the DawSync worktree at track-E. This is a git worktree checkout on branch `feature/precloud-full-test-coverage-wave` -- it has its own working tree with separate files from the main DawSync checkout. Per user decision: "Validate in DawSync worktree as well."

```bash
bash setup/install-guard-tests.sh --package com.dawsync --target "C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E"
```

After installation, verify the generated files:
1. Check `konsist-guard/build.gradle.kts` contains `konsist:0.17.3` (not `__ROOT_PACKAGE__`)
2. Check generated .kt files contain `com.dawsync` (not `__ROOT_PACKAGE__`)
3. Check `settings.gradle.kts` contains `include(":konsist-guard")`

Then run the guard tests:
```bash
cd "C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E" && ./gradlew :konsist-guard:test
```

The worktree may have different source files than the main checkout (it's on a feature branch with test coverage work). The guard tests should still work because:
- Same root package (`com.dawsync`)
- Same module structure (worktree is same project, different branch)
- `assumeTrue` guards handle any layers that may have fewer/more files

**If the worktree has Gradle wrapper issues** (e.g., gradlew not executable, missing gradle-wrapper.jar): the worktree shares the same `.git` directory but has its own file tree. Ensure `./gradlew` is executable: `chmod +x gradlew` if needed.

**If architecture violations differ from main checkout**: expected -- different branch may have different code. Capture and report as with Task 1.

**If template fixes were applied in Task 1**: re-run install here with `--force` to pick up the updated templates. The install script reads from the toolkit's `guard-templates/` directory, so fixes made in Task 1 automatically apply here.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E" && ./gradlew :konsist-guard:test 2>&1 | tail -20</automated>
  </verify>
  <done>DawSync worktree (track-E) konsist-guard/ module exists with all 5 generated test files + build.gradle.kts. Guard tests compile and execute. Canary assertion confirms non-empty scope under com.dawsync. This validates that guard test installation works correctly in git worktree checkouts.</done>
</task>

<task type="auto">
  <name>Task 3: Install and validate guard tests in OmniSound</name>
  <files>
    C:/Users/34645/AndroidStudioProjects/OmniSound/konsist-guard/build.gradle.kts,
    C:/Users/34645/AndroidStudioProjects/OmniSound/konsist-guard/src/test/kotlin/com/omnisound/konsist/guard/GuardScopeFactory.kt,
    C:/Users/34645/AndroidStudioProjects/OmniSound/settings.gradle.kts
  </files>
  <action>
Run the install script against OmniSound:

```bash
bash setup/install-guard-tests.sh --package com.omnisound --target "C:/Users/34645/AndroidStudioProjects/OmniSound"
```

After installation, run the guard tests:
```bash
cd "C:/Users/34645/AndroidStudioProjects/OmniSound" && ./gradlew :konsist-guard:test
```

OmniSound is a canary validation target -- we need:
1. Templates install without errors
2. Generated files contain `com.omnisound` (not `__ROOT_PACKAGE__`)
3. `settings.gradle.kts` has `include(":konsist-guard")`
4. Tests compile and the canary scope assertion passes (production files exist under com.omnisound)
5. Tests that find no files for a layer (assumeTrue) are correctly skipped, not errored

OmniSound uses flat module names (`core-data`, `core-model`, `core-domain`) which maps to packages like `com.omnisound.core.data`, `com.omnisound.core.domain`, etc. The guard templates should work because they use `__ROOT_PACKAGE__.core.data..` patterns.

**If kotlin("jvm") version issue occurs** (same as DawSync): apply the same fix from Task 1. OmniSound may use a different Kotlin version.

**If OmniSound has architecture violations**: capture and report (same as DawSync -- report, don't fix).
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/OmniSound" && ./gradlew :konsist-guard:test 2>&1 | tail -20</automated>
  </verify>
  <done>OmniSound konsist-guard/ module exists. Guard tests compile and execute. Canary assertion passes (non-empty scope under com.omnisound). Missing layers are gracefully skipped via assumeTrue.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify guard test results and triage violations</name>
  <action>Guard test templates have been installed and validated in DawSync (main + worktree track-E) and OmniSound. All three targets have a konsist-guard/ module with parameterized architecture enforcement tests. User reviews results and triages any architecture violations found during the scan.</action>
  <what-built>Guard test templates installed and validated in DawSync main checkout (full scan), DawSync worktree track-E (worktree validation per user decision), and OmniSound (canary pass). All three consumer targets have a konsist-guard/ module with parameterized architecture enforcement tests.</what-built>
  <how-to-verify>
    1. Review the test execution output from DawSync (main), DawSync worktree (track-E), and OmniSound
    2. If DawSync had architecture violations: review the violation report and note items to fix in your track-E terminal
    3. Verify DawSync's konsist-guard/ module exists: `ls C:/Users/34645/AndroidStudioProjects/DawSync/konsist-guard/`
    4. Verify DawSync worktree's konsist-guard/ module exists: `ls "C:/Users/34645/AndroidStudioProjects/DawSync/.claude/worktrees/track-E/konsist-guard/"`
    5. Verify OmniSound's konsist-guard/ module exists: `ls C:/Users/34645/AndroidStudioProjects/OmniSound/konsist-guard/`
    6. Optionally re-run: `cd DawSync && ./gradlew :konsist-guard:test`
  </how-to-verify>
  <verify>User confirms test results and approves guard test validation</verify>
  <done>User has reviewed guard test results from all three targets (DawSync main, DawSync worktree, OmniSound), triaged any architecture violations, and approved the validation.</done>
  <resume-signal>Type "approved" to confirm guard tests are validated, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd DawSync && ./gradlew :konsist-guard:test` compiles and executes (may have architecture violation failures -- that's expected)
2. `cd "DawSync/.claude/worktrees/track-E" && ./gradlew :konsist-guard:test` compiles and executes in worktree
3. `cd OmniSound && ./gradlew :konsist-guard:test` compiles and passes canary
4. `grep -r "com.dawsync" DawSync/konsist-guard/` shows substituted package (no __ROOT_PACKAGE__ tokens)
5. `grep -r "com.dawsync" "DawSync/.claude/worktrees/track-E/konsist-guard/"` shows substituted package in worktree
6. `grep -r "com.omnisound" OmniSound/konsist-guard/` shows substituted package
7. `grep "konsist-guard" DawSync/settings.gradle.kts` confirms module inclusion
8. `grep "konsist-guard" "DawSync/.claude/worktrees/track-E/settings.gradle.kts"` confirms module inclusion in worktree
9. `grep "konsist-guard" OmniSound/settings.gradle.kts` confirms module inclusion
</verification>

<success_criteria>
- DawSync konsist-guard/ module installed with all guard test files
- DawSync guard tests compile and canary passes (scope is non-empty)
- DawSync worktree (track-E) konsist-guard/ module installed and canary passes
- OmniSound konsist-guard/ module installed with canary pass
- All three settings.gradle.kts files include ":konsist-guard"
- No __ROOT_PACKAGE__ tokens remain in generated files
- Any architecture violations reported (not fixed)
- User approves at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/07-consumer-guard-tests/07-02-SUMMARY.md`
</output>
