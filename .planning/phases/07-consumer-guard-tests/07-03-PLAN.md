---
phase: 07-consumer-guard-tests
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - guard-templates/build.gradle.kts.template
  - setup/install-guard-tests.sh
  - setup/Install-GuardTests.ps1
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GUARD-03]
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md GUARD-03 names DawSync and OmniSound as the validated consumers, matching what was actually done"
    - "build.gradle.kts.template contains __KOTLIN_VERSION__ token in the kotlin(jvm) plugin declaration"
    - "Running install script against a consumer with libs.versions.toml produces a build.gradle.kts with the detected Kotlin version pinned"
  artifacts:
    - path: "guard-templates/build.gradle.kts.template"
      provides: "Build template with __KOTLIN_VERSION__ token for Kotlin plugin version"
      contains: "__KOTLIN_VERSION__"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected GUARD-03 consumer names"
      contains: "DawSync"
  key_links:
    - from: "setup/install-guard-tests.sh"
      to: "guard-templates/build.gradle.kts.template"
      via: "sed __KOTLIN_VERSION__ substitution"
      pattern: "__KOTLIN_VERSION__"
    - from: "setup/Install-GuardTests.ps1"
      to: "guard-templates/build.gradle.kts.template"
      via: "Replace __KOTLIN_VERSION__ substitution"
      pattern: "__KOTLIN_VERSION__"
---

<objective>
Close two verification gaps from 07-VERIFICATION.md: (1) fix REQUIREMENTS.md GUARD-03 to name DawSync/OmniSound as the validated consumers instead of WakeTheCave, and (2) add the missing __KOTLIN_VERSION__ token to build.gradle.kts.template so the Kotlin version auto-detection in install scripts actually applies.

Purpose: The verification report found a documentation inconsistency (GUARD-03 naming WakeTheCave when DawSync/OmniSound were validated per user decision) and a dead code path (scripts detect Kotlin version but template has no token to receive it). Both are cleanup issues, not functional failures.

Output: Updated REQUIREMENTS.md, updated build.gradle.kts.template with __KOTLIN_VERSION__ token. No consumer re-installation needed (existing consumers already work with pluginManagement resolution).
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-consumer-guard-tests/07-CONTEXT.md
@.planning/phases/07-consumer-guard-tests/07-VERIFICATION.md
@.planning/phases/07-consumer-guard-tests/07-02-SUMMARY.md

<interfaces>
<!-- Current state of files to modify -->

From guard-templates/build.gradle.kts.template (current -- NO __KOTLIN_VERSION__ token):
```kotlin
plugins {
    kotlin("jvm")
}

group = "__ROOT_PACKAGE__"
version = "1.0.0"
```

From setup/install-guard-tests.sh (lines 86-98 -- detects version, lines 144/163 -- substitutes __KOTLIN_VERSION__):
```bash
KOTLIN_VERSION=""
VERSIONS_TOML="$TARGET/gradle/libs.versions.toml"
if [ -f "$VERSIONS_TOML" ]; then
    KOTLIN_VERSION=$(grep -E '^kotlin\s*=' "$VERSIONS_TOML" | head -1 | sed 's/.*"\(.*\)".*/\1/')
fi
# ... fallback to 2.3.10
# Line 144: sed -e "s/__ROOT_PACKAGE__/$PACKAGE/g" -e "s/__KOTLIN_VERSION__/$KOTLIN_VERSION/g" ...
# Line 163: sed -e "s/__ROOT_PACKAGE__/$PACKAGE/g" -e "s/__KOTLIN_VERSION__/$KOTLIN_VERSION/g" ...
```

From setup/Install-GuardTests.ps1 (lines 76-88 -- detects version, lines 135/155 -- substitutes):
```powershell
$KotlinVersion = ""
# ... detection from libs.versions.toml, fallback to 2.3.10
# Line 135: .Replace("__KOTLIN_VERSION__", $KotlinVersion)
# Line 155: .Replace("__KOTLIN_VERSION__", $KotlinVersion)
```

From .planning/REQUIREMENTS.md (line 29 -- current GUARD-03 text):
```markdown
- [x] **GUARD-03**: Guard tests validated against at least one consuming project (WakeTheCave)
```

From 07-CONTEXT.md deferred section:
```markdown
- WakeTheCave validation -- project is outdated, defer until it's updated
```

From 07-CONTEXT.md specifics section:
```markdown
- WakeTheCave is NOT the validation target (outdated) -- use DawSync (most updated) and OmniSound (stable)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix __KOTLIN_VERSION__ dead substitution in build template</name>
  <files>
    guard-templates/build.gradle.kts.template
  </files>
  <action>
Add the `__KOTLIN_VERSION__` token to `guard-templates/build.gradle.kts.template` in the `kotlin("jvm")` plugin declaration.

Change line 2 from:
```kotlin
    kotlin("jvm")
```
to:
```kotlin
    kotlin("jvm") version "__KOTLIN_VERSION__"
```

This completes the Kotlin version auto-detection feature that was half-implemented in Plan 02. The install scripts (both SH and PS1) already:
1. Detect the consumer's Kotlin version from `libs.versions.toml`
2. Fall back to `2.3.10` if detection fails
3. Run `sed -e "s/__KOTLIN_VERSION__/$KOTLIN_VERSION/g"` (SH) or `.Replace("__KOTLIN_VERSION__", $KotlinVersion)` (PS1)

But the template had no `__KOTLIN_VERSION__` token, so the substitution was a no-op. After this fix, running `bash install-guard-tests.sh --package com.dawsync --target /path/to/DawSync` will produce a `build.gradle.kts` with `kotlin("jvm") version "2.1.20"` (or whatever version DawSync uses).

Do NOT modify the install scripts -- they already have the correct detection and substitution logic. Only the template needs the token added.

Leave the rest of the template file unchanged (group, version, dependencies, tasks blocks all stay the same).
  </action>
  <verify>
    <automated>grep -q '__KOTLIN_VERSION__' guard-templates/build.gradle.kts.template && grep -q 'kotlin("jvm") version "__KOTLIN_VERSION__"' guard-templates/build.gradle.kts.template && echo "PASS: __KOTLIN_VERSION__ token present in plugin declaration" || echo "FAIL"</automated>
  </verify>
  <done>build.gradle.kts.template contains `kotlin("jvm") version "__KOTLIN_VERSION__"`. Install scripts' existing Kotlin version detection will now produce a build.gradle.kts with the consumer's pinned Kotlin version.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md GUARD-03 to match validated consumers</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
Update REQUIREMENTS.md line 29 to replace the WakeTheCave reference with DawSync and OmniSound, which are the consumers that were actually validated per user decision in 07-CONTEXT.md.

Change from:
```markdown
- [x] **GUARD-03**: Guard tests validated against at least one consuming project (WakeTheCave)
```

To:
```markdown
- [x] **GUARD-03**: Guard tests validated against consuming projects (DawSync full scan, OmniSound canary)
```

This aligns REQUIREMENTS.md with:
1. The user's explicit decision in 07-CONTEXT.md: "WakeTheCave is NOT the validation target (outdated) -- use DawSync (most updated) and OmniSound (stable)"
2. The deferred items in 07-CONTEXT.md: "WakeTheCave validation -- project is outdated, defer until it's updated"
3. The ROADMAP success criteria: "Guard tests pass in DawSync (full scan) and OmniSound (canary pass)"
4. What was actually done in Plan 02 (validated against DawSync main, DawSync worktree, and OmniSound)

Do NOT change:
- The checked status `[x]` (GUARD-03 IS satisfied)
- Any other requirements in the file
- The traceability table at the bottom (already correct)
  </action>
  <verify>
    <automated>grep "GUARD-03" .planning/REQUIREMENTS.md | grep -q "DawSync" && ! grep "GUARD-03" .planning/REQUIREMENTS.md | grep -q "WakeTheCave" && echo "PASS: GUARD-03 updated to DawSync/OmniSound" || echo "FAIL"</automated>
  </verify>
  <done>REQUIREMENTS.md GUARD-03 references DawSync and OmniSound as validated consumers. No mention of WakeTheCave in GUARD-03. Documentation now matches user decisions, ROADMAP criteria, and actual execution.</done>
</task>

</tasks>

<verification>
1. `grep '__KOTLIN_VERSION__' guard-templates/build.gradle.kts.template` shows the token in the plugin line
2. `grep '__ROOT_PACKAGE__' guard-templates/build.gradle.kts.template` still shows the group token (unchanged)
3. `grep 'GUARD-03' .planning/REQUIREMENTS.md` shows DawSync/OmniSound, not WakeTheCave
4. `bash setup/install-guard-tests.sh --package com.test --target /tmp/test-gap-closure --dry-run 2>&1 | grep "Kotlin version"` shows detected version (confirming detection still works)
</verification>

<success_criteria>
- build.gradle.kts.template has `kotlin("jvm") version "__KOTLIN_VERSION__"` (dead substitution fixed)
- REQUIREMENTS.md GUARD-03 names DawSync and OmniSound (documentation inconsistency resolved)
- No changes to install scripts (their detection/substitution logic was already correct)
- Both verification gaps from 07-VERIFICATION.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/07-consumer-guard-tests/07-03-SUMMARY.md`
</output>
