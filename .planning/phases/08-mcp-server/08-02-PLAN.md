---
phase: 08-mcp-server
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - mcp-server/src/resources/docs.ts
  - mcp-server/src/resources/skills.ts
  - mcp-server/src/resources/changelog.ts
  - mcp-server/src/resources/index.ts
  - mcp-server/src/prompts/architecture-review.ts
  - mcp-server/src/prompts/pr-review.ts
  - mcp-server/src/prompts/onboarding.ts
  - mcp-server/src/prompts/index.ts
  - mcp-server/tests/unit/resources/docs.test.ts
  - mcp-server/tests/unit/resources/skills.test.ts
  - mcp-server/tests/unit/resources/changelog.test.ts
  - mcp-server/tests/unit/prompts/architecture-review.test.ts
  - mcp-server/tests/unit/prompts/pr-review.test.ts
  - mcp-server/tests/unit/prompts/onboarding.test.ts
  - docs/enterprise-integration-proposal.md
autonomous: true
requirements:
  - MCP-02
  - MCP-04

must_haves:
  truths:
    - "MCP client can list all 9 pattern doc resources and read their full Markdown content"
    - "MCP client can list skill resources and read any SKILL.md content"
    - "MCP client can read a changelog resource showing recent toolkit changes"
    - "MCP client can retrieve architecture-review prompt with code argument"
    - "MCP client can retrieve pr-review prompt with diff argument"
    - "MCP client can retrieve onboarding prompt for new developer guidance"
    - "All pattern docs are in English (Spanish doc translated)"
  artifacts:
    - path: "mcp-server/src/resources/docs.ts"
      provides: "9 pattern doc resources with docs:// URI scheme"
      exports: ["registerDocResources"]
    - path: "mcp-server/src/resources/skills.ts"
      provides: "Skill definition resources with skills:// URI scheme"
      exports: ["registerSkillResources"]
    - path: "mcp-server/src/resources/changelog.ts"
      provides: "Changelog/what's-new resource"
      exports: ["registerChangelogResource"]
    - path: "mcp-server/src/prompts/architecture-review.ts"
      provides: "Architecture review prompt template"
      exports: ["registerArchitectureReviewPrompt"]
    - path: "mcp-server/src/prompts/pr-review.ts"
      provides: "PR-level review prompt template"
      exports: ["registerPrReviewPrompt"]
    - path: "mcp-server/src/prompts/onboarding.ts"
      provides: "Onboarding prompt template"
      exports: ["registerOnboardingPrompt"]
    - path: "docs/enterprise-integration-proposal.md"
      provides: "English translation of Spanish integration doc"
      contains: "Enterprise Integration"
  key_links:
    - from: "mcp-server/src/resources/docs.ts"
      to: "docs/*.md"
      via: "readFile with getDocsDir()"
      pattern: "readFile.*docs"
    - from: "mcp-server/src/resources/skills.ts"
      to: "skills/*/SKILL.md"
      via: "readFile with getSkillsDir()"
      pattern: "readFile.*skills"
    - from: "mcp-server/src/resources/index.ts"
      to: "mcp-server/src/resources/docs.ts"
      via: "import registerDocResources"
      pattern: "registerDocResources"
    - from: "mcp-server/src/prompts/index.ts"
      to: "mcp-server/src/prompts/architecture-review.ts"
      via: "import registerArchitectureReviewPrompt"
      pattern: "registerArchitectureReviewPrompt"
---

<objective>
Implement all MCP resources (pattern docs, skills, changelog) and MCP prompts (architecture review, PR review, onboarding) with full test coverage.

Purpose: This plan delivers the read-only content layer of the MCP server -- agents can discover and read all pattern documentation, skill definitions, and retrieve structured prompts for architecture review workflows.

Output: 9 pattern doc resources, dynamic skill resources, changelog resource, 3 prompt templates -- all tested via InMemoryTransport.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mcp-server/08-RESEARCH.md
@.planning/phases/08-mcp-server/08-CONTEXT.md
@.planning/phases/08-mcp-server/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- server factory and utilities the executor needs -->

From mcp-server/src/server.ts:
```typescript
export function createServer(): McpServer;
```

From mcp-server/src/utils/paths.ts:
```typescript
export function getToolkitRoot(): string;
export function getDocsDir(): string;
export function getSkillsDir(): string;
export function getScriptsDir(): string;
```

From mcp-server/src/utils/logger.ts:
```typescript
export const logger: {
  info: (msg: string) => void;
  warn: (msg: string) => void;
  error: (msg: string) => void;
  debug: (msg: string) => void;
};
```

From mcp-server/src/types/results.ts:
```typescript
export interface ValidationResult { status: "PASS" | "FAIL" | "ERROR" | "TIMEOUT"; summary: string; details: ValidationDetail[]; duration_ms: number; }
export interface ValidationDetail { check: string; status: "PASS" | "FAIL" | "WARN"; message: string; file?: string; }
export interface ToolResult { tool: string; result: ValidationResult; }
```

From mcp-server/src/resources/index.ts (stub from Plan 01):
```typescript
export function registerResources(server: McpServer): void; // replace stub
```

From mcp-server/src/prompts/index.ts (stub from Plan 01):
```typescript
export function registerPrompts(server: McpServer): void; // replace stub
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MCP resources (pattern docs, skills, changelog) and translate Spanish doc</name>
  <files>
    mcp-server/src/resources/docs.ts,
    mcp-server/src/resources/skills.ts,
    mcp-server/src/resources/changelog.ts,
    mcp-server/src/resources/index.ts,
    mcp-server/tests/unit/resources/docs.test.ts,
    mcp-server/tests/unit/resources/skills.test.ts,
    mcp-server/tests/unit/resources/changelog.test.ts,
    docs/enterprise-integration-proposal.md
  </files>
  <behavior>
    - client.listResources() returns at least 9 pattern doc resources with docs:// URIs
    - client.readResource({ uri: "docs://androidcommondoc/kmp-architecture" }) returns Markdown content containing "KMP"
    - client.readResource({ uri: "docs://androidcommondoc/enterprise-integration" }) returns English content (not Spanish)
    - client.listResources() includes skill resources with skills:// URIs (at least 10)
    - client.readResource({ uri: "skills://androidcommondoc/run" }) returns SKILL.md content
    - client.readResource({ uri: "changelog://androidcommondoc/latest" }) returns changelog content
    - Reading a non-existent resource returns an error (not a crash)
  </behavior>
  <action>
1. **Translate Spanish doc** -- Create `docs/enterprise-integration-proposal.md`:
   - Translate `docs/propuesta-integracion-enterprise.md` (330 lines) from Spanish to English
   - Keep the same Markdown structure, headings, tables, and formatting
   - Keep the original Spanish file in place (do not delete it)
   - The translated file name is `enterprise-integration-proposal.md`

2. **src/resources/docs.ts** -- Pattern doc resources (Pattern 2 from RESEARCH.md):
   - Export `registerDocResources(server: McpServer): void`
   - Register 9 pattern docs as static resources with `docs://androidcommondoc/{name}` URI scheme:
     - compose-resources-patterns, gradle-patterns, kmp-architecture, offline-first-patterns,
       enterprise-integration (reads enterprise-integration-proposal.md),
       resource-management-patterns, testing-patterns, ui-screen-patterns, viewmodel-state-patterns
   - Each resource: name is the doc slug, URI is `docs://androidcommondoc/{slug}`,
     metadata has title (titlecased slug), description, mimeType: "text/markdown"
   - Handler reads file from `getDocsDir()` using `readFile(path, "utf-8")`
   - Map "enterprise-integration" slug to filename "enterprise-integration-proposal.md"
   - Use dynamic filesystem scan to build the doc list (read `docs/` directory, filter `.md` files, exclude the Spanish original `propuesta-integracion-enterprise.md`)
   - Handle file-not-found gracefully: throw McpError with appropriate error code

3. **src/resources/skills.ts** -- Skill resources (Pattern 3 ResourceTemplate from RESEARCH.md):
   - Export `registerSkillResources(server: McpServer): void`
   - Use `ResourceTemplate` with URI pattern `skills://androidcommondoc/{skillName}`
   - Implement `list` callback: scan `getSkillsDir()` for subdirectories, return each as a resource
   - Handler reads `{skillDir}/{skillName}/SKILL.md`
   - Handle missing skill gracefully

4. **src/resources/changelog.ts** -- Changelog resource:
   - Export `registerChangelogResource(server: McpServer): void`
   - Register a single static resource at `changelog://androidcommondoc/latest`
   - Content: Run `git log --oneline -20` from toolkit root via `execFile("git", ["log", ...])` to get recent history. Combine with `.planning/RETROSPECTIVE.md` summary if available. Format as Markdown changelog.
   - Use `execFile` from `node:child_process` (NOT exec) for git commands. Fall back to a static message if git is unavailable.

5. **src/resources/index.ts** -- Replace the stub from Plan 01:
   - Import and call all three register functions: registerDocResources, registerSkillResources, registerChangelogResource

6. **Tests** (write FIRST):
   - `tests/unit/resources/docs.test.ts`: Connect via InMemoryTransport, list resources (expect >= 9 with docs:// URI), read kmp-architecture (expect "KMP" in content), read enterprise-integration (expect English content, not Spanish), verify error on non-existent resource
   - `tests/unit/resources/skills.test.ts`: List resources (expect skills:// URIs present), read a known skill
   - `tests/unit/resources/changelog.test.ts`: Read changelog resource, verify it returns non-empty Markdown

IMPORTANT: All imports must use `.js` extension. Use `import.meta.url` for path resolution (no `__dirname`). Use `execFile` (never exec) for any child process spawning.
  </action>
  <verify>
    <automated>cd mcp-server && npx tsc && npx vitest run tests/unit/resources/</automated>
  </verify>
  <done>
    - 9 pattern doc resources listed and readable via docs:// URI scheme
    - Skill resources listed and readable via skills:// URI scheme
    - Changelog resource readable with recent changes
    - enterprise-integration-proposal.md exists in docs/ as English translation
    - All resource tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MCP prompts (architecture review, PR review, onboarding)</name>
  <files>
    mcp-server/src/prompts/architecture-review.ts,
    mcp-server/src/prompts/pr-review.ts,
    mcp-server/src/prompts/onboarding.ts,
    mcp-server/src/prompts/index.ts,
    mcp-server/tests/unit/prompts/architecture-review.test.ts,
    mcp-server/tests/unit/prompts/pr-review.test.ts,
    mcp-server/tests/unit/prompts/onboarding.test.ts
  </files>
  <behavior>
    - client.listPrompts() returns at least 3 prompts (architecture-review, pr-review, onboarding)
    - client.getPrompt("architecture-review", { code: "class Foo { ... }" }) returns messages array with role "user" and content containing the code and pattern references
    - client.getPrompt("architecture-review", { code: "...", layer: "viewmodel" }) returns messages focusing on ViewModel patterns
    - client.getPrompt("pr-review", { diff: "diff --git..." }) returns messages with diff and pattern references
    - client.getPrompt("pr-review", { diff: "...", focusAreas: "viewmodel,testing" }) returns messages filtered to those areas
    - client.getPrompt("onboarding", {}) returns messages with toolkit overview and setup instructions
  </behavior>
  <action>
1. **src/prompts/architecture-review.ts** -- Architecture review prompt (Pattern 4 from RESEARCH.md):
   - Export `registerArchitectureReviewPrompt(server: McpServer): void`
   - Register prompt "architecture-review" with argsSchema:
     - `code`: z.string() -- Kotlin code to review
     - `layer`: z.enum(["ui", "viewmodel", "domain", "data", "model"]).optional() -- focus layer
   - Handler:
     - Load relevant pattern docs from disk based on `layer` argument:
       - "ui" -> ui-screen-patterns.md, compose-resources-patterns.md
       - "viewmodel" -> viewmodel-state-patterns.md
       - "domain" -> kmp-architecture.md
       - "data" -> offline-first-patterns.md, kmp-architecture.md
       - "model" -> kmp-architecture.md
       - no layer -> kmp-architecture.md (general)
     - Build prompt message: System context with loaded patterns + user message with code to review
     - Return `{ messages: [{ role: "user", content: { type: "text", text: promptText } }] }`
   - Include in the prompt: "Review the following Kotlin code against AndroidCommonDoc architecture patterns. Identify violations, suggest improvements, cite specific pattern rules."

2. **src/prompts/pr-review.ts** -- PR-level review prompt (locked decision from CONTEXT.md):
   - Export `registerPrReviewPrompt(server: McpServer): void`
   - Register prompt "pr-review" with argsSchema:
     - `diff`: z.string() -- Git diff output
     - `focusAreas`: z.string().optional() -- Comma-separated areas (e.g., "viewmodel,testing")
   - Handler:
     - Load pattern docs based on focusAreas (map each area to relevant docs)
     - If no focusAreas, load all pattern docs (or a curated essential subset to stay within token limits -- use kmp-architecture, viewmodel-state-patterns, testing-patterns as the default set)
     - Build prompt: "Review this PR diff against AndroidCommonDoc patterns. For each changed Kotlin file, check: architecture layer compliance, naming conventions, ViewModel state patterns, error handling with Result<T>, testing patterns. Report violations with file paths and line references."
     - Return messages array

3. **src/prompts/onboarding.ts** -- Onboarding prompt (locked decision from CONTEXT.md):
   - Export `registerOnboardingPrompt(server: McpServer): void`
   - Register prompt "onboarding" with argsSchema:
     - `projectType`: z.enum(["android", "kmp", "ios"]).optional() -- developer's focus area
   - Handler:
     - Load the toolkit's CLAUDE.md (or a curated summary of it) plus relevant pattern docs
     - Build a welcoming onboarding prompt that walks through: toolkit purpose, directory structure, key pattern docs, how to run quality gates, how to adopt patterns, common mistakes
     - Tailor content based on projectType if provided
     - Return messages array

4. **src/prompts/index.ts** -- Replace the stub from Plan 01:
   - Import and call all three register functions

5. **Tests** (write FIRST):
   - `tests/unit/prompts/architecture-review.test.ts`: List prompts (expect "architecture-review"), get prompt with code arg (verify messages returned), get prompt with layer filter (verify layer-specific content)
   - `tests/unit/prompts/pr-review.test.ts`: Get prompt with diff (verify messages), get with focusAreas
   - `tests/unit/prompts/onboarding.test.ts`: Get prompt (verify welcoming content), get with projectType

IMPORTANT: All imports must use `.js` extension.
  </action>
  <verify>
    <automated>cd mcp-server && npx tsc && npx vitest run tests/unit/prompts/</automated>
  </verify>
  <done>
    - 3 prompts registered and retrievable via MCP client
    - Architecture review prompt accepts code and optional layer, returns messages with pattern references
    - PR review prompt accepts diff and optional focusAreas, returns messages with review instructions
    - Onboarding prompt returns toolkit introduction and setup guidance
    - All prompt tests pass
  </done>
</task>

</tasks>

<verification>
- `cd mcp-server && npx tsc` compiles clean
- `cd mcp-server && npx vitest run` all tests pass (resources + prompts + existing utils/integration tests)
- `docs/enterprise-integration-proposal.md` exists and contains English content
- `grep -r "console.log" mcp-server/src/` returns zero matches
</verification>

<success_criteria>
- 9 pattern doc resources accessible via docs:// URI scheme
- Skill resources dynamically scanned and accessible via skills:// URI
- Changelog resource provides recent toolkit changes
- 3 prompt templates retrievable with arguments (architecture-review, pr-review, onboarding)
- Spanish enterprise integration doc translated to English
- All tests green, TypeScript compiles clean, no console.log in source
</success_criteria>

<output>
After completion, create `.planning/phases/08-mcp-server/08-02-SUMMARY.md`
</output>
