---
phase: 08-mcp-server
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - mcp-server/README.md
  - .github/workflows/mcp-server-ci.yml
  - mcp-server/tests/integration/stdio-transport.test.ts
  - mcp-server/src/server.ts
autonomous: false
requirements:
  - MCP-05

must_haves:
  truths:
    - "claude mcp add registers the server and Claude Code can list tools, resources, and prompts"
    - "Full integration test passes: server connects via InMemoryTransport and handles resource reads, tool invocations, and prompt retrieval"
    - "GitHub Actions CI runs npm test and npm run lint on push/PR"
    - "README.md documents setup, architecture, and API reference"
    - "Server launches on Windows with no stdio corruption"
  artifacts:
    - path: "mcp-server/README.md"
      provides: "Setup instructions, architecture overview, API reference"
      contains: "claude mcp add"
    - path: ".github/workflows/mcp-server-ci.yml"
      provides: "GitHub Actions CI workflow"
      contains: "vitest"
    - path: "mcp-server/tests/integration/stdio-transport.test.ts"
      provides: "Full integration test with all handlers"
      min_lines: 50
  key_links:
    - from: ".github/workflows/mcp-server-ci.yml"
      to: "mcp-server/package.json"
      via: "npm test and npm run lint commands"
      pattern: "npm test"
    - from: "mcp-server/README.md"
      to: "mcp-server/build/index.js"
      via: "claude mcp add registration command"
      pattern: "claude mcp add"
---

<objective>
Complete the MCP server with full integration testing, GitHub Actions CI, README documentation, and a manual verification checkpoint to confirm Claude Code registration works on Windows.

Purpose: This is the capstone plan that wires everything together, adds CI quality enforcement, documents the server for adoption, and verifies the complete stack works end-to-end on Windows with no stdio corruption.

Output: Production-ready MCP server with CI, docs, and verified Claude Code registration.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mcp-server/08-RESEARCH.md
@.planning/phases/08-mcp-server/08-CONTEXT.md
@.planning/phases/08-mcp-server/08-01-SUMMARY.md
@.planning/phases/08-mcp-server/08-02-SUMMARY.md
@.planning/phases/08-mcp-server/08-03-SUMMARY.md

<interfaces>
<!-- All modules are implemented at this point. Integration test exercises the full surface. -->

From mcp-server/src/server.ts:
```typescript
export function createServer(): McpServer;
// Internally calls registerResources, registerTools, registerPrompts
```

Registration commands from RESEARCH.md:
```bash
claude mcp add --transport stdio --env ANDROID_COMMON_DOC="C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc" androidcommondoc -- node "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server/build/index.js"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full integration test, CI workflow, and README</name>
  <files>
    mcp-server/tests/integration/stdio-transport.test.ts,
    .github/workflows/mcp-server-ci.yml,
    mcp-server/README.md,
    mcp-server/src/server.ts
  </files>
  <action>
1. **Update integration test** -- `tests/integration/stdio-transport.test.ts`:
   - The basic test from Plan 01 verified server startup. Now expand to exercise the full surface:
   - Test resources: `client.listResources()` returns >= 9 docs + skills, `client.readResource()` for a known doc returns Markdown
   - Test tools: `client.listTools()` returns >= 7 tools, `client.callTool("setup-check", {})` returns JSON with status field
   - Test prompts: `client.listPrompts()` returns >= 3 prompts, `client.getPrompt("architecture-review", { code: "class Foo" })` returns messages
   - Test stdio cleanliness: Create a child process running `node build/index.js`, send a JSON-RPC initialize request on stdin, verify only valid JSON-RPC appears on stdout (no stray bytes). Kill after test. This is the CRITICAL Windows MCP-05 test.
   - Use InMemoryTransport for the handler tests (fast, reliable). Use a real subprocess for the stdio cleanliness test.

2. **Ensure server.ts is correctly wired** -- If Plans 02 and 03 updated the stub registration files, verify `server.ts` imports and calls them correctly. The factory function should call:
   - `registerResources(server)` from `./resources/index.js`
   - `registerTools(server)` from `./tools/index.js`
   - `registerPrompts(server)` from `./prompts/index.js`
   - If server.ts already has these imports from Plan 01, just verify they work with the implemented modules.

3. **GitHub Actions CI** -- `.github/workflows/mcp-server-ci.yml` at the REPO ROOT (locked decision):
   - GitHub only reads workflows from the repo root `.github/workflows/` directory
   - Trigger: push to main/master, pull_request to main/master
   - Job: ubuntu-latest + windows-latest matrix (MCP-05 requires Windows testing)
   - Steps: checkout, setup-node (v24), `cd mcp-server && npm ci`, `npm run build`, `npm test`, `npm run lint`

4. **README.md** (locked decision: full README with setup, architecture, API reference):
   - Title: AndroidCommonDoc MCP Server
   - Sections:
     - **Overview**: What the server does, MCP protocol, stdio transport
     - **Prerequisites**: Node.js >= 18, bash (Git Bash on Windows)
     - **Installation**: `cd mcp-server && npm install && npm run build`
     - **Registration**: `claude mcp add` command with exact paths
     - **Architecture**: Server factory pattern, resources/tools/prompts modules, utility modules
     - **Resources**: Table of all resources (docs://, skills://, changelog://) with descriptions
     - **Tools**: Table of all tools with input schemas and descriptions
     - **Prompts**: Table of all prompts with argument schemas
     - **Development**: How to run tests, lint, format
     - **CI**: GitHub Actions workflow description
     - **Troubleshooting**: Common issues (stdio corruption, path resolution, Windows npx)

5. Run full test suite and lint to verify everything works together.
  </action>
  <verify>
    <automated>cd mcp-server && npx tsc && npx vitest run && npx eslint src/</automated>
  </verify>
  <done>
    - Full integration test passes (resources, tools, prompts, stdio cleanliness)
    - GitHub Actions CI workflow exists at .github/workflows/mcp-server-ci.yml
    - README.md covers setup, architecture, and complete API reference
    - npm run build, npm test, npm run lint all pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Claude Code MCP registration on Windows</name>
  <files></files>
  <action>
Human verifies end-to-end MCP server registration and communication with Claude Code on Windows.

What was built: Complete MCP server with all resources (9 pattern docs, skills, changelog), 7 tools (5 individual + validate-all + setup-check), 3 prompts (architecture-review, pr-review, onboarding), full test suite, CI workflow, and README documentation. The server is compiled and ready for Claude Code registration.

How to verify:
1. Build the server: `cd mcp-server && npm run build`
2. Register with Claude Code: `claude mcp add --transport stdio --env ANDROID_COMMON_DOC="C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc" androidcommondoc -- node "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server/build/index.js"`
3. Verify registration: `claude mcp list` -- expect "androidcommondoc" in the list
4. In a Claude Code session, verify the server works:
   - Ask Claude to list MCP tools -- should see check-doc-freshness, validate-all, setup-check, etc.
   - Ask Claude to read a resource -- e.g., "Read the kmp-architecture pattern doc via MCP"
   - Ask Claude to invoke setup-check tool
   - Verify no "Connection closed" errors (stdio corruption test)
5. Confirm it works on Windows (this IS the Windows machine, so if steps 2-4 work, MCP-05 is satisfied)
  </action>
  <verify>User confirms Claude Code can connect and use the MCP server</verify>
  <done>
    - Claude Code successfully registers the MCP server
    - Resources, tools, and prompts are accessible from Claude Code
    - No stdio corruption on Windows (no "Connection closed" errors)
    - MCP-05 requirement verified
  </done>
</task>

</tasks>

<verification>
- `cd mcp-server && npm run build && npm test && npm run lint` all pass
- `claude mcp list` shows androidcommondoc server registered
- Claude Code can list and use resources, tools, and prompts from the server
- No stdio corruption on Windows
</verification>

<success_criteria>
- Full test suite green (unit + integration)
- CI workflow ready for GitHub
- README documents complete API surface
- Claude Code successfully registers and communicates with the MCP server
- Windows stdio works without corruption (MCP-05 verified by human)
</success_criteria>

<output>
After completion, create `.planning/phases/08-mcp-server/08-04-SUMMARY.md`
</output>
