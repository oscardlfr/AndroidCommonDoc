---
phase: 14.1-docs-subdirectory-reorganization
plan: 06
type: execute
wave: 5
depends_on: ["14.1-03", "14.1-04", "14.1-05"]
files_modified:
  - mcp-server/tests/integration/doc-structure.test.ts
autonomous: false
requirements: [REORG-01, REORG-02, REORG-03, REORG-04, REORG-05, REORG-06, REORG-07]

must_haves:
  truths:
    - "validate-doc-structure passes across all 3 projects with zero errors"
    - "Scanner discovers all docs from new subdirectory structure"
    - "find-pattern --category returns correct results"
    - "Vault sync completes successfully with category-mirrored structure"
    - "vault-status shows healthy state with no orphans"
    - "Full MCP test suite passes with no regressions"
    - "Human confirms Obsidian vault opens with correct category navigation"
  artifacts:
    - path: "mcp-server/tests/integration/doc-structure.test.ts"
      provides: "Integration test verifying doc structure across projects"
      contains: "doc-structure"
  key_links:
    - from: "mcp-server/src/tools/validate-doc-structure.ts"
      to: "docs/, shared-kmp-libs/docs/, DawSync/docs/"
      via: "Cross-project validation"
      pattern: "discoverProjects"
    - from: "mcp-server/src/vault/sync-engine.ts"
      to: "mcp-server/src/vault/collector.ts"
      via: "Vault sync uses updated collector with category routing"
      pattern: "collectL0Sources"
---

<objective>
Run end-to-end verification across all 3 projects: validate-doc-structure, vault re-sync with --init (clean slate for new paths), full test suite, and Obsidian human verification.

Purpose: Confirm the entire reorganization works correctly: docs discoverable, tools functional, vault reflects new structure, no regressions. This is the quality gate before phase completion.

Output: Passing integration tests, re-synced vault, verified Obsidian navigation, human approval.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14.1-docs-subdirectory-reorganization/14.1-CONTEXT.md
@.planning/phases/14.1-docs-subdirectory-reorganization/14.1-RESEARCH.md
@.planning/phases/14.1-docs-subdirectory-reorganization/14.1-02-SUMMARY.md
@.planning/phases/14.1-docs-subdirectory-reorganization/14.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-project validation + vault re-sync + integration tests</name>
  <files>mcp-server/tests/integration/doc-structure.test.ts</files>
  <action>
    1. **Create integration test** at mcp-server/tests/integration/doc-structure.test.ts:
       - Test: scanDirectory on AndroidCommonDoc docs/ discovers all 42+ docs from subdirectories
       - Test: every discovered doc has a `category` field in metadata
       - Test: no docs with scope/sources/targets exist at docs/ root (all in subdirectories)
       - Test: find-pattern with category="testing" returns only testing-related docs
       - Test: L1 and L2 docs are discoverable via project-discovery + scanDirectory

    2. **Run validate-doc-structure across all projects:**
       ```bash
       cd mcp-server && node dist/cli/validate-doc-structure.js
       ```
       Or invoke via MCP tool. Expect zero errors across AndroidCommonDoc, shared-kmp-libs, DawSync.
       If errors found: fix them before proceeding.

    3. **Run vault sync with --init mode** (clean slate to avoid orphans from old flat paths):
       Use the sync-vault MCP tool or CLI. The --init flag creates a fresh vault with the new category-mirrored structure.

    4. **Run vault-status** to verify health: file count, zero orphans, all projects discovered.

    5. **Run full MCP test suite:**
       ```bash
       cd mcp-server && npx vitest run --reporter=verbose
       ```
       All tests must pass including unit and integration.

    6. **Verify scanner backward compatibility:** The scanner should still work for projects that have NOT been reorganized (flat docs/ with no subdirectories). This is important because WakeTheCave and OmniSound may be discovered but have flat structures.
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Integration tests pass, validate-doc-structure shows zero errors across all 3 projects, vault re-synced with category structure, full test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Obsidian vault and docs structure</name>
  <files>none (verification only)</files>
  <action>
    What was built across Plans 01-06:
    - AndroidCommonDoc (L0): 42 docs in 13 subdirectories
    - shared-kmp-libs (L1): 27 docs in 9 subdirectories, 2 renames, 3 archived
    - DawSync (L2): all flat files moved, CODEX_AUDITY archived, drawio moved
    - validate-doc-structure MCP tool built and registered
    - find-pattern extended with --category filter
    - Vault re-synced with category-mirrored structure and category-grouped MOCs
    - /doc-reorganize skill created

    How to verify:
    1. Open the Obsidian vault at ~/AndroidStudioProjects/kmp-knowledge-vault
    2. Verify Home.md shows category-based navigation tree (not flat link list)
    3. Navigate to "All Patterns" MOC -- verify entries are grouped by category headers
    4. Navigate to "By Layer" MOC -- verify L0/L1/L2 sections show category groupings
    5. Click a few wikilinks to verify they still resolve correctly
    6. Check graph view -- nodes should show the new structure
    7. Verify docs/README.md renders correctly in each of the 3 repos (view on GitHub or locally)
  </action>
  <verify>Human visual verification of Obsidian vault navigation and README rendering</verify>
  <done>Human approves: vault navigation works with category grouping, wikilinks resolve, README renders correctly</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Full quality gate:
```bash
cd mcp-server && npx vitest run --reporter=verbose
```
</verification>

<success_criteria>
- validate-doc-structure passes with zero errors across all 3 projects
- Scanner discovers all docs from subdirectories
- find-pattern --category filter works correctly
- Vault sync completes with category-mirrored structure
- vault-status shows healthy state
- Full MCP test suite passes (unit + integration)
- Obsidian vault opens correctly with category-grouped navigation
- Human approves the visual result
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-docs-subdirectory-reorganization/14.1-06-SUMMARY.md`
</output>
