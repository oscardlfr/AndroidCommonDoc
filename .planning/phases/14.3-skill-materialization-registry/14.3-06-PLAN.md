---
phase: 14.3-skill-materialization-registry
plan: 06
type: execute
wave: 3
depends_on: [14.3-01]
files_modified:
  - CLAUDE.md
  - DawSync/CLAUDE.md
  - shared-kmp-libs/CLAUDE.md
autonomous: true
requirements: [MATL-07]

must_haves:
  truths:
    - "~/.claude/CLAUDE.md stays as-is (101 lines) providing shared KMP rules for all projects"
    - "AndroidCommonDoc/CLAUDE.md contains only toolkit-specific rules, no duplicated KMP rules"
    - "DawSync/CLAUDE.md slimmed to project-specific rules only, under 120 lines"
    - "shared-kmp-libs/CLAUDE.md stays module-focused, no duplicated KMP rules"
    - "No behavioral rule is lost -- every rule in the ecosystem is accessible via ~/.claude/ + project combo"
  artifacts:
    - path: "CLAUDE.md"
      provides: "AndroidCommonDoc toolkit-specific rules (no KMP rule duplication)"
    - path: "DawSync/CLAUDE.md"
      provides: "DawSync project-specific rules only"
    - path: "shared-kmp-libs/CLAUDE.md"
      provides: "shared-kmp-libs module-focused rules"
  key_links:
    - from: "~/.claude/CLAUDE.md"
      to: "CLAUDE.md"
      via: "Claude Code auto-loads both (no explicit import needed)"
      pattern: "auto-loaded by Claude Code"
    - from: "~/.claude/CLAUDE.md"
      to: "DawSync/CLAUDE.md"
      via: "Claude Code auto-loads both"
      pattern: "auto-loaded by Claude Code"
---

<objective>
De-duplicate CLAUDE.md files across the ecosystem. Claude Code auto-loads `~/.claude/CLAUDE.md` + `{project}/CLAUDE.md`, so project files should contain ONLY project-specific rules. Remove all duplicated KMP rules from project CLAUDE.md files.

Purpose: Eliminate the 245-line DawSync CLAUDE.md bloat and the exact duplication between ~/.claude/ and AndroidCommonDoc. Every rule still accessible, zero duplication.
Output: Three slimmed CLAUDE.md files with clear separation of concerns
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14.3-skill-materialization-registry/14.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit rules and create de-duplication map</name>
  <files>CLAUDE.md</files>
  <action>
    **Step 1: Read all CLAUDE.md files**
    - `~/.claude/CLAUDE.md` (101 lines) -- the shared base
    - `AndroidCommonDoc/CLAUDE.md` (101 lines) -- currently exact duplicate
    - `shared-kmp-libs/CLAUDE.md` (57 lines) -- module-focused
    - `DawSync/CLAUDE.md` (245 lines) -- bloated with duplicates

    **Step 2: Extract every rule from each file**
    Create a rule inventory. For each rule, note:
    - Rule text (e.g., "ALWAYS rethrow CancellationException in catch blocks")
    - Which files contain it
    - Whether the project version adds context beyond ~/.claude/ version

    **Step 3: Classify each rule in project files**
    - PURE DUPLICATE: Identical to ~/.claude/ version, no added context -> DELETE from project file
    - REFINEMENT: Adds project-specific context beyond ~/.claude/ -> KEEP shortened version
    - PROJECT-ONLY: Not in ~/.claude/ at all -> KEEP as-is

    **Step 4: Rewrite AndroidCommonDoc/CLAUDE.md**
    This is the L0 toolkit project. Remove ALL shared KMP rules (they come from ~/.claude/). Keep only:
    - Toolkit-specific context (what AndroidCommonDoc is, its MCP server, registry)
    - L0 skills/agents/commands reference
    - Contribution/development patterns specific to this repo
    - Target: <80 lines

    Add a comment at the top: `# AndroidCommonDoc Project Rules` and note: "Shared KMP rules in ~/.claude/CLAUDE.md (auto-loaded by Claude Code)"
  </action>
  <verify>
    <automated>wc -l "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/CLAUDE.md" | awk '{if ($1 < 150) exit 0; else exit 1}'</automated>
  </verify>
  <done>AndroidCommonDoc/CLAUDE.md rewritten to toolkit-specific only, under 100 lines, no duplicated KMP rules</done>
</task>

<task type="auto">
  <name>Task 2: Slim DawSync and shared-kmp-libs CLAUDE.md files</name>
  <files>DawSync/CLAUDE.md, shared-kmp-libs/CLAUDE.md</files>
  <action>
    **DawSync/CLAUDE.md (245 lines -> target <120 lines):**
    1. Delete all rules that are identical to ~/.claude/CLAUDE.md:
       - KMP Source Set Rules (exact duplicate)
       - Module Naming (exact duplicate)
       - Architecture table (exact duplicate)
       - Error Handling (exact duplicate)
       - ViewModel Rules (exact duplicate)
       - Testing Rules (exact duplicate, but check for DawSync-specific additions)
       - DI section (exact duplicate)
       - Navigation (exact duplicate)
       - Compose Resources (exact duplicate)
       - File Naming table (exact duplicate)
       - Build Patterns (exact duplicate)
       - Git Flow for Agent Teams (exact duplicate)
       - Worktree Rules (exact duplicate)
       - Agent Team File Ownership (exact duplicate)

    2. Keep rules that add DawSync-specific context (shortened):
       - Developer Context: Add DawSync-specific context (main project, domain architecture)
       - Domain-specific: Ableton integration, DAW modules, wave tracking, offline-first
       - DawSync-specific module structure or naming
       - Any testing rule that adds Windows file-locking rationale beyond ~/.claude/

    3. Add header: `# DawSync Project Rules` with note: "Shared KMP rules in ~/.claude/CLAUDE.md (auto-loaded by Claude Code)"

    **shared-kmp-libs/CLAUDE.md (57 lines):**
    1. Read carefully and compare each rule with ~/.claude/CLAUDE.md
    2. Remove pure duplicates (module naming, architecture, error handling if duplicated)
    3. Keep module-specific content (what shared-kmp-libs IS, its module catalog pattern, version catalog authority)
    4. Add header note: "Shared KMP rules in ~/.claude/CLAUDE.md"
    5. Target: <60 lines (may already be close if most is unique)

    **CRITICAL: Do NOT remove rules that add project-specific detail.**
    The test: If ~/.claude/ says "Tests run sequentially: maxParallelForks = 1" and DawSync adds "(due to Windows file locking and MMKV shared state)", keep the DawSync version as a one-liner refinement.
  </action>
  <verify>
    <automated>wc -l "C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md" | awk '{if ($1 < 150) exit 0; else exit 1}' && wc -l "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md" | awk '{if ($1 < 80) exit 0; else exit 1}'</automated>
  </verify>
  <done>DawSync/CLAUDE.md under 120 lines, project-specific only; shared-kmp-libs/CLAUDE.md under 60 lines, module-focused only; no behavioral rule lost (all accessible via ~/.claude/ + project combo)</done>
</task>

</tasks>

<verification>
- `wc -l CLAUDE.md` shows AndroidCommonDoc under 100 lines
- `wc -l DawSync/CLAUDE.md` shows under 120 lines
- `wc -l shared-kmp-libs/CLAUDE.md` shows under 60 lines
- Diff of ~/.claude/CLAUDE.md vs each project CLAUDE.md shows no pure rule duplication
- All project-specific refinements preserved
</verification>

<success_criteria>
- Zero pure rule duplication between ~/.claude/ and any project CLAUDE.md
- DawSync slimmed from 245 to <120 lines
- Every behavioral rule accessible via the ~/.claude/ + project combination
- Context budget met: <150 lines per file, <4000 tokens initial load per project
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-skill-materialization-registry/14.3-06-SUMMARY.md`
</output>
