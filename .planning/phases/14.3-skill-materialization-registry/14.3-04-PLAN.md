---
phase: 14.3-skill-materialization-registry
plan: 04
type: execute
wave: 2
depends_on: [14.3-01]
files_modified:
  - mcp-server/src/tools/validate-skills.ts
  - mcp-server/tests/unit/tools/validate-skills.test.ts
  - mcp-server/src/tools/index.ts
  - .claude/commands/android-test.md
  - .claude/commands/auto-cover.md
  - .claude/commands/coverage.md
  - .claude/commands/coverage-full.md
  - .claude/commands/extract-errors.md
  - .claude/commands/run.md
  - .claude/commands/sbom.md
  - .claude/commands/sbom-analyze.md
  - .claude/commands/sbom-scan.md
  - .claude/commands/sync-versions.md
  - .claude/commands/test.md
  - .claude/commands/test-changed.md
  - .claude/commands/test-full.md
  - .claude/commands/test-full-parallel.md
  - .claude/commands/validate-patterns.md
  - .claude/commands/verify-kmp.md
  - adapters/claude-adapter.sh
autonomous: true
requirements: [MATL-04, MATL-06]

must_haves:
  truths:
    - "Skill validation tool parses all SKILL.md files and verifies frontmatter completeness"
    - "Validation checks referenced scripts exist on disk"
    - "Validation checks registry.json is in sync with filesystem"
    - "16 adapter-generated commands removed from L0 .claude/commands/ (Claude Code reads skills directly)"
    - "16 standalone commands remain in .claude/commands/"
    - "Copilot adapter retained and still works"
  artifacts:
    - path: "mcp-server/src/tools/validate-skills.ts"
      provides: "MCP tool for skill validation: frontmatter, scripts, registry sync"
      exports: ["validateSkills", "registerValidateSkillsTool"]
    - path: "mcp-server/tests/unit/tools/validate-skills.test.ts"
      provides: "Unit tests for skill validation"
  key_links:
    - from: "mcp-server/src/tools/validate-skills.ts"
      to: "mcp-server/src/registry/skill-registry.ts"
      via: "generateRegistry for filesystem truth vs registry.json comparison"
      pattern: "generateRegistry"
    - from: "mcp-server/src/tools/validate-skills.ts"
      to: "mcp-server/src/tools/index.ts"
      via: "MCP tool registration"
      pattern: "registerValidateSkillsTool"
---

<objective>
Build the skill validation MCP tool (MATL-06) and simplify the Claude Code adapter pipeline by removing the 16 redundant adapter-generated commands from L0 (MATL-04). Claude Code discovers skills/*/SKILL.md directly -- no intermediate command generation needed.

Purpose: Validation tool provides a safety net against broken skills. Adapter simplification removes redundant intermediaries.
Output: `validate-skills.ts` MCP tool + 16 generated commands deleted from L0 + updated adapter
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14.3-skill-materialization-registry/14.3-RESEARCH.md
@.planning/phases/14.3-skill-materialization-registry/14.3-01-SUMMARY.md

<interfaces>
From mcp-server/src/registry/skill-registry.ts (created in Plan 01):
```typescript
export interface SkillRegistryEntry { name, type, path, hash, dependencies, ... }
export interface SkillRegistry { version, generated, l0_root, entries }
export function generateRegistry(rootDir: string): Promise<SkillRegistry>;
```

From mcp-server/src/tools/index.ts (existing):
```typescript
// Tools are registered by calling registerXxxTool(server) in the tool index
// Follow the existing pattern from validate-doc-structure.ts, monitor-sources.ts, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build validate-skills MCP tool</name>
  <files>mcp-server/src/tools/validate-skills.ts, mcp-server/tests/unit/tools/validate-skills.test.ts, mcp-server/src/tools/index.ts</files>
  <behavior>
    - Test: validateSkills() returns success when all SKILL.md files have valid frontmatter (name, description, allowed-tools)
    - Test: validateSkills() reports error when SKILL.md missing required frontmatter field (name or description)
    - Test: validateSkills() reports warning when SKILL.md references non-existent script in dependencies
    - Test: validateSkills() reports error when registry.json entry has hash mismatch with actual file content
    - Test: validateSkills() reports error when filesystem has skill not in registry.json
    - Test: validateSkills() reports error when registry.json has entry for file that doesn't exist
    - Test: validateSkills() checks agent frontmatter (tools field present)
    - Test: validateSkills() returns structured JSON with errors, warnings, and summary counts
    - Test: validateSkills() accepts optional project parameter for cross-project validation (checks materialized copies match manifest checksums)
  </behavior>
  <action>
    Create `mcp-server/src/tools/validate-skills.ts` that:

    1. Define validation result types:
       - `ValidationIssue = { level: "error" | "warning"; category: string; file: string; message: string }`
       - `ValidationResult = { valid: boolean; errors: number; warnings: number; issues: ValidationIssue[]; summary: string }`

    2. `validateSkillFrontmatter(skillDir: string): Promise<ValidationIssue[]>`
       - For each skills/*/SKILL.md: parse frontmatter, check required fields (name, description)
       - Check allowed-tools is array
       - Check disable-model-invocation is boolean if present

    3. `validateDependencies(rootDir: string, entries: SkillRegistryEntry[]): Promise<ValidationIssue[]>`
       - For each entry with dependencies: check each referenced file exists on disk

    4. `validateRegistrySync(rootDir: string): Promise<ValidationIssue[]>`
       - Generate fresh registry from filesystem
       - Compare against existing skills/registry.json
       - Report: entries in file but not on disk, entries on disk but not in file, hash mismatches

    5. `validateProjectSync(projectRoot: string): Promise<ValidationIssue[]>` (optional project param)
       - Read l0-manifest.json from project
       - For each checksum entry: verify file exists and matches hash
       - Report orphaned materialized files (have l0_source header but not in manifest)

    6. `registerValidateSkillsTool(server: McpServer)` - Register as MCP tool with name "validate-skills", accepts optional project path parameter

    7. Update `mcp-server/src/tools/index.ts` to include the new tool registration

    Write tests FIRST. Use temp directories with fixture SKILL.md files.
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/tools/validate-skills.test.ts --reporter=verbose</automated>
  </verify>
  <done>validate-skills MCP tool registered and tested; catches frontmatter issues, missing scripts, registry drift, and project sync problems</done>
</task>

<task type="auto">
  <name>Task 2: Remove 16 adapter-generated commands from L0 and simplify adapter</name>
  <files>.claude/commands/android-test.md, .claude/commands/auto-cover.md, .claude/commands/coverage.md, .claude/commands/coverage-full.md, .claude/commands/extract-errors.md, .claude/commands/run.md, .claude/commands/sbom.md, .claude/commands/sbom-analyze.md, .claude/commands/sbom-scan.md, .claude/commands/sync-versions.md, .claude/commands/test.md, .claude/commands/test-changed.md, .claude/commands/test-full.md, .claude/commands/test-full-parallel.md, .claude/commands/validate-patterns.md, .claude/commands/verify-kmp.md, adapters/claude-adapter.sh</files>
  <action>
    Claude Code already discovers `skills/*/SKILL.md` natively. The 16 commands in `.claude/commands/` that were generated by `adapters/claude-adapter.sh` are redundant duplicates of skills.

    1. Identify the 16 generated commands by checking for the `<!-- GENERATED by claude-adapter` comment pattern in each .claude/commands/*.md file. Delete all 16.

    2. Verify the remaining 16 standalone commands are NOT generated (no GENERATED comment): brainstorm, bump-version, changelog, doc-check, doc-update, feature-audit, merge-track, metrics, package, pre-release, prioritize, start-track, sync-roadmap, sync-tech-versions, unlock-tests, verify-migrations.

    3. Update `adapters/claude-adapter.sh`:
       - Add a deprecation notice at the top: "# DEPRECATED: Claude Code reads skills/*/SKILL.md directly. This script is retained only for historical reference."
       - OR simply delete it if `adapters/copilot-adapter.sh` and `adapters/generate-all.sh` can work independently (check if generate-all.sh calls claude-adapter.sh)

    4. Keep `adapters/copilot-adapter.sh` and Copilot-related adapters intact for enterprise Microsoft support.

    5. Update `skills/registry.json` by re-running the registry generator (since 16 commands are now gone, entry count drops from 70 to 54: 27 skills + 11 agents + 16 standalone commands).

    6. Run the full MCP test suite to ensure nothing broke.
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run --reporter=verbose && ls ../.claude/commands/ | wc -l</automated>
  </verify>
  <done>16 adapter-generated commands deleted from L0; 16 standalone commands remain; registry.json updated (54 entries); Copilot adapter retained; MCP tests pass</done>
</task>

</tasks>

<verification>
- `cd mcp-server && npx vitest run` passes all tests including new validate-skills tests
- `.claude/commands/` contains exactly 16 standalone commands (no generated duplicates)
- `skills/registry.json` has 54 entries (27 skills + 11 agents + 16 commands)
- validate-skills MCP tool is registered and callable
- Copilot adapter is untouched and still works
</verification>

<success_criteria>
- Skill validation catches frontmatter issues, missing scripts, registry drift
- Redundant adapter-generated commands eliminated from L0
- Registry updated to reflect the 54 remaining L0 assets
- Claude Code adapter pipeline simplified (skills read directly)
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-skill-materialization-registry/14.3-04-SUMMARY.md`
</output>
