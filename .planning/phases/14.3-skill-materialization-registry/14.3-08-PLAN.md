---
phase: 14.3-skill-materialization-registry
plan: 08
type: execute
wave: 4
depends_on: [14.3-05, 14.3-06, 14.3-07]
files_modified:
  - skills/registry.json
  - setup/README.md
  - README.md
autonomous: false
requirements: [MATL-01, MATL-02, MATL-03, MATL-04, MATL-05, MATL-06, MATL-07, MATL-08]

must_haves:
  truths:
    - "Registry in sync with filesystem (validate-skills passes)"
    - "All manifests valid, all materialized copies match L0 hashes"
    - "No orphaned delegates remain anywhere in the ecosystem"
    - "Vault re-synced with renamed files and consolidated categories"
    - "CLAUDE.md layering correct across all projects (no duplication)"
    - "All existing validators pass across all 3 projects"
    - "READMEs updated to reflect new distribution model"
    - "Human-verified Obsidian graph view is clean and readable"
  artifacts:
    - path: "skills/registry.json"
      provides: "Final registry reflecting all changes (adapter cleanup, sync-l0 skill)"
    - path: "README.md"
      provides: "Updated project README referencing /sync-l0 instead of install-claude-skills.sh"
  key_links:
    - from: "skills/registry.json"
      to: "skills/**/SKILL.md"
      via: "hash comparison"
      pattern: "sha256:"
    - from: "DawSync/l0-manifest.json"
      to: "skills/registry.json"
      via: "checksum matching"
      pattern: "sha256:"
---

<objective>
Final comprehensive ecosystem validation. Verify the entire materialization system is in harmony: registry matches filesystem, manifests match registry, no orphaned delegates, vault reflects all changes, CLAUDE.md is clean, and all validators pass. This is the mandatory final wave per user decision.

Purpose: Without this validation, the phase is not complete. This ensures every change from Plans 01-07 works together as a coherent system.
Output: Clean validation report, updated READMEs, re-synced vault, human-verified Obsidian graph
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14.3-skill-materialization-registry/14.3-RESEARCH.md
@.planning/phases/14.3-skill-materialization-registry/14.3-05-SUMMARY.md
@.planning/phases/14.3-skill-materialization-registry/14.3-06-SUMMARY.md
@.planning/phases/14.3-skill-materialization-registry/14.3-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all validators and fix any issues found</name>
  <files>skills/registry.json, README.md, setup/README.md</files>
  <action>
    **Validation Battery (all must pass):**

    1. **Registry sync validation:**
       - Re-generate registry.json from filesystem
       - Compare with existing registry.json -- should be identical or update it
       - Run validate-skills MCP tool: `cd mcp-server && npx tsx -e "..." ` or invoke via test

    2. **Manifest validation (shared-kmp-libs):**
       - Read shared-kmp-libs/l0-manifest.json
       - For each checksum entry: verify file exists and hash matches
       - Report any drift

    3. **Manifest validation (DawSync):**
       - Read DawSync/l0-manifest.json
       - For each checksum entry: verify file exists and hash matches
       - Verify no l0_source headers on L2-specific files

    4. **No orphaned delegates:**
       - `grep -r "delegate: true" AndroidCommonDoc/.claude/ shared-kmp-libs/.claude/ DawSync/.claude/ DawSync/skills/` -- must return nothing
       - `grep -r "canonical:" AndroidCommonDoc/.claude/ shared-kmp-libs/.claude/ DawSync/.claude/` -- must return nothing (old delegate pattern)

    5. **CLAUDE.md validation:**
       - Verify ~/.claude/CLAUDE.md is unchanged (101 lines)
       - Verify AndroidCommonDoc/CLAUDE.md is under 100 lines
       - Verify DawSync/CLAUDE.md is under 120 lines
       - Verify shared-kmp-libs/CLAUDE.md is under 60 lines
       - Diff check: no rules from ~/.claude/ appear verbatim in project files

    6. **Cross-project validate-doc-structure:**
       - Run against AndroidCommonDoc/docs/
       - Run against shared-kmp-libs/docs/
       - Run against DawSync/docs/
       - All should report 0 errors
       - Category values from approved vocabulary

    7. **Naming validation:**
       - No UPPERCASE filenames in active DawSync docs (excluding diagrams/prefix and archive/)

    8. **MCP test suite:**
       - `cd mcp-server && npx vitest run` -- all tests pass

    9. **Script-parity and skill-script-alignment validators** (existing quality gates)

    **Fix any issues found** before proceeding to Task 2.

    **Update READMEs:**
    - Update `README.md` to reference `/sync-l0` skill instead of `install-claude-skills.sh` for skill distribution
    - Update `setup/README.md` (if exists) to reflect that install-claude-skills.sh is deleted, replaced by /sync-l0
    - Add brief documentation of the registry + manifest + materialization model
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server" && npx vitest run --reporter=verbose && cd .. && grep -r "delegate: true" .claude/ 2>/dev/null | wc -l | awk '{if ($1 == 0) exit 0; else exit 1}'</automated>
  </verify>
  <done>All validators pass; no orphaned delegates; READMEs updated; registry in sync; manifests valid; CLAUDE.md layering correct</done>
</task>

<task type="auto">
  <name>Task 2: Vault re-sync with all changes</name>
  <files>skills/registry.json</files>
  <action>
    Re-sync the Obsidian vault to reflect all Phase 14.3 changes:

    1. Run vault sync: `cd mcp-server && npx tsx -e "import { syncVault } from './src/tools/sync-vault.js'; syncVault({mode: 'sync'})"` or equivalent CLI command

    2. The vault should now reflect:
       - Renamed DawSync docs (lowercase-kebab-case filenames)
       - Consolidated categories (9 unified, not 23 fragmented)
       - Updated CLAUDE.md files (slimmer)
       - New sync-l0 skill in skills section
       - No stale delegate-pattern files
       - Wikilinks updated to match renamed files

    3. Verify vault health:
       - No orphaned files from old UPPERCASE names
       - Category-based graph grouping uses 9 categories
       - MOC pages reflect new structure

    4. Run vault-status check for health metrics
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server" && npx vitest run tests/unit/vault/ --reporter=verbose</automated>
  </verify>
  <done>Vault re-synced reflecting all Phase 14.3 changes; no orphaned files; categories consolidated; wikilinks correct</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of ecosystem harmony</name>
  <files>skills/registry.json</files>
  <action>
    Present the complete Phase 14.3 results to the user for visual and functional verification. The automated validators have all passed -- this checkpoint confirms the human-perceptible qualities: Obsidian graph readability, navigation flow, skill discovery, and overall ecosystem coherence.
  </action>
  <verify>Human confirms the ecosystem is in harmony by checking Obsidian graph, skill invocations, and doc navigation</verify>
  <done>User types "approved" confirming: (1) Obsidian graph is clean with 9 category color groups, (2) DawSync doc links work, (3) materialized skills are functional, (4) CLAUDE.md files are appropriately slim</done>
</task>

</tasks>

<verification>
- All 6+ quality gate validators pass across all 3 projects
- `cd mcp-server && npx vitest run` passes (all tests green)
- Zero "delegate: true" in the ecosystem
- Vault re-synced and healthy
- Human-verified Obsidian graph view
</verification>

<success_criteria>
- Every MATL requirement verified as complete
- Ecosystem is in harmony: registry, manifests, materialized copies, vault, CLAUDE.md all consistent
- No regressions introduced
- Human approval of visual quality (Obsidian graph, doc navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-skill-materialization-registry/14.3-08-SUMMARY.md`
</output>
