---
phase: 14.3-skill-materialization-registry
plan: 07
type: execute
wave: 3
depends_on: [14.3-01]
files_modified:
  - DawSync/docs/**/*.md
  - mcp-server/src/vault/transformer.ts
  - mcp-server/src/tools/validate-doc-structure.ts
autonomous: true
requirements: [MATL-08]

must_haves:
  truths:
    - "All ~84 active non-diagram UPPERCASE DawSync docs renamed to lowercase-kebab-case"
    - "All cross-references updated: parent fields, wikilinks, hub Sub-documents sections, l0_refs, README"
    - "Category tags consolidated from 23 to 9 unified categories across all layers"
    - "Naming validation added to vault sync pipeline to prevent regression"
    - "Archive files keep original names (not renamed)"
    - "Diagram files keep their A01-H format (already lowercase-kebab-case with valid prefix)"
  artifacts:
    - path: "DawSync/docs/"
      provides: "Renamed docs with lowercase-kebab-case naming"
    - path: "mcp-server/src/vault/transformer.ts"
      provides: "Updated vault transformer with naming validation"
    - path: "mcp-server/src/tools/validate-doc-structure.ts"
      provides: "Updated validator with naming check and category vocabulary"
  key_links:
    - from: "DawSync/docs/**/*.md"
      to: "DawSync/docs/**/parent: field"
      via: "frontmatter parent references must use new lowercase names"
      pattern: "parent:"
    - from: "mcp-server/src/tools/validate-doc-structure.ts"
      to: "DawSync/docs/"
      via: "naming validation during structure check"
      pattern: "validateNaming"
---

<objective>
Rename all ~84 active UPPERCASE DawSync docs to lowercase-kebab-case at source, update every cross-reference (frontmatter parent, wikilinks, hub Sub-documents, l0_refs, README), consolidate 23 category tags to 9 unified categories, and add naming validation to prevent regression.

Purpose: Consistent lowercase-kebab-case naming is the convention standard. Category consolidation makes the Obsidian graph readable. This is the vault hygiene that MATL-08 requires.
Output: Renamed docs, updated cross-references, consolidated categories, naming validation
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14.3-skill-materialization-registry/14.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build rename map and execute bulk rename with cross-reference updates</name>
  <files>DawSync/docs/**/*.md</files>
  <action>
    **Phase A: Build rename map**
    1. Scan DawSync/docs/ recursively for all active (non-archive/) .md files with UPPERCASE characters in their filename
    2. For each: compute new name using `lowercase-kebab-case` conversion (PRODUCT_SPEC.md -> product-spec.md, CLAUDE_CODE_WORKFLOW.md -> claude-code-workflow.md)
    3. Skip files already lowercase-kebab-case
    4. Skip archive/ files (keep original names per Phase 14.1-03 decision)
    5. Skip diagram files in docs/architecture/diagrams/ (A01-H format already lowercase-kebab with valid section prefix)
    6. Handle directory names: uppercase directory names like docs/architecture/diagrams/A-System-Global/ -> docs/architecture/diagrams/a-system-global/ (only if they have uppercase letters -- the A-H prefix stays)
    7. Build Map<oldPath, newPath> for all renames

    **Phase B: Execute renames with git mv**
    1. Use `git mv OLD NEW` for each file to preserve git history
    2. Rename directories FIRST (parent before children), bottom-up order to avoid path conflicts
    3. Handle Windows path issues: use forward slashes in git commands

    **Phase C: Update cross-references**
    For each renamed file, update all references across the entire DawSync/docs/ tree:
    1. `parent:` frontmatter field -- update to use new lowercase-kebab filename (without .md extension)
    2. `## Sub-documents` sections in hub docs -- update file references
    3. Wikilinks `[[OLD_NAME]]` -> `[[new-name]]`
    4. Relative path references `./OLD_NAME.md` -> `./new-name.md`
    5. `slug:` frontmatter field -- update to match new filename
    6. DawSync docs/README.md -- update any file references
    7. l0_refs should be unaffected (they reference L0 slugs, not DawSync filenames)

    **Phase D: Verify**
    1. Run `find DawSync/docs/ -name "*.md" -not -path "*/archive/*" | grep '[A-Z]'` -- should return ONLY diagram section-prefix files (A01-xxx) and LEGEND/README if they exist
    2. Run validate-doc-structure against DawSync to catch broken cross-references

    **CRITICAL: Use git mv, not manual rename.** This preserves git history. On Windows, git mv handles case changes correctly.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/DawSync" && find docs/ -name "*.md" -not -path "*/archive/*" -not -path "*/diagrams/*" | grep '[A-Z]' | grep -v README | grep -v LEGEND | wc -l | awk '{if ($1 == 0) exit 0; else exit 1}'</automated>
  </verify>
  <done>All ~84 active UPPERCASE docs renamed to lowercase-kebab-case; all cross-references updated; archive files untouched; diagram files untouched; git history preserved via git mv</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate categories and add naming validation</name>
  <files>mcp-server/src/tools/validate-doc-structure.ts, mcp-server/src/vault/transformer.ts, DawSync/docs/**/*.md, shared-kmp-libs/docs/**/*.md, docs/**/*.md</files>
  <action>
    **Part 1: Category consolidation**
    Apply the 9 unified category vocabulary from RESEARCH.md across all layers:

    | Category | Replaces |
    |----------|----------|
    | architecture | architecture, compose, di, navigation, foundation |
    | testing | testing |
    | data | offline-first, storage, resources, io |
    | security | security, oauth |
    | build | gradle, tech |
    | guides | guides |
    | domain | domain, firebase, error-handling (partial -- error-handling docs about domain modeling) |
    | product | product, business, legal, references |
    | ui | ui |

    Special handling:
    - `error-handling` docs: if about exception mapping/catching patterns -> `domain`; if about error UI patterns -> `ui`
    - `archive` is NOT a category -- remove category field from archived docs (status: archived is the indicator)
    - Docs with `status: archived` should not have a category field (or it should be removed)

    For each doc across all 3 projects:
    1. Read frontmatter `category:` field
    2. Map to new unified category using the table above
    3. Update the frontmatter in place
    4. Track changes for verification

    **Part 2: Naming validation in validate-doc-structure**
    Add a new validation check to `mcp-server/src/tools/validate-doc-structure.ts`:
    - `validateNaming(filePath: string)`: checks that active (non-archive) doc filenames are lowercase-kebab-case
    - Allow: `a-z`, `0-9`, `-`, `.` (for extension)
    - Allow: diagram section prefixes like `A01-` within diagrams/ directories
    - Reject: UPPERCASE letters, underscores, spaces
    - Report as warning (not error) to avoid blocking on edge cases

    **Part 3: Category vocabulary validation in validate-doc-structure**
    Add a check that category values come from the approved vocabulary:
    - Approved: architecture, testing, data, security, build, guides, domain, product, ui
    - Report unknown categories as warnings

    **Part 4: Naming validation in vault transformer**
    Add a naming check in `mcp-server/src/vault/transformer.ts` that logs a warning when ingesting a file with non-lowercase-kebab-case naming, so vault sync surfaces naming regressions.

    Run the full MCP test suite after changes.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Categories consolidated to 9 unified vocabulary across all 3 projects; naming validation added to validate-doc-structure and vault transformer; MCP tests pass</done>
</task>

</tasks>

<verification>
- No active DawSync docs have UPPERCASE filenames (excluding diagrams/ prefix and archive/)
- `cd mcp-server && npx vitest run` passes
- validate-doc-structure reports 0 naming warnings for active docs
- Category values across all 3 projects come from the 9 approved categories
- Git history preserved for renamed files
</verification>

<success_criteria>
- ~84 active DawSync docs renamed to lowercase-kebab-case at source
- All cross-references (parent, wikilinks, Sub-documents, README) updated
- 23 categories consolidated to 9 unified categories
- Naming validation prevents regression via validate-doc-structure
- Vault transformer warns on non-standard naming
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-skill-materialization-registry/14.3-07-SUMMARY.md`
</output>
