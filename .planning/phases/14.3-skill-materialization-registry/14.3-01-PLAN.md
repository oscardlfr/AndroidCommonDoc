---
phase: 14.3-skill-materialization-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/registry/skill-registry.ts
  - mcp-server/tests/unit/registry/skill-registry.test.ts
  - skills/registry.json
autonomous: true
requirements: [MATL-01]

must_haves:
  truths:
    - "registry.json catalogs all 27 skills, 11 agents, and 32 commands with correct metadata"
    - "SHA-256 content hashes are deterministic and reproducible"
    - "registry.json is auto-generated from filesystem scanning, never hand-edited"
    - "Each entry has name, type, path, description, category, tier, hash, dependencies"
  artifacts:
    - path: "mcp-server/src/registry/skill-registry.ts"
      provides: "Skill registry generator: scans skills/, .claude/agents/, .claude/commands/"
      exports: ["generateRegistry", "RegistryEntry", "SkillRegistry"]
    - path: "mcp-server/tests/unit/registry/skill-registry.test.ts"
      provides: "Unit tests for registry generation and SHA-256 hashing"
    - path: "skills/registry.json"
      provides: "Auto-generated catalog of all L0 assets"
  key_links:
    - from: "mcp-server/src/registry/skill-registry.ts"
      to: "mcp-server/src/registry/frontmatter.ts"
      via: "parseFrontmatter for metadata extraction"
      pattern: "parseFrontmatter"
    - from: "mcp-server/src/registry/skill-registry.ts"
      to: "skills/registry.json"
      via: "JSON.stringify output"
      pattern: "writeFile.*registry\\.json"
---

<objective>
Build the L0 skill registry generator that scans all skills, agents, and commands, computes SHA-256 content hashes, extracts metadata from frontmatter, and outputs `skills/registry.json` as the single source of truth for downstream discovery.

Purpose: Registry is the foundation for the entire materialization system. Manifest resolution, sync engine, and validation all depend on this catalog.
Output: `skill-registry.ts` module + `registry.json` generated file + unit tests
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.3-skill-materialization-registry/14.3-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From mcp-server/src/registry/types.ts:
```typescript
export type Layer = "L0" | "L1" | "L2";
export interface PatternMetadata {
  scope: string[]; sources: string[]; targets: string[];
  version?: number; last_updated?: string; description?: string;
  slug?: string; status?: string; category?: string; l0_refs?: string[];
  // ... more optional fields
}
export interface FrontmatterResult {
  data: Record<string, unknown>;
  content: string;
}
```

From mcp-server/src/registry/frontmatter.ts:
```typescript
export function parseFrontmatter(raw: string): FrontmatterResult | null;
```

From mcp-server/src/registry/scanner.ts:
```typescript
export async function scanDirectory(
  dirPath: string, layer: Layer, project?: string
): Promise<RegistryEntry[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create skill-registry.ts module with registry generation</name>
  <files>mcp-server/src/registry/skill-registry.ts, mcp-server/tests/unit/registry/skill-registry.test.ts</files>
  <behavior>
    - Test: scanSkills() discovers all SKILL.md files under skills/ directory, returns entries with type "skill"
    - Test: scanAgents() discovers all .md files under .claude/agents/, returns entries with type "agent"
    - Test: scanCommands() discovers all .md files under .claude/commands/, returns entries with type "command"
    - Test: computeHash() returns deterministic SHA-256 string prefixed with "sha256:"
    - Test: computeHash() returns same hash for identical content
    - Test: generateRegistry() combines skills + agents + commands into SkillRegistry object with version, generated timestamp, l0_root, entries
    - Test: extractDependencies() finds script references in SKILL.md content (e.g., scripts/sh/*.sh, scripts/ps1/*.ps1)
    - Test: categorize() assigns categories based on skill name/content using the 9 unified categories (architecture, testing, data, security, build, guides, domain, product, ui)
    - Test: tierize() assigns tier based on category/name (core for testing/build/quality, extended for docs/monitoring, web for web-specific)
    - Test: Commands with "GENERATED by claude-adapter" comment are marked with generated_from field
    - Test: name+type combination is unique key (no duplicates)
  </behavior>
  <action>
    Create `mcp-server/src/registry/skill-registry.ts` that:
    1. Defines `SkillRegistryEntry` interface: name, type ("skill"|"agent"|"command"), path (relative to L0 root), description, category, tier ("core"|"extended"|"web"), hash (SHA-256), dependencies (string[]), generated_from? (string), frontmatter (Record<string, unknown>)
    2. Defines `SkillRegistry` interface: version (1), generated (ISO timestamp), l0_root (absolute path), entries (SkillRegistryEntry[])
    3. `computeHash(filePath: string): Promise<string>` - reads file, returns `sha256:${hex}`
    4. `scanSkills(rootDir: string): Promise<SkillRegistryEntry[]>` - reads skills/**/SKILL.md, parses frontmatter for description/allowed-tools/disable-model-invocation, extracts script dependencies from content
    5. `scanAgents(rootDir: string): Promise<SkillRegistryEntry[]>` - reads .claude/agents/*.md, parses frontmatter for tools/model/skills
    6. `scanCommands(rootDir: string): Promise<SkillRegistryEntry[]>` - reads .claude/commands/*.md, detects generated-from-adapter via comment pattern `<!-- GENERATED by claude-adapter`
    7. `categorize(name: string, frontmatter?: Record<string, unknown>): string` - maps to unified category vocabulary (testing, build, guides, architecture, data, security, domain, product, ui)
    8. `tierize(name: string, category: string): string` - core/extended/web tier assignment
    9. `generateRegistry(rootDir: string): Promise<SkillRegistry>` - orchestrates scanning and outputs full registry object
    10. `writeRegistry(rootDir: string): Promise<void>` - generates and writes to skills/registry.json with 2-space indent

    Use `node:crypto` for SHA-256 hashing. Reuse `parseFrontmatter` from frontmatter.ts. Follow the scanner.ts pattern for filesystem walking. Skip `params.json`, `params.schema.json`, and non-SKILL.md files in skills/ subdirectories.

    Write tests FIRST (RED), then implement (GREEN). Use Vitest with temp directories for isolated testing. Create fixture skills/agents/commands in temp dirs.
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/registry/skill-registry.test.ts --reporter=verbose</automated>
  </verify>
  <done>skill-registry.ts exports generateRegistry and writeRegistry; all unit tests pass; SHA-256 hashes are deterministic</done>
</task>

<task type="auto">
  <name>Task 2: Generate initial registry.json from live filesystem</name>
  <files>skills/registry.json</files>
  <action>
    Run the registry generator against the actual L0 filesystem to produce `skills/registry.json`. This is the initial generation that catalogs all 27 skills, 11 agents, and 32 commands.

    Create a small runner script or use the module directly:
    1. Import generateRegistry from skill-registry.ts
    2. Call writeRegistry with the AndroidCommonDoc root path
    3. Verify the output contains exactly 27 skill entries, 11 agent entries, and 32 command entries (70 total)
    4. Verify all entries have non-empty hash, path, name, type, category, tier fields
    5. Verify no duplicate name+type combinations
    6. Spot-check: "test" skill has dependencies including scripts/sh/gradle-run.sh; "brainstorm" command has no generated_from

    Add a `generate-registry` npm script to package.json for future re-generation: `"generate-registry": "tsx src/registry/generate-registry-cli.ts"`. Create the small CLI entry point that calls writeRegistry.
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/registry/skill-registry.test.ts --reporter=verbose && node -e "const r = require('../skills/registry.json'); console.log('entries:', r.entries.length); const types = {}; r.entries.forEach(e => types[e.type] = (types[e.type]||0)+1); console.log(types); process.exit(r.entries.length >= 70 ? 0 : 1)"</automated>
  </verify>
  <done>skills/registry.json exists with 70 entries (27 skills + 11 agents + 32 commands), all with valid hashes and metadata; npm script available for re-generation</done>
</task>

</tasks>

<verification>
- `cd mcp-server && npx vitest run tests/unit/registry/skill-registry.test.ts` passes
- `skills/registry.json` exists, is valid JSON, has 70 entries
- Each entry has: name, type, path, description, category, tier, hash, dependencies
- SHA-256 hashes are deterministic (running twice produces identical output)
- No duplicate name+type combinations
</verification>

<success_criteria>
- Registry generator module tested and operational
- registry.json auto-generated with full L0 catalog (27 skills, 11 agents, 32 commands)
- Content hashes enable downstream drift detection
- npm script for regeneration available
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-skill-materialization-registry/14.3-01-SUMMARY.md`
</output>
