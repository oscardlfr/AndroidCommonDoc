---
phase: 14.3-skill-materialization-registry
plan: 05
type: execute
wave: 3
depends_on: [14.3-03, 14.3-04]
files_modified:
  - shared-kmp-libs/l0-manifest.json
  - shared-kmp-libs/.claude/commands/auto-cover.md
  - shared-kmp-libs/.claude/commands/coverage-full.md
  - shared-kmp-libs/.claude/commands/coverage.md
  - shared-kmp-libs/.claude/commands/extract-errors.md
  - shared-kmp-libs/.claude/commands/sync-versions.md
  - shared-kmp-libs/.claude/commands/test-changed.md
  - shared-kmp-libs/.claude/commands/test-full.md
  - shared-kmp-libs/.claude/commands/test.md
  - shared-kmp-libs/.claude/commands/verify-kmp.md
  - DawSync/l0-manifest.json
  - DawSync/.claude/commands/*.md
  - DawSync/.claude/agents/*.md
  - DawSync/.claude/skills/*
  - setup/install-claude-skills.sh
  - setup/Install-ClaudeSkills.ps1
  - setup/templates/
autonomous: true
requirements: [MATL-05]

must_haves:
  truths:
    - "All 9 shared-kmp-libs delegate commands are replaced with materialized copies"
    - "All 29 DawSync delegate commands are replaced with materialized copies"
    - "All 6 DawSync delegate agents are replaced with materialized copies"
    - "All 6 DawSync junction-based delegate skills are replaced with materialized copies"
    - "10 DawSync L2-specific commands are untouched"
    - "5 DawSync L2-specific agents are untouched"
    - "install-claude-skills.sh and setup/templates/ are deleted"
    - "No delegate: true frontmatter remains anywhere in the ecosystem"
  artifacts:
    - path: "shared-kmp-libs/l0-manifest.json"
      provides: "Manifest for shared-kmp-libs L1 project"
    - path: "DawSync/l0-manifest.json"
      provides: "Manifest for DawSync L2 project"
  key_links:
    - from: "shared-kmp-libs/l0-manifest.json"
      to: "skills/registry.json"
      via: "l0_source path resolution"
      pattern: "l0_source.*AndroidCommonDoc"
    - from: "DawSync/l0-manifest.json"
      to: "skills/registry.json"
      via: "l0_source path resolution"
      pattern: "l0_source.*AndroidCommonDoc"
---

<objective>
Execute the big-bang migration: convert all path-based delegate stubs in shared-kmp-libs (9 commands) and DawSync (29 commands + 6 agents + 6 junction skills) to materialized copies using the sync engine. Delete all old delegate infrastructure (install-claude-skills.sh, setup/templates/).

Purpose: This is the critical migration that replaces the fragile delegate pattern with professional registry-based distribution. After this, no project depends on filesystem paths to L0.
Output: Manifests created in both projects, all delegates replaced with full copies, old infrastructure deleted
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14.3-skill-materialization-registry/14.3-RESEARCH.md
@.planning/phases/14.3-skill-materialization-registry/14.3-03-SUMMARY.md
@.planning/phases/14.3-skill-materialization-registry/14.3-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manifests and run sync for shared-kmp-libs and DawSync</name>
  <files>shared-kmp-libs/l0-manifest.json, DawSync/l0-manifest.json</files>
  <action>
    **CRITICAL ORDER: Materialize BEFORE deleting old infrastructure. Never leave a gap.**

    **Step 1: shared-kmp-libs migration**
    1. Create `shared-kmp-libs/l0-manifest.json`:
       - l0_source: "../AndroidCommonDoc"
       - mode: "include-all"
       - exclude_commands: ["start-track", "sync-roadmap", "merge-track"] (GSD-specific)
       - l2_specific: empty (no L2-specific files in shared-kmp-libs)
       - checksums: empty (first sync)
    2. Run sync engine: `cd mcp-server && npx tsx src/sync/sync-l0-cli.ts --project-root ../../shared-kmp-libs --l0-root ..`
    3. Verify: 9 delegate commands replaced with materialized copies containing l0_source/l0_hash headers
    4. Verify: no `delegate: true` remains in shared-kmp-libs `.claude/`
    5. Verify: materialized files have proper content (not delegate stubs)

    **Step 2: DawSync migration**
    1. Remove Windows NTFS junctions FIRST:
       - For each of the 6 junction skill directories in DawSync/.claude/skills/ (accessibility, best-practices, core-web-vitals, performance, seo, web-quality-audit):
       - Run `cmd /c "rmdir DawSync\\.claude\\skills\\{name}"` to remove junction without affecting target
    2. Create `DawSync/l0-manifest.json`:
       - l0_source: "../AndroidCommonDoc"
       - mode: "include-all"
       - l2_specific.commands: ["deploy-web", "lint-web", "merge-track", "nuke-builds", "roadmap", "run-clean", "start-track", "sync-roadmap", "test-m4l", "validate-strings"]
       - l2_specific.agents: ["daw-guardian", "data-layer-specialist", "domain-model-specialist", "freemium-gate-checker", "producer-consumer-validator"]
       - checksums: empty (first sync)
    3. Run sync engine: `cd mcp-server && npx tsx src/sync/sync-l0-cli.ts --project-root ../../DawSync --l0-root ..`
    4. Verify: 29 delegate commands replaced with materialized copies
    5. Verify: 6 delegate agents replaced with materialized copies
    6. Verify: 6 junction skills replaced with real skill directories containing materialized SKILL.md
    7. Verify: 10 L2-specific commands are UNTOUCHED (no l0_source header added)
    8. Verify: 5 L2-specific agents are UNTOUCHED
    9. Verify: no `delegate: true` remains in DawSync `.claude/`

    **Step 3: Spot-check materialized content**
    - Read DawSync/.claude/commands/test.md -- should have full skill invocation content + l0_source/l0_hash comment header
    - Read DawSync/.claude/agents/test-specialist.md -- should have full agent content + l0_source/l0_hash in frontmatter
    - Read DawSync/.claude/skills/test/SKILL.md -- should have full skill content + l0_source/l0_hash in frontmatter
    - Read shared-kmp-libs/.claude/commands/test.md -- same verification

    **IMPORTANT Windows pitfalls:**
    - Remove junctions with `cmd /c rmdir` not `rm -rf` (rm would delete target data)
    - Path separators: use forward slashes in Node.js, backslashes only for cmd.exe
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects" && grep -r "delegate: true" shared-kmp-libs/.claude/ DawSync/.claude/ 2>/dev/null | head -5; echo "Exit: $?"</automated>
  </verify>
  <done>Both manifests created; all delegates replaced with materialized copies; L2-specific files untouched; zero "delegate: true" remaining in either project</done>
</task>

<task type="auto">
  <name>Task 2: Delete old delegate infrastructure from L0</name>
  <files>setup/install-claude-skills.sh, setup/Install-ClaudeSkills.ps1, setup/templates/</files>
  <action>
    Now that materialized copies are in place, delete the old distribution infrastructure:

    1. Delete `setup/install-claude-skills.sh` (replaced by /sync-l0)
    2. Delete `setup/Install-ClaudeSkills.ps1` (replaced by /sync-l0)
    3. Delete `setup/templates/` directory entirely (wrapper templates no longer needed)
    4. Update `adapters/generate-all.sh` to remove references to claude-adapter.sh if still referenced
    5. If `adapters/claude-adapter.sh` was not already deleted in Plan 04, delete it now (marked deprecated)

    6. Verify remaining setup/ scripts still work:
       - `setup/install-copilot-prompts.sh` -- kept (Copilot distribution)
       - `setup/Install-CopilotPrompts.ps1` -- kept
       - `setup/install-guard-tests.sh` -- kept (Konsist guard test distribution)
       - `setup/Install-GuardTests.ps1` -- kept
       - `setup/install-hooks.sh` -- kept
       - `setup/Install-Hooks.ps1` -- kept
       - `setup/setup-toolkit.sh` -- kept
       - `setup/setup-toolkit.ps1` -- kept

    7. Update registry.json (re-run generator) since sync-l0 skill was added in Plan 03.

    8. Run MCP tests to ensure nothing references deleted files.
  </action>
  <verify>
    <automated>test ! -f "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/setup/install-claude-skills.sh" && test ! -d "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/setup/templates" && cd "C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>install-claude-skills.sh deleted; setup/templates/ deleted; remaining setup scripts verified; MCP tests pass; registry.json updated</done>
</task>

</tasks>

<verification>
- `grep -r "delegate: true" shared-kmp-libs/.claude/ DawSync/.claude/` returns nothing
- `test ! -f setup/install-claude-skills.sh` passes
- `test ! -d setup/templates` passes
- DawSync L2-specific commands/agents have no l0_source headers
- `cd mcp-server && npx vitest run` passes
- Both l0-manifest.json files are valid JSON with populated checksums
</verification>

<success_criteria>
- All 50+ delegate stubs across shared-kmp-libs and DawSync replaced with materialized copies
- Old install script and template infrastructure completely removed
- L2-specific files preserved untouched
- Zero "delegate: true" remaining in the ecosystem
- Registry updated to include sync-l0 skill
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-skill-materialization-registry/14.3-05-SUMMARY.md`
</output>
