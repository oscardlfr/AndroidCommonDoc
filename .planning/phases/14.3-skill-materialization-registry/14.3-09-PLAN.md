---
phase: 14.3-skill-materialization-registry
plan: 09
type: execute
wave: 1
depends_on: [14.3-07, 14.3-08]
files_modified:
  - DawSync/docs/guides/testing.md
  - DawSync/docs/guides/testing-advanced.md
  - DawSync/docs/guides/testing-e2e.md
  - DawSync/docs/guides/testing-fakes.md
  - DawSync/docs/guides/testing-patterns.md
  - DawSync/docs/guides/accessibility.md
  - DawSync/docs/architecture/patterns-offline-first.md
  - DawSync/docs/architecture/patterns-ui-viewmodel.md
  - DawSync/docs/tech/sbom.md
  - mcp-server/src/tools/validate-doc-structure.ts
autonomous: true
requirements: [MATL-08]
gap_closure: true

must_haves:
  truths:
    - "DawSync docs covering testing topics use category testing, not guides"
    - "DawSync docs covering UI topics use category ui, not architecture"
    - "DawSync docs covering data/offline-first topics use category data, not architecture"
    - "DawSync docs covering security topics use category security, not build"
    - "SUBDIR_TO_CATEGORIES mapping updated to accept re-categorized values without moving files"
    - "validate-doc-structure reports 0 errors for DawSync after re-categorization"
    - "DawSync uses at least 7 of 9 approved categories (was 4)"
  artifacts:
    - path: "DawSync/docs/guides/testing.md"
      provides: "Testing hub doc with category: testing"
      contains: "category: testing"
    - path: "DawSync/docs/guides/accessibility.md"
      provides: "Accessibility doc with category: ui"
      contains: "category: ui"
    - path: "DawSync/docs/architecture/patterns-offline-first.md"
      provides: "Offline-first patterns with category: data"
      contains: "category: data"
    - path: "DawSync/docs/tech/sbom.md"
      provides: "SBOM doc with category: security"
      contains: "category: security"
    - path: "mcp-server/src/tools/validate-doc-structure.ts"
      provides: "Updated SUBDIR_TO_CATEGORIES mapping accepting new categories in existing subdirectories"
      contains: "SUBDIR_TO_CATEGORIES"
  key_links:
    - from: "DawSync/docs/guides/testing*.md"
      to: "APPROVED_CATEGORIES"
      via: "category frontmatter field"
      pattern: "category: testing"
    - from: "mcp-server/src/tools/validate-doc-structure.ts"
      to: "DawSync docs validation"
      via: "SUBDIR_TO_CATEGORIES mapping"
      pattern: "guides.*testing.*ui"
---

<objective>
Close the DawSync category consolidation gap identified in 14.3-VERIFICATION.md (Truth 8, MATL-08 partial).

DawSync currently uses only 4 of 9 approved categories (product, guides, architecture, build). Nine DawSync docs are semantically misaligned -- testing guides are tagged `guides` instead of `testing`, UI/accessibility docs are tagged `architecture`/`guides` instead of `ui`, an offline-first doc is tagged `architecture` instead of `data`, and an SBOM doc is tagged `build` instead of `security`. The SUBDIR_TO_CATEGORIES workaround in validate-doc-structure.ts needs updating to accept the new category values in their existing subdirectories (per the Plan 07 decision: "category consolidation changes frontmatter only, not physical subdirectory structure").

Purpose: Achieve proper category coverage so the Obsidian graph shows meaningful color grouping for DawSync docs and the 9-category unified vocabulary is actually used.
Output: 9 DawSync docs re-categorized, SUBDIR_TO_CATEGORIES mapping updated, validator passes with 0 errors.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.3-skill-materialization-registry/14.3-07-SUMMARY.md
@.planning/phases/14.3-skill-materialization-registry/14.3-08-SUMMARY.md
@.planning/phases/14.3-skill-materialization-registry/14.3-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From mcp-server/src/tools/validate-doc-structure.ts:
```typescript
export const APPROVED_CATEGORIES = new Set([
  "architecture", "testing", "data", "security",
  "build", "guides", "domain", "product", "ui",
]);

export const SUBDIR_TO_CATEGORIES: Record<string, string[]> = {
  architecture: ["architecture"],
  compose: ["architecture"],
  di: ["architecture"],
  navigation: ["architecture"],
  testing: ["testing"],
  "offline-first": ["data"],
  storage: ["data"],
  resources: ["data"],
  io: ["data"],
  security: ["security"],
  oauth: ["security"],
  "error-handling": ["domain", "ui"],
  gradle: ["build"],
  tech: ["build"],
  guides: ["guides"],
  domain: ["domain"],
  firebase: ["domain"],
  foundation: ["architecture"],
  product: ["product"],
  business: ["product"],
  legal: ["product"],
  references: ["product"],
  ui: ["ui"],
};
```

Usage at line 616:
```typescript
const acceptedCategories = SUBDIR_TO_CATEGORIES[topDir] ?? [topDir];
if (topDir !== category && !acceptedCategories.includes(category)) {
  errors.push(
    `${filename}: category "${category}" but found in "${topDir}/" (expected one of: ${acceptedCategories.join(", ")})`,
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-categorize DawSync docs and update SUBDIR_TO_CATEGORIES mapping</name>
  <files>
    DawSync/docs/guides/testing.md,
    DawSync/docs/guides/testing-advanced.md,
    DawSync/docs/guides/testing-e2e.md,
    DawSync/docs/guides/testing-fakes.md,
    DawSync/docs/guides/testing-patterns.md,
    DawSync/docs/guides/accessibility.md,
    DawSync/docs/architecture/patterns-offline-first.md,
    DawSync/docs/architecture/patterns-ui-viewmodel.md,
    DawSync/docs/tech/sbom.md,
    mcp-server/src/tools/validate-doc-structure.ts
  </files>
  <action>
    **Part A: Re-categorize 9 DawSync docs** (change `category:` frontmatter field only, do NOT move files):

    1. Testing docs (5 files in `DawSync/docs/guides/`):
       - `testing.md` -- change `category: guides` to `category: testing`
       - `testing-advanced.md` -- change `category: guides` to `category: testing`
       - `testing-e2e.md` -- change `category: guides` to `category: testing`
       - `testing-fakes.md` -- change `category: guides` to `category: testing`
       - `testing-patterns.md` -- change `category: guides` to `category: testing`
       Rationale: These docs have `scope: [dawsync, testing]` and contain testing patterns, test strategy, fakes, e2e. They ARE testing content, not generic guides.

    2. UI docs (2 files):
       - `DawSync/docs/guides/accessibility.md` -- change `category: guides` to `category: ui`
         Rationale: Accessibility is a UI concern (WCAG compliance, platform-specific UI support).
       - `DawSync/docs/architecture/patterns-ui-viewmodel.md` -- change `category: architecture` to `category: ui`
         Rationale: This doc covers UI patterns (Screen/Content, design system) and ViewModel patterns (sealed UiState, events). These are `ui` per the unified vocabulary.

    3. Data doc (1 file):
       - `DawSync/docs/architecture/patterns-offline-first.md` -- change `category: architecture` to `category: data`
         Rationale: Offline-first patterns (processing coordination, outbox, DLQ, SQL) are `data` concerns per the unified vocabulary where `offline-first` maps to `data`.

    4. Security doc (1 file):
       - `DawSync/docs/tech/sbom.md` -- change `category: build` to `category: security`
         Rationale: SBOM has `scope: [dawsync, security, compliance]`. It is a security/compliance artifact, not a build concern.

    Use sed to update the `category:` line in each file. Only modify the frontmatter `category:` field -- do NOT change any other content, scope, parent, description, or any other field.

    **Part B: Update SUBDIR_TO_CATEGORIES mapping** in `mcp-server/src/tools/validate-doc-structure.ts`:

    The `guides` subdirectory currently maps to `["guides"]`. After re-categorization, it will contain docs with `category: testing` and `category: ui`. Update the mapping:

    ```
    guides: ["guides", "testing", "ui"],
    ```

    The `architecture` subdirectory currently maps to `["architecture"]`. After re-categorization, it will contain docs with `category: data` and `category: ui`. Update:

    ```
    architecture: ["architecture", "data", "ui"],
    ```

    The `tech` subdirectory currently maps to `["build"]`. After re-categorization, it will contain a doc with `category: security`. Update:

    ```
    tech: ["build", "security"],
    ```

    These updates follow the established pattern from Plan 07: "SUBDIR_TO_CATEGORIES mapping allows old subdirectory names with consolidated category values." The mapping is intentionally permissive because category consolidation changes frontmatter only, not physical subdirectory structure (locked decision from 14.3-07).

    **Part C: Run MCP tests** to verify no regressions:

    ```bash
    cd mcp-server && npx vitest run --reporter=verbose
    ```

    All tests must pass. If any validate-doc-structure tests fail, fix them (they may have hardcoded expectations about DawSync category counts).
  </action>
  <verify>
    <automated>cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 9 DawSync docs have updated category values: 5 testing, 2 ui, 1 data, 1 security
    - SUBDIR_TO_CATEGORIES mapping updated for guides, architecture, and tech subdirectories
    - All MCP tests pass
    - DawSync now uses at least 7 of 9 approved categories (product, guides, architecture, build, testing, ui, data + security = 8)
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate re-categorization across ecosystem and verify category coverage</name>
  <files>
    DawSync/docs/guides/testing.md,
    DawSync/docs/guides/accessibility.md,
    DawSync/docs/architecture/patterns-offline-first.md,
    DawSync/docs/architecture/patterns-ui-viewmodel.md,
    DawSync/docs/tech/sbom.md
  </files>
  <action>
    **Part A: Run validate-doc-structure against DawSync** to confirm 0 errors after re-categorization. Use the MCP tool or run the validation directly:

    ```bash
    cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc/mcp-server
    npx vitest run tests/ --reporter=verbose
    ```

    **Part B: Count categories in DawSync** to verify coverage improvement:

    ```bash
    cd C:/Users/34645/AndroidStudioProjects/DawSync
    grep -r "^category:" docs/ --include="*.md" -h | sort | uniq -c | sort -rn
    ```

    Expected output should show at least 7-8 distinct categories (was 4: product, guides, architecture, build; now adds testing, ui, data, security).

    **Part C: Verify no broken cross-references.** The category field change does not affect parent/slug/cross-references, but confirm:

    ```bash
    cd C:/Users/34645/AndroidStudioProjects/DawSync
    # Verify parent fields still resolve (testing sub-docs have parent: testing)
    grep -r "^parent:" docs/guides/testing-*.md --include="*.md" -h
    # Expected: all show "parent: testing" (unchanged)
    ```

    **Part D: Commit changes.** Commit in DawSync repo (category changes) and AndroidCommonDoc repo (SUBDIR_TO_CATEGORIES update) separately:

    In DawSync repo:
    ```bash
    cd C:/Users/34645/AndroidStudioProjects/DawSync
    git add docs/guides/testing.md docs/guides/testing-advanced.md docs/guides/testing-e2e.md docs/guides/testing-fakes.md docs/guides/testing-patterns.md docs/guides/accessibility.md docs/architecture/patterns-offline-first.md docs/architecture/patterns-ui-viewmodel.md docs/tech/sbom.md
    git commit -m "fix(14.3-09): re-categorize 9 docs to use unified category vocabulary"
    ```

    In AndroidCommonDoc repo:
    ```bash
    cd C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc
    git add mcp-server/src/tools/validate-doc-structure.ts
    git commit -m "fix(14.3-09): update SUBDIR_TO_CATEGORIES for DawSync re-categorization"
    ```
  </action>
  <verify>
    <automated>cd C:/Users/34645/AndroidStudioProjects/DawSync && grep -r "^category:" docs/ --include="*.md" -h 2>/dev/null | sort -u | wc -l</automated>
  </verify>
  <done>
    - validate-doc-structure reports 0 errors for DawSync
    - DawSync uses 8 of 9 approved categories (product, guides, architecture, build, testing, ui, data, security) -- only "domain" is absent, which is correct since DawSync has no domain-modeling docs (its domain docs are architecture patterns)
    - All parent/slug cross-references intact
    - Changes committed in both repos
  </done>
</task>

</tasks>

<verification>
Run the following to verify the gap is closed:

1. **Category coverage:** `cd DawSync && grep -r "^category:" docs/ --include="*.md" -h | sort -u` should show 8 categories
2. **MCP tests:** `cd mcp-server && npx vitest run` should show all tests passing
3. **No stale categories:** `cd DawSync && grep -r "^category:" docs/ --include="*.md" -h | sort -u` should only contain values from the approved 9
4. **Validator clean:** Run validate-doc-structure MCP tool against DawSync -- 0 errors expected
</verification>

<success_criteria>
- DawSync docs use 8 of 9 approved categories (up from 4)
- 9 docs re-categorized to semantically correct category values
- SUBDIR_TO_CATEGORIES mapping updated to accept new categories in existing subdirectories
- validate-doc-structure reports 0 errors across all 3 projects
- All MCP tests pass
- VERIFICATION.md Truth 8 gap resolved from PARTIAL to VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-skill-materialization-registry/14.3-09-SUMMARY.md`
</output>
