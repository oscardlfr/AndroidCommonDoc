---
phase: 09-pattern-registry-discovery
plan: 06
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - docs/error-handling-patterns.md
autonomous: true
requirements: [REG-01]

must_haves:
  truths:
    - "Generic KMP/Android patterns from DawSync/.claude/ agents are promoted to AndroidCommonDoc docs/ as L0 patterns with frontmatter"
    - "DawSync-specific patterns remain as L1 overrides in DawSync/.androidcommondoc/docs/"
    - "DawSync/.androidcommondoc/docs/ directory structure exists with at least one L1 pattern doc"
  artifacts:
    - path: "DawSync/.androidcommondoc/docs/"
      provides: "L1 project-specific pattern overrides for DawSync"
  key_links:
    - from: "DawSync/.androidcommondoc/docs/"
      to: "docs/"
      via: "L1 overrides L0 by same slug"
      pattern: "same filename = override"
---

<objective>
Audit DawSync/.claude/ agent docs for generic KMP/Android patterns, promote reusable patterns to AndroidCommonDoc L0 docs with YAML frontmatter, and set up DawSync/.androidcommondoc/docs/ for project-specific L1 overrides.

Purpose: This fulfills the locked "DawSync doc migration" decision from CONTEXT.md. DawSync's .claude/ agents contain patterns that benefit all KMP projects (e.g., error handling, data layer, testing). Generic patterns become L0 base docs; DawSync-specific patterns stay as L1 overrides. This ensures the registry launches with comprehensive, complete content.

Output: New L0 pattern docs in docs/ (with frontmatter), DawSync/.androidcommondoc/docs/ directory with L1 override docs.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-pattern-registry-discovery/09-CONTEXT.md
@.planning/phases/09-pattern-registry-discovery/09-01-SUMMARY.md

<interfaces>
<!-- Frontmatter schema established by Plan 01: -->
```yaml
---
scope: [string]      # architectural layers/concerns
sources: [string]    # libraries/APIs referenced
targets: [string]    # platforms: android, desktop, ios, jvm
version: number      # doc version
last_updated: string # ISO month "2026-03"
description: string  # one-line summary
slug: string         # explicit slug
status: string       # active | draft | deprecated
---
```

<!-- DawSync/.claude/ agents to audit (11 agent docs): -->
<!-- beta-readiness-agent.md, cross-platform-validator.md, data-layer-specialist.md -->
<!-- daw-guardian.md, doc-alignment-agent.md, domain-model-specialist.md -->
<!-- freemium-gate-checker.md, producer-consumer-validator.md -->
<!-- release-guardian-agent.md, test-specialist.md, ui-specialist.md -->

<!-- L1 directory convention (from CONTEXT.md decisions): -->
<!-- DawSync/.androidcommondoc/docs/{slug}.md -->

<!-- Existing L0 docs in docs/ (9 + 12 sub-docs after Plans 01-02): -->
<!-- testing-patterns, kmp-architecture, compose-resources-patterns, gradle-patterns -->
<!-- offline-first-patterns, resource-management-patterns, ui-screen-patterns -->
<!-- viewmodel-state-patterns, enterprise-integration-proposal -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit DawSync agents and promote generic patterns to L0</name>
  <files>
    docs/error-handling-patterns.md
  </files>
  <action>
    Read all 11 agent docs in DawSync/.claude/agents/ (absolute path: C:/Users/34645/AndroidStudioProjects/DawSync/.claude/agents/):
    - beta-readiness-agent.md
    - cross-platform-validator.md
    - data-layer-specialist.md
    - daw-guardian.md
    - doc-alignment-agent.md
    - domain-model-specialist.md
    - freemium-gate-checker.md
    - producer-consumer-validator.md
    - release-guardian-agent.md
    - test-specialist.md
    - ui-specialist.md

    For each agent doc, classify its patterns as:
    **Generic KMP/Android** (promote to L0): Patterns about error handling, Result type usage, CancellationException handling, repository patterns, ViewModel patterns, DI patterns, testing patterns that are not already covered by existing L0 docs.
    **DawSync-specific** (keep as L1): Patterns about DAW-specific domain models, freemium gating, producer-consumer audio patterns, DawSync-specific naming conventions.
    **Already covered** (skip): Patterns that already exist in the 9 L0 docs (testing, viewmodel state, UI screens, etc.).

    For each generic pattern NOT already covered by existing L0 docs:
    1. Create a new .md file in docs/ with full YAML frontmatter (following Plan 01 schema)
    2. Extract and restructure the pattern content to be project-agnostic (remove DawSync-specific references)
    3. Ensure the doc follows the same format as existing L0 docs

    Expected new L0 doc(s) -- review agents and create as appropriate. Likely candidate:
    - `docs/error-handling-patterns.md`: Result type patterns, CancellationException handling, DomainException hierarchy, error mapping between layers. Sources: [core-result, kotlinx-coroutines]. Scope: [error-handling, architecture, data]. This is a cross-cutting concern referenced by multiple agents but not covered by any existing L0 doc.

    If other genuinely generic patterns are found that are NOT already covered, create additional L0 docs. But do NOT create docs that duplicate existing L0 content -- the existing 9 docs already cover testing, viewmodel, UI, architecture, gradle, resources, and offline-first.

    NOTE: The files_modified list above includes the most likely new doc. If additional docs are created, they will be discovered automatically by the scanner (they have frontmatter). Update the SUMMARY to list all actually created files.
  </action>
  <verify>
    <automated>cd mcp-server && npx tsc && node -e "import('./build/registry/scanner.js').then(m => m.scanDirectory(require('path').join(process.cwd(), '..', 'docs'), 'L0')).then(r => { const count = r.length; console.error(count + ' L0 docs found'); if (count < 10) { console.error('Expected at least 10 (9 original + 1 new from DawSync migration)'); process.exit(1); } })"</automated>
  </verify>
  <done>
    - All 11 DawSync agent docs audited and classified
    - Generic KMP patterns promoted to new L0 docs with valid YAML frontmatter
    - No duplication with existing L0 docs
    - Scanner discovers the new L0 docs alongside existing ones (at least 10 total)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DawSync L1 override directory with project-specific patterns</name>
  <files>
    (external: C:/Users/34645/AndroidStudioProjects/DawSync/.androidcommondoc/docs/)
  </files>
  <action>
    Create the L1 directory structure in DawSync:

    1. Create directory: `C:/Users/34645/AndroidStudioProjects/DawSync/.androidcommondoc/docs/`

    2. For DawSync-specific patterns identified in Task 1, create L1 override docs. These are patterns that:
       - Are specific to DawSync's domain (DAW, audio, producer-consumer, freemium)
       - Extend or customize a generic L0 pattern for DawSync's needs
       - Should NOT be loaded by other projects

    3. Each L1 doc follows the same YAML frontmatter schema but with DawSync-specific content:
       ```yaml
       ---
       scope: [domain-specific-scope]
       sources: [dawsync-specific-libs]
       targets: [android, desktop]
       version: 1
       last_updated: "2026-03"
       description: "DawSync-specific: ..."
       slug: pattern-name
       status: active
       ---
       ```

    4. If a DawSync agent doc contains patterns that customize an existing L0 doc (e.g., DawSync-specific testing conventions that extend testing-patterns.md), create an L1 doc with the SAME slug. The resolver will pick the L1 version for DawSync context.

    5. Create at minimum one L1 doc for DawSync's domain-specific patterns (e.g., `dawsync-domain-patterns.md` covering producer-consumer, freemium gating, audio domain models).

    6. Add a `.androidcommondoc/README.md` in DawSync explaining:
       - What L1 overrides are
       - How they interact with L0 base patterns
       - How to add new project-specific patterns
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dir='C:/Users/34645/AndroidStudioProjects/DawSync/.androidcommondoc/docs';if(!fs.existsSync(dir)){console.error('DawSync/.androidcommondoc/docs/ does not exist');process.exit(1)}const files=fs.readdirSync(dir).filter(f=>f.endsWith('.md'));if(files.length===0){console.error('No .md files in DawSync L1 docs');process.exit(1)}for(const f of files){const c=fs.readFileSync(dir+'/'+f,'utf-8');if(!c.startsWith('---')){console.error(f+' missing frontmatter');process.exit(1)}}console.error(files.length+' L1 docs found with frontmatter in DawSync')"</automated>
  </verify>
  <done>
    - DawSync/.androidcommondoc/docs/ directory exists
    - At least one L1 pattern doc with valid YAML frontmatter
    - DawSync-specific patterns preserved as L1 (not lost in migration)
    - README.md explains L1 override mechanism
  </done>
</task>

</tasks>

<verification>
1. New L0 docs in docs/ have valid YAML frontmatter and are scanner-discoverable
2. DawSync/.androidcommondoc/docs/ exists with at least one L1 doc
3. No duplication between new L0 docs and existing L0 docs
4. `cd mcp-server && npm test` passes (existing tests still green)
5. DawSync domain-specific patterns preserved as L1, not promoted to L0
</verification>

<success_criteria>
- DawSync agent docs audited and classified (generic vs project-specific)
- Generic patterns promoted to L0 with frontmatter
- DawSync-specific patterns preserved as L1 in .androidcommondoc/docs/
- Registry discovers new L0 docs automatically
- No existing content broken
</success_criteria>

<output>
After completion, create `.planning/phases/09-pattern-registry-discovery/09-06-SUMMARY.md`
</output>
