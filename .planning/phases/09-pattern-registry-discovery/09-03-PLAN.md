---
phase: 09-pattern-registry-discovery
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - mcp-server/src/registry/resolver.ts
  - mcp-server/src/registry/project-discovery.ts
  - mcp-server/src/utils/paths.ts
  - mcp-server/tests/unit/registry/resolver.test.ts
  - mcp-server/tests/unit/registry/project-discovery.test.ts
  - mcp-server/tests/unit/utils/paths.test.ts
autonomous: true
requirements: [REG-03, REG-04]

must_haves:
  truths:
    - "L0/L1/L2 layer resolution returns the highest-priority doc with full replacement (L1 > L2 > L0)"
    - "Consumer projects are discovered from settings.gradle.kts includeBuild paths pointing to AndroidCommonDoc"
    - "Project discovery falls back to ~/.androidcommondoc/projects.yaml when settings.gradle.kts not parseable"
    - "paths.ts provides L1 and L2 directory resolution functions"
  artifacts:
    - path: "mcp-server/src/registry/resolver.ts"
      provides: "Layer resolution logic"
      exports: ["resolvePattern", "resolveAllPatterns"]
    - path: "mcp-server/src/registry/project-discovery.ts"
      provides: "Consumer project auto-discovery"
      exports: ["discoverProjects", "ProjectInfo"]
    - path: "mcp-server/src/utils/paths.ts"
      provides: "Extended path resolution with L1/L2 dirs"
      exports: ["getToolkitRoot", "getDocsDir", "getSkillsDir", "getScriptsDir", "getL1DocsDir", "getL2Dir", "getL2DocsDir"]
    - path: "mcp-server/tests/unit/registry/resolver.test.ts"
      provides: "Layer resolution unit tests"
    - path: "mcp-server/tests/unit/registry/project-discovery.test.ts"
      provides: "Project discovery unit tests"
  key_links:
    - from: "mcp-server/src/registry/resolver.ts"
      to: "mcp-server/src/registry/scanner.ts"
      via: "import scanDirectory to scan L0/L1/L2 dirs"
      pattern: "scanDirectory"
    - from: "mcp-server/src/registry/resolver.ts"
      to: "mcp-server/src/registry/types.ts"
      via: "import RegistryEntry, Layer"
      pattern: "RegistryEntry|Layer"
    - from: "mcp-server/src/registry/project-discovery.ts"
      to: "mcp-server/src/utils/paths.ts"
      via: "import getToolkitRoot for sibling project scanning"
      pattern: "getToolkitRoot"
---

<objective>
Implement the three-layer resolver (L0 > L1 > L2 with full replacement semantics) and project auto-discovery from settings.gradle.kts includeBuild paths.

Purpose: This enables per-project pattern customization without forking -- a consuming project can override any L0 pattern doc by placing a same-named file in their .androidcommondoc/docs/ directory, and the resolver picks the highest-priority version automatically.

Output: resolver.ts for layer resolution, project-discovery.ts for consumer detection, extended paths.ts with L1/L2 directory helpers.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-pattern-registry-discovery/09-RESEARCH.md
@.planning/phases/09-pattern-registry-discovery/09-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 (mcp-server/src/registry/types.ts) -->
```typescript
export type Layer = "L0" | "L1" | "L2";

export interface PatternMetadata {
  scope: string[];
  sources: string[];
  targets: string[];
  version?: number;
  last_updated?: string;
  description?: string;
  slug?: string;
  status?: string;
  excludable_sources?: string[];
}

export interface RegistryEntry {
  slug: string;
  filepath: string;
  metadata: PatternMetadata;
  layer: Layer;
  project?: string;
}

export interface FrontmatterResult {
  data: Record<string, unknown>;
  content: string;
}
```

<!-- Scanner from Plan 01 (mcp-server/src/registry/scanner.ts) -->
```typescript
export async function scanDirectory(dirPath: string, layer: Layer, project?: string): Promise<RegistryEntry[]>;
```

<!-- Existing paths (mcp-server/src/utils/paths.ts) -->
```typescript
export function getToolkitRoot(): string;
export function getDocsDir(): string;
export function getSkillsDir(): string;
export function getScriptsDir(): string;
```

<!-- Consumer project layout: -->
<!-- DawSync/settings.gradle.kts contains: includeBuild("../AndroidCommonDoc") -->
<!-- OmniSound/settings.gradle.kts contains: includeBuild("../AndroidCommonDoc") -->
<!-- L1 docs: DawSync/.androidcommondoc/docs/testing-patterns.md -->
<!-- L2 docs: ~/.androidcommondoc/docs/testing-patterns.md -->
<!-- L2 prefs: ~/.androidcommondoc/preferences.yaml -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layer resolver with L0/L1/L2 full replacement semantics</name>
  <files>
    mcp-server/src/registry/resolver.ts,
    mcp-server/src/utils/paths.ts,
    mcp-server/tests/unit/registry/resolver.test.ts,
    mcp-server/tests/unit/utils/paths.test.ts
  </files>
  <behavior>
    Resolver:
    - resolvePattern("testing-patterns", undefined) returns L0 entry when no L1/L2 override exists
    - resolvePattern("testing-patterns", "DawSync") returns L1 entry if DawSync/.androidcommondoc/docs/testing-patterns.md exists, otherwise falls through to L2 then L0
    - resolvePattern("testing-patterns", undefined) returns L2 entry if ~/.androidcommondoc/docs/testing-patterns.md exists and no project specified
    - Priority chain: L1 (project) > L2 (user) > L0 (base) -- full replacement, no merging
    - resolvePattern("nonexistent", undefined) returns null
    - resolveAllPatterns(undefined) returns all L0 docs resolved through L2
    - resolveAllPatterns("DawSync") returns all docs with L1 > L2 > L0 per-slug resolution

    Paths extension:
    - getL1DocsDir("DawSync") returns path to DawSync/.androidcommondoc/docs/
    - getL2Dir() returns path to ~/.androidcommondoc/
    - getL2DocsDir() returns path to ~/.androidcommondoc/docs/
  </behavior>
  <action>
    1. Extend `mcp-server/src/utils/paths.ts` with:
       - `getL2Dir(): string` -- returns `path.join(os.homedir(), '.androidcommondoc')`. Import `os` from `node:os`.
       - `getL2DocsDir(): string` -- returns `path.join(getL2Dir(), 'docs')`
       - `getL1DocsDir(projectPath: string): string` -- returns `path.join(projectPath, '.androidcommondoc', 'docs')`
       - Update existing paths.test.ts with tests for the new functions

    2. Create `mcp-server/src/registry/resolver.ts`:
       - Import `scanDirectory` from `./scanner.js`
       - Import `getDocsDir`, `getL1DocsDir`, `getL2DocsDir` from `../utils/paths.js`
       - Import `RegistryEntry` from `./types.js`

       - `async function resolvePattern(slug: string, project?: string): Promise<RegistryEntry | null>`
         - If project provided: scan L1 dir for that project, check if slug exists in results
         - Scan L2 dir, check if slug exists
         - Scan L0 dir (getDocsDir()), check if slug exists
         - Return first match (highest priority) or null
         - Use layer priority: L1 (if project) > L2 > L0

       - `async function resolveAllPatterns(project?: string): Promise<RegistryEntry[]>`
         - Scan L0 to get all base patterns
         - Scan L2 to get user overrides
         - If project, scan L1 to get project overrides
         - Build resolved map: for each slug, highest-priority entry wins (full replacement)
         - Return array of resolved entries

       - `async function resolveAllPatternsWithExcludes(project?: string, excludes?: { sources?: string[] }): Promise<RegistryEntry[]>`
         - Call resolveAllPatterns, then filter out entries whose `sources` array intersects with `excludes.sources`
         - This implements the doc-level excludes mechanism per research discretion recommendation

    3. Write tests FIRST (RED), then implement (GREEN). Tests should use temp directories with mock .md files containing valid frontmatter to avoid coupling to real docs.
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/registry/resolver.test.ts tests/unit/utils/paths.test.ts</automated>
  </verify>
  <done>
    - resolver.ts exports resolvePattern, resolveAllPatterns, resolveAllPatternsWithExcludes
    - L1 > L2 > L0 priority order enforced with full replacement (no merging)
    - paths.ts extended with getL1DocsDir, getL2Dir, getL2DocsDir
    - All tests pass
    - `cd mcp-server && npx tsc` compiles clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project auto-discovery from settings.gradle.kts</name>
  <files>
    mcp-server/src/registry/project-discovery.ts,
    mcp-server/tests/unit/registry/project-discovery.test.ts
  </files>
  <behavior>
    - discoverProjects() finds DawSync and OmniSound by scanning sibling dirs of toolkit root for settings.gradle.kts containing includeBuild("../AndroidCommonDoc")
    - discoverProjects() falls back to ~/.androidcommondoc/projects.yaml when no sibling settings.gradle.kts found
    - discoverProjects() excludes the toolkit root directory itself from results (no circular discovery)
    - discoverProjects() returns ProjectInfo[] with name and path for each discovered project
    - discoverProjects() returns empty array on errors (not throw)
    - ProjectInfo has at minimum: name (string), path (string), hasL1Docs (boolean)
  </behavior>
  <action>
    Create `mcp-server/src/registry/project-discovery.ts`:

    1. Define `ProjectInfo` interface:
       ```typescript
       export interface ProjectInfo {
         name: string;     // project directory name (e.g., "DawSync")
         path: string;     // absolute path to project root
         hasL1Docs: boolean; // whether .androidcommondoc/docs/ exists
       }
       ```

    2. `export async function discoverProjects(): Promise<ProjectInfo[]>`
       - Get toolkit root via `getToolkitRoot()`
       - Get parent directory of toolkit root (sibling projects live here)
       - List all directories in parent
       - Exclude toolkit root's own directory name (avoid circular discovery per pitfall 4 from research)
       - For each sibling directory:
         a. Check if `settings.gradle.kts` exists
         b. If yes, read it and check for `includeBuild` referencing AndroidCommonDoc (regex: `includeBuild\s*\(\s*["'].*AndroidCommonDoc["']`)
         c. If match found, create ProjectInfo with:
            - name: directory name
            - path: absolute path
            - hasL1Docs: check if `.androidcommondoc/docs/` subdirectory exists
       - If no projects found via settings.gradle.kts, try fallback:
         a. Read `~/.androidcommondoc/projects.yaml` (if exists)
         b. Parse with `yaml` package
         c. Expect format: `projects: [{ name: "DawSync", path: "/abs/path/to/DawSync" }]`
       - Return empty array on any error (log warning, don't throw)
       - Use `readFile`, `readdir`, `stat` from `node:fs/promises`
       - Use `logger` from `../utils/logger.js` for warnings

    3. Write tests FIRST using temp directories. Create mock project structures:
       - Create temp dir with a sibling dir containing `settings.gradle.kts` that has `includeBuild("../toolkit")`
       - Test that it discovers the sibling
       - Test circular exclusion (toolkit dir itself is not returned)
       - Test projects.yaml fallback
       - Test empty parent directory returns empty array
       - Test malformed settings.gradle.kts returns empty array
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/registry/project-discovery.test.ts</automated>
  </verify>
  <done>
    - project-discovery.ts exports discoverProjects and ProjectInfo
    - Discovers projects from settings.gradle.kts includeBuild paths
    - Falls back to projects.yaml
    - Excludes toolkit root from results
    - Returns empty array on errors
    - All tests pass
    - `cd mcp-server && npx tsc` compiles clean
  </done>
</task>

</tasks>

<verification>
1. `cd mcp-server && npm test` -- all tests pass (new + existing)
2. `cd mcp-server && npx tsc` -- TypeScript compiles clean
3. Resolver correctly prioritizes L1 > L2 > L0
4. Project discovery finds sibling projects with includeBuild references
5. No circular discovery (toolkit root excluded)
</verification>

<success_criteria>
- Three-layer resolution works with full replacement semantics
- Project discovery auto-detects DawSync and OmniSound from their settings.gradle.kts
- paths.ts extended with L1/L2 directory helpers
- All unit tests pass with mock filesystem
</success_criteria>

<output>
After completion, create `.planning/phases/09-pattern-registry-discovery/09-03-SUMMARY.md`
</output>
