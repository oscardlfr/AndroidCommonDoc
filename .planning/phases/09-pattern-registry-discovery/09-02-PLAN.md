---
phase: 09-pattern-registry-discovery
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - docs/testing-patterns.md
  - docs/compose-resources-patterns.md
  - docs/offline-first-patterns.md
  - docs/viewmodel-state-patterns.md
  - docs/testing-patterns-coroutines.md
  - docs/testing-patterns-fakes.md
  - docs/testing-patterns-coverage.md
  - docs/compose-resources-configuration.md
  - docs/compose-resources-usage.md
  - docs/compose-resources-troubleshooting.md
  - docs/offline-first-architecture.md
  - docs/offline-first-sync.md
  - docs/offline-first-caching.md
  - docs/viewmodel-state-management.md
  - docs/viewmodel-navigation.md
  - docs/viewmodel-events.md
autonomous: true
requirements: [REG-02, REG-07]

must_haves:
  truths:
    - "All 9 pattern docs are validated against current official sources and any stale version refs or deprecated APIs are fixed"
    - "Large docs (>400 lines) are split into focused sub-docs with independent YAML frontmatter"
    - "Original slugs remain as composite entry points that reference sub-docs"
    - "Each sub-doc is independently loadable and has complete frontmatter metadata"
  artifacts:
    - path: "docs/testing-patterns.md"
      provides: "Testing patterns hub doc (trimmed to overview + references)"
      contains: "---"
    - path: "docs/testing-patterns-coroutines.md"
      provides: "Coroutine testing patterns sub-doc"
      contains: "scope:"
    - path: "docs/compose-resources-configuration.md"
      provides: "Compose resources configuration sub-doc"
      contains: "scope:"
    - path: "docs/offline-first-architecture.md"
      provides: "Offline-first architecture sub-doc"
      contains: "scope:"
    - path: "docs/viewmodel-state-management.md"
      provides: "ViewModel state management sub-doc"
      contains: "scope:"
  key_links:
    - from: "docs/testing-patterns.md"
      to: "docs/testing-patterns-coroutines.md"
      via: "reference link in hub doc"
      pattern: "testing-patterns-coroutines"
    - from: "docs/compose-resources-patterns.md"
      to: "docs/compose-resources-configuration.md"
      via: "reference link in hub doc"
      pattern: "compose-resources-configuration"
---

<objective>
Audit all 9 pattern docs for freshness against current official sources, fix stale references, and split the 4 largest docs (>400 lines) into focused, independently-loadable sub-docs for token-efficient agent consumption.

Purpose: Agents should load only the specific pattern slice they need (e.g., "coroutine testing" not "all testing"), and the registry launches with verified, current content. This directly serves the project's core value of token-efficiency.

Output: Audited pattern docs with current version references, plus 12 focused sub-docs split from the 4 largest originals.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-pattern-registry-discovery/09-CONTEXT.md
@.planning/phases/09-pattern-registry-discovery/09-RESEARCH.md

<interfaces>
<!-- Pattern docs to audit and split. Line counts from research: -->
<!-- compose-resources-patterns.md: 763 lines -> split into config/usage/troubleshooting -->
<!-- offline-first-patterns.md: 850 lines -> split into architecture/sync/caching -->
<!-- viewmodel-state-patterns.md: 733 lines -> split into state-mgmt/navigation/events -->
<!-- testing-patterns.md: 700 lines -> split into coroutines/fakes/coverage -->

<!-- Frontmatter schema (from 09-RESEARCH.md): -->
```yaml
---
scope: [string]      # architectural layers/concerns
sources: [string]    # libraries/APIs referenced
targets: [string]    # platforms: android, desktop, ios, jvm
version: number      # doc version
last_updated: string # ISO month "2026-03"
description: string  # one-line summary
slug: string         # explicit slug
status: string       # active | draft | deprecated
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freshness audit of all 9 pattern docs</name>
  <files>
    docs/testing-patterns.md,
    docs/kmp-architecture.md,
    docs/compose-resources-patterns.md,
    docs/gradle-patterns.md,
    docs/offline-first-patterns.md,
    docs/resource-management-patterns.md,
    docs/ui-screen-patterns.md,
    docs/viewmodel-state-patterns.md,
    docs/enterprise-integration-proposal.md
  </files>
  <action>
    Audit each of the 9 pattern docs against current official sources. For each doc:

    1. Read the full content
    2. Check version references against the project's versions-manifest.json (if it exists) and against known current versions:
       - Kotlin: 2.3.10
       - Compose Multiplatform: 1.8.x
       - Ktor: 3.x
       - Room: 2.7.x (KMP)
       - Navigation3: current
       - kotlinx-coroutines-test: 1.10.x
       - Koin: 4.1.1
       - Konsist: 0.17.3
       - AGP: 9.x
    3. Fix any stale version references
    4. Check for deprecated API recommendations (e.g., old Dispatchers.Main usage, old resource access patterns)
    5. Verify patterns align with current KMP best practices from CLAUDE.md (e.g., sealed interface for UiState, SharedFlow for events, stateIn with WhileSubscribed(5_000))
    6. Update or add notes where patterns have changed

    Focus areas per doc:
    - **testing-patterns.md**: Verify coroutine test patterns match kotlinx-coroutines-test 1.10.x API. Check runTest usage, UnconfinedTestDispatcher patterns, turbine patterns if referenced.
    - **kmp-architecture.md**: Verify source set hierarchy matches Kotlin 2.3.10. Check applyDefaultHierarchyTemplate() usage.
    - **compose-resources-patterns.md**: Verify Compose Multiplatform resource API (Res.string vs StringResource), generateResClass patterns.
    - **gradle-patterns.md**: Verify AGP 9.x compatibility notes, version catalog patterns.
    - **offline-first-patterns.md**: Verify Room KMP patterns, DataStore patterns, Ktor 3.x sync patterns.
    - **resource-management-patterns.md**: Verify lifecycle patterns, Compose lifecycle owner patterns.
    - **ui-screen-patterns.md**: Verify Navigation3 patterns, Material3 patterns.
    - **viewmodel-state-patterns.md**: Verify sealed interface UiState, SharedFlow events, stateIn patterns.
    - **enterprise-integration-proposal.md**: Verify managed configurations API.

    For docs that Plan 01 has already added frontmatter to: preserve the frontmatter, only modify the body content.
    Since Plan 02 runs after Plan 01 (wave 2), all docs will already have frontmatter. Preserve existing frontmatter; only modify body content.

    NOTE: This plan runs in Wave 2 after Plan 01, so all docs already have YAML frontmatter from Plan 01. Preserve the existing frontmatter and only modify the body content during the audit.
  </action>
  <verify>
    <automated>cd mcp-server && npm test && node -e "const fs=require('fs');const path=require('path');const docsDir=path.join(process.cwd(),'..','docs');const stalePatterns=[/Kotlin 1.[0-9]/i,/Ktor 2.[0-9]/i,/AGP 7.[0-9]/i,/AGP 8.[0-9]/i,/Koin 3.[0-9]/i,/coroutines-test 1.[0-8]/i];let issues=[];for(const f of fs.readdirSync(docsDir).filter(f=>f.endsWith('.md'))){const c=fs.readFileSync(path.join(docsDir,f),'utf-8');for(const p of stalePatterns){const m=c.match(p);if(m)issues.push(f+': '+m[0])}}if(issues.length){console.error('STALE refs found: '+issues.join(', '));process.exit(1)}console.error('No known-stale version strings found')"</automated>
  </verify>
  <done>
    - All 9 pattern docs audited for freshness
    - Stale version references updated
    - Deprecated API recommendations fixed
    - No doc references APIs that are deprecated in current versions
  </done>
</task>

<task type="auto">
  <name>Task 2: Split 4 large docs into focused sub-docs</name>
  <files>
    docs/testing-patterns.md,
    docs/testing-patterns-coroutines.md,
    docs/testing-patterns-fakes.md,
    docs/testing-patterns-coverage.md,
    docs/compose-resources-patterns.md,
    docs/compose-resources-configuration.md,
    docs/compose-resources-usage.md,
    docs/compose-resources-troubleshooting.md,
    docs/offline-first-patterns.md,
    docs/offline-first-architecture.md,
    docs/offline-first-sync.md,
    docs/offline-first-caching.md,
    docs/viewmodel-state-patterns.md,
    docs/viewmodel-state-management.md,
    docs/viewmodel-navigation.md,
    docs/viewmodel-events.md
  </files>
  <action>
    Split the 4 largest pattern docs (all >400 lines) into focused sub-docs. Each sub-doc is independently loadable with its own YAML frontmatter. The original doc becomes a "hub" that contains an overview section and references to sub-docs.

    **Splitting strategy per doc:**

    1. **testing-patterns.md** (700 lines) -> 3 sub-docs:
       - `testing-patterns-coroutines.md`: Coroutine test patterns (runTest, dispatchers, TestScope, StateFlow testing)
         - scope: [testing, coroutines]
         - sources: [kotlinx-coroutines-test, junit5]
       - `testing-patterns-fakes.md`: Fake/mock patterns (FakeRepository, FakeClock, test doubles, DI in tests)
         - scope: [testing, fakes, di]
         - sources: [koin-test, junit5]
       - `testing-patterns-coverage.md`: Coverage and CI patterns (Kover config, coverage thresholds, CI pipeline)
         - scope: [testing, coverage, ci]
         - sources: [kover, junit5]
       - Hub doc keeps: title, overview section (~50-100 lines), links to sub-docs

    2. **compose-resources-patterns.md** (763 lines) -> 3 sub-docs:
       - `compose-resources-configuration.md`: Build configuration (generateResClass, source sets, multi-module setup)
         - scope: [resources, compose, configuration]
         - sources: [compose-resources, compose-multiplatform, gradle]
       - `compose-resources-usage.md`: Runtime usage patterns (string resources, image loading, font loading, qualifiers)
         - scope: [resources, compose, usage]
         - sources: [compose-resources, compose-multiplatform]
       - `compose-resources-troubleshooting.md`: Common issues and solutions (missing resources, platform differences, build errors)
         - scope: [resources, compose, troubleshooting]
         - sources: [compose-resources, compose-multiplatform]
       - Hub doc keeps: title, overview, links to sub-docs

    3. **offline-first-patterns.md** (850 lines) -> 3 sub-docs:
       - `offline-first-architecture.md`: Architecture patterns (offline-first principles, data layer design, repository pattern)
         - scope: [data, architecture, offline]
         - sources: [room, datastore]
       - `offline-first-sync.md`: Synchronization patterns (conflict resolution, sync strategies, background sync)
         - scope: [data, sync, offline]
         - sources: [ktor-client, kotlinx-coroutines, workmanager]
       - `offline-first-caching.md`: Caching patterns (cache invalidation, TTL, memory/disk cache layers)
         - scope: [data, caching, offline]
         - sources: [room, datastore, ktor-client]
       - Hub doc keeps: title, overview, links to sub-docs

    4. **viewmodel-state-patterns.md** (733 lines) -> 3 sub-docs:
       - `viewmodel-state-management.md`: State patterns (sealed interface UiState, StateFlow, stateIn, loading/error/content)
         - scope: [viewmodel, state, compose]
         - sources: [lifecycle-viewmodel, kotlinx-coroutines, compose-runtime]
       - `viewmodel-navigation.md`: Navigation patterns (state-driven nav, Navigation3, route serialization)
         - scope: [viewmodel, navigation]
         - sources: [navigation3, lifecycle-viewmodel, kotlinx-serialization]
       - `viewmodel-events.md`: Event patterns (SharedFlow events, one-shot actions, side effects)
         - scope: [viewmodel, events]
         - sources: [lifecycle-viewmodel, kotlinx-coroutines]
       - Hub doc keeps: title, overview, links to sub-docs

    **For each sub-doc:**
    1. Create new file with YAML frontmatter (following the schema from Plan 01)
    2. Set `version: 1` (new doc), `last_updated: "2026-03"`, status: active
    3. Each sub-doc has `targets` inherited from the parent doc
    4. Content extracted from the parent -- not rewritten, just reorganized
    5. Ensure each sub-doc is self-contained (no "see above" references that break when loaded independently)

    **For each hub doc (original):**
    1. Keep YAML frontmatter (update description to note it's a hub doc)
    2. Keep the title and overview/introduction section
    3. Replace extracted sections with a reference list:
       ```markdown
       ## Sub-documents

       This document is split into focused sub-docs for token-efficient loading:

       - **[testing-patterns-coroutines](testing-patterns-coroutines.md)**: Coroutine test patterns
       - **[testing-patterns-fakes](testing-patterns-fakes.md)**: Fake and test double patterns
       - **[testing-patterns-coverage](testing-patterns-coverage.md)**: Coverage and CI patterns
       ```
    4. Hub doc should be under ~150 lines (overview + references)
  </action>
  <verify>
    <automated>cd mcp-server && npx tsc && node -e "
      const fs = require('fs');
      const path = require('path');
      const docsDir = path.join(process.cwd(), '..', 'docs');
      const expected = [
        'testing-patterns-coroutines.md', 'testing-patterns-fakes.md', 'testing-patterns-coverage.md',
        'compose-resources-configuration.md', 'compose-resources-usage.md', 'compose-resources-troubleshooting.md',
        'offline-first-architecture.md', 'offline-first-sync.md', 'offline-first-caching.md',
        'viewmodel-state-management.md', 'viewmodel-navigation.md', 'viewmodel-events.md'
      ];
      let missing = expected.filter(f => !fs.existsSync(path.join(docsDir, f)));
      if (missing.length > 0) { console.error('Missing sub-docs: ' + missing.join(', ')); process.exit(1); }
      for (const f of expected) {
        const content = fs.readFileSync(path.join(docsDir, f), 'utf-8');
        if (!content.startsWith('---')) { console.error(f + ' missing frontmatter'); process.exit(1); }
      }
      console.error('All 12 sub-docs exist with frontmatter');
    "
    </automated>
  </verify>
  <done>
    - 12 new sub-doc files created in docs/
    - Each sub-doc has valid YAML frontmatter with scope, sources, targets
    - 4 original hub docs trimmed to overview + sub-doc references (<150 lines each)
    - No content lost -- all sections from originals present in sub-docs
    - Each sub-doc is self-contained (loadable independently)
  </done>
</task>

</tasks>

<verification>
1. All 9+12 = 21 .md files in docs/ have valid YAML frontmatter (except propuesta-integracion-enterprise.md)
2. No stale version references in any pattern doc
3. Each sub-doc is independently loadable with complete frontmatter
4. Hub docs are under 150 lines with links to sub-docs
5. `cd mcp-server && npm test` passes (existing tests still green)
</verification>

<success_criteria>
- All pattern docs verified against current official sources
- 4 large docs split into 12 focused sub-docs
- Hub docs serve as navigation + overview
- Every .md file (except Spanish original) has valid YAML frontmatter
- No content lost in splitting process
</success_criteria>

<output>
After completion, create `.planning/phases/09-pattern-registry-discovery/09-02-SUMMARY.md`
</output>
