---
phase: 05-tech-debt-foundation
plan: 02
type: execute
wave: 1
depends_on: []
# Note: Files are fully independent from plan 01 (no overlap). RESEARCH.md recommends
# sequential execution (DEBT-01/02 before DEBT-03/04) but this is a preference, not a
# dependency. Both plans can safely run in parallel since they touch disjoint file sets.
files_modified:
  - .claude/agents/quality-gate-orchestrator.md
  - scripts/sh/validate-phase01-pattern-docs.sh
  - scripts/sh/validate-phase01-param-manifest.sh
  - scripts/sh/validate-phase01-skill-pipeline.sh
  - scripts/sh/validate-phase01-agents-md.sh
  - scripts/sh/validate-phase01-param-drift.sh
autonomous: true
requirements: [DEBT-03, DEBT-04]

must_haves:
  truths:
    - "Quality-gate-orchestrator reads individual agent .md files at runtime and follows their instructions"
    - "Updating an individual gate agent automatically changes what the orchestrator checks -- no manual orchestrator sync needed"
    - "Individual gate agents remain independently invocable for debugging"
    - "Orchestrator produces the same unified report format as before"
    - "Token Cost Summary section stays inline in orchestrator"
    - "The 5 orphaned validate-phase01-*.sh scripts no longer exist in scripts/sh/"
    - "No active code or configuration references the deleted scripts"
  artifacts:
    - path: ".claude/agents/quality-gate-orchestrator.md"
      provides: "Delegation-based orchestrator that reads individual agents"
      contains: "Read .claude/agents/"
  key_links:
    - from: ".claude/agents/quality-gate-orchestrator.md"
      to: ".claude/agents/script-parity-validator.md"
      via: "Read tool at runtime"
      pattern: "Read.*script-parity-validator\\.md"
    - from: ".claude/agents/quality-gate-orchestrator.md"
      to: ".claude/agents/skill-script-alignment.md"
      via: "Read tool at runtime"
      pattern: "Read.*skill-script-alignment\\.md"
    - from: ".claude/agents/quality-gate-orchestrator.md"
      to: ".claude/agents/template-sync-validator.md"
      via: "Read tool at runtime"
      pattern: "Read.*template-sync-validator\\.md"
    - from: ".claude/agents/quality-gate-orchestrator.md"
      to: ".claude/agents/doc-code-drift-detector.md"
      via: "Read tool at runtime"
      pattern: "Read.*doc-code-drift-detector\\.md"
---

<objective>
Refactor quality-gate-orchestrator.md to delegate to individual gate agents instead of inlining their logic, and delete the 5 orphaned validate-phase01-*.sh scripts.

Purpose: Eliminate the drift problem where the orchestrator's inlined copy of gate logic falls behind individual agent updates (this already caused INT-05 in v1.0). Clean up orphaned scripts that produce noise in quality gate runs.
Output: Slim orchestrator (~80-100 lines) that reads individual agents at runtime. 5 orphaned scripts deleted.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-tech-debt-foundation/05-CONTEXT.md
@.planning/phases/05-tech-debt-foundation/05-RESEARCH.md
@.claude/agents/quality-gate-orchestrator.md
@.claude/agents/script-parity-validator.md
@.claude/agents/skill-script-alignment.md
@.claude/agents/template-sync-validator.md
@.claude/agents/doc-code-drift-detector.md

<interfaces>
<!-- Current orchestrator: 274 lines, manually replicates checking logic from all 4 agents -->
<!-- Individual agents are already independently invocable with their own frontmatter -->

From .claude/agents/quality-gate-orchestrator.md:
  Frontmatter: name, description, tools (Read, Grep, Glob, Bash), model (sonnet), memory (project)
  Sections: Script Parity, Skill-Script Alignment, Template Sync, Doc-Code Drift, Token Cost Summary, Report Format
  Report format: PASS/FAIL per gate, overall PASS/FAIL

From individual agents (all share this pattern):
  Each has: YAML frontmatter (name, description, tools, model, memory)
  Each defines: specific checks to perform with exact procedures
  Each produces: section-level PASS/FAIL with detailed findings

Orphaned scripts to delete (confirmed zero active references outside .planning/):
  scripts/sh/validate-phase01-pattern-docs.sh
  scripts/sh/validate-phase01-param-manifest.sh
  scripts/sh/validate-phase01-skill-pipeline.sh
  scripts/sh/validate-phase01-agents-md.sh
  scripts/sh/validate-phase01-param-drift.sh
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor quality-gate-orchestrator to delegate to individual agents</name>
  <files>.claude/agents/quality-gate-orchestrator.md</files>
  <action>
Rewrite quality-gate-orchestrator.md to use the "read and follow" delegation pattern instead of inlining all gate logic.

**Keep unchanged:**
- YAML frontmatter (name, description, tools, model, memory)
- Report Format section (the exact output template)
- Token Cost Summary section (stays inline per user decision -- only meaningful in unified report context)
- Overall PASS/FAIL logic (sections 1-4 determine status, Token Cost is informational)

**Replace the 4 inlined gate sections** (Script Parity, Skill-Script Alignment, Template Sync, Doc-Code Drift -- currently ~200 lines of duplicated checking logic) with delegation instructions:

```markdown
## Execution

For each gate below, follow this exact process:
1. Use the Read tool to load the agent's .md file
2. Read and understand EVERY step and sub-step described in that file
3. Execute ALL checks exactly as the agent file describes -- do not summarize, skip, or simplify any steps
4. Record the section status (PASS/FAIL) and all findings for the unified report

### Gate 1: Script Parity
Read `.claude/agents/script-parity-validator.md` and execute all checks described there.
Record section status (PASS/FAIL) and all findings.

### Gate 2: Skill-Script Alignment
Read `.claude/agents/skill-script-alignment.md` and execute all checks described there.
Record section status (PASS/FAIL) and all findings.

### Gate 3: Template Sync
Read `.claude/agents/template-sync-validator.md` and execute all checks described there.
Record section status (PASS/FAIL) and all findings.

### Gate 4: Doc-Code Drift
Read `.claude/agents/doc-code-drift-detector.md` and execute all checks described there.
Record section status (PASS/FAIL) and all findings.
```

**Be explicit about execution fidelity (Pitfall 3 from RESEARCH.md):** The delegation instructions MUST say "execute EVERY step and sub-step" and "do not summarize, skip, or simplify." This prevents the AI model from interpreting "follow its instructions" loosely and producing fewer findings than individual agents would.

**Expected result:** Orchestrator shrinks from ~274 lines to ~80-100 lines. The 4 individual agent files become the single source of truth for their respective checks. When an agent is updated, the orchestrator automatically picks up the changes.

**Structure of the rewritten file:**
1. YAML frontmatter (unchanged)
2. Introduction paragraph (updated to describe delegation approach)
3. Execution section (4 gate delegations as above)
4. Token Cost Summary section (kept inline, unchanged from current)
5. Report Format section (unchanged -- same output template)
  </action>
  <verify>
    <automated>wc -l .claude/agents/quality-gate-orchestrator.md | awk '{print ($1 < 120) ? "PASS: reduced size ("$1" lines)" : "FAIL: still too large ("$1" lines)"}'; grep -c "Read.*agents/" .claude/agents/quality-gate-orchestrator.md | xargs -I{} bash -c 'test {} -ge 4 && echo "PASS: delegates to 4 agents" || echo "FAIL: missing agent references"'; grep -q "Token Cost Summary" .claude/agents/quality-gate-orchestrator.md && echo "PASS: token cost inline" || echo "FAIL: token cost missing"</automated>
  </verify>
  <done>Orchestrator delegates to 4 individual agents via Read tool. Token Cost Summary remains inline. Report Format preserved. File is ~80-100 lines (down from 274). No inlined checking logic remains.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned validate-phase01-*.sh scripts</name>
  <files>
    scripts/sh/validate-phase01-pattern-docs.sh
    scripts/sh/validate-phase01-param-manifest.sh
    scripts/sh/validate-phase01-skill-pipeline.sh
    scripts/sh/validate-phase01-agents-md.sh
    scripts/sh/validate-phase01-param-drift.sh
  </files>
  <action>
**Step 1: Final reference audit (safety check before deletion)**

Before deleting, verify zero active references. Run these checks:

```bash
# Check for references outside .planning/ directory
grep -r "validate-phase01" --include="*.sh" --include="*.ps1" --include="*.md" . | grep -v ".planning/" | grep -v "validate-phase01-" | head -20
```

If any active references are found (outside .planning/ and not self-references), STOP and report. The RESEARCH.md audit confirmed zero active references, but this is a safety check.

Also confirm no PS1 counterparts exist (research says they don't):
```bash
ls scripts/ps1/validate-phase01-* 2>/dev/null
```

**Step 2: Delete the 5 orphaned scripts**

```bash
rm scripts/sh/validate-phase01-pattern-docs.sh
rm scripts/sh/validate-phase01-param-manifest.sh
rm scripts/sh/validate-phase01-skill-pipeline.sh
rm scripts/sh/validate-phase01-agents-md.sh
rm scripts/sh/validate-phase01-param-drift.sh
```

**Step 3: Verify deletion**

```bash
ls scripts/sh/validate-phase01-* 2>&1  # Should show "No such file or directory"
```

**What NOT to do:**
- Do NOT modify archived v1.0 planning docs that reference these scripts (user decision: leave history untouched)
- Do NOT delete any validate-phase03-* or validate-phase04-* scripts (those are active)
  </action>
  <verify>
    <automated>ls scripts/sh/validate-phase01-* 2>&1 | grep -q "No such file" && echo "PASS: orphans deleted" || echo "FAIL: orphans still exist"; grep -r "validate-phase01" --include="*.sh" --include="*.ps1" --include="*.md" . 2>/dev/null | grep -v ".planning/" | grep -v "SUMMARY" | wc -l | xargs -I{} bash -c 'test {} -eq 0 && echo "PASS: zero active references" || echo "FAIL: active references remain"'</automated>
  </verify>
  <done>All 5 validate-phase01-*.sh scripts deleted from scripts/sh/. Zero active references remain outside .planning/ archive. Archived v1.0 planning docs left untouched.</done>
</task>

</tasks>

<verification>
1. quality-gate-orchestrator.md references all 4 individual agents by file path
2. Orchestrator file is ~80-100 lines (down from 274)
3. Token Cost Summary section is preserved inline in orchestrator
4. Report Format section is preserved in orchestrator
5. All 5 validate-phase01-*.sh scripts are deleted
6. No active references to validate-phase01 exist outside .planning/
7. validate-phase03-* and validate-phase04-* scripts are untouched
</verification>

<success_criteria>
- Orchestrator delegates to individual agents via Read tool -- no inlined checking logic
- Individual agents remain independently invocable
- Token Cost Summary stays inline in orchestrator
- Report format unchanged
- 5 orphaned scripts deleted with zero active reference breakage
</success_criteria>

<output>
After completion, create `.planning/phases/05-tech-debt-foundation/05-02-SUMMARY.md`
</output>
