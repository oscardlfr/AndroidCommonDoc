---
phase: 17-documentation-structural-homogeneity
plan: 03
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - docs/compose/compose-patterns.md
  - docs/storage/storage-patterns-guide.md
  - docs/navigation/navigation-patterns.md
  - docs/resources/resource-management.md
  - docs/guides/guides-index.md
  - docs/offline-first/offline-first-patterns.md
  - docs/ui/ui-patterns.md
autonomous: true
requirements:
  - P17-L0HUB
  - P17-L0STRUCT

must_haves:
  truths:
    - "Every L0 docs/ subdirectory with 2+ files has a hub doc with Sub-documents section"
    - "All L0 hub docs are under 100 lines"
    - "All L0 sub-docs are under 300 lines"
    - "validate-vault checkStructuralHomogeneity reports 0 errors for L0"
  artifacts:
    - path: "docs/compose/compose-patterns.md"
      provides: "Compose category hub linking 4 sub-docs"
      contains: "## Sub-documents"
    - path: "docs/navigation/navigation-patterns.md"
      provides: "Navigation category hub"
      contains: "## Sub-documents"
    - path: "docs/resources/resource-management.md"
      provides: "Resource management category hub"
      contains: "## Sub-documents"
    - path: "docs/guides/guides-index.md"
      provides: "Guides category hub linking workflow, template, consumption docs"
      contains: "## Sub-documents"
  key_links:
    - from: "docs/compose/compose-patterns.md"
      to: "docs/compose/compose-resources-*.md"
      via: "Sub-documents section links"
      pattern: "compose-resources"
    - from: "docs/guides/guides-index.md"
      to: "docs/guides/*.md"
      via: "Sub-documents section links"
      pattern: "agent-consumption|claude-code|doc-template"
---

<objective>
Create missing hub files for L0 docs/ subdirectories (compose, navigation, resources, guides) and verify all existing hubs (architecture, di, error-handling, gradle, offline-first, storage, testing, ui) conform to the <100 line limit with proper Sub-documents sections. Every subdirectory must follow the same hub->sub-doc structural pattern.

Purpose: L0 is the pattern source of truth. If L0 isn't structurally homogeneous, L1/L2 can't follow its example. compose/ has 4 sub-docs but no hub; navigation/ has 1 doc that should be a hub; resources/ has 3 docs but no hub; guides/ has 5 docs with no index.
Output: All 14 L0 docs/ subdirectories structurally homogeneous with hub->sub-doc pattern.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-documentation-structural-homogeneity/17-CONTEXT.md

@docs/compose/compose-resources-patterns.md
@docs/navigation/navigation3-patterns.md
@docs/resources/resource-management-patterns.md
@docs/guides/agent-consumption-guide.md
@docs/storage/storage-patterns.md
@docs/ui/ui-screen-patterns.md
@docs/offline-first/offline-first-patterns.md

<interfaces>
<!-- Hub doc structure (consistent across all subdirs): -->
<!-- - YAML frontmatter (10 fields) -->
<!-- - Title (# Category Patterns) -->
<!-- - 1-paragraph summary -->
<!-- - ## Sub-documents table linking all sub-docs in the directory -->
<!-- - Total: <100 lines -->
<!-- Sub-doc structure: -->
<!-- - YAML frontmatter with parent: hub-slug -->
<!-- - Content focused on single topic -->
<!-- - Total: <300 lines -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create missing hub files for compose, navigation, resources, guides</name>
  <files>docs/compose/compose-patterns.md, docs/navigation/navigation-patterns.md, docs/resources/resource-management.md, docs/guides/guides-index.md</files>
  <action>
Create 4 new hub files. Each hub must follow this exact structure:
- 10-field YAML frontmatter (scope, sources, targets, slug, status: active, layer: L0, category matching subdir name, description, version: "1.0", last_updated: today's date)
- `# Title` heading
- 1-paragraph overview (2-3 sentences max)
- `## Sub-documents` section with markdown table: | Document | Description |
- Link each sub-doc in the subdirectory using relative paths
- MUST be under 100 lines total

**docs/compose/compose-patterns.md** (hub for compose/):
- Links: compose-resources-patterns.md, compose-resources-configuration.md, compose-resources-configuration-setup.md, compose-resources-usage.md, compose-resources-troubleshooting.md
- Update each sub-doc's frontmatter to add `parent: compose-patterns` if not present

**docs/navigation/navigation-patterns.md** (hub for navigation/):
- Links: navigation3-patterns.md (single sub-doc currently)
- The existing navigation3-patterns.md becomes a sub-doc with parent: navigation-patterns

**docs/resources/resource-management.md** (hub for resources/):
- Links: resource-management-patterns.md, resource-management-lifecycle.md, resource-management-memory.md
- Note: resource-management-patterns.md already exists as a general doc -- if it reads like a hub, rename/restructure it to be the hub. If it's content-focused, create resource-management.md as the new hub and make the existing file a sub-doc.
- Check actual content of resource-management-patterns.md first. If it already has Sub-documents section, it IS the hub -- just verify line count and add missing sub-doc links.

**docs/guides/guides-index.md** (hub for guides/):
- Links: agent-consumption-guide.md, claude-code-workflow.md, claude-md-template.md, doc-template.md
- Skip canonical-rules.json (not a markdown doc)
- Update each guide's frontmatter to add `parent: guides-index` if not present
  </action>
  <verify>
    <automated>for f in docs/compose/compose-patterns.md docs/navigation/navigation-patterns.md docs/resources/resource-management.md docs/guides/guides-index.md; do if [ ! -f "$f" ]; then echo "MISSING: $f"; exit 1; fi; lines=$(wc -l < "$f"); echo "$f: $lines lines"; if [ "$lines" -gt 100 ]; then echo "OVER LIMIT: $f ($lines > 100)"; exit 1; fi; if ! grep -q "## Sub-documents" "$f"; then echo "MISSING Sub-documents section: $f"; exit 1; fi; done && echo "All hub files valid"</automated>
  </verify>
  <done>All 4 new hub files exist, each under 100 lines, with Sub-documents sections linking all sub-docs in their directory. Sub-docs have parent fields.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix existing hub docs for line limits and completeness</name>
  <files>docs/offline-first/offline-first-patterns.md, docs/ui/ui-screen-patterns.md, docs/storage/storage-patterns.md</files>
  <action>
Audit ALL existing hub docs across L0 docs/:

1. Check each existing hub for line count:
   - architecture/kmp-architecture.md
   - di/di-patterns.md
   - error-handling/error-handling-patterns.md
   - gradle/gradle-patterns.md
   - offline-first/offline-first-patterns.md
   - storage/storage-patterns.md
   - testing/testing-patterns.md
   - ui/ui-screen-patterns.md (and viewmodel-state-patterns.md if it's also a hub)

2. For any hub exceeding 100 lines:
   - Move excess content into existing sub-docs or create new sub-doc
   - Hub retains only: frontmatter + title + 1-paragraph summary + Sub-documents table
   - Per research: offline-first-patterns (101 lines), testing-patterns (108 lines), viewmodel-state-patterns (175 lines), compose-resources-patterns (102 lines) were over limit in Phase 14.2

3. For hubs missing Sub-documents links for files in their directory:
   - Add missing sub-doc entries to the table

4. For ui/ subdirectory specifically:
   - It has two hub-like docs: ui-screen-patterns.md and viewmodel-state-patterns.md
   - If both are hubs, that's fine (two topic areas in one category)
   - Ensure BOTH have complete Sub-documents sections and are under 100 lines
   - viewmodel-state-management.md is also potentially a hub -- check and consolidate if needed

5. Run validate-vault checkStructuralHomogeneity against L0 docs/ after all fixes.
  </action>
  <verify>
    <automated>cd mcp-server && node -e "const {checkStructuralHomogeneity} = require('./dist/tools/validate-vault.js'); checkStructuralHomogeneity([{name:'AndroidCommonDoc',path:'.',layer:'L0'}]).then(r => {console.log(JSON.stringify(r,null,2)); process.exit(r.filter(i=>i.severity==='error').length > 0 ? 1 : 0)})"</automated>
  </verify>
  <done>All L0 hub docs under 100 lines. All subdirectories with 2+ files have a hub. validate-vault structural check passes for L0 with 0 errors.</done>
</task>

</tasks>

<verification>
Run validate-vault structural homogeneity check against L0:
```bash
cd mcp-server && npm run build && node -e "..."
```
Also verify no hub exceeds 100 lines:
```bash
for f in docs/*/; do hub=$(grep -rl "## Sub-documents" "$f" 2>/dev/null | head -1); if [ -n "$hub" ]; then lines=$(wc -l < "$hub"); echo "$hub: $lines lines"; fi; done
```
</verification>

<success_criteria>
- All 14 L0 docs/ subdirectories have hub files (or are single-file dirs)
- Every hub doc is under 100 lines
- Every sub-doc is under 300 lines
- Every sub-doc has a parent field in frontmatter
- validate-vault checkStructuralHomogeneity returns 0 errors for L0
</success_criteria>

<output>
After completion, create `.planning/phases/17-documentation-structural-homogeneity/17-03-SUMMARY.md`
</output>
