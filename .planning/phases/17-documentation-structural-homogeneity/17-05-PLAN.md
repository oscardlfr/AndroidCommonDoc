---
phase: 17-documentation-structural-homogeneity
plan: 05
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/architecture-hub.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/docs/business/business-hub.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/docs/guides/guides-hub.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/docs/legal/legal-hub.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/docs/product/product-hub.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/docs/references/references-hub.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/docs/tech/tech-hub.md
autonomous: true
requirements:
  - P17-L2HUB
  - P17-L2STRUCT
  - P17-L2CLEAN

must_haves:
  truths:
    - "DawSync .planning/codebase/ and .planning/research/ content excluded from vault collection"
    - "Every DawSync docs/ subdirectory with 2+ files has a hub doc"
    - "All L2 hub docs under 100 lines, sub-docs under 300 lines"
    - "DawSyncWeb and SessionRecorder-VST3 subproject naming normalized"
    - "validate-vault reports 0 errors for L2"
  artifacts:
    - path: "docs/architecture/architecture-hub.md"
      provides: "L2 architecture category hub linking 62+ diagram sub-docs"
      contains: "## Sub-documents"
    - path: "docs/product/product-hub.md"
      provides: "L2 product category hub"
      contains: "## Sub-documents"
  key_links:
    - from: "DawSync docs/*/hub.md"
      to: "L0/L1 via l0_refs"
      via: "frontmatter l0_refs field"
      pattern: "l0_refs"
---

<objective>
Restructure DawSync (L2) source docs for structural homogeneity: create hub files for each docs/ subdirectory, clean up .planning/ content overlap (already handled by excludeGlobs in 17-01 but verify), normalize DawSyncWeb subproject naming in vault config, and ensure all docs follow the same hub->sub-doc pattern as L0/L1.

Purpose: DawSync is the most document-heavy project (100+ docs) and the primary source of vault quality issues. Its docs/ subdirectories lack hub files, the architecture/ directory has 62+ diagram files with no index, and subproject naming is inconsistent.
Output: All DawSync docs/ subdirectories structurally homogeneous with L0/L1 pattern.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-documentation-structural-homogeneity/17-CONTEXT.md

<interfaces>
<!-- DawSync docs/ structure: -->
<!-- architecture/ - 62+ diagram files in A-H prefixed subdirectories + some root docs -->
<!-- business/ - business plan, positioning, pricing docs -->
<!-- guides/ - workflow guides, cheatsheets -->
<!-- legal/ - terms, privacy, fiscal docs -->
<!-- product/ - product spec, feature docs (many split into sub-docs in Phase 14.2) -->
<!-- references/ - reference materials -->
<!-- tech/ - technical docs -->
<!-- archive/ - excluded from vault (38 files) -->
<!-- images/ - not markdown, skip -->

<!-- Hub doc structure (same as L0/L1): -->
<!-- 10-field frontmatter, layer: L2, l0_refs where applicable -->
<!-- Under 100 lines -->
<!-- ## Sub-documents table -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hub files for DawSync docs/ subdirectories</name>
  <files>
    C:/Users/34645/AndroidStudioProjects/DawSync/docs/architecture/architecture-hub.md,
    C:/Users/34645/AndroidStudioProjects/DawSync/docs/business/business-hub.md,
    C:/Users/34645/AndroidStudioProjects/DawSync/docs/guides/guides-hub.md,
    C:/Users/34645/AndroidStudioProjects/DawSync/docs/legal/legal-hub.md,
    C:/Users/34645/AndroidStudioProjects/DawSync/docs/product/product-hub.md,
    C:/Users/34645/AndroidStudioProjects/DawSync/docs/references/references-hub.md,
    C:/Users/34645/AndroidStudioProjects/DawSync/docs/tech/tech-hub.md
  </files>
  <action>
For each DawSync docs/ subdirectory (excluding archive/ and images/), check if a hub exists. If not, create one.

1. **architecture/** -- This directory has 62+ diagram files organized in A-H prefixed subdirectories. Check first:
   - List all .md files at the architecture/ root level
   - If there are root-level docs (like system-architecture.md, patterns.md), those are the main docs
   - The A-H subdirectories contain diagram files (mermaid diagrams from Phase 14)
   - Create architecture-hub.md that:
     - Links root-level architecture docs as sub-docs
     - Has a "## Diagram Collections" section summarizing the A-H subdirectories (not listing all 62 individually)
     - l0_refs: [kmp-architecture]
     - Under 100 lines

2. **business/** -- Create business-hub.md linking business plan, positioning, pricing docs. No l0_refs (business is L2-specific).

3. **guides/** -- Create guides-hub.md linking workflow guides. l0_refs: [guides-index] (L0 guides hub created in 17-03).

4. **legal/** -- Create legal-hub.md linking terms, privacy, fiscal docs. No l0_refs (legal is L2-specific).

5. **product/** -- Product docs were split into hub+sub-doc in Phase 14.2. Check if a product hub already exists. If product-spec.md was split and has a "## Sub-documents" section, it IS the hub -- verify line count. Otherwise create product-hub.md.

6. **references/** -- Create references-hub.md linking reference materials.

7. **tech/** -- Create tech-hub.md linking technical docs. l0_refs where applicable.

For EACH hub:
- 10-field YAML frontmatter with layer: L2, category matching subdir name
- Under 100 lines
- ## Sub-documents table
- Add parent field to each sub-doc's frontmatter

NOTE: DawSync docs use lowercase-kebab-case naming (fixed in Phase 14.3-07). Some older files may still be UPPERCASE -- rename them if found. The architecture diagram files in A-H subdirectories may retain their existing naming if they're already kebab-case.
  </action>
  <verify>
    <automated>cd "C:/Users/34645/AndroidStudioProjects/DawSync" && for d in docs/*/; do base=$(basename "$d"); if [ "$base" != "archive" ] && [ "$base" != "images" ]; then count=$(find "$d" -maxdepth 1 -name "*.md" | wc -l); hub=$(grep -rl "## Sub-documents" "$d" 2>/dev/null | head -1); if [ "$count" -gt 1 ] && [ -z "$hub" ]; then echo "MISSING HUB: $d ($count files)"; fi; fi; done</automated>
  </verify>
  <done>Every DawSync docs/ subdirectory with 2+ files has a hub doc under 100 lines. All sub-docs have parent fields.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DawSync frontmatter completeness and naming normalization</name>
  <files>C:/Users/34645/AndroidStudioProjects/DawSync/docs/README.md</files>
  <action>
1. Scan ALL .md files in DawSync/docs/ (excluding archive/ and images/).
2. Check each file:
   - Has all 10 frontmatter fields
   - layer field is "L2"
   - Sub-docs have parent field
   - Filename is lowercase-kebab-case (flag any UPPERCASE files found)
   - No file exceeds limits (hub <100, sub-doc <300)

3. If any files still have UPPERCASE names, rename to lowercase-kebab-case and update any references (frontmatter parent fields, hub Sub-documents links).

4. Verify DawSyncWeb subproject naming:
   - In vault-config.json, the subproject is named "DawSyncWeb"
   - Ensure this is the canonical name used consistently (no "web" vs "DawSyncWeb" collision)
   - If DawSyncWeb has .planning/codebase/ files, they should be excluded (handled by 17-01 excludeGlobs)

5. Verify SessionRecorder-VST3 docs (if any exist) have proper L2 frontmatter with C++/JUCE scope per Phase 14.2-08 decision.

6. Run validate-vault checkStructuralHomogeneity for L2.
  </action>
  <verify>
    <automated>find "C:/Users/34645/AndroidStudioProjects/DawSync/docs" -name "*.md" -not -path "*/archive/*" -not -path "*/images/*" | xargs grep -L "^---" | head -10; echo "---"; find "C:/Users/34645/AndroidStudioProjects/DawSync/docs" -name "*[A-Z]*" -not -path "*/archive/*" -not -path "*/images/*" | head -10</automated>
  </verify>
  <done>All DawSync docs have complete frontmatter. No UPPERCASE filenames remain (outside archive/). DawSyncWeb naming normalized. validate-vault structural check passes for L2 with 0 errors.</done>
</task>

</tasks>

<verification>
Run validate-vault against L2:
```bash
cd mcp-server && npm run build && node -e "checkStructuralHomogeneity + checkReferenceIntegrity for DawSync"
```
Check no UPPERCASE filenames:
```bash
find "C:/Users/34645/AndroidStudioProjects/DawSync/docs" -name "*[A-Z]*" -not -path "*/archive/*" -not -path "*/images/*" | wc -l
```
</verification>

<success_criteria>
- All DawSync multi-file subdirectories have hub docs
- All hub docs under 100 lines, sub-docs under 300 lines
- All docs have complete 10-field frontmatter with layer: L2
- No UPPERCASE filenames in active docs (archive excluded)
- DawSyncWeb/SessionRecorder-VST3 naming normalized
- validate-vault returns 0 errors for L2
</success_criteria>

<output>
After completion, create `.planning/phases/17-documentation-structural-homogeneity/17-05-SUMMARY.md`
</output>
