---
phase: 17-documentation-structural-homogeneity
plan: 06
type: execute
wave: 3
depends_on: ["17-01", "17-03", "17-04", "17-05"]
files_modified:
  - ~/.claude/CLAUDE.md
  - CLAUDE.md
  - C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md
autonomous: false
requirements:
  - P17-CLAUDEMD
  - P17-SYNC
  - P17-HUMAN

must_haves:
  truths:
    - "All 4 CLAUDE.md files follow Boris Cherny style with categorized sections"
    - "Each CLAUDE.md is under 150 lines"
    - "validate-vault returns 0 errors across all 4 dimensions for all layers"
    - "Vault resync produces 0 duplicates and 0 orphans"
    - "Human verifies Obsidian graph view, MOC navigation, and wikilink connectivity"
  artifacts:
    - path: "~/.claude/CLAUDE.md"
      provides: "L0 global rules (Boris Cherny style)"
      min_lines: 50
    - path: "CLAUDE.md"
      provides: "L0 toolkit rules (Boris Cherny style)"
      min_lines: 30
    - path: "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md"
      provides: "L1 rules (Boris Cherny style)"
      min_lines: 30
    - path: "C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md"
      provides: "L2 rules (Boris Cherny style)"
      min_lines: 40
  key_links:
    - from: "validate-vault"
      to: "vault resync"
      via: "validation gate before materialization"
      pattern: "validate-vault.*passed.*true"
    - from: "~/.claude/CLAUDE.md"
      to: "project CLAUDE.md files"
      via: "convention-based delegation (auto-loaded by Claude Code)"
      pattern: "Inherits.*L0"
---

<objective>
Restructure all 4 CLAUDE.md files to Boris Cherny style (concise categorized sections: Workflow, Tasks, Principles), run ALL validation tools as a final gate, perform vault resync, and present the result for human checkpoint verification in Obsidian.

Purpose: This is the final plan. CLAUDE.md files need to be optimized for agent consumption with Boris Cherny's proven structure. The vault resync must pass all validation checks (0 duplicates, 0 homogeneity errors, 0 broken references, 0 broken wikilinks) before presenting to the user.
Output: Boris Cherny-aligned CLAUDE.md files + clean validated vault that the user can approve in Obsidian.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-documentation-structural-homogeneity/17-CONTEXT.md

@~/.claude/CLAUDE.md
@CLAUDE.md
@C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md
@C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md

@.planning/phases/17-documentation-structural-homogeneity/17-01-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-02-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-03-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-04-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-05-SUMMARY.md

<interfaces>
<!-- Boris Cherny CLAUDE.md style: -->
<!-- ## Workflow Orchestration -->
<!--   - Plan Mode Default -->
<!--   - Subagent Strategy -->
<!--   - Self-Improvement Loop -->
<!--   - Verification Before Done -->
<!--   - Demand Elegance -->
<!--   - Autonomous Bug Fixing -->
<!-- ## Task Management -->
<!--   - Plan First -->
<!--   - Verify Plan -->
<!--   - Track Progress -->
<!--   - Explain Changes -->
<!--   - Document Results -->
<!--   - Capture Lessons -->
<!-- ## Core Principles -->
<!--   - Simplicity First -->
<!--   - No Laziness -->
<!--   - Minimal Impact -->

<!-- Adapt these categories to KMP context: -->
<!-- ## Workflow (plan mode, verification, GSD usage) -->
<!-- ## Architecture (layer rules, source sets, module naming) -->
<!-- ## Patterns (error handling, viewmodel, testing) -->
<!-- ## Constraints (line limits, naming, platform rules) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure all 4 CLAUDE.md files to Boris Cherny style</name>
  <files>~/.claude/CLAUDE.md, CLAUDE.md, C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md, C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md</files>
  <action>
Read all 4 current CLAUDE.md files. Restructure each to Boris Cherny style while preserving ALL existing rules (use the canonical rule checklist from Phase 15-01 as verification -- no rule may be dropped).

Boris Cherny adaptation for KMP ecosystem:

**~/.claude/CLAUDE.md (L0 Global, <150 lines):**
Reorganize into categorized sections:
- `## Workflow` -- GSD active, .planning/ directories, verification patterns
- `## Architecture` -- 5-layer model (UI/ViewModel/Domain/Data/Model), source set rules (commonMain, jvmMain, appleMain), module naming (FLAT)
- `## Patterns` -- Error handling (Result<T>, CancellationException), ViewModel (sealed interface UiState, StateFlow, state-based events), Testing (fakes, runTest, sequential), DI (Koin 4.1.1)
- `## Constraints` -- File naming suffixes, Compose Resources path, Navigation3, Build patterns

Each rule as a terse bullet (1 line, action-oriented). No prose paragraphs. No explanatory text beyond what's needed for correct behavior. Tables only where they reduce tokens vs bullets.

**CLAUDE.md (L0 Toolkit, <150 lines):**
- `## Identity` -- Layer model, what this project is
- `## Workflow` -- MCP server testing, Konsist testing, build commands
- `## Structure` -- Skills, agents, commands, docs, vault sync
- `## Constraints` -- Hub <100 lines, sub-doc <300, frontmatter schema, kebab-case naming

**shared-kmp-libs/CLAUDE.md (L1, <150 lines):**
- `## Identity` -- Version authority, API/-impl separation
- `## Workflow` -- Build commands, module catalog reference
- `## Architecture` -- Module categories, convention plugins, composite builds
- `## Constraints` -- README requirements, l0_refs for L0 cross-references

**DawSync/CLAUDE.md (L2, <150 lines):**
- `## Identity` -- Producer/Consumer model, main project
- `## Workflow` -- Feature gates, DO NOT DISTURB rules, wave strategy
- `## Architecture` -- SSOT patterns, offline-first, subprojects (DawSyncWeb, SessionRecorder-VST3)
- `## Constraints` -- No Turbine, state-based events, platform-specific rules

After rewriting, run `validate-claude-md` MCP tool against each file to verify:
- Under 150 lines
- No dropped rules vs canonical checklist (90%+ coverage)
- No circular references
- No upward L0->L1/L2 references
  </action>
  <verify>
    <automated>wc -l ~/.claude/CLAUDE.md CLAUDE.md "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md" "C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md" | grep -v total && cd mcp-server && npm run build && node -e "const {validateClaudeMd} = require('./dist/tools/validate-claude-md.js'); Promise.all([validateClaudeMd({filePath:process.env.HOME+'/.claude/CLAUDE.md'}), validateClaudeMd({filePath:'./CLAUDE.md'}), validateClaudeMd({filePath:'C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/CLAUDE.md'}), validateClaudeMd({filePath:'C:/Users/34645/AndroidStudioProjects/DawSync/CLAUDE.md'})]).then(results => {const errors = results.flatMap(r => r.filter(i => i.severity === 'error')); console.log('Errors:', errors.length); if(errors.length > 0) {console.log(JSON.stringify(errors,null,2)); process.exit(1);}})"</automated>
  </verify>
  <done>All 4 CLAUDE.md files under 150 lines, Boris Cherny style with categorized sections, no dropped rules.</done>
</task>

<task type="auto">
  <name>Task 2: Run all validation tools and perform vault resync</name>
  <files>mcp-server/src/tools/sync-vault.ts</files>
  <action>
1. Run validate-vault with ALL 4 checks against all 3 projects:
   ```bash
   cd mcp-server && npm run build
   # Run validate-vault checkDuplicates against vault output
   # Run validate-vault checkStructuralHomogeneity against L0, L1, L2
   # Run validate-vault checkReferenceIntegrity against L0, L1, L2
   ```
   ALL checks must return 0 errors. Warnings acceptable but should be minimized.

2. If any errors found, fix them before proceeding. Do NOT resync with errors.

3. Run validate-doc-structure across all 3 projects to confirm doc quality.

4. Run validate-claude-md against all 4 CLAUDE.md files.

5. Run validate-skills to confirm no regressions.

6. Perform vault resync:
   ```bash
   cd mcp-server && node dist/cli/sync-vault-cli.js sync
   ```

7. After sync, run validate-vault checkDuplicates against the materialized vault to confirm 0 duplicates.

8. Run validate-vault checkWikilinkCoverage against the vault to confirm 0 broken wikilinks.

9. Count total vault files and compare to Phase 16 count (605 files -- should be fewer now that duplicates are eliminated).
  </action>
  <verify>
    <automated>cd mcp-server && npm test</automated>
  </verify>
  <done>All validation tools pass with 0 errors. Vault resynced with 0 duplicates, 0 broken wikilinks. Full MCP test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Obsidian vault quality</name>
  <files>C:/Users/34645/AndroidStudioProjects/kmp-knowledge-vault/Home.md</files>
  <action>
Present the completed vault for human verification. All automated validation has passed (0 duplicates, 0 homogeneity errors, 0 broken references, 0 broken wikilinks). The user must visually confirm the vault quality in Obsidian.

What was built across Phase 17:
- Fixed 64 duplicate files (collector double-write bug, excludeGlobs for .planning/codebase and .planning/research, naming normalization)
- Built validate-vault MCP tool with 4 validation dimensions (duplicates, structural homogeneity, reference integrity, wikilink coverage)
- Created hub files for all docs/ subdirectories across L0/L1/L2
- Restructured CLAUDE.md files to Boris Cherny style (categorized sections, terse rules)
- Vault resynced with all validations passing

Verification steps for user:
1. Open Obsidian and point it at the vault directory (kmp-knowledge-vault/)
2. Check Graph View: nodes should be cleanly connected, no isolated clusters of duplicates
3. Check All Patterns MOC page: links navigate to correct docs, no broken links
4. Check All Modules MOC page: L1 module READMEs accessible
5. Navigate from a hub doc to its sub-docs via wikilinks -- verify they work
6. Search for any UPPERCASE filenames in the vault (should be 0)
7. Verify Home.md provides clear entry point to all content
8. Spot-check 2-3 hub docs from each layer: should be under 100 lines with Sub-documents table
  </action>
  <verify>
    <automated>find "C:/Users/34645/AndroidStudioProjects/kmp-knowledge-vault" -name "*[A-Z]*" -name "*.md" -not -name "Home.md" -not -name "MOC*" -not -name "All *" -not -name "README.md" | wc -l</automated>
  </verify>
  <done>Human approves: Obsidian graph view is clean, MOC navigation works, wikilinks connect, no duplicates visible, hub->sub-doc pattern consistent across all layers.</done>
</task>

</tasks>

<verification>
Full validation gate before presenting to user:
```bash
cd mcp-server && npm test  # All tests green
# validate-vault: 0 errors across all 4 dimensions
# validate-doc-structure: 0 errors across L0/L1/L2
# validate-claude-md: all 4 files pass
# validate-skills: no regressions
```
</verification>

<success_criteria>
- All 4 CLAUDE.md files under 150 lines in Boris Cherny style
- validate-vault: 0 duplicates, 0 homogeneity errors, 0 broken references, 0 broken wikilinks
- Vault resynced successfully with reduced file count (fewer than 605)
- All MCP tests green
- Human approves Obsidian graph view, MOC navigation, wikilink connectivity
</success_criteria>

<output>
After completion, create `.planning/phases/17-documentation-structural-homogeneity/17-06-SUMMARY.md`
</output>
