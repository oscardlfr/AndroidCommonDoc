---
phase: 17-documentation-structural-homogeneity
plan: 07
type: execute
wave: 4
depends_on: ["17-06"]
files_modified:
  - README.md
  - C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/README.md
  - C:/Users/34645/AndroidStudioProjects/DawSync/README.md
  - mcp-server/src/tools/validate-vault.ts
  - mcp-server/tests/unit/tools/validate-vault.test.ts
autonomous: false
requirements:
  - P17-AUDIT
  - P17-README
  - P17-HUMAN

must_haves:
  truths:
    - "validate-vault passes all 4 dimensions across L0/L1/L2 with 0 errors"
    - "validate-doc-structure passes across L0/L1/L2 with 0 errors"
    - "validate-claude-md passes for all 4 CLAUDE.md files with 0 errors"
    - "validate-skills passes with 0 errors"
    - "All 3 project READMEs follow the same structural template"
    - "Full vault resync produces 0 duplicates, 0 orphans, consistent naming"
    - "Human approves final Obsidian vault"
  artifacts:
    - path: "README.md"
      provides: "L0 project README (homogeneous structure)"
      contains: "## Overview"
    - path: "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/README.md"
      provides: "L1 project README (homogeneous structure)"
      contains: "## Overview"
    - path: "C:/Users/34645/AndroidStudioProjects/DawSync/README.md"
      provides: "L2 project README (homogeneous structure)"
      contains: "## Overview"
  key_links:
    - from: "validate-vault"
      to: "vault output"
      via: "0-error gate across all dimensions"
      pattern: "errors.*0"
---

<objective>
Final ecosystem-wide audit and README homogeneity pass. Run ALL validation tools (validate-vault, validate-doc-structure, validate-claude-md, validate-skills) across ALL layers. Fix any issues found. Update project READMEs to follow a homogeneous structure across L0/L1/L2. Perform final vault resync and present for human checkpoint.

Purpose: This is the definitive quality gate. Every validation tool must pass with 0 errors across the entire ecosystem. READMEs must be structurally homogeneous. The vault must be clean. The user must approve the final result.
Output: All validators green, homogeneous READMEs, clean vault, human approval.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-documentation-structural-homogeneity/17-CONTEXT.md

@.planning/phases/17-documentation-structural-homogeneity/17-01-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-02-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-03-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-04-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-05-SUMMARY.md
@.planning/phases/17-documentation-structural-homogeneity/17-06-SUMMARY.md

@README.md
@C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/README.md
@C:/Users/34645/AndroidStudioProjects/DawSync/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full ecosystem validation audit</name>
  <files>mcp-server/src/tools/validate-vault.ts</files>
  <action>
Run ALL validation tools across ALL layers. Fix any issues found before proceeding.

1. **validate-vault** (4 dimensions):
   - checkDuplicates: Run against vault output directory (kmp-knowledge-vault/)
   - checkStructuralHomogeneity: Run against L0 (AndroidCommonDoc), L1 (shared-kmp-libs), L2 (DawSync)
   - checkReferenceIntegrity: Run against L0, L1, L2 (verify L2->L1->L0 refs resolve)
   - checkWikilinkCoverage: Run against vault output

2. **validate-doc-structure**: Run against L0, L1, L2 docs/ directories

3. **validate-claude-md**: Run against all 4 CLAUDE.md files (L0 global, L0 toolkit, L1, L2)

4. **validate-skills**: Run against L0 skills registry

5. For EACH error found:
   - Diagnose root cause
   - Fix the source file (not the vault -- vault is read-only output)
   - Re-run the specific validator to confirm fix

6. After all errors resolved, generate a validation report summarizing:
   - Total checks run per tool
   - Errors found and fixed
   - Warnings (acceptable but noted)
   - Final pass/fail status per dimension
  </action>
  <verify>
    <automated>cd mcp-server && npm test && npm run build && node -e "
const {checkDuplicates, checkStructuralHomogeneity} = require('./dist/tools/validate-vault.js');
const vault = 'C:/Users/34645/AndroidStudioProjects/kmp-knowledge-vault';
Promise.all([
  checkDuplicates(vault),
  checkStructuralHomogeneity([
    {name:'AndroidCommonDoc',path:'C:/Users/34645/AndroidStudioProjects/AndroidCommonDoc',layer:'L0'},
    {name:'shared-kmp-libs',path:'C:/Users/34645/AndroidStudioProjects/shared-kmp-libs',layer:'L1'},
    {name:'DawSync',path:'C:/Users/34645/AndroidStudioProjects/DawSync',layer:'L2'}
  ])
]).then(([dupes,struct]) => {
  const errs = [...dupes,...struct].filter(i=>i.severity==='error');
  console.log('Errors:', errs.length);
  if(errs.length > 0) { console.log(JSON.stringify(errs,null,2)); process.exit(1); }
  console.log('ALL CHECKS PASSED');
})"</automated>
  </verify>
  <done>All validation tools pass with 0 errors across L0/L1/L2. Full MCP test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Homogenize project READMEs across L0/L1/L2</name>
  <files>README.md, C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/README.md, C:/Users/34645/AndroidStudioProjects/DawSync/README.md</files>
  <action>
Read all 3 project READMEs. Restructure ALL to follow the same homogeneous template:

```markdown
# {Project Name}

> {One-line description}

## Overview

{2-3 sentence project description. Layer identification. What it provides.}

## Quick Start

{Build/run commands in a code block}

## Architecture

{Brief architecture description or link to docs/architecture/ hub}

## Documentation

{Link to docs/ directory. List key entry points (hub docs).}

## Project Structure

{Brief module/directory overview. Table format for multi-module projects.}

## Development

{Build commands, test commands, common workflows.}
```

Rules:
- Same sections in same order across all 3 READMEs
- Same heading levels
- Same formatting patterns (tables vs bullets)
- L0: Emphasize toolkit purpose (docs, skills, MCP server, vault sync)
- L1: Emphasize module catalog (foundation, I/O, security, etc.)
- L2: Emphasize app purpose (Creative Project OS), architecture (Producer/Consumer)
- Each README <200 lines
- No duplicate information with CLAUDE.md (README is for humans browsing GitHub, CLAUDE.md is for AI agents)
  </action>
  <verify>
    <automated>for f in README.md "C:/Users/34645/AndroidStudioProjects/shared-kmp-libs/README.md" "C:/Users/34645/AndroidStudioProjects/DawSync/README.md"; do lines=$(wc -l < "$f"); echo "$f: $lines lines"; if [ "$lines" -gt 200 ]; then echo "OVER LIMIT"; exit 1; fi; for section in "## Overview" "## Quick Start" "## Architecture" "## Documentation" "## Development"; do if ! grep -q "$section" "$f"; then echo "MISSING $section in $f"; exit 1; fi; done; done && echo "All READMEs homogeneous"</automated>
  </verify>
  <done>All 3 READMEs follow identical structure with same sections in same order. Each under 200 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Final vault resync with validation gate</name>
  <files>kmp-knowledge-vault/</files>
  <action>
1. Run validate-vault ALL dimensions one final time to confirm clean state after README updates.

2. Perform vault resync:
   ```bash
   cd mcp-server && node dist/cli/sync-vault-cli.js sync
   ```

3. Post-sync validation:
   - checkDuplicates against materialized vault: must be 0
   - Count total files: compare to Phase 16 (369 files) -- should be similar or fewer (no duplicates)
   - Verify Home.md, All Patterns, All Modules, All Skills, All Decisions MOC pages exist and link correctly

4. Prepare checkpoint summary with:
   - Total validation checks run
   - File counts per layer (L0, L1, L2)
   - Errors found and fixed during audit
   - Screenshot-ready verification steps for the user
  </action>
  <verify>
    <automated>find "C:/Users/34645/AndroidStudioProjects/kmp-knowledge-vault" -name "*.md" | wc -l && find "C:/Users/34645/AndroidStudioProjects/kmp-knowledge-vault" -name "*[A-Z]*[A-Z]*" -name "*.md" -not -name "Home.md" -not -path "*/00-MOC/*" -not -name "README.md" -not -name "CLAUDE.md" | head -5</automated>
  </verify>
  <done>Vault resynced. 0 duplicates. 0 UPPERCASE files (except Home.md, MOC pages). File count consistent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final human verification of complete ecosystem</name>
  <files>C:/Users/34645/AndroidStudioProjects/kmp-knowledge-vault/Home.md</files>
  <action>
Present the COMPLETE ecosystem for human verification. This is the final checkpoint for Phase 17 and the entire v1.2 documentation milestone.

What was built across Phase 17:
- Fixed 64 vault duplicates (collector bug, excludeGlobs, naming normalization)
- Built validate-vault MCP tool (4 validation dimensions)
- Restructured ALL docs across L0/L1/L2 for hub->sub-doc homogeneity
- Aligned all CLAUDE.md files to Boris Cherny style
- Homogenized all project READMEs
- Full ecosystem audit with 0 errors across all validators
- Clean vault resync

User verification steps:
1. Open Obsidian vault at kmp-knowledge-vault/
2. Graph View: clean, no isolated duplicate clusters, logical grouping
3. Home.md: clear navigation to all content areas
4. All Patterns / All Modules / All Skills / All Decisions: complete and linked
5. Hub->sub-doc navigation: pick any hub, verify sub-doc links work
6. No UPPERCASE filenames in file explorer (except Home.md, MOC pages)
7. DawSync docs: no duplicates from .planning/, categories consistent
8. READMEs: check L0, L1, L2 READMEs on GitHub -- same structure
9. CLAUDE.md: check any CLAUDE.md -- Boris Cherny style, concise, categorized
  </action>
  <verify>
    <automated>echo "Awaiting human verification"</automated>
  </verify>
  <done>Human approves: entire ecosystem documentation is structurally homogeneous, vault is clean, all validators pass, READMEs consistent, CLAUDE.md files optimized.</done>
</task>

</tasks>

<verification>
Full validation gate:
```bash
cd mcp-server && npm test  # All tests green
# validate-vault: 0 errors across all 4 dimensions for all layers
# validate-doc-structure: 0 errors across L0/L1/L2
# validate-claude-md: all 4 files pass
# validate-skills: no regressions
# All READMEs follow homogeneous template
```
</verification>

<success_criteria>
- ALL validation tools pass with 0 errors across entire ecosystem
- All 3 project READMEs follow identical structural template (<200 lines each)
- Vault resynced with 0 duplicates, 0 orphans, 0 UPPERCASE files
- All MCP tests green
- Human approves final ecosystem state in Obsidian
</success_criteria>

<output>
After completion, create `.planning/phases/17-documentation-structural-homogeneity/17-07-SUMMARY.md`
</output>
</content>
</invoke>