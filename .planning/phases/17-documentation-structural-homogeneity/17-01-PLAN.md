---
phase: 17-documentation-structural-homogeneity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/vault/collector.ts
  - mcp-server/src/vault/config.ts
  - mcp-server/src/vault/transformer.ts
  - mcp-server/src/vault/sync-engine.ts
  - mcp-server/tests/unit/vault/collector.test.ts
  - mcp-server/tests/unit/vault/config.test.ts
  - mcp-server/tests/unit/vault/transformer.test.ts
  - mcp-server/tests/unit/vault/sync-engine.test.ts
  - ~/.dawsync/vault-config.json
autonomous: true
requirements:
  - P17-BUG
  - P17-DEDUP
  - P17-EXCLUDE
  - P17-NAMING

must_haves:
  truths:
    - "Each source file maps to exactly one vault path (no double-writes)"
    - "DawSync .planning/codebase/ and .planning/research/ files are excluded from vault collection"
    - "Vault sync aborts with error if duplicate vault paths detected pre-materialization"
    - "All vault filenames are lowercase-kebab-case (no UPPERCASE_SNAKE)"
  artifacts:
    - path: "mcp-server/src/vault/collector.ts"
      provides: "Fixed path routing with source-dedup"
      contains: "dedup"
    - path: "mcp-server/src/vault/config.ts"
      provides: "Updated default excludes"
      contains: ".planning/research"
    - path: "mcp-server/src/vault/sync-engine.ts"
      provides: "Pre-materialization dedup gate"
      contains: "detectDuplicates"
  key_links:
    - from: "mcp-server/src/vault/sync-engine.ts"
      to: "mcp-server/src/vault/collector.ts"
      via: "collectAll produces deduplicated entries"
      pattern: "collectAll"
    - from: "mcp-server/src/vault/sync-engine.ts"
      to: "detectDuplicates"
      via: "pre-materialization gate"
      pattern: "detectDuplicates"
---

<objective>
Fix the three vault sync bugs that caused 64 duplicate files in Phase 16: collector double-write bug, missing excludeGlobs for .planning/codebase/ and .planning/research/, and transformer naming normalization. Add pre-materialization dedup detection gate to prevent future regressions.

Purpose: These bugs are the root cause of the user's Phase 16 checkpoint rejection. Must be fixed before any source doc restructuring or resync.
Output: Bug-free vault pipeline that produces exactly one vault entry per source file, excludes planning infrastructure files, and rejects sync on duplicates.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-documentation-structural-homogeneity/17-CONTEXT.md
@.planning/phases/17-documentation-structural-homogeneity/17-RESEARCH.md

@mcp-server/src/vault/collector.ts
@mcp-server/src/vault/config.ts
@mcp-server/src/vault/transformer.ts
@mcp-server/src/vault/sync-engine.ts
@mcp-server/src/vault/types.ts
@mcp-server/tests/unit/vault/collector.test.ts
@mcp-server/tests/unit/vault/config.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix collector double-write bug and excludeGlobs</name>
  <files>mcp-server/src/vault/collector.ts, mcp-server/src/vault/config.ts, mcp-server/tests/unit/vault/collector.test.ts, mcp-server/tests/unit/vault/config.test.ts</files>
  <action>
1. In `config.ts`, add `.planning/codebase/**` and `.planning/research/**` to `getDefaultExcludes()` alongside the existing `.planning/phases/**` exclude.

2. In `collector.ts`, fix the double-write bug in `collectProjectSources`:
   - The `subdivisionIdx` calculation using `indexOf("planning/")` produces duplicate vault paths for `.planning/codebase/` files. Add source-path deduplication: track absolute source paths already processed in a `Set<string>`, skip duplicates.
   - After all entries are collected in `collectAll`, add a final dedup pass by normalized absolute source path, keeping the first occurrence.

3. Update vault-config.json at `~/.dawsync/vault-config.json`:
   - Add `**/.planning/codebase/**` and `**/.planning/research/**` to DawSync excludeGlobs
   - Add `**/.planning/codebase/**` and `**/.planning/research/**` to DawSyncWeb excludeGlobs
   - Remove `.planning/codebase/**/*.md` from shared-kmp-libs collectGlobs (planning infrastructure, not docs)

4. Add tests:
   - collector.test.ts: Test that a `.planning/codebase/ARCHITECTURE.md` file produces exactly 1 vault entry (not 2)
   - collector.test.ts: Test that `.planning/research/` files are excluded
   - config.test.ts: Test that `getDefaultExcludes()` includes `.planning/research/**` and `.planning/codebase/**`
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/vault/collector.test.ts tests/unit/vault/config.test.ts -x</automated>
  </verify>
  <done>No source file produces more than one vault entry. .planning/codebase/ and .planning/research/ are excluded by default. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add pre-materialization dedup gate and naming normalization</name>
  <files>mcp-server/src/vault/sync-engine.ts, mcp-server/src/vault/transformer.ts, mcp-server/tests/unit/vault/transformer.test.ts</files>
  <action>
1. Create a `detectDuplicates(entries: VaultEntry[]): string[]` function in sync-engine.ts (or a new dedup.ts file if sync-engine is too large). This function checks:
   - Case-insensitive vault path collisions (two entries whose `vaultPath.toLowerCase()` match)
   - Same `vault_source` mapped to multiple vault paths
   Returns an array of error strings. Empty = no duplicates.

2. In sync-engine.ts `syncVault()` or equivalent orchestration function, call `detectDuplicates()` AFTER collection+transformation but BEFORE `writeVault()`. If duplicates found, throw an error with the duplicate list. Do NOT materialize files.

3. In `transformer.ts`, ensure the slug derivation and vault path generation ALWAYS produce lowercase-kebab-case output. If a source file is `ARCHITECTURE.md`, the vault filename must be `architecture.md`. Check `deriveSlug()` and any `toVaultPath()` logic. The transformer should normalize ALL filenames to lowercase-kebab-case, replacing underscores with hyphens.

4. Add tests:
   - Test `detectDuplicates` with: empty array (no errors), unique entries (no errors), case-insensitive collision (error), same source two paths (error)
   - Test transformer slug derivation produces lowercase-kebab for UPPERCASE_SNAKE input
  </action>
  <verify>
    <automated>cd mcp-server && npx vitest run tests/unit/vault/transformer.test.ts -x && npx vitest run --passWithNoTests tests/unit/vault/dedup.test.ts tests/unit/vault/sync-engine.test.ts -x</automated>
  </verify>
  <done>Vault sync rejects materialization when duplicates detected. UPPERCASE_SNAKE filenames transform to lowercase-kebab-case. All tests pass.</done>
</task>

</tasks>

<verification>
Run the full MCP test suite to ensure no regressions:
```bash
cd mcp-server && npm test
```
All existing tests must still pass alongside new dedup and exclude tests.
</verification>

<success_criteria>
- 0 duplicate vault entries for any source file configuration
- .planning/codebase/ and .planning/research/ excluded by default
- Sync aborts with clear error message if duplicates detected
- UPPERCASE filenames normalize to lowercase-kebab-case in vault
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/17-documentation-structural-homogeneity/17-01-SUMMARY.md`
</output>
