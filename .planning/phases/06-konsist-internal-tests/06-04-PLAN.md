---
phase: 06-konsist-internal-tests
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/NamingConventionTest.kt
  - konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/PackageConventionTest.kt
  - konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/ArchitectureTest.kt
  - konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/ScriptParityTest.kt
autonomous: false
gap_closure: true
requirements: [KONS-02, KONS-03, KONS-04]
must_haves:
  truths:
    - "A class named FooManager loaded from naming-violation fixture causes Konsist naming check to throw KoAssertionFailedException"
    - "A class MisplacedRule in the wrong package loaded from package-violation fixture causes a package placement failure"
    - "ArchitectureTest asserts the actual file name or import path in the error message (not just the layer name)"
    - "cd konsist-tests && ./gradlew test produces BUILD SUCCESSFUL with all tests passing (no known-failing tests)"
  artifacts:
    - path: "konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/NamingConventionTest.kt"
      provides: "Negative fixture test proving naming violation detection"
      contains: "fixtureScope.*naming-violation"
    - path: "konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/PackageConventionTest.kt"
      provides: "Negative fixture test proving package placement violation detection"
      contains: "fixtureScope.*package-violation"
    - path: "konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/ArchitectureTest.kt"
      provides: "Strengthened assertion on error message content"
    - path: "konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/ScriptParityTest.kt"
      provides: "Clean pass with orphaned scripts removed or allowlisted"
  key_links:
    - from: "NamingConventionTest.kt"
      to: "fixtures/naming-violation/FooManager.kt"
      via: "ScopeFactory.fixtureScope(\"naming-violation\")"
      pattern: "fixtureScope.*naming-violation"
    - from: "PackageConventionTest.kt"
      to: "fixtures/package-violation/MisplacedRule.kt"
      via: "ScopeFactory.fixtureScope(\"package-violation\")"
      pattern: "fixtureScope.*package-violation"
---

<objective>
Close 3 verification gaps from 06-VERIFICATION.md: wire orphaned naming and package violation fixtures to negative tests, strengthen architecture error message assertions, and resolve ScriptParityTest known failure so the full test suite passes.

Purpose: ROADMAP SC #3 requires proof that FooManager causes a test failure. SC #2 requires error messages naming the offending file. "Run reliably" means BUILD SUCCESSFUL with no known-failing tests.
Output: All Konsist tests pass, all fixtures are exercised, all verification truths satisfied.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-konsist-internal-tests/06-VERIFICATION.md
@.planning/phases/06-konsist-internal-tests/06-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from existing code. -->

From konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/support/ScopeFactory.kt:
```kotlin
object ScopeFactory {
    fun detektRulesScope(): KoScope        // >= 6 files, canary assertion
    fun detektRulesTestScope(): KoScope    // >= 5 files, canary assertion
    fun buildLogicScope(): KoScope         // >= 1 file, canary assertion
    fun fixtureScope(fixturePath: String): KoScope  // >= 1 file, canary assertion
}
```

From Konsist API (discovered in 06-03):
```kotlin
// Architecture assertions require this scope pattern:
with(KoArchitectureCreator) {
    scope.assertArchitecture { /* layer rules */ }
}

// Konsist throws KoAssertionFailedException (NOT AssertionError)
import com.lemonappdev.konsist.core.exception.KoAssertionFailedException

// Declaration assertion:
import com.lemonappdev.konsist.api.verify.assertTrue
import com.lemonappdev.konsist.api.verify.assertFalse
```

Fixture files already exist:
- `konsist-tests/src/test/resources/fixtures/naming-violation/FooManager.kt` -- `package com.example.data; class FooManager`
- `konsist-tests/src/test/resources/fixtures/package-violation/MisplacedRule.kt` -- `package com.example.data; class MisplacedRule`

Orphaned SH scripts (no PS1 counterpart):
- `scripts/sh/validate-phase03-build-logic.sh`
- `scripts/sh/validate-phase03-copilot.sh`
- `scripts/sh/validate-phase03-hooks.sh`
- `scripts/sh/validate-phase03-setup.sh`
- `scripts/sh/validate-phase04-integration.sh`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire orphaned fixtures to negative tests and delete orphaned scripts</name>
  <files>
    konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/NamingConventionTest.kt
    konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/PackageConventionTest.kt
    scripts/sh/validate-phase03-build-logic.sh
    scripts/sh/validate-phase03-copilot.sh
    scripts/sh/validate-phase03-hooks.sh
    scripts/sh/validate-phase03-setup.sh
    scripts/sh/validate-phase04-integration.sh
  </files>
  <action>
    **Gap 1 fix -- NamingConventionTest:** Add a negative test method to NamingConventionTest.kt that loads `ScopeFactory.fixtureScope("naming-violation")` and proves that a naming convention check FAILS when FooManager is present. The fixture file `naming-violation/FooManager.kt` has `package com.example.data` and `class FooManager` -- this class does NOT end with "Rule" even though it is in a data package (wrong suffix for any convention). The test should:
    1. Load `ScopeFactory.fixtureScope("naming-violation")`
    2. Get classes from the scope
    3. Apply a naming check (e.g., assert all classes end with an expected suffix like "Repository" or "DataSource" for a data package -- the point is proving the naming convention mechanism catches FooManager)
    4. Use `assertThrows<KoAssertionFailedException>` to prove the check fails
    5. Assert the error message contains "FooManager" so SC #3 is satisfied
    6. Add import for `com.lemonappdev.konsist.core.exception.KoAssertionFailedException` and `org.junit.jupiter.api.assertThrows`

    The test name should be something like: `fixture FooManager in wrong naming convention is detected`.

    **Gap 1 fix -- PackageConventionTest:** Add a negative test method to PackageConventionTest.kt that loads `ScopeFactory.fixtureScope("package-violation")` and proves that a package placement check FAILS when MisplacedRule is present. The fixture `package-violation/MisplacedRule.kt` has `package com.example.data` and `class MisplacedRule` -- a Rule-suffixed class in a data package instead of a rules package. The test should:
    1. Load `ScopeFactory.fixtureScope("package-violation")`
    2. Get classes ending with "Rule"
    3. Assert they reside in a rules package using Konsist's `assertTrue { it.resideInPackage("..rules..") }`
    4. Use `assertThrows<KoAssertionFailedException>` to prove the check fails
    5. Assert the error message contains "MisplacedRule"
    6. Add necessary imports

    The test name should be something like: `fixture MisplacedRule in wrong package is detected`.

    **ScriptParity fix -- Delete orphaned scripts:** Delete the 5 orphaned validate-phase*.sh scripts that have no PS1 counterparts. These are v1.0 phase-specific validation scripts that were superseded by the quality-gate-orchestrator in Phase 5. They are no longer referenced by any quality gate or CI process:
    - `scripts/sh/validate-phase03-build-logic.sh`
    - `scripts/sh/validate-phase03-copilot.sh`
    - `scripts/sh/validate-phase03-hooks.sh`
    - `scripts/sh/validate-phase03-setup.sh`
    - `scripts/sh/validate-phase04-integration.sh`

    After deletion, ScriptParityTest should pass because all remaining SH scripts have PS1 counterparts (and vice versa).

    IMPORTANT: Do NOT modify ScriptParityTest.kt itself -- the test logic is correct. The fix is removing the orphaned scripts.
  </action>
  <verify>
    <automated>cd konsist-tests && ./gradlew test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - NamingConventionTest has a negative test that loads fixtureScope("naming-violation"), catches KoAssertionFailedException, and asserts "FooManager" is in the error message
    - PackageConventionTest has a negative test that loads fixtureScope("package-violation"), catches KoAssertionFailedException, and asserts "MisplacedRule" is in the error message
    - 5 orphaned validate-phase*.sh scripts deleted from scripts/sh/
    - `cd konsist-tests && ./gradlew test` produces BUILD SUCCESSFUL
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify BUILD SUCCESSFUL and architecture error message content</name>
  <what-built>
    Wired orphaned fixture files to negative tests proving violation detection works.
    Deleted 5 orphaned validate-phase*.sh scripts so ScriptParityTest passes.
    All Konsist tests should now pass with BUILD SUCCESSFUL.
  </what-built>
  <how-to-verify>
    1. Run `cd konsist-tests && ./gradlew test` -- confirm BUILD SUCCESSFUL with all tests passing
    2. In the test output, find the ArchitectureTest failure messages. Specifically:
       - Look at the KoAssertionFailedException message for `fixture with data-imports-ui violation is detected by assertArchitecture`
       - Check if the error message contains "DataImportsUi" (file name) or "com.example.ui.SomeUiClass" (import path), not just the layer name "Data"
       - If YES: KONS-02 SC #2 is fully met, no further action needed
       - If NO (only layer name): we need to strengthen ArchitectureTest to wrap and re-throw with enhanced message. Report what the actual message says.
    3. Verify NamingConventionTest's new negative test passes (catches FooManager fixture violation)
    4. Verify PackageConventionTest's new negative test passes (catches MisplacedRule fixture violation)
    5. Confirm the 5 validate-phase*.sh scripts are no longer in scripts/sh/
  </how-to-verify>
  <resume-signal>Type "approved" if BUILD SUCCESSFUL and all gaps are closed. If the architecture error message lacks file names, paste the actual message content so we can decide whether to add an enhanced re-throw wrapper.</resume-signal>
</task>

</tasks>

<verification>
1. `cd konsist-tests && ./gradlew test` -- BUILD SUCCESSFUL, all tests pass (no failures, no skips)
2. `git diff --name-only` shows NamingConventionTest.kt and PackageConventionTest.kt modified, 5 .sh files deleted
3. No orphaned fixtures remain: every file in `src/test/resources/fixtures/` is referenced by at least one test
4. grep confirms: `fixtureScope("naming-violation")` appears in NamingConventionTest.kt, `fixtureScope("package-violation")` appears in PackageConventionTest.kt
</verification>

<success_criteria>
- VERIFICATION.md Gap 1 (KONS-03 SC #3) closed: FooManager fixture causes a detected naming violation with the class name in the error message
- VERIFICATION.md Gap 1 (package-violation) closed: MisplacedRule fixture causes a detected package placement violation
- ScriptParityTest passes: BUILD SUCCESSFUL with no known-failing tests
- Human confirms KONS-02 SC #2 architecture error message content
- All 06-VERIFICATION.md truths satisfied (11/11)
</success_criteria>

<output>
After completion, create `.planning/phases/06-konsist-internal-tests/06-04-SUMMARY.md`
</output>
