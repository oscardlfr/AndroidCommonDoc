---
phase: 06-konsist-internal-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - konsist-tests/build.gradle.kts
  - konsist-tests/settings.gradle.kts
  - konsist-tests/gradle/wrapper/gradle-wrapper.properties
  - konsist-tests/gradlew
  - konsist-tests/gradlew.bat
  - konsist-tests/gradle/wrapper/gradle-wrapper.jar
  - konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/support/ScopeFactory.kt
  - konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/ClasspathSpikeTest.kt
autonomous: true
requirements:
  - KONS-01
  - KONS-05
must_haves:
  truths:
    - "konsist-tests module compiles and resolves Konsist 0.17.3 from Maven Central"
    - "ScopeFactory.detektRulesScope() returns a non-empty scope containing detekt-rules production files"
    - "ScopeFactory.buildLogicScope() returns a non-empty scope containing build-logic production files"
    - "./gradlew :konsist-tests:test runs and passes the classpath spike canary test"
    - "Running ./gradlew :konsist-tests:test twice in a row never shows UP-TO-DATE -- second run re-executes"
  artifacts:
    - path: "konsist-tests/build.gradle.kts"
      provides: "Standalone JVM-only module with Konsist 0.17.3 testImplementation"
      contains: "outputs.upToDateWhen"
    - path: "konsist-tests/settings.gradle.kts"
      provides: "Standalone Gradle project declaration"
      contains: "konsist-tests"
    - path: "konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/support/ScopeFactory.kt"
      provides: "Centralized scope creation with canary assertions"
      exports: ["detektRulesScope", "buildLogicScope", "detektRulesTestScope", "fixtureScope"]
    - path: "konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/ClasspathSpikeTest.kt"
      provides: "Canary test proving Konsist works in isolated module"
  key_links:
    - from: "konsist-tests/build.gradle.kts"
      to: "com.lemonappdev:konsist:0.17.3"
      via: "testImplementation dependency"
      pattern: "konsist:0\\.17\\.3"
    - from: "ScopeFactory.kt"
      to: "../detekt-rules/src/main/kotlin"
      via: "scopeFromDirectory with relative path"
      pattern: "scopeFromDirectory"
---

<objective>
Bootstrap the konsist-tests standalone JVM module, validate Konsist 0.17.3 classpath isolation alongside Kotlin 2.3.10, and create the shared ScopeFactory utility that all subsequent test classes depend on.

Purpose: This is the critical foundation -- if the classpath spike fails, the entire phase approach must change. Getting this right first de-risks everything else.
Output: A working konsist-tests/ Gradle module with passing canary tests and shared scope utilities.
</objective>

<execution_context>
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/workflows/execute-plan.md
@C:/Users/34645/.ccs/instances/cuenta1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-konsist-internal-tests/06-CONTEXT.md
@.planning/phases/06-konsist-internal-tests/06-RESEARCH.md

<interfaces>
<!-- Existing module patterns the executor needs to replicate -->

From detekt-rules/settings.gradle.kts:
```kotlin
rootProject.name = "detekt-rules"
```

From detekt-rules/build.gradle.kts (template for konsist-tests):
```kotlin
plugins {
    kotlin("jvm") version "2.3.10"
}
group = "com.androidcommondoc"
version = "1.0.0"
repositories {
    mavenCentral()
}
dependencies {
    testImplementation("org.junit.jupiter:junit-jupiter:5.11.4")
    testRuntimeOnly("org.junit.platform:junit-platform-launcher")
    testImplementation("org.assertj:assertj-core:3.27.3")
}
tasks.withType<Test> {
    useJUnitPlatform()
}
```

Scan targets (files that ScopeFactory must find):
- detekt-rules/src/main/kotlin/ -- 6 .kt files (1 provider + 5 rules)
- detekt-rules/src/test/kotlin/ -- 5 .kt files (5 test classes)
- build-logic/src/main/kotlin/ -- 1 .kt file (AndroidCommonDocExtension.kt)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create konsist-tests module with Gradle wrapper and UP-TO-DATE bypass</name>
  <files>
    konsist-tests/build.gradle.kts,
    konsist-tests/settings.gradle.kts,
    konsist-tests/gradle/wrapper/gradle-wrapper.properties,
    konsist-tests/gradlew,
    konsist-tests/gradlew.bat,
    konsist-tests/gradle/wrapper/gradle-wrapper.jar
  </files>
  <action>
    Create the `konsist-tests/` directory as a standalone JVM-only Gradle project (NOT a subproject of any other build).

    1. **settings.gradle.kts**: `rootProject.name = "konsist-tests"` -- nothing else. No includeBuild, no composite build references.

    2. **build.gradle.kts**: Follow the exact pattern from 06-RESEARCH.md:
       ```kotlin
       plugins {
           kotlin("jvm") version "2.3.10"
       }
       group = "com.androidcommondoc"
       version = "1.0.0"
       repositories {
           mavenCentral()
       }
       dependencies {
           testImplementation("com.lemonappdev:konsist:0.17.3")
           testImplementation("org.junit.jupiter:junit-jupiter:5.11.4")
           testRuntimeOnly("org.junit.platform:junit-platform-launcher")
           testImplementation("org.assertj:assertj-core:3.27.3")
       }
       tasks.withType<Test> {
           useJUnitPlatform()
           outputs.upToDateWhen { false }  // KONS-05: never UP-TO-DATE
       }
       ```
       CRITICAL: Do NOT apply the Detekt plugin. Only `kotlin("jvm")`. This prevents classpath conflict with kotlin-compiler-embeddable.

    3. **Gradle wrapper**: Copy the Gradle wrapper from `detekt-rules/` (which uses Gradle 9.1.0) into `konsist-tests/`:
       - `gradle/wrapper/gradle-wrapper.properties` (same Gradle version)
       - `gradle/wrapper/gradle-wrapper.jar`
       - `gradlew` and `gradlew.bat`

       Alternatively, run `gradle wrapper --gradle-version 9.1.0` from within `konsist-tests/` if Gradle is available on PATH. The goal is matching the existing project's Gradle version.

    4. **Verify module compiles**: Run `cd konsist-tests && ./gradlew dependencies --configuration testCompileClasspath` to confirm Konsist 0.17.3 resolves and its transitive `kotlin-compiler-embeddable:2.0.21` is pulled.
  </action>
  <verify>
    <automated>cd konsist-tests && ./gradlew dependencies --configuration testCompileClasspath 2>&1 | grep -E "konsist|kotlin-compiler-embeddable"</automated>
  </verify>
  <done>konsist-tests/ module exists as standalone Gradle project, Konsist 0.17.3 resolves with kotlin-compiler-embeddable:2.0.21 transitive, outputs.upToDateWhen { false } configured on test task, no Detekt plugin present</done>
</task>

<task type="auto">
  <name>Task 2: Create ScopeFactory utility and ClasspathSpikeTest canary</name>
  <files>
    konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/support/ScopeFactory.kt,
    konsist-tests/src/test/kotlin/com/androidcommondoc/konsist/ClasspathSpikeTest.kt
  </files>
  <action>
    1. **ScopeFactory.kt** in `com.androidcommondoc.konsist.support` package:
       Create a singleton object with scope factory methods. Every method MUST include a canary assertion (require non-empty) to prevent vacuous passes.

       Methods to implement:
       - `detektRulesScope(): KoScope` -- scans `../detekt-rules/src/main/kotlin`. Canary: must find >= 6 files.
       - `detektRulesTestScope(): KoScope` -- scans `../detekt-rules/src/test/kotlin`. Canary: must find >= 5 files.
       - `buildLogicScope(): KoScope` -- scans `../build-logic/src/main/kotlin`. Canary: must find >= 1 file.
       - `fixtureScope(fixturePath: String): KoScope` -- scans `src/test/resources/fixtures/$fixturePath`. Canary: must find >= 1 file.

       IMPORTANT path resolution: Use `Konsist.scopeFromDirectory()` with relative paths. The working directory when running `./gradlew test` from `konsist-tests/` is `konsist-tests/` itself. So `../detekt-rules/src/main/kotlin` resolves correctly.

       If relative paths fail on Windows, implement fallback using `File("../detekt-rules/src/main/kotlin").canonicalPath` to get an absolute path. Add a comment explaining why.

       Each method should have a descriptive canary failure message, e.g.: "Canary: detekt-rules scope is empty (expected >= 6 files). Working dir: ${System.getProperty("user.dir")}. Check that konsist-tests/ is adjacent to detekt-rules/."

    2. **ClasspathSpikeTest.kt** in `com.androidcommondoc.konsist` package:
       A focused canary test class that validates KONS-01 (classpath isolation):

       ```
       @Test fun `Konsist resolves detekt-rules scope from isolated module`()
       ```
       - Calls `ScopeFactory.detektRulesScope()`, asserts it has >= 6 files
       - Finds `AndroidCommonDocRuleSetProvider` file by name, asserts it exists
       - Verifies the provider file has package `com.androidcommondoc.detekt`

       ```
       @Test fun `Konsist resolves build-logic scope from isolated module`()
       ```
       - Calls `ScopeFactory.buildLogicScope()`, asserts it has >= 1 file
       - Finds `AndroidCommonDocExtension` class, asserts it exists

       ```
       @Test fun `Konsist parses Kotlin 2_3_10 source files without error`()
       ```
       - Calls `ScopeFactory.detektRulesScope()`, iterates all classes, calls `.name` on each
       - This exercises the kotlin-compiler-embeddable 2.0.21 parsing Kotlin 2.3.10 code
       - If any ClassCastException or parse error occurs, this test fails immediately

       Use JUnit 5 annotations and AssertJ assertions. Import from `com.androidcommondoc.konsist.support.ScopeFactory`.

    3. **Run and verify**: Execute `cd konsist-tests && ./gradlew test` to confirm all 3 canary tests pass. This validates the full stack: Gradle resolution, Konsist classpath isolation, scope path resolution, and Kotlin version compatibility.

    4. **Verify UP-TO-DATE bypass (KONS-05)**: Run `cd konsist-tests && ./gradlew test` a second time and confirm the output does NOT contain "UP-TO-DATE" for the test task.
  </action>
  <verify>
    <automated>cd konsist-tests && ./gradlew test 2>&1 | tail -20</automated>
  </verify>
  <done>ScopeFactory provides 4 scope methods with canary assertions. ClasspathSpikeTest has 3 passing tests proving Konsist 0.17.3 works in isolated module, resolves both detekt-rules and build-logic scopes, and parses Kotlin 2.3.10 code. Running ./gradlew test twice never shows UP-TO-DATE.</done>
</task>

</tasks>

<verification>
1. `cd konsist-tests && ./gradlew test` passes all 3 canary tests
2. `cd konsist-tests && ./gradlew test` run a second time does NOT show "UP-TO-DATE" for :test task
3. `cd konsist-tests && ./gradlew dependencies --configuration testCompileClasspath | grep konsist` shows 0.17.3
4. No Detekt plugin in konsist-tests/build.gradle.kts (grep -c "detekt" konsist-tests/build.gradle.kts returns 0)
</verification>

<success_criteria>
- konsist-tests/ is a standalone Gradle project with own settings.gradle.kts
- Konsist 0.17.3 resolves and its kotlin-compiler-embeddable:2.0.21 is on testCompileClasspath
- ScopeFactory resolves detekt-rules (6+ files), build-logic (1+ files) via relative paths
- All canary tests pass (no ClassCastException, no empty scopes)
- Test task never shows UP-TO-DATE on repeated runs
</success_criteria>

<output>
After completion, create `.planning/phases/06-konsist-internal-tests/06-01-SUMMARY.md`
</output>
